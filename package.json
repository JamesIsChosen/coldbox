{
  "name": "coldbox",
  "version": "0.0.0",
  "private": true,
  "description": "A single-file, offline-first crypto toolkit.",
  "license": "MIT",
  "engines": {
    "node": "24.16.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "node scripts/lint.js",
    "verify-vendor": "node scripts/verify-vendor.js",
    "check-docs": "node scripts/check-docs.js",
    "test:browser": "node scripts/run-browser-harness.js",
    "test": "node --test --test-concurrency=1"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
