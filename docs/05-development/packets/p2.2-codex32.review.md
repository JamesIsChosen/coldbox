# Review: P2.2 — codex32

**VERDICT: FAIL**

Findings: 2 (2 blocking, 0 advisory — all must be addressed)
Reviewed commit: `3219685bef8f52af0e0abe160ea8ba1b6acfb35f`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-12
Independent evidence bundle SHA-256: `31c56955b824d265deee00d7da2a1a7b2b831c1a56ce64335adea4f43571abe8`

## 1. What I verified

Fresh independent review used a full-history clone and separate detached worktrees. For this item I verified the exact product head and merge base, `git diff --check`, `npm ci`, `npm run verify-vendor`, `npm run lint`, `npm run check-docs`, the focused item test, the full `npm test` suite, two same-worktree builds, and a third build from a different detached path/timezone/locale. Vendor-manifest corruption was also exercised and correctly made `verify-vendor` exit non-zero. The cold CSP probe retained `connect-src 'none'`, found no `allow-same-origin`, and found no storage/network/warm-message surface in the item module.

The three builds were byte-identical and matched the packet exactly: `dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7`, 2,227,044 bytes, including the different path/TZ/locale build.

The previous P2.2 findings are materially remediated on this head: the guide now treats recovery as direct BIP-32 master-seed bytes rather than BIP-39 words, later worksheet/completion/fingerprint/Health surfaces are no longer claimed as current functionality, and the identifier input is blank by default so generation uses fresh randomness unless the user explicitly supplies an identifier.

The focused codex32 suite and full suite both exited 0. Chromium completed the codex32 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, teardown, and existing security regressions.

## 2. What I could not verify

The exact P2.2 head did not complete a fresh Firefox run. After Chromium passed, Playwright exited 1 while closing a Firefox page/browser context with:

`page.close: Protocol error (Browser.removeBrowserContext): can't access property "_maybeDontRestoreTabs", this._windows[aWindow.__SSi] is undefined`

This verification gap is F2.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.2 codex32` | ❌ | Official/focused vectors, full tests, reproducible builds, Chromium behavior, corrected direct-seed semantics, and randomized blank identifier behavior were verified, but the security build gate accepts executable `Math.random()` in the cold module (F1) and fresh Firefox verification did not complete (F2). |

## 4. Findings

### F1 — Build/lint gate fails open to executable `Math.random()` in codex32 security code

**Severity:** blocking security
**Location:** `scripts/lint.js` `scanSourceFile()`; exercised path `src/cold/codex32.js`.
**Observed:** I appended `void Math.random();` to `src/cold/codex32.js` in the disposable review worktree and ran `npm run build`. It exited **0**. The original module bytes were restored exactly. The lint implementation only applies its `Math.random` rule to `src/capabilities.js`, not to secret-handling `src/cold/**`.
**Expected:** A security-relevant cold module containing executable `Math.random()` must make the build fail non-zero.
**Required action:** Extend the lint/security gate over the complete cold secret-handling surface, add a regression test that this mutation makes the build exit non-zero, and rerun the independent negative gate.

### F2 — Fresh independent Firefox coverage is incomplete

**Severity:** blocking verification
**Location:** P2.2 Chromium/Firefox browser verification.
**Observed:** Chromium item assertions passed, but the run exited 1 on the Firefox `page.close`/`Browser.removeBrowserContext` protocol error before a P2.2 Firefox item PASS.
**Expected:** The exact reviewed P2.2 head must complete the requested Chromium/Firefox `file://` harness.
**Required action:** Re-run the standard disposable-cache browser workflow on the remediated P2.2 head and obtain a clean Firefox codex32 PASS; diagnose the lifecycle error if it reproduces.

## 5. Verdict rationale

The prior P2.2 documentation and identifier findings are resolved and the artifact reproduces exactly. The item still cannot PASS because its build gate permits executable `Math.random()` in secret-handling code and the fresh independent Firefox run did not complete. Leave P2.2 `[~]`; re-review a new remediation head.

FAIL


## Re-review round 3 — 2026-08-12

**VERDICT: FAIL**

Findings: 1 (1 blocking, 0 advisory — all must be addressed)
Reviewed commit: `3e6f2d34eb251ad37c4d1a88f0e5f8d1bd3069a6`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `672d5ddb511b731798eadf30a14c7abee13d326b25b158e6b690072f1dd16c88`

### 1. What I verified this round

I freshly reviewed the exact PR #49 tip against `main@5f0fa5e442524033e91bb31006a0ca6288385dc0` from a full-history clone and separate detached worktrees.

Independent gates passed: full/remediation `git diff --check`, `npm ci`, vendor verification, lint, docs, the focused codex32 suite, the new lint/build security regression tests, the full 329-test suite, vendor-corruption rejection, and the complete Chromium/Firefox `file://` browser harness.

Three builds were byte-identical across path/timezone/locale variation at `ffcb031c913fb9260dcb98675baa1ea055645e549064ec40578d72038cf34e06`, 2,227,044 bytes.

A reviewer-only executable `Math.random()` mutation in `src/cold/codex32.js` made both lint and build exit non-zero and was restored exactly. The codex32 item-specific browser assertions passed in both engines. The prior round's two findings — the fail-open cold-path `Math.random()` gate and incomplete Firefox evidence — are therefore resolved.

### 2. What I could not verify

The packet's round-3 current build digest does not reproduce from the exact committed head. This is R3-F1.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.2 codex32` | ❌ | Vectors, direct-seed semantics, random identifier behavior, security gates, reproducibility, and Chromium/Firefox all pass, but the packet's current artifact claim does not match the exact reviewed head (R3-F1). |

### 4. Findings

#### R3-F1 — Round-3 packet build evidence does not reproduce from the exact committed head

**Severity:** blocking evidence
**Location:** `docs/05-development/packets/p2.2-codex32.md`, round-3 `Current evidence` build block.
**Observed:** The packet records `dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7` / 2,227,044 bytes. Fresh independent A1, A2, and different-path/TZ/locale builds of exact head `3e6f2d34eb251ad37c4d1a88f0e5f8d1bd3069a6` all produced `ffcb031c913fb9260dcb98675baa1ea055645e549064ec40578d72038cf34e06` / 2,227,044 bytes.
**Expected:** Current packet evidence must reproduce from the exact committed tip under review.
**Required action:** Rebuild from the exact committed remediation state after the build-affecting changes are committed and update the packet/self-review to the actual digest. Request fresh independent re-review afterward.

### 5. Verdict rationale

The two previous P2.2 findings are resolved and the product/security/browser evidence is clean. The stale current artifact claim is still a finding under the repository's binary protocol, so P2.2 remains `[~]` and must not merge.

FAIL


## Re-review round 4 — 2026-08-12

**VERDICT: PASS**

Findings: 0
Reviewed commit: `0bab5eefaa6f52b75bfb4f3c101eadc0e0b0be0a`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `b924639296ec7e9d7f62717b1c5f11b786121beed38c37928701fcb1a3fe5238`

### 1. What I verified

I freshly reviewed PR #49 at exact head `0bab5eefaa6f52b75bfb4f3c101eadc0e0b0be0a` against `main@5f0fa5e442524033e91bb31006a0ca6288385dc0` from a fresh full-history clone.

Independent gates passed:

- Full-PR and round-4-only `git diff --check`.
- `npm ci`, vendor verification, lint, documentation hygiene, focused codex32 tests, security regression tests, and the full suite.
- Three byte-identical builds across two worktrees and different path/timezone/locale settings: `ffcb031c913fb9260dcb98675baa1ea055645e549064ec40578d72038cf34e06`, 2,227,044 bytes.
- The packet's corrected committed-head build claim matches that artifact.
- The complete `file://` browser harness passed in Chromium and Firefox, including the codex32 item-specific assertions.
- A reviewer-only executable `Math.random()` mutation in `src/cold/codex32.js` made both lint and build exit non-zero and was restored byte-for-byte.
- Deliberate vendor-manifest corruption made `verify-vendor` exit non-zero and was restored.
- The independent BIP-93 source/vector material remained consistent with the positive codex32 fixtures and the shipped direct-seed behavior.

The collector's overall exit code was 1 because its provenance helper treated the intentionally invalid checksum string `ms10testsxxxxxxxxxxxxxxxxxxxxxxxxxx4nzvca9cmczlq` as though it were required to be a positive upstream vector. The product test explicitly requires that string to throw a checksum error. I classified this as a collector instrumentation false positive, not a product or packet finding.

All findings from prior independent rounds are resolved.

### 2. What I could not verify

Nothing required by the P2.2 acceptance surface remains unverified in this review.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.2 codex32` | ✅ | Independent BIP-93 vectors/semantics, fail-closed randomness, cold-realm isolation, security gates, reproducible exact-head artifact, and Chromium/Firefox UI behavior all pass. |

### 4. Findings

None.

### 5. Verdict rationale

The round-4 packet now reproduces from the exact committed tip, the previous security and Firefox findings remain resolved, all required independent gates pass, and I have no findings of any severity. P2.2 is independently verified. Per repository governance, its branch ROADMAP marker may be changed from `[~]` to `[x]`. The user explicitly prohibited merging in this review batch, so this PASS records closeout on the branch without merging PR #49.

PASS
