# Review: P2.2 — codex32

**VERDICT: FAIL**

Findings: 2 (2 blocking, 0 advisory — all must be addressed)
Reviewed commit: `3219685bef8f52af0e0abe160ea8ba1b6acfb35f`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-12
Independent evidence bundle SHA-256: `31c56955b824d265deee00d7da2a1a7b2b831c1a56ce64335adea4f43571abe8`

## 1. What I verified

Fresh independent review used a full-history clone and separate detached worktrees. For this item I verified the exact product head and merge base, `git diff --check`, `npm ci`, `npm run verify-vendor`, `npm run lint`, `npm run check-docs`, the focused item test, the full `npm test` suite, two same-worktree builds, and a third build from a different detached path/timezone/locale. Vendor-manifest corruption was also exercised and correctly made `verify-vendor` exit non-zero. The cold CSP probe retained `connect-src 'none'`, found no `allow-same-origin`, and found no storage/network/warm-message surface in the item module.

The three builds were byte-identical and matched the packet exactly: `dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7`, 2,227,044 bytes, including the different path/TZ/locale build.

The previous P2.2 findings are materially remediated on this head: the guide now treats recovery as direct BIP-32 master-seed bytes rather than BIP-39 words, later worksheet/completion/fingerprint/Health surfaces are no longer claimed as current functionality, and the identifier input is blank by default so generation uses fresh randomness unless the user explicitly supplies an identifier.

The focused codex32 suite and full suite both exited 0. Chromium completed the codex32 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, teardown, and existing security regressions.

## 2. What I could not verify

The exact P2.2 head did not complete a fresh Firefox run. After Chromium passed, Playwright exited 1 while closing a Firefox page/browser context with:

`page.close: Protocol error (Browser.removeBrowserContext): can't access property "_maybeDontRestoreTabs", this._windows[aWindow.__SSi] is undefined`

This verification gap is F2.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.2 codex32` | ❌ | Official/focused vectors, full tests, reproducible builds, Chromium behavior, corrected direct-seed semantics, and randomized blank identifier behavior were verified, but the security build gate accepts executable `Math.random()` in the cold module (F1) and fresh Firefox verification did not complete (F2). |

## 4. Findings

### F1 — Build/lint gate fails open to executable `Math.random()` in codex32 security code

**Severity:** blocking security
**Location:** `scripts/lint.js` `scanSourceFile()`; exercised path `src/cold/codex32.js`.
**Observed:** I appended `void Math.random();` to `src/cold/codex32.js` in the disposable review worktree and ran `npm run build`. It exited **0**. The original module bytes were restored exactly. The lint implementation only applies its `Math.random` rule to `src/capabilities.js`, not to secret-handling `src/cold/**`.
**Expected:** A security-relevant cold module containing executable `Math.random()` must make the build fail non-zero.
**Required action:** Extend the lint/security gate over the complete cold secret-handling surface, add a regression test that this mutation makes the build exit non-zero, and rerun the independent negative gate.

### F2 — Fresh independent Firefox coverage is incomplete

**Severity:** blocking verification
**Location:** P2.2 Chromium/Firefox browser verification.
**Observed:** Chromium item assertions passed, but the run exited 1 on the Firefox `page.close`/`Browser.removeBrowserContext` protocol error before a P2.2 Firefox item PASS.
**Expected:** The exact reviewed P2.2 head must complete the requested Chromium/Firefox `file://` harness.
**Required action:** Re-run the standard disposable-cache browser workflow on the remediated P2.2 head and obtain a clean Firefox codex32 PASS; diagnose the lifecycle error if it reproduces.

## 5. Verdict rationale

The prior P2.2 documentation and identifier findings are resolved and the artifact reproduces exactly. The item still cannot PASS because its build gate permits executable `Math.random()` in secret-handling code and the fresh independent Firefox run did not complete. Leave P2.2 `[~]`; re-review a new remediation head.

FAIL
