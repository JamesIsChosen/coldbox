# Review: P0.11 — Vault format v1

**VERDICT: PASS**

Findings: 0
Reviewed commit: `456702740f12daa1302623f0cad00db65b2ab2eb`
Commit message: `docs(vault): reconcile P0.11 review evidence`
Immediate predecessor: `1eecfe8160491ac0943e66b91fb27c211ff4c477`
PR: #19
Branch: `p0.11-vault-format`
Base: `main`
Reviewed by: GPT-5.6 Thinking, independent reviewer
Date: 2026-08-04

This is a fresh independent current-tip review. It supersedes the prior FAIL for the current PR tip `456702740f12daa1302623f0cad00db65b2ab2eb` while preserving the historical disposition of F1-F11 below. The documentation-only successor did not rewrite history into a PASS: before this review, the committed reviewer-authored report still ended in FAIL and recorded F6 and F11 as open.

P0.11 is human-merge-only. This review did not merge, commit, push, rebase, retarget, reset, stash, or rewrite history.

## 0. Human merge handoff

The review verdict is PASS, but P0.11 touches the vault format and therefore remains reserved for a human merge under `docs/05-development/review-protocol.md`.

If the human elects to merge after reviewing this report and the PR diff:

```powershell
gh pr merge 19 --merge --delete-branch
git checkout main
git pull
git fetch --prune
```

The reviewer did not run those commands.

## 1. What I verified

### Current-tip identity, base, cleanliness, and documentation-only successor

Before substantive review, the reviewer-directed host run established all required identity gates:

```text
PR #19 state: open
PR base: main
PR head branch: p0.11-vault-format
GitHub PR head SHA: 456702740f12daa1302623f0cad00db65b2ab2eb
git ls-remote origin p0.11-vault-format:
  456702740f12daa1302623f0cad00db65b2ab2eb
origin/p0.11-vault-format:
  456702740f12daa1302623f0cad00db65b2ab2eb
review checkout HEAD:
  456702740f12daa1302623f0cad00db65b2ab2eb
working tree: clean
```

The successor commit was independently checked:

```text
parent:
  1eecfe8160491ac0943e66b91fb27c211ff4c477
subject:
  docs(vault): reconcile P0.11 review evidence
changed paths:
  docs/05-development/packets/p0.11-vault-format.review.md
  docs/05-development/packets/p0.11-vault-format.self-review.md
```

`git diff --check` passed for both the successor and the full `main...HEAD` PR diff. No P0.12/P0.13 implementation path appears in the PR path set.

The repository does not contain `docs/05-development/browser-runner-flow.md`. I did not invent or silently substitute that path. A repository-governance search over `AGENTS.md`, `review-protocol.md`, `ROADMAP.md`, `testing.md`, and `build.md` found no requirement for that literal file, so its absence is not a finding.

### Documents and diff

I read the requested current-tip versions of:

- `AGENTS.md`
- `docs/05-development/review-protocol.md`
- `docs/05-development/ROADMAP.md`
- `docs/01-spec/SPEC.md`
- `docs/01-spec/architecture.md`
- `docs/01-spec/vault-format.md`
- `docs/02-security/threat-model.md`
- `docs/05-development/testing.md`
- `docs/05-development/packets/p0.11-vault-format.md`
- `docs/05-development/packets/p0.11-vault-format.self-review.md`
- `docs/05-development/packets/p0.11-vault-format.review.md`

I also inspected `scripts/run-browser-harness.js`, the P0.11 implementation sources/tests, the full PR path set, and the repository's actual build/testing/review documentation.

### F6 — current evidence consistency

F6 is resolved.

The live PR #19 body now states:

```text
Base: main
Branch: p0.11-vault-format
Current tip: 456702740f12daa1302623f0cad00db65b2ab2eb
Focused P0.11 vault tests: 10 passed / 0 failed
Full Node suite: 49 passed / 0 failed
Chromium/Firefox file:// browser harness: PASS
Artifact bytes: 344415
Artifact SHA-256:
dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553
Alternate path/locale/timezone reproducibility: PASS, byte-identical
Corrupted-vendor verifier negative control: PASS, non-zero
Corrupted-vendor build negative control: PASS, non-zero
F11 remains open pending fresh independent execution
P0.11 remains human-merge-only
```

The PR body explicitly identifies its Chromium/Firefox result as author/remediation evidence rather than a substitute for independent review. The reviewer-run superseded-token scan found no old current-evidence placeholders/values in the live body.

The current `p0.11-vault-format.self-review.md` contains one canonical current-evidence record. It explicitly says the previous layout mixed stale “Current-tip” sections, intentionally does not repeat those historical measurements as current evidence, preserves the reviewer-authored FAIL, and leaves F11 for a fresh independent reviewer.

The committed `p0.11-vault-format.review.md` at the start of this review still carried the prior reviewer-authored FAIL for implementation tip `1eecfe8`, including F6 and F11. The documentation successor therefore preserved review history rather than rewriting it into a fabricated PASS.

Historical/superseded evidence is not treated as a finding where it is clearly labeled historical.

### F1-F10 substantive re-verification

Independent source inspection plus a reviewer-authored source probe and the focused tests established:

- **F1 resolved:** vault creation consumes the `profileId` reported by the actual derivation when constructing header KDF metadata; it does not use mutable active/request state for that decision.
- **F2 resolved:** all public vault entry points fail closed unless the cold health gate proves cold realm readiness, CSP canary success, runtime network guard installation, randomness capability, AES-GCM crypto health, green airgap state, no lockdown, and allowed vault-operation state.
- **F3/F4 resolved by scope removal:** `createVaultSession`, `openVaultSession`, public `openSession`, session persistence/save/load behavior, and explicit P0.13 session tests are absent. F3 was not “fixed” by retaining hardened session code.
- **F5 resolved:** the independently reproduced production artifact is exactly 344415 bytes with SHA-256 `dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553`.
- **F6 resolved:** live PR/current packet/current self-review/current artifact/current test/browser evidence is coherent, with historical evidence clearly distinguished.
- **F7 resolved:** unknown explicitly requested and unknown derivation-reported KDF profiles fail closed rather than normalizing to Standard.
- **F8 resolved:** `cryptoLayer.profiles` is the vault runtime KDF-parameter source, including fallback.
- **F9 resolved:** vault mode consumes the established airgap snapshot; `vault.js` has no independent `navigator.onLine` or `navigator.connection` path.
- **F10 resolved:** the public 64 MiB file-size refusal is distinct from authentication/corruption failure.

The same review independently confirmed:

- online opening does not enter the secret HKDF/decrypt path;
- all 65 header bytes are authentication-sensitive as AAD;
- wrong-passphrase and authenticated-corruption failures remain indistinguishable;
- compartment padding remains on 64 KiB boundaries;
- secret-subkey derivation is not publicly exported;
- the vault API remains confined to the cold layer;
- no P0.12 benchmark implementation is present;
- no P0.13 session/save/load implementation is present.

### F11 — pinned-toolchain and browser execution

F11 is resolved by reviewer-directed independent execution on the Windows development host. The human physically executed reviewer-authored runner v1.4; the reviewer independently inspected the returned raw evidence bundle.

Evidence bundle SHA-256:

```text
eb9967b42947b8e408b4f4ad676ed96a79b4672a052bb669db1627c0651b7579
```

Pinned environment and dependencies:

```text
git: 2.54.0.windows.1
node: v24.16.0
npm: 11.13.0
.nvmrc: 24.16.0
package engines.node: 24.16.0
package devDependencies.playwright: 1.62.1
locked playwright: 1.62.1, dev=true
locked playwright-core: 1.62.1, dev=true
npm ci: PASS
installed Playwright: 1.62.1
Chromium/Firefox installation: PASS
```

Upstream and offline vendor verification both passed for all pinned vendored packages.

Lint passed.

Focused P0.11 tests:

```text
tests 10
pass 10
fail 0
```

Complete Node suite:

```text
tests 49
pass 49
fail 0
```

Two primary deterministic builds independently reproduced:

```text
bytes: 344415
SHA-256:
dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553
```

A detached alternate worktree used a different filesystem path containing spaces, `TZ=Pacific/Honolulu`, and `LANG`/`LC_ALL=fr_FR.UTF-8`. Its build was byte-identical:

```text
bytes: 344415
SHA-256:
dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553
```

Reviewer-controlled vendor corruption in a separate disposable worktree produced the required non-zero failures:

```text
offline vendor verifier exit: 1
build exit: 1
build message:
  Build refused: vendored artifacts failed offline verification
```

The Chromium and Firefox `file://` browser harness independently passed. Among the explicitly observed deliberate controls in **both** engines:

- cold-realm creation failure entered explicit lockdown;
- cold-realm readiness timeout removed the frame and locked down;
- typed handshake timeout locked down;
- cold-only CSP failure locked down and refused vault operations;
- warm-only CSP failure locked down and refused vault operations;
- both-CSP failure locked down and refused vault operations;
- missing `getRandomValues` locked down and refused vault operations;
- built-artifact byte tampering triggered `script-src` rejection and prevented execution;
- deliberate CSP violation was detected;
- byte-tampered inline script rejection was reported;
- cold-realm fetch, XMLHttpRequest, WebSocket, EventSource, and sendBeacon were runtime-blocked.

The harness also proved:

```text
Playwright is dev-only; dependency-free build matches byte-for-byte
Browser harness passed in Chromium and Firefox.
```

After the browser run, the production artifact still measured 344415 bytes with the same SHA-256.

Final review state:

```text
HEAD:
456702740f12daa1302623f0cad00db65b2ab2eb

origin/p0.11-vault-format:
456702740f12daa1302623f0cad00db65b2ab2eb

git diff --check: PASS
working tree: clean
```

## 2. What I could not verify

Nothing required for P0.11 or for this review remains unverified.

No physical iOS Safari, Android Chrome, macOS Safari/Chrome, Linux Firefox, or Tor Browser result is inferred here; the roadmap assigns the physical-device/browser workflow matrix to P0.19, not P0.11.

## 3. Acceptance criteria — verbatim from ROADMAP.md

| # | Verbatim roadmap criterion | Met? | Independent evidence |
|---|---|---|---|
| 1 | `Serializer and parser per [vault-format.md](../01-spec/vault-format.md): header, AAD, multi-record wrapped-DEK block, two compartments, HKDF subkeys, 64 KiB padding.` | ✅ | Independent source inspection, reviewer probe, 10/10 focused vault tests, exact artifact reproduction, and Chromium/Firefox cold-boundary execution verify the v1 header/AAD layout, wrapped-DEK records, public/secret compartments, HKDF separation, and 64 KiB padding. |
| 2 | `Accept: round-trips; tampering with any header byte fails authentication; wrong passphrase and corrupted file are indistinguishable in the error; padding always lands on a 64 KiB boundary; the secret subkey has no derivation path reachable while online.` | ✅ | Independent focused tests and source probe verify round-trip behavior, all 65 header bytes as authentication-sensitive AAD, one generic wrong/corrupt authentication boundary, 64 KiB padding, and no online entry into secret-subkey derivation/decryption. Chromium/Firefox independently verify the cold-only boundary and fail-closed runtime controls. |

## 4. Historical F1-F11 disposition

| Historical finding | Current-tip disposition |
|---|---|
| F1 — header KDF could reflect mutable module/request state rather than actual derivation | **RESOLVED** — header metadata consumes the derivation-reported profile. |
| F2 — vault entry points could operate without proven cold health | **RESOLVED** — every entry point is gated on the complete required cold health state and fails closed. |
| F3 — retained session could preserve secret-capable state across mode change | **RESOLVED BY SCOPE REMOVAL** — the premature session primitive is absent. |
| F4 — P0.13 session/save/load implementation leaked into P0.11 | **RESOLVED** — session APIs, persistence behavior, and P0.13 tests are absent. |
| F5 — bundle/artifact evidence was stale | **RESOLVED** — independently reproduced 344415 bytes / `dfd88b...f9553`. |
| F6 — live PR/self-review evidence was stale and contradictory | **RESOLVED** — live PR, packet, self-review, browser provenance, build/test evidence, and current tip are coherent; history remains clearly labeled. |
| F7 — unknown KDF profiles silently normalized to Standard | **RESOLVED** — unknown requested/reported profiles fail closed. |
| F8 — duplicate vault runtime KDF parameter table | **RESOLVED** — `cryptoLayer.profiles` is the single runtime source, including fallback. |
| F9 — vault maintained an independent navigator network-state path | **RESOLVED** — vault consumes the established airgap snapshot. |
| F10 — 64 MiB refusal was conflated with authentication/corruption | **RESOLVED** — size refusal is distinct. |
| F11 — independent pinned-toolchain/upstream/Chromium/Firefox verification was incomplete | **RESOLVED** — reviewer-directed v1.4 execution completed every required gate on Node 24.16.0 with locked Playwright 1.62.1, upstream access, Chromium, and Firefox; raw evidence was independently inspected. |

## 5. Findings

None.

## 6. Verdict rationale

All P0.11 roadmap acceptance criteria are met verbatim and independently verified. The original substantive findings F1-F5 and F7-F10 remain resolved; F6 is now resolved by coherent live/canonical evidence without erasing historical FAIL records; and F11 is now resolved through complete reviewer-directed execution on the pinned Windows host. Upstream and offline vendor verification, lint, 10/10 focused tests, 49/49 full Node tests, deterministic and alternate-environment builds, exact artifact size/hash, deliberate corrupted-vendor non-zero failures, Chromium/Firefox `file://` security fixtures, Playwright dev-only/zero-production-build contribution, and final clean-tree/tip checks all pass. I have no finding, advisory, cosmetic concern, documentation inconsistency, scope issue, or unresolved review uncertainty.

P0.11 remains human-merge-only. The reviewer does not merge it; the merge decision is handed back to the human.

PASS
