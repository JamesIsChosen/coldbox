# Review: P1.5 — EVM and arbitrary-path derivation

**VERDICT: PASS**

Findings: 0. No acceptance, security, reproducibility, documentation, or scope findings remain after one review round.

Reviewed commit: `30d57fc` (`Document P1.5 verification evidence`)

Reviewed by: Codex self-review gate (same session; independent-review filter applied)

Date: 2026-08-10

Review rounds: 1

## 1. What I verified

I re-read `docs/05-development/review-protocol.md` before this gate. The implementation commit is `981778c`; the reviewed commit adds only the required packet, so the code under review is unchanged by the packet commit.

The focused derivation suite passed six tests:

```text
node --test test/derivation.test.js
ℹ tests 6
ℹ pass 6
ℹ fail 0
```

The full repository suite passed:

```text
npm.cmd test
ℹ tests 272
ℹ pass 272
ℹ fail 0
```

Repository lint and documentation checks passed:

```text
npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 146 markdown file(s) checked, 0 warning(s).
```

Vendored artifacts passed local and upstream verification:

```text
npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.
```

Two production builds under different locale/time-zone settings produced the same hash:

```text
f8b447caa324bf74dbf700b2d99dd0ad842923e60f4f3f4ee2cfa8b7a45a49a7
f8b447caa324bf74dbf700b2d99dd0ad842923e60f4f3f4ee2cfa8b7a45a49a7
```

A separate disposable copy at `C:\Users\semaj\AppData\Local\Temp\coldbox-p1.5-review-20260810`, built with `LC_ALL=fr-FR` and `TZ=Europe/Paris`, produced the same hash. A deliberate `eval` insertion into that copy was rejected with `tampered-build-exit=1`. The disposable copy was removed afterward.

The Chromium and Firefox `file://` browser harness passed. It exercised the integrated cold boundary, CSP/network blocking, vault lock/zeroization, Seed Forge, provider neutering, provenance, help content, and tamper paths. This item has no UI flow or protocol message to exercise.

## 2. Acceptance criteria checked verbatim

The roadmap contains this exact P1.5 line and no separate acceptance bullet:

> P1.5 — Derivation: EVM and generic arbitrary-path mode

The title is met by these checked outcomes:

| Wording in the roadmap title | Review evidence |
| --- | --- |
| EVM derivation | Standard account/change/index path, bounded batch, account xpub output, and account-level xpub watch-only derivation. The external ethereumjs fixture checks the private key and address at `m/44'/60'/0'/0/0`. |
| EVM address encoding | Keccak-256 over the uncompressed secp256k1 public-key body and ERC-55 casing match the official specification vectors. |
| Generic arbitrary-path mode | Canonical BIP-32 seed paths return cold-local raw keys, xprv/xpub, and compressed WIF; public extended-key paths reject hardened children and contain no private fields. |

No roadmap scope was reinterpreted or omitted. No later registry, clipboard, QR, or hardware workflow was pulled into this branch.

## 3. Security and scope review

- The secret-bearing generic result is cold-local only and was not added to the protocol schema.
- Public EVM and xpub results contain no private key, xprv, seed, passphrase, or arbitrary prose.
- Keccak is from the existing pinned vendor artifact; the cold dependency guard fails closed if it is absent.
- Invalid addresses, malformed paths, hardened public paths, wrong account depth, and oversized batches fail closed.
- No CSP relaxation, network request, runtime dependency, or warm secret storage was added.
- The branch contains one roadmap item only.
- The roadmap status is `[~]`, not `[x]`.

## 4. Findings

None. Count: 0.

The known device/platform gaps are recorded in the packet as untested environments, not hidden support claims. They do not block the P1.5 engine criteria, and P1.9 owns final hardware-wallet validation.

## 5. Verdict rationale

PASS. The implementation meets the literal P1.5 scope, uses independent ERC-55 and ethereumjs fixtures, preserves the two-realm boundary even for the intentionally secret-bearing generic API, builds reproducibly, passes the full suite and browser harness, and rejects deliberate source tampering. A future reviewer must still apply the repository's independent review protocol; this same-session self-review is not an independent approval and does not authorize marking the roadmap item `[x]` or merging the PR.
