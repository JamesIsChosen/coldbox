# Batch run 2026-08-03

**Scope requested:** next roadmap item onward, starting at P0.5
**Completed:** P0.5, P0.6, P0.7, P0.8
**Stopped at:** P0.8 publication — reason below

## Items

| Item | Branch | PR | Self-review | Rounds |
|------|--------|-----|------|--------|
| P0.5 | p0.5-warm-shell-skeleton | #13, open against `main` | PASS | 1 |
| P0.6 | p0.6-cold-realm-bootstrap | #14, open against `p0.5-warm-shell-skeleton` | PASS | 2 |
| P0.7 | p0.7-message-handshake | #15, open against `p0.6-cold-realm-bootstrap` | PASS | 1 |
| P0.8 | p0.8-csp-canary-airgap-guard | Not opened — `gh` unavailable in the execution environment | PASS | 1 |

## Why it stopped

P0.8 is complete locally, its packet and self-review PASS report are committed, and its branch was pushed successfully to `origin/p0.8-csp-canary-airgap-guard`. The external GitHub CLI is not installed in the execution environment, so the stacked PR could not be created. No merge was attempted and P0.9 was not started.

This is a hard external-publication stop, not a code, test, or review failure.

## Review these in dependency order

PR #13 (`main` → P0.5) → PR #14 (`p0.5-warm-shell-skeleton` → P0.6) → PR #15 (`p0.6-cold-realm-bootstrap` → P0.7) → P0.8 (`p0.7-message-handshake` → pending PR)

After P0.8 is published, its recorded self-review PASS must be available to the human reviewer before any merge or P0.9 work.

## What I'd scrutinise hardest

P0.8's exact canary sequencing and fail-closed behavior: the warm and cold documents must accept only the deliberate `connect-src` violation for the invalid canary URL; the cold realm must run its native `fetch` canary before installing the five non-configurable runtime blockers; a missing/stripped CSP or failed blocker installation must refuse vault operations; and a runtime violation must drive the red lockdown state. Also scrutinise the green offline/amber online transitions, the five-second bound, and the unchanged warm allowlist/cold `connect-src 'none'` policy.

## Known-weak areas

- No item was tested on real iOS Safari, Android Chrome from Files, macOS Safari, Linux Firefox, or Tor Browser; P0.19 owns the physical matrix.
- `navigator.onLine` reports interface state rather than guaranteed Internet reachability; the security guarantee remains the cold CSP and runtime guard.
- The `.invalid` canary deliberately produces expected browser network noise; the harness allowlists only canary-specific messages.
- The local-file artifact omits the header-only `frame-ancestors` directive from the child meta policy; the limitation and compensating sandbox/frame controls are documented in `docs/02-security/csp-policy.md`.
- P0.8 is pushed but its PR remains unopened until the required `gh pr create` command is run.
- All four PRs remain unmerged. This batch never merges.

## Required publication command

```powershell
gh pr create --repo JamesIsChosen/coldbox --base p0.7-message-handshake --head p0.8-csp-canary-airgap-guard --title "P0.8 - CSP canary and airgap guard" --body-file 'C:\Users\semaj\Projects\coldbox\docs\05-development\packets\p0.8-csp-canary-airgap-guard.md'
```
