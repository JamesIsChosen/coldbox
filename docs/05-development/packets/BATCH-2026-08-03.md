# Batch run 2026-08-03

**Scope requested:** next roadmap item onward, starting at P0.5
**Completed:** P0.5, P0.6, P0.7
**Stopped at:** P0.7 publication — reason below

## Items

| Item | Branch | PR | Self-review | Rounds |
|------|--------|-----|------|--------|
| P0.5 | p0.5-warm-shell-skeleton | #13, open against `main` | PASS | 1 |
| P0.6 | p0.6-cold-realm-bootstrap | #14, open against `p0.5-warm-shell-skeleton` | PASS | 2 |
| P0.7 | p0.7-message-handshake | Not opened — push approval blocked | PASS | 1 |

## Why it stopped

P0.7 is complete locally, its packet and self-review PASS report are committed, and its correct base is `p0.6-cold-realm-bootstrap`. The environment rejected publishing this new private branch to the configured GitHub remote because explicit authorization for this branch payload and destination was required. The branch was not pushed, no P0.7 PR was created, and P0.8 was not started. No merge was attempted.

This is a hard external-publication stop, not a code or test failure.

## Review these in dependency order

PR #13 (`main` → P0.5) → PR #14 (`p0.5-warm-shell-skeleton` → P0.6) → P0.7 (`p0.6-cold-realm-bootstrap` → pending PR)

After P0.7 is published, its recorded self-review PASS must be available to the human reviewer before any merge or P0.8 work.

## What I'd scrutinise hardest

P0.7's handshake sequencing and schema boundary: the bootstrap signal must be payload-free, the port transfer must happen once, the typed ready reply must arrive on the private port, unknown fields must be stripped, and no secret-bearing field may cross the boundary. Also scrutinise the post-handshake global-message discard/log behavior and the P0.6 inherited CSP hash set.

## Known-weak areas

- No item was tested on real iOS Safari, Android Chrome from Files, macOS Safari, Linux Firefox, or Tor Browser; P0.19 owns the physical matrix.
- The local-file artifact omits the header-only `frame-ancestors` directive from the child meta policy; the limitation and compensating sandbox/frame controls are documented in `docs/02-security/csp-policy.md`.
- P0.7's payload-free `cold.ready` signal remains only as a liveness bootstrap; application traffic requires the transferred port and typed validator.
- P0.7 remains local until the required push and PR commands below are run.

## Required publication commands

```powershell
git -C 'C:\Users\semaj\Projects\coldbox' push -u origin p0.7-message-handshake
gh pr create --repo JamesIsChosen/coldbox --base p0.6-cold-realm-bootstrap --head p0.7-message-handshake --title "P0.7 - MessageChannel handshake and schema validator" --body-file 'C:\Users\semaj\Projects\coldbox\docs\05-development\packets\p0.7-message-handshake.md'
```
