# Batch run 2026-08-03

**Scope requested:** next roadmap item onward, starting at P0.5
**Completed:** P0.5, P0.6
**Stopped at:** P0.6 publication — reason below

## Items

| Item | Branch | PR | Self-review | Rounds |
|------|--------|-----|------|--------|
| P0.5 | p0.5-warm-shell-skeleton | #13, open against `main` | PASS | 1 |
| P0.6 | p0.6-cold-realm-bootstrap | Not opened — push approval blocked | PASS | 2 |

## Why it stopped

P0.6 is complete locally, its packet and self-review PASS report are committed, and its correct base is `p0.5-warm-shell-skeleton`. The environment rejected publishing this new branch to the configured GitHub remote because explicit authorization for this branch and destination was required. The branch was not pushed, no P0.6 PR was created, and P0.7 was not started.

This is a hard external-publication stop, not a code or test failure. No merge was attempted.

## Review these in dependency order

PR #13 (`main` → P0.5) → P0.6 (`p0.5-warm-shell-skeleton` → pending PR)

After P0.6 is published, it must receive an independent review before any merge or P0.7 work.

## What I'd scrutinise hardest

P0.6's inherited CSP hash set and opaque sandbox boundary: the outer policy must allow only the exact child hashes needed for `srcdoc` inheritance, while the child retains `connect-src 'none'` and the iframe remains free of `allow-same-origin`. Also scrutinise the terminal failure latch so a late readiness signal cannot reopen a failed boot.

## Known-weak areas

- No item was tested on real iOS Safari, Android Chrome from Files, macOS Safari, Linux Firefox, or Tor Browser; P0.19 owns the physical matrix.
- The local-file artifact omits the header-only `frame-ancestors` directive from the child meta policy; the limitation and compensating sandbox/frame controls are documented in `docs/02-security/csp-policy.md`.
- P0.6's provisional `cold.ready` signal is intentionally not a protocol. P0.7 must replace it with the typed MessageChannel handshake and validator before secret-bearing operations exist.
- P0.6 remains local until the required push and PR commands below are run.
