# Self-review: P0.7 — MessageChannel handshake and schema validator

**VERDICT: PASS**

Findings: 0
Reviewed commit: 01696bf
Reviewed by: Codex self-review gate (not an independent PR review)
Date: 2026-08-03
Rounds: 1

## 1. What I verified

- Re-read `docs/05-development/review-protocol.md` and inspected the complete P0.7 diff from the corrected P0.6 dependency.
- Confirmed `git diff p0.6-cold-realm-bootstrap...HEAD -- docs/05-development/ROADMAP.md` changes exactly one roadmap item: P0.7.
- Ran `npm ci`: `added 2 packages, and audited 3 packages`; `found 0 vulnerabilities`.
- Ran `npm run verify-vendor`: all six local vendor artifacts and all six upstream releases verified.
- Ran `npm run lint`: `Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.`
- Ran `npm test -- --test-concurrency=1`: 29 tests passed, 0 failed.
- Ran `npm run build`, removed only the generated `build/` directory, and ran `npm run build` again: both emitted `384c21a8942c0fa31f972f70cf3f60e1ae4de3ff052e7faea706e0343dd37f31`.
- Built a clean copied tree under a different temporary path with `LC_ALL=de-DE` and `TZ=Pacific/Honolulu`: the SHA-256 was `384C21A8942C0FA31F972F70CF3F60E1AE4DE3FF052E7FAEA706E0343DD37F31`.
- Ran `npm run test:browser`: Chromium and Firefox passed private-channel readiness, exact port/source checks, visible warm/cold anomaly warnings, global-message discard, cold CSP/opacity/network checks, creation failure, readiness timeout, and distinct typed-handshake-timeout lockdown.
- Added `eval` to a temporary LF-preserving copy of `src/cold/main.js`: the build reported `Forbidden construct "eval"` and returned exit code `1`.
- Confirmed Node protocol tests cover every message type, unknown-field stripping, the documented 14-collection allowlist, xprv/WIF/mnemonic-shaped content rejection, 4 MiB/64 MiB bounds, and a mutation fixture that fails when either post-handshake discard or transferred-port checks are removed.

## 2. What I could not verify

No automated P0.7 criterion remained unverifiable in Chromium, Firefox, and Node. Manual Safari, mobile, macOS, Linux, Tor, and physical-device behavior remains untested and is explicitly assigned to P0.19; no result is inferred from the automated runs.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Handshake transferring a port; typed whitelist schema per [architecture.md](../../01-spec/architecture.md); global `message` handler ignored after handshake. | ✅ | Both browser engines establish the typed private channel, and Node/browser guards require the source, transferred-port count, validator, and post-handshake discard branches. |
| 2 | Schema rejects unknown types and strips unknown fields. | ✅ | Protocol unit tests reject unknown types and verify unknown envelope, payload, collection, and public-record fields are absent from sanitized output. |
| 3 | A test asserts **no message type can carry a mnemonic, private key, xprv, passphrase, or secret-compartment plaintext**. | ✅ | Every cold-to-warm type is tested with secret field names; allowed text fields are also tested with xprv-shaped, WIF-shaped, and a 12-word BIP-39 test mnemonic. Over-limit messages are rejected before crossing the boundary. |
| 4 | Messages injected on the global handler post-handshake are discarded and logged. | ✅ | Chromium and Firefox inject messages into both realms and observe counters, fixed warnings, and visible warning elements; no injected message reaches the private protocol. |

## 4. Findings

None.

## 5. Verdict rationale

The private channel is source- and port-gated, the schema is allowlisted and fail-closed against both secret names and recognizable secret content, the public collection list matches the data model, aggregate payload bounds prevent unbounded cross-realm allocation, and anomaly handling is logged and visible. Reproducibility, vendor verification, lint, Node tests, browser tests, and deliberate non-zero failure checks all pass. **PASS.**
