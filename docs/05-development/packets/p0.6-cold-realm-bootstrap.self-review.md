# Self-review: P0.6 — Cold realm bootstrap

**VERDICT: PASS**

Findings: 0
Reviewed commit: c6d6cc2
Reviewed by: Codex self-review gate (not an independent PR review)
Date: 2026-08-03
Rounds: 2

## 1. What I verified

- Re-read `docs/05-development/review-protocol.md` and inspected the complete P0.6 diff from the corrected P0.5 dependency.
- Confirmed `git diff p0.5-warm-shell-skeleton...HEAD -- docs/05-development/ROADMAP.md` changes exactly one roadmap item: P0.6.
- Ran `npm ci`: `added 2 packages, and audited 3 packages`; `found 0 vulnerabilities`.
- Ran `npm run verify-vendor -- --offline`: all six pinned local vendor artifacts verified; upstream release verification was not exercised offline.
- Ran `npm run lint`: `Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.`
- Ran `npm test -- --test-concurrency=1`: 20 tests passed, 0 failed.
- Ran `npm run build`: emitted `9592e53ec350bec2944e8b6ca621c48733ed54a2e21b844ab44ff0745dba5241` (48,231 bytes). The browser harness also confirmed the dependency-free build matches byte-for-byte.
- Built a clean copied tree under a different temporary path with `LC_ALL=ja-JP` and `TZ=Asia/Tokyo`: the SHA-256 was `33D1E173DB3767A22BDBD40DDD25A4B3B9CAD29722C6F193FA81C8FAB44A7C07`.
- Ran `npm run test:browser`: Chromium and Firefox passed the exact sandbox, child CSP hashes, per-primitive CSP violation correlation for fetch/XHR/WebSocket, DOM/variable/contentDocument opacity checks, routes, and both creation-failure and suppressed-ready timeout lockdown fixtures.
- Added `eval` to a temporary LF-preserving copy of `src/cold/main.js`: the build reported `Forbidden construct "eval"` and returned exit code `1`.
- Confirmed the P0.6 acceptance wording and implementation now restore the original literal throw contract: `fetch`, XHR, and WebSocket probes all report `threw` after their native CSP-blocked attempts, while the standalone native fixture preserves exact browser-specific CSP evidence. No ADR rewrites that contract.

## 2. What I could not verify

No automated P0.6 criterion remained unverifiable in the available Chromium and Firefox environment. Manual Safari, mobile, macOS, Linux, Tor, and header-delivered CSP behavior remain intentionally untested; the roadmap assigns physical-device confirmation to P0.19, and the product artifact is a local file rather than an HTTP deployment.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `srcdoc` iframe with `sandbox="allow-scripts allow-downloads"` and its own CSP including `connect-src 'none'`. | ✅ | The build assembles the cold document, injects its exact child hashes into the parent, and the Node/browser checks confirm the exact sandbox, no `allow-same-origin`, and the child policy. A negative fixture adding `allow-top-navigation` fails the exact-token assertion. |
| 2 | **Accept:** iframe instantiates; `fetch`, `XHR`, and `WebSocket` inside it are blocked by the cold CSP; warm shell cannot read its DOM or variables; **app fails closed with an explanation if the iframe cannot be established.** | ✅ | Both engines show a matching `connect-src` violation for each primitive, reject or omit DOM/marker/contentDocument reads, and lock down with no attached frame for both creation failure and a suppressed-ready timeout. |

## 4. Findings

None.

## 5. Verdict rationale

The cold realm is an opaque, exact-token sandbox with its own hash-pinned `connect-src 'none'` policy. The browser checks are tied to the policy event rather than a DNS-sensitive error shape, timeout failure removes any child frame before exposing lockdown, build determinism and fail-closed lint remain intact, and the branch changes exactly P0.6 on top of corrected P0.5. **PASS.**
