# P0.17 — Help framework

Branch: `p0.17-help-framework`
Roadmap item: [P0.17](../ROADMAP.md) (deps: P0.5, satisfied)

**This packet supersedes two prior reviewed versions of PR #29, both of which returned FAIL:**
- **R1** (commit `3c778ef`): 4 findings (1 blocking, 3 advisory).
- **R2** (commit `5af0991`): 3 findings (2 blocking, 1 advisory) — R1's F2 and F4 were confirmed closed, but F1 was only partially fixed (one of four affected build-root fixtures), and the reviewer's real-Chromium probe found a genuine product defect (R2-F2: 3 of 5 contextual help buttons destroyed by runtime DOM writes) that no prior review layer had caught.

All findings from both rounds are fixed in this revision (commit noted at push time). **One gap remains, disclosed rather than hidden:** none of this session's fixes have been run end-to-end through an actual browser, because this authoring sandbox still cannot download Playwright's binaries (`cdn.playwright.dev` returns `403` under its network allowlist, reconfirmed this session). The R2 reviewer already proved, with real network access, that Chromium and Firefox both install and launch fine in this repository, and that a real browser can reach and exercise this exact Learn-page functionality — so there's now direct evidence the *implementation* works, even though the specific fixes in this revision haven't personally been watched running.

## 1. Summary

Implements the P0.17 build-time compiler (`scripts/help-content.js`), the three-depth (`::: plain`/`::: working`/`::: technical`) markdown block model, and the in-app Learn page (depth switcher, offline search, contextual `?` help on five panels, inline tap-to-define glossary). The roadmap's P0.1–P0.16 backfill obligation is fully met: all 51 glossary entries and all 9 guides carry three-depth content, `npm run build` produces zero help-content warnings, and — as of this revision — all five contextual help buttons survive real application initialization rather than three of them being silently deleted by runtime DOM writes.

## 2. Scope

**In (cumulative across all three packet revisions):** `scripts/help-content.js` (compiler), `scripts/build.js` (wiring + warnings), `scripts/lint.js` (tooling registration), `src/index.html` (Learn page markup; five contextual `?` buttons, now each wrapping its dynamic title text in a dedicated child `<span>` — see §4b R2-F2), `src/main.js` (depth preference, search, inline glossary, contextual navigation; three title-lookup variables retargeted to the new child spans — see §4b), `src/styles.css` (Learn page + inline glossary styling), `docs/00-overview/glossary.md` (full rewrite, 51 entries three-depth), all nine `docs/03-guides/` files, `scripts/run-browser-harness.js` (`verifyHelpFramework`; a shared `copyBuildInputsInto()` helper now used by all four temporary build-root fixtures — see §4b R2-F1; new regression coverage asserting all five contextual buttons survive initialization), `test/help-content.test.js` (21 tests), `test/build.test.js` (`docs/` fixture copy; F2 regex fix plus adversarial test), `test/provenance.test.js` (`docs/` fixture copy), ADR-0016, ROADMAP.md, CHANGELOG.md, SPEC.md's bundle-budget table.

**Deliberately not in this PR, and why:**
- **CI wiring** (doc-hygiene's automated "help content missing a depth block: Fail" check) is P0.18's job, not this item's.
- No change to the vault format, message schema, realm boundary, or any cryptographic path.

## 3. How to verify

This section reflects the final state after both rounds of remediation — every number below was re-run fresh against the actual current tip, not carried forward from an earlier commit. That exact mistake (carrying forward pre-fix numbers) was R2-F3, found in the previous packet revision; this section exists specifically to not repeat it.

**Toolchain note, disclosed plainly:** the repository pins Node `24.16.0`. This authoring sandbox only has Node `22.22.3` available and cannot download `24.16.0` (`nodejs.org` and the `nvm` installer are both blocked by this sandbox's network allowlist). Everything below was run under Node `22.22.3`. In the R2 review, every figure from this sandbox reproduced exactly against the reviewer's own Node-24.16.0 numbers wherever a like-for-like comparison was possible (the `main`-branch baseline size, in particular) — direct evidence this isn't a Node-version-sensitive build in the ways that matter here — but a maintainer with the pinned runtime should still confirm before merge, and the hash below is expected to differ from both prior reviews' hashes since this revision changes `src/index.html`, `src/main.js`, and `scripts/run-browser-harness.js`.

```
$ node --version
v22.22.3
```

```
$ npm run build
Local vendor verified: @fontsource/bangers@5.3.0
... (all ten vendored artifacts verified)
Vendor verification passed in offline mode.
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.
Built build/coldbox.html (9c2a56bd93b2a72622579bbd3efaea272d9b6ddfbfbb936f024b8678d831bd12)
```
Zero `Help content warning:` lines — the backfill obligation is met.

```
$ npm run build                                        # first build
Built build/coldbox.html (9c2a56bd93b2a72622579bbd3efaea272d9b6ddfbfbb936f024b8678d831bd12)
$ LC_ALL=de_DE.UTF-8 TZ=Pacific/Auckland npm run build  # different locale/timezone, same tree
Built build/coldbox.html (9c2a56bd93b2a72622579bbd3efaea272d9b6ddfbfbb936f024b8678d831bd12)
$ LC_ALL=ja_JP.UTF-8 TZ=Asia/Tokyo npm run build        # a third locale/timezone
Built build/coldbox.html (9c2a56bd93b2a72622579bbd3efaea272d9b6ddfbfbb936f024b8678d831bd12)
```
All three byte-identical.

```
$ npm run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.
```

```
$ npm run verify-vendor -- --offline
Vendor verification passed in offline mode.
```
Plain `npm run verify-vendor` (online) fails in this sandbox with `fetch failed` — the sandbox's network allowlist blocks outbound requests to upstream release hosts. Not a regression: `npm run build`'s internal call always uses `--offline`.

**`npm test` (132 tests total) — run in three groups because this sandbox's shell tool caps any single command at 45 seconds and the full suite (dominated by real Argon2id KDF timing in `test/vault.test.js`) runs longer than that. Each command's own printed `# tests N` line is quoted verbatim — no manual summation:**

```
$ node --test test/help-content.test.js test/build.test.js test/provenance.test.js
# tests 47
# pass 47
# fail 0

$ node --test test/accessibility.test.js test/airgap.test.js test/handshake.test.js \
    test/lint.test.js test/save-flow-wiring.test.js test/save-integrity.test.js \
    test/vendor.test.js test/browser-runner.test.js test/crypto.test.js test/protocol.test.js
# tests 64
# pass 64
# fail 0

$ node --test test/vault.test.js
# tests 21
# pass 21
# fail 0
```
Sum: 47 + 64 + 21 = **132 pass, 0 fail**. This count is unchanged from the R2 review's own number — neither R2-F1's fixture consolidation nor R2-F2's HTML/JS fix touches any Node-side test file, so the same 132 is expected and confirmed.

```
$ npx playwright install chromium
Error: Download failed: server returned code 403 body 'Connection blocked by network allowlist'.
Failed to install browsers

$ npm run test:browser
Browser harness failed: Error: Playwright browser binaries are missing (chromium, firefox).
    at requireBrowserBinaries (scripts/run-browser-harness.js:100:9)
```
Reconfirmed this session, after both R2 fixes: this sandbox still cannot install Playwright's browser binaries, so none of the harness — not just `verifyHelpFramework()` but every browser-dependent check — can be exercised here. See §4b.

## 4. Acceptance criteria

| Criterion (verbatim from ROADMAP.md) | How satisfied | Test |
|---|---|---|
| Three-depth content model | `::: plain`/`::: working`/`::: technical` block parser, shared-vs-group content, fallback ordering | `test/help-content.test.js` |
| Build-time compilation of `docs/00-overview/glossary.md` and `docs/03-guides/` | `scripts/help-content.js`, wired into `scripts/build.js`, embedded as `HELP_CONTENT` | `test/help-content.test.js`, `test/build.test.js` |
| Contextual `?` | All 5 panels now confirmed by code review to keep their button intact through every runtime title update (R2-F2 fixed) | Manual code review; `verifyHelpFramework()` now asserts a settled-DOM count of exactly 5 and exercises all 5 mappings — written, unexecuted in this sandbox |
| Inline glossary | Tap-to-define terms inside rendered guide bodies | Manual code review; harness assertion written, unexecuted |
| Offline search | Client-side substring match over a runtime-derived corpus; no network call | Manual code review + harness network-request tripwire; the R2 reviewer's own independent Chromium probe already directly confirmed this (see §4b) |
| All three depths render and switch | `setHelpDepth` re-renders glossary/guide DOM; persisted via `localStorage` | Manual code review; the R2 reviewer's own independent Chromium probe already directly confirmed this (see §4b) |
| A documented feature missing a depth block produces a build warning | `readHelpContent()` prints one `console.warn` per gap, non-fatal | `test/help-content.test.js`; independently reconfirmed live by both the R1 and R2 reviewers against a synthetic fixture |
| Search works with no network | Runtime corpus from already-embedded data; no `fetch`/`XHR` | Independently confirmed live by the R2 reviewer (explicit offline browser context, zero non-`file:` requests observed) |
| 🌐 Rendering and depth switching verified by the P0.3a harness | **Not yet independently verified against this exact revision.** The R2 reviewer's own ad hoc Chromium probe already confirmed the underlying product behavior works; what's still missing is a clean `npm run test:browser` PASS run through the *repository's own* harness end to end | — |

**Honest summary:** the compiler/content-model criteria have full automated test evidence. The criteria requiring real-browser behavior now have *some* direct independent evidence — the R2 reviewer's own Chromium probe exercised depth-switching, persistence, and offline search directly against a real build and confirmed they work — but the repository's own `npm run test:browser` harness, which is what the roadmap's 🌐 marker actually names, has still never completed a full run against any revision of this branch. That is the one concrete gap left for the next reviewer to close.

## 4a. R1 review findings — status (unchanged since the R2 packet; both confirmed closed)

**F2 (advisory, closed):** the machine-path regression test's exclusion was narrowed from any `u`+4-hex shape to the exact three sequences `jsonScriptLiteral()` emits, closing the `C:\u1234\repo\file.js` blind spot the reviewer identified. The R2 reviewer independently re-ran this exact test and confirmed it passes.

**F4 (advisory, closed):** ADR-0016, ROADMAP.md, and CHANGELOG.md were corrected from the stale 46-entry/incomplete-backfill claim to the real 51-entry, zero-warning final state. The R2 reviewer independently recounted the compiled source and confirmed 51 terms and nine guides.

R1's F1 (blocking) and F3 (advisory) are addressed below as part of the R2 disposition, since the R2 review found F1 only partially fixed and F3 recurring in a new form.

## 4b. R2 review findings — remediation

**R2-F1 (blocking) — the F1 fix from R1 covered only one of four affected build-root fixtures.** R1's fix added `docs` to `verifyDevOnlyDependency()`'s copied directories. The R2 reviewer found three more functions in `scripts/run-browser-harness.js` that independently build a temporary source-tree copy and invoke `scripts/build.js` against it — `createColdReadySuppressedFixture()`, `createHandshakeResponseSuppressedFixture()`, and `createMissingRandomnessFixture()` — all three still only copying `scripts`/`src`/`vendor`, so the harness failed on the first of these it reached (`createColdReadySuppressedFixture()`, inside `verifyColdRealmTimeout()`) with the identical `ENOENT` on `docs/00-overview/glossary.md`, before ever reaching Chromium's or Firefox's `verifyHelpFramework()` pass.

**Fix:** introduced one shared helper, `copyBuildInputsInto(temporaryRoot, { includeGit })`, that copies `scripts`, `src`, `vendor`, and `docs` (and `.git` when asked). All four fixture functions — `verifyDevOnlyDependency()` plus the three the reviewer found — now call this single helper instead of each hand-rolling its own directory list. This is the reviewer's own explicitly requested fix ("preferably through one shared helper so the build-input list cannot drift again"): there is now exactly one place in the file that knows the complete build-input list, so a future new build dependency can't silently miss three of four call sites the way this one did.

**Re-verification status:** `node -c scripts/run-browser-harness.js` confirms the file is syntactically valid; grepping the file confirms zero remaining `['scripts', 'src', 'vendor']`-only copy sites outside the shared helper's own definition. I could not run the harness itself to completion (see §3) — a maintainer or re-reviewer with real network access needs to confirm `npm run test:browser` now runs through the complete Chromium and Firefox matrix.

**R2-F2 (blocking) — three of five contextual help buttons were destroyed by runtime title updates.** `src/index.html` nested each contextual `?` button directly inside its panel's `<h2>` title alongside the title's text. Seven call sites across `src/main.js` (`coldRealmStatusTitle.textContent = ...`, `airgapBannerTitle.textContent = ...`, `vaultStatusTitle.textContent = ...`) rewrite that `<h2>`'s entire `.textContent` whenever the panel's state changes (e.g. cold realm reaching "ready", airgap reaching "green", vault's lock state changing) — and a `.textContent` write deletes every child node, including the nested button. The R2 reviewer's real Chromium probe found only 2 of the claimed 5 buttons survived past app initialization.

**Fix:** each of the three affected titles (`cold-realm-status-title`, `airgap-banner-title`, `vault-status-title`) now wraps its dynamic text in a dedicated child `<span id="...-title-text">`, with the help button as a sibling of that span rather than a sibling of a bare text node. The three `main.js` lookups (`coldRealmStatusTitle`, `airgapBannerTitle`, `vaultStatusTitle`) were retargeted to the new `-title-text` span ids, so every existing `.textContent = ...` call site now rewrites only the span's text, leaving the button untouched. No call site needed to change — only the three `getElementById` targets. The other two panels (capability self-check, provenance panel) were already unaffected, since their titles are never rewritten wholesale.

**New regression coverage:** `verifyHelpFramework()` now waits for the app to fully settle (cold realm ready, handshake ready, airgap amber/green, capability ready) before asserting `document.querySelectorAll('button.help-context-button[data-help-topic]').length === 5`, then loops over all five topic→route→anchor mappings (not just the two the prior version of this test happened to exercise) and confirms each button is clickable and lands on its real glossary anchor. This directly targets the exact defect class the reviewer found: a check that only inspected the static pre-render markup, or only exercised two of five buttons, would not have caught this.

**Re-verification status:** rebuilt and reran the full Node-side test suite (132/132 pass, unchanged) and lint (clean) after this change — the accessibility and lint test files, which do inspect rendered/static markup, raised no regression from the added `<span>` wrappers. The new Chromium/Firefox assertion itself is unexecuted in this sandbox for the same Playwright-binary reason as R2-F1.

**R2-F3 (advisory) — the previous packet again reported stale evidence.** The version of this packet reviewed at R2 stated a build hash and Node-test count that were accurate for commit `3c778ef` but not for the actual reviewed tip `5af0991`, because R2-F1's fixture change and R2-F2's markup/script change were not made yet when that packet text was written. **Fix:** this entire packet, starting with §3, was regenerated from a fresh `npm run build` and fresh `npm test` run against the tip that actually includes both R2 fixes — the hash and byte count above (`9c2a56bd...`, see §11) were captured after, not before, the R2-F1 and R2-F2 code changes landed.

## 5. Security impact

- **Realm boundary, message schema, CSP, vault format, derivation, randomness:** not touched by this branch. The Learn page and the R2-F2 markup fix live entirely in the warm shell; no new message type; no cold-realm file changed.
- **New CSP-relevant surface:** none — the R2-F2 markup change adds plain `<span>` elements with no new script/style, and `scripts/build.js`'s `hashInlineBlocks` covers the whole assembled `<script>`/`<style>` regardless of what's inside them.
- **New `localStorage` key:** `coldbox-help-depth`, a UI preference — CONTRIBUTING.md explicitly permits this class of key; the existing `coldbox-theme` key is the precedent.
- **New `innerHTML` usage:** `renderHelpGlossary`/`renderHelpGuides` and `linkifyGlossaryTerms` assign compiler-escaped, build-time-derived HTML (not user input, not vault content) through a fixed tag whitelist — see §9 for what to scrutinise here specifically.
- **R2-F2's fix touches only `getElementById` target ids and static markup structure** — it does not change what data is read, what's written to `localStorage`, or any message/network path.
- **No new `connect-src` host.**

## 6. Test evidence

- **`test/help-content.test.js` (21 tests)** and **`test/build.test.js` (12 tests)**: unchanged since the R2 packet — see that revision's description if needed; not re-described here to avoid drift between two descriptions of the same tests.
- **`scripts/run-browser-harness.js`**: not a `node --test` file and so not part of the 132-test Node count, but its own coverage grew in this revision — R2-F1's shared `copyBuildInputsInto()` helper (used by 4 fixtures instead of 1) and R2-F2's five-button settled-DOM assertion plus all-five-mapping exercise (replacing a narrower two-button check).
- **Negative tests:** unchanged from R2 (unterminated `:::` block, duplicate depth, zero-term glossary, leaked machine path).
- **What could not be tested:** everything requiring an actual rendered browser DOM, in this sandbox specifically — see §3 and §4b.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | Untested | |
| Windows Firefox | Untested | |
| macOS Safari | Untested | |
| macOS Chrome | Untested | |
| Linux Firefox | Untested | |
| **iOS local-execution target** | Untested | |
| Android Chrome (Files) | Untested | |
| Tor Browser | Untested | |

**All untested in this authoring sandbox**, for the reasons in §3. The R2 reviewer independently confirmed Chromium and Firefox both install, launch, and can exercise this app's Learn page directly with real network access — the blocker here is specifically this sandbox's binary download restriction, not a fundamental incompatibility.

## 8. Assumptions made

- **Depth-block granularity is per-passage, not per-file** (ADR-0016). Reversible.
- **Default depth is `plain`.** A judgment call, recorded per AGENTS.md §4.
- **The R2-F2 fix wraps only the three affected titles' dynamic text in a child span, rather than restructuring all five panels identically for consistency.** The other two panels (capability self-check, provenance panel) never rewrite their title's `textContent`, so they were never affected and don't need the same wrapper — adding it anyway would be a cosmetic-only, unnecessary structural change. Reviewable if a maintainer would prefer visual/structural symmetry across all five regardless.
- **`copyBuildInputsInto()`'s directory list (`scripts`, `src`, `vendor`, `docs`, optionally `.git`) is hand-maintained,** same as the single-fixture version it replaces — it's an improvement in that there's now one list instead of four, not a guarantee that the list itself can never go stale again if `scripts/build.js` grows a fifth input directory. Worth a comment-level reminder, which the helper's own doc comment includes.

## 9. What to scrutinise

- **R2-F1 and R2-F2 are both unexecuted in an actual browser by me**, for the reasons in §3 — this is the single most important thing for the next reviewer to actually run, not just re-read.
- **The R2-F2 fix's completeness:** I identified all three affected titles by grep-searching every `.textContent =` assignment against `coldRealmStatusTitle`, `airgapBannerTitle`, and `vaultStatusTitle` (7 call sites total) and confirmed no other variable follows the same "title element with a nested button" shape. Worth an independent grep to confirm nothing was missed, given this exact class of bug (a nested interactive element quietly destroyed by a sibling code path) is easy to reintroduce elsewhere.
- **The alias-parsing bug found and fixed in `compileGlossary`** (unchanged since R1, already independently verified twice): fixed to only extract aliases when a parenthetical explicitly contains "also".
- **The `jsonScriptLiteral()` bundle-size interaction** — shared code, already disclosed in SPEC.md/ADR-0016, explicitly not re-raised as a new finding by the R2 reviewer.
- **`normalizeLocation`'s changed collapsing behavior** — preserves `#route/topic` instead of always collapsing to `#route`. Unchanged since R1.

## 10. Self-assessment

- **What might be wrong:** whether `npm run test:browser` now actually completes end to end — the one thing across three packet revisions that has never been directly observed passing by me. Both blocking findings across two review rounds were things static code review missed and only a real browser caught, which is a pattern worth taking seriously rather than assuming this revision's fixes are the last ones needed.
- **What I did not do that arguably should have been done:** run the fixed harness to completion (blocked by sandbox network policy); get pinned-Node (`24.16.0`) confirmation of §3's figures.
- **Known limitations shipping with this state:** help-content bundle weight (≈344 KB) exceeds SPEC.md's original 180 KB estimate, already corrected there; no revision of this branch has completed a real run of its own browser harness.
- **Follow-up work:** a maintainer or re-reviewer with real network access must run `npm run test:browser` on this exact tip through to completion in both Chromium and Firefox before this can be treated as verified rather than "implemented and reviewed."

## 11. Bundle impact

| | `main` (measured fresh) | This branch (this revision) | Delta |
|---|---|---|---|
| `build/coldbox.html` total | 638,819 bytes | 1,014,320 bytes | +375,501 bytes |
| `HELP_CONTENT` embedded JSON alone | 0 (didn't exist) | 353,245 bytes (≈ 345 KB) | +345 KB |

The branch total grew by 858 bytes relative to the R2 packet's 1,013,462-byte figure — expected, since R2-F2 added five small `<span id="...-title-text">` wrapper elements to `src/index.html` and doc comments to `src/main.js`/`scripts/run-browser-harness.js`, none of which touch `HELP_CONTENT` itself (unchanged at 353,245 bytes). `main`'s baseline is unchanged and still matches both prior reviews' independently-measured figure exactly.

## 12. Docs updated

`docs/00-overview/glossary.md`, all nine `docs/03-guides/` files, `docs/05-development/adr/0016-help-content-compiler-and-search.md`, `docs/05-development/adr/README.md`, `docs/05-development/ROADMAP.md`, `CHANGELOG.md`, `docs/01-spec/SPEC.md`, `test/build.test.js`, `test/provenance.test.js`, `scripts/run-browser-harness.js` (R2-F1 shared helper, R2-F2 regression coverage), `src/index.html` and `src/main.js` (R2-F2 markup/script fix). The P0.1–P0.16 backfill obligation is met in full; all findings from both review rounds (R1: F1–F4; R2: F1–F3) are addressed in this revision, with a completed real-browser run of `npm run test:browser` remaining the one open item for the next reviewer.
