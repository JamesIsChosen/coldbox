# P1.13 - Clipboard volatility canary PR packet

## 1. Closeout summary

This packet records the author remediation after the preserved independent FAIL
for the exact P1.13 product head `e63d395281516eeb9b8449c408097f9503c3d409`.
PR #46 remains a draft and is not merged. The product remediation is committed
at `24074675917e31859e2afd45ab8821493da2db2d`; the branch is ready for a fresh
independent review after this packet and self-review are committed and pushed.

- PR: #46, `p1.13-clipboard-canary`, open / draft / unmerged
- Historical independent FAIL: `docs/05-development/packets/p1.13-clipboard-canary.review.md`
- Historical FAIL preservation commit: `aae3f2692024fba3266c2f38c8bc37dd6e294952`
- Reviewed product head that failed: `e63d395281516eeb9b8449c408097f9503c3d409`
- Merged `main` used for reconciliation: `dcce625590965bcbc0b7d6c5bb72974ab5266760`
- Preserved P1.12 PASS closeout: `75dcc33e01fa5d1a1f7495c392239eec2c4ee4ac`
- Normal reconciliation merge: `e168afdf066815e6253987e160f1a4857e7cd89f`
- P1.13 product remediation: `24074675917e31859e2afd45ab8821493da2db2d`
- Roadmap: P1.12 `[x]`; P1.13 `[~]`
- Merge: prohibited until a fresh independent PASS

The reconciliation was a normal no-fast-forward merge of the verified `main`
commit. It did not rebase, force-push, reset, or rewrite the preserved P1.12
history. PR #46 was retargeted from the preserved P1.12 branch to `main` only
after the reconciled P1.13 branch had been pushed. P1.12 remains preserved and
is not part of this remediation.

## 2. Scope and acceptance

The canary remains explicitly opt-in and off by default. It establishes a
baseline only after the user enables it, re-reads the clipboard once after a
delay without user action, and reports an observed change as affirmative
clipboard volatility. Denied permission, absent APIs, and unexpected read
failures are visibly unavailable; ordinary address comparison remains usable.

The remediation closes both findings from the preserved FAIL:

- The controller generation guard remains authoritative for state, baseline,
  and timers. The warm UI now has a separate operation generation. Disable,
  enable, and retry each invalidate earlier UI operations; a late result or
  rejection is ignored unless it belongs to the current UI operation.
- A synchronous `permissions.query()` exception is contained and represented as
  unknown permission. The controller still attempts the actual clipboard read;
  a successful read may arm the canary. The focused test, implementation, and
  this packet now state that same contract.

The change message continues to name clipboard managers, sync tools, and
remote-desktop clients before malware. The canary is warm-only and never enters
the opaque cold frame.

## 3. Dependency merge and security boundary

The original P1.13 branch was based on the pre-merge dependency state. The
historical FAIL was preserved first in `aae3f26`, then `origin/main` at
`dcce625` was merged normally in `e168afd`. That merge carries the independent
P1.12 PASS closeout at `75dcc33`, leaves P1.12 marked `[x]`, and leaves P1.13
marked `[~]` for fresh review.

The canary carries no vault, seed, private-key, xprv, passphrase, or
secret-compartment data. It reads only the browser's public clipboard API in
the warm realm, reports state enums and bounded user-facing copy, and never
sends clipboard text across the realm boundary. It does not claim that a stable
read proves clipboard safety.

## 4. Test evidence

Focused canary, address, and protocol tests:

```text
node --test test/clipboard-canary.test.js test/address-verification.test.js test/protocol.test.js --test-concurrency=1
tests 33
pass 33
fail 0
```

The focused cases cover synchronous permission-query exceptions, disable while
permission is pending, stale read rejection after a newer enable, and the
off-by-default, denied, absent, changed, and stable states. The committed
browser harness adds delayed first-permission overlap for disable/re-enable and
delayed first-permission overlap for retry/retry; both assert that the final
armed controller state and checked UI toggle agree.

Complete merged suite, run with the Git-dependent provenance checks enabled:

```text
npm.cmd test
tests 321
pass 321
fail 0
```

## 5. Required gates

Commands were run from `C:\Users\semaj\Projects\coldbox`.

```text
npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: @fontsource/bangers@5.3.0
Upstream release verified: @fontsource/comic-neue@5.3.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Upstream release verified: argon2-browser@1.18.0
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 179 markdown file(s) checked, 0 warning(s).
```

Two builds were run with different checkout paths, timezones, and locales:

```text
$env:TZ = 'UTC'; $env:LC_ALL = 'C'; $env:LANG = 'C'; npm.cmd run build
Built build/coldbox.html (c145cb96aea19d6f14417afacdeba1b09ed8358343b9a9712158760d3750b2f7)
build-1 hash=c145cb96aea19d6f14417afacdeba1b09ed8358343b9a9712158760d3750b2f7 size=2177736 path=C:\Users\semaj\Projects\coldbox\build\coldbox.html

$env:TZ = 'America/Los_Angeles'; $env:LC_ALL = 'de_DE.UTF-8'; $env:LANG = 'de_DE.UTF-8'; npm.cmd run build
Built build/coldbox.html (c145cb96aea19d6f14417afacdeba1b09ed8358343b9a9712158760d3750b2f7)
build-2 hash=c145cb96aea19d6f14417afacdeba1b09ed8358343b9a9712158760d3750b2f7 size=2177736 path=C:\Users\semaj\.codex\visualizations\2026\08\11\019ff2ea-b634-7a71-9482-336447fc45bc\coldbox-p1-13-build-63e9c823dbc8486c9a1ada2ae134af33\repo\build\coldbox.html
```

The second build was made from a copied checkout and the temporary copy was
removed after the matching hash and size were recorded.

## 6. Browser evidence

The full `file://` browser harness passed in both installed desktop engines.
The deterministic P1.13 control script drives the actual warm form controls,
including delayed permission/read resolution and final controller/UI agreement.

```text
Chromium: clipboard canary off-by-default, denied/retry, API-absent fallback, and affirmative change UI passed
Firefox: clipboard canary off-by-default, denied/retry, API-absent fallback, and affirmative change UI passed
Browser harness passed in Chromium and Firefox.
```

The harness also exercised fresh off-by-default state, denied permission,
retry without reload, API absence with address comparison still enabled,
disable/re-enable overlap, retry overlap, and an affirmative delayed change
message naming benign clipboard causes before malware.

Not tested by this author session:

- a real browser/OS permission prompt;
- physical clipboard managers, remote-desktop clients, or mobile clipboard
  behavior;
- iOS Safari or Android Files execution.

Those limits are explicit; the deterministic UI matrix and unit tests do not
claim to replace physical-device validation.

## 7. Finding closure and self-assessment

F1 is closed by the UI-layer generation token in `src/main.js`. The token is
advanced before disable and at the start of enable/retry. Every success and
failure continuation checks that token before touching the checkbox or
unavailable UI. The browser harness commits and drives the delayed
disable/re-enable and retry overlap regressions, and it asserts the final
`data-state="armed"` plus checked toggle.

F2 is closed by aligning the packet with the existing deliberate contract:
`queryPermission()` catches synchronous query failure as `unknown`, then the
actual `readText()` result determines whether the canary arms. The focused test
still proves that a throwing query followed by a successful read arms the
controller.

A fresh reviewer should still scrutinise the UI generation invalidation,
controller generation checks, delayed timer cancellation, retry semantics, the
warm-only placement, and the fact that clipboard content never enters the cold
protocol. No new ADR was needed: the remediation makes no new vault-format,
realm-boundary, or cryptographic-choice decision.

## 8. Handoff

The product remediation is complete at `24074675917e31859e2afd45ab8821493da2db2d`.
Commit this packet and the matching self-review, push only the existing
`p1.13-clipboard-canary` branch, keep PR #46 open/draft/unmerged, and request a
fresh independent review. Do not merge PR #46 or mark P1.13 `[x]` in this
author/remediation session. Do not modify PR #47 or delete the preserved P1.12
branch.
