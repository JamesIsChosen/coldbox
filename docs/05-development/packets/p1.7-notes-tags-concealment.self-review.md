# Self-review: P1.7 Notes, tags, and concealment levels

**VERDICT: PASS**

Findings: 0
Reviewed commit: `f5db40a`
Reviewed by: author self-review gate, final round after two pre-gate fixes
Date: 2026-08-10

## What I verified

- Re-read `docs/05-development/review-protocol.md` before this gate.
- Read the complete P1.7 diff from `a726082` through `f5db40a`; the final committed diff passed `git diff --check` after the ADR whitespace correction.
- The focused P1.7 tests passed 43/43, including protocol, registry, concealment, and secret-compartment coverage.
- `npm.cmd test`: 282/282 passed.
- `npm.cmd run verify-vendor`: local files and all ten upstream releases passed.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 153 markdown files and 0 warnings after the packet and self-review were added.
- `npm.cmd run build`: SHA-256 `76a9eff7c252c800970176bcc599bf9535dce491a8d0115d07821ccc3655600e`, size 1,917,400 bytes.
- A separate temporary checkout with copied Git metadata, different path, `LC_ALL`, and `TZ` produced the same hash and size twice.
- Deliberately appending `eval('self-review-tamper');` to a disposable copy made `node scripts/lint.js --root .` exit 1; the tampered copy was removed afterward.
- The prescribed browser harness passed over `file://` in Chromium and Firefox, including CSP, airgap, responsive viewport, lock, zeroization, and teardown checks.
- The cold CSP remains unchanged with `connect-src 'none'`; the new protocol messages carry only empty objects, booleans, or `{ dirty: true }`; online secret-compartment access returns null or rejects; and lock/panic teardown clears secret-note DOM, timers, session state, and byte copies.

## What I could not verify

Physical mobile/iOS/Edge/Safari/Tor/Android Files sessions were unavailable. The desktop Chromium and Firefox harness did pass, including responsive viewport checks, but those are not substitutes for the untested device matrix entries. The P1.7 roadmap line has no separate browser acceptance clause, so this gap is disclosed rather than inferred as coverage; independent review must resolve it before merge.

## Acceptance criteria

The roadmap contains one exact P1.7 line and no separate `Accept:` block:

| Criterion | Result | Evidence |
|---|---|---|
| `P1.7 Notes, tags, and concealment levels` | Met | Public note/tag CRUD and search, concealment and privacy-blur tests, cold-local secret-note search/masking/timed reveal, full suite, and Chromium/Firefox harness |

## Security review

- Public protocol schemas reject secret visibility, secret-shaped values, unknown text carriers, and unbounded note content.
- Secret-note queries and matches stay in the cold editor; no secret-note plaintext, query, phrase, or compartment bytes cross the realm boundary.
- The warm hidden-record reveal request contains no phrase and receives only a boolean result after cold re-authentication.
- The cold realm remains offline-only; no network host, runtime dependency, or CSP exception was added.
- Privacy blur is warm local UI state only and is not represented as cryptographic protection.
- Lock, panic hide, online transition, and failed session paths fail closed and clear secret-note UI and state.

## Review history

- First pre-gate failure: a UI string containing the forbidden word `require` broke deterministic lint/build checks; fixed in `ccf82ac`.
- Second pre-gate finding: the initial cold lock path hid secret-note UI without clearing its DOM values; fixed in `249e924`.
- The final cold-local search addition was reviewed in `7e14271`.
- The final gate also found trailing whitespace in the new ADR; fixed in `f5db40a`, then `git diff --check` and documentation hygiene were rerun successfully.

## Verdict rationale

All item-scope behavior and negative tests pass, the full suite and upstream vendor verification pass, builds are reproducible across locale/timezone/path changes, the deliberate lint tamper exits non-zero, and the browser harness passes in both available desktop engines. The mobile and additional-browser gaps are explicitly disclosed. PASS for the batch gate, with independent review still required before merge.
