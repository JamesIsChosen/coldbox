# PR packet - P1.9 Verification workflows (remediation)

**Status:** SOFTWARE GATES PASS; STOPPED AT THE HUMAN-REQUIRED HARDWARE
VALIDATION GATE.

This packet replaces the stale stopped-round evidence for the P1.9 branch. It
does not edit the historical stacked review. The software remediation is
ready for independent review; the roadmap's final real-device validation has
not been attempted because it requires the human's physical hardware.

## 1. Summary

P1.9 now has a cold-local Verify Bench that consumes the current public
identity from Seed Forge. Seed Forge is the only mnemonic/passphrase entry
surface. Verify Bench displays the selected network, script family, account
path, account xpub, and bounded receive/change ranges for all four Bitcoin
families, then compares independently entered public device or backup values.

The four public comparison panels are device fingerprint, receive address,
account xpub, and metal backup. The fifth roadmap check, passphrase
verification, is performed by selecting and confirming the exact passphrase in
Seed Forge and comparing the resulting public fingerprint or other public
identity; there is no duplicate passphrase shell.

Remediated findings:

- F9: this packet has no blank line at EOF; the final stacked diff passes
  `git diff --check`.
- F10: the browser harness opens every collapsed workflow before interacting
  with it and checks attached/reset state after lock rather than requiring a
  collapsed status to be visible.
- F11: Seed Forge owns passphrase semantics, including leading, trailing, and
  internal spaces; unit tests prove distinct passphrases derive distinct
  identities.
- F12: external fingerprints, extended keys, Base58 addresses, and Bech32
  addresses are strict public inputs with canonical encoding, checksum,
  network, witness-version, and program-length validation. Mixed-case Bech32
  is rejected.
- F15: Verify Bench has an explicit **Use current Seed Forge wallet** handoff,
  displays the active public identity and all four family xpubs, and clears
  the linked identity when Seed Forge changes, the network changes, or the
  cold session tears down.
- F16: the standalone BIP-39 passphrase panel and duplicate mnemonic/
  passphrase controls are removed.

F13 remains open by design: real hardware-wallet validation is a human gate.
No hardware transport, screen-reading, or authenticity claim was added.

## 2. Scope

In scope:

- Cold-only public identity derivation from the currently generated or
  validated Seed Forge wallet.
- Four public comparison panels plus passphrase verification through the
  authoritative Seed Forge state.
- Strict validation of external public fingerprints, xpubs, Base58 addresses,
  and Bech32/Bech32m addresses.
- Public-only UI rendering, explicit identity invalidation, lock/panic
  teardown, and external-value clearing.
- Chromium and Firefox `file://` coverage, including collapsed `<details>`
  interaction, independent external inputs, mixed-case and invalid-public
  negatives, duplicate-secret-surface absence, and lock teardown.
- Updated guide, ADR-0034, changelog, roadmap state, tests, and this packet.

Deliberately out of scope:

- WebHID, WebUSB, vendor transport, signing, or device authenticity.
- P1.11 address verification state, P1.12 clipboard round trips, and P1.13
  clipboard volatility.
- Marking P1.9 `[x]`; the marker remains `[~]` for independent review.
- Physical hardware-wallet, mobile-browser, Safari, Tor Browser, Android
  Files, or iOS local-execution validation.

## 3. Branch and review provenance

Roadmap: [P1.9 Verification workflows](../ROADMAP.md).

Branch: `p1.9-verification-workflows`

Dependency/base: `p1.8-device-registry` at
`7ae8df6e4afe8f370a2cde60f99719f361d008b9`

Final product/remediation commit:
`8a359a13e6f1de34ac6e12435dec40fa009daf47`

The original reviewed product head was
`4c814a20665baaecb6a4ea8882e300dc10ad1741`.

The original 14-finding review commit remains preserved at
`1d2049894fa003cc6dc2d2f041b91bd096cac303`. The append-only F15/F16 review
commit remains preserved at `4ad879f0c699ff9ac5db0bffc2aa7dd6fa10cee4`.

The combined review SHA-256 remains
`bbcdd2b3ca0c9f8c42af1d7ece8d8a8e218b4c1484c922185e10346d53c43d53`.

The safety tags remain intact:

```text
1d2049894fa003cc6dc2d2f041b91bd096cac303 refs/tags/runner/pr37-42-append-f15-f16-fail-addendum-01/pre
4c814a20665baaecb6a4ea8882e300dc10ad1741 refs/tags/runner/pr37-42-record-stacked-fail-01/pre
```

The historical review file has the same blob identity at the original review
commit and its rebased replay:

```text
1d2049894fa003cc6dc2d2f041b91bd096cac303:docs/05-development/packets/BATCH-2026-08-10.review.md -> eaec5a84e61ce4d000be69feeda17eb2e199d0ff
158b5ac:docs/05-development/packets/BATCH-2026-08-10.review.md -> eaec5a84e61ce4d000be69feeda17eb2e199d0ff
```

## 4. How to verify

The following evidence was run from
`C:\Users\semaj\Projects\coldbox` against product commit
`8a359a13e6f1de34ac6e12435dec40fa009daf47`.

```text
> git diff --check p1.8-device-registry...HEAD
(no output; exit 0 after this packet was recorded)

> node --test test/verification.test.js
tests 6
pass 6
fail 0

> npm.cmd test
tests 293
pass 293
fail 0
cancelled 0
skipped 0

> npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

> npm.cmd run check-docs
Documentation hygiene check passed: 161 markdown file(s) checked, 0 warning(s).

> npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: @fontsource/bangers@5.3.0
Upstream release verified: @fontsource/comic-neue@5.3.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Upstream release verified: argon2-browser@1.18.0
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.

> npm.cmd run build
Built build/coldbox.html (03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46)

> Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
SHA256 03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46
SIZE 1987610
```

The dependency-free alternate-path reproducibility run used a detached
worktree at the same commit. The two builds used different timezone/locale
environment values and produced identical bytes:

```text
REPRO HEAD 8a359a1
FIRST TZ Pacific/Honolulu LC_ALL C HASH 03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46 SIZE 1987610
SECOND TZ UTC LC_ALL de_DE.UTF-8 HASH 03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46 SIZE 1987610
```

The final browser command passed in both engines. Relevant real output:

```text
> npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46)
Chromium: cold-local Seed Forge handoff, public fingerprint/xpub/address/backup comparisons, strict external-value negatives, and lock teardown passed
Firefox: cold-local Seed Forge handoff, public fingerprint/xpub/address/backup comparisons, strict external-value negatives, and lock teardown passed
Browser harness passed in Chromium and Firefox.
```

The same harness also reported CSP and airgap blocks, runtime lockdown and
zeroization, built-artifact byte tampering rejection, source/provenance
checks, and the existing P1.4-P1.8 browser regressions as passed.

## 5. Acceptance criterion

The roadmap criterion is:

> P1.9 Verification workflows: fingerprint, receive address, xpub, backup,
> passphrase - implementable by agent; final validation needs real hardware
> wallets

The implementable software and browser portion is met at the final product
commit. The final real-device validation is explicitly **not met yet** and is
the reason this batch stops here. The roadmap remains `[~]`; this branch does
not claim independent verification or hardware completion.

## 6. Findings addressed

### F9 - packet EOF

The stale packet's blank EOF was removed. The final stacked diff is checked by
`git diff --check p1.8-device-registry...HEAD` and produces no output.

### F10 - collapsed details and teardown

`verifyVerificationWorkflows()` now opens each workflow's `<details>` element
before filling or clicking it. After the shared Escape teardown it waits for
attached idle state, confirms the linked wallet is empty and the external
fields are cleared, and confirms all four comparison buttons are disabled.
It no longer asks a collapsed status element to be visible. Chromium and
Firefox each execute and pass these assertions.

### F11 - passphrase semantics and duplicate secret APIs

The passphrase and mnemonic remain in Seed Forge, whose exact passphrase value
is used without trimming or normalization beyond the BIP-39 implementation's
defined semantics. The focused tests prove that `TREZOR`, ` TREZOR`,
`TREZOR `, and `T REZOR` produce distinct fingerprints. The Verify Bench
source and DOM contain no duplicate mnemonic/passphrase workflow API.

### F12 - strict public comparisons

Fingerprint input is printable ASCII and exactly eight hexadecimal characters.
Extended-key input is printable canonical Base58 text and is decoded through
the existing Bitcoin derivation validator. Address input is decoded and
validated against the selected network and script family, including Base58
version/payload length and Bech32/Bech32m checksum, witness version, and
program length. Fullwidth compatibility text, whitespace, invalid extended
keys, mixed-case Bech32, and bad checksums are negative-tested.

### F13 - real hardware gate

No hardware wallet was connected, read, or trusted. This is not silently
claimed as passed. The next action is a human-run physical device validation;
the batch stops before any further item.

### F15 - active Seed Forge wallet handoff

The user explicitly chooses **Use current Seed Forge wallet** after generating
or validating a phrase. Verify Bench displays the source, master fingerprint,
selected account path, selected xpub, receive/change ranges, and all four
family xpubs. The linked object contains only public values. Seed Forge
replacement, invalid derivation, network changes, lock, panic, and teardown
clear the link and public comparison state. Chromium and Firefox cover the
handoff, public display, independent inputs, replacement/teardown path, and
lock cleanup.

### F16 - no standalone passphrase shell

The old passphrase `<details>` panel and all duplicate mnemonic/passphrase
inputs were removed. The browser harness asserts that the Verify Bench has no
passphrase panel and no mnemonic/passphrase inputs. Passphrase identity
differences remain covered through Seed Forge's independent unit tests.

## 7. Security impact

- The change remains cold-local and adds no message type or warm/cold payload.
- `verification.js` derives from Seed Forge's 64-byte seed inside the cold
  realm and returns only public identity and verdict fields. The linked UI
  object stores no mnemonic, passphrase, private key, or xprv.
- External device and backup fields are public-only. Strict parsing fails
  closed before comparison when a canonical encoding or checksum cannot be
  established.
- The cold `connect-src 'none'` policy and existing airgap/zeroization paths
  remain unchanged and are covered by the final browser harness.
- No hardware transport or authenticity claim is made. A manual match still
  requires reading the device's own screen and checking the physical setup.

## 8. Assumptions and gaps

- Seed Forge remains the authoritative location for mnemonic language and
  passphrase selection; Verify Bench does not duplicate either control.
- The current workflow exposes Bitcoin account 0 and five receive plus five
  change addresses per family, as bounded public verification ranges. Future
  account/range expansion is outside P1.9.
- The automated desktop matrix is Windows Chromium and Firefox over `file://`.
  macOS Safari/Chrome, Linux Firefox, Android Chrome from Files, Tor Browser,
  and the iOS local-execution target were not tested.
- No physical hardware wallet was available to this session. The final
  hardware validation gate is therefore human-required and remains open.
- P1.12 owns later clipboard authority and registry-linked address state;
  this item only compares manually entered public values in the cold realm.

## 9. What to scrutinise hardest

1. The exact Seed Forge-to-Verify Bench invalidation paths: replacement,
   network change, lock, panic, and cold-health teardown must never leave an
   old public identity usable.
2. The assembled cold script, not only the source module, must keep all secret
   inputs out of the warm realm and message channel.
3. The extended-key and Bech32 validators must continue to reject malformed or
   ambiguous external text rather than silently normalize it.
4. The physical device-screen comparison remains manual and is not evidence
   of device authenticity; verify the human-required gate independently.

## 10. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chromium | PASS | Complete harness, including cold-local Verify Bench workflow and teardown. |
| Windows Firefox | PASS | Complete harness, including cold-local Verify Bench workflow and teardown. |
| macOS Safari | Untested | Requires a human device/session. |
| macOS Chrome | Untested | Requires a human device/session. |
| Linux Firefox | Untested | Requires a human device/session. |
| iOS local-execution target | Untested | Exact device/iOS result remains a human gate. |
| Android Chrome (Files) | Untested | Requires a human device/session. |
| Tor Browser | Untested | Requires a human device/session. |
| Physical hardware wallet | Untested | Required before P1.9 can be fully accepted. |

## 11. Bundle impact

Final committed product artifact: 1,987,610 bytes, SHA-256
`03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46`.
The browser harness is development-only and is not embedded in the shipped
HTML.

## 12. Docs updated

- `docs/03-guides/verify-a-hardware-wallet.md` - Seed Forge-only secret
  selection and public-value comparison instructions.
- `docs/05-development/adr/0034-cold-local-verification-workflows.md` -
  active-wallet handoff, strict external parsing, invalidation, and rejected
  duplicate secret surface.
- `docs/05-development/ROADMAP.md` - current P1.9 software state, `[~]`
  marker, and open hardware gate.
- `CHANGELOG.md` - corrected P1.9 workflow description.
- `docs/05-development/packets/p1.9-verification-workflows.self-review.md` -
  fresh software-gate self-review and hardware stop.
