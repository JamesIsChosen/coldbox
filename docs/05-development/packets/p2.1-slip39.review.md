# Review: P2.1 — SLIP-39

**VERDICT: FAIL**

Findings: 3 (3 blocking, 0 advisory — all must be addressed)
Reviewed commit: `a56d1877e707950445219f3719ace9a7024f422d`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-12
Independent evidence bundle SHA-256: `31c56955b824d265deee00d7da2a1a7b2b831c1a56ce64335adea4f43571abe8`

## 1. What I verified

Fresh independent review used a full-history clone and separate detached worktrees. For this item I verified the exact product head and merge base, `git diff --check`, `npm ci`, `npm run verify-vendor`, `npm run lint`, `npm run check-docs`, the focused item test, the full `npm test` suite, two same-worktree builds, and a third build from a different detached path/timezone/locale. Vendor-manifest corruption was also exercised and correctly made `verify-vendor` exit non-zero. The cold CSP probe retained `connect-src 'none'`, found no `allow-same-origin`, and found no storage/network/warm-message surface in the item module.

The three builds were byte-identical and matched the packet exactly: `d087a791e20b111b1f4867cff09588472ad6ffc1ed0b44f48f924393e0134b6f`, 2,249,789 bytes. Build A used `TZ=Asia/Kathmandu`, `LANG=de-DE`; build B used `TZ=Pacific/Kiritimati`, `LANG=fr-FR`.

The remediation guide now accurately says P2.1 does not create a completion record or fingerprint, does not print cards, and does not provide Registry/Health/reminder surfaces. Those prior documentation findings are resolved.

Chromium completed the P2.1 SLIP-39 feature/security assertions, including cold gating, masked reveal, 20-word output, local two-share recovery, indexed checksum failure, warm isolation, teardown, CSP/runtime-airgap, tamper, and regression checks.

The v0.1.9 tag independently resolved to commit `414a73b0a28f2fc966514b8f318eb6f68bf9979a`. The exact pinned upstream `src/slip39_helper.js` bytes reproduce Git blob SHA-1 `8bcb9568c6c6191e686de2d34864088c644fc7b2`.

The collector's final bookkeeping later raised a PowerShell `.Count` error after all four item runs had emitted their evidence. I independently verified the ZIP against its SHA-256 sidecar and based this report on the per-gate logs rather than that bookkeeping result.

## 2. What I could not verify

The exact P2.1 head did not complete a fresh Firefox run. After Chromium passed, Playwright exited 1 while closing a Firefox page/browser context with:

`page.close: Protocol error (Browser.removeBrowserContext): can't access property "_maybeDontRestoreTabs", this._windows[aWindow.__SSi] is undefined`

This verification gap is F3.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.1 SLIP-39` | ❌ | Focused/full tests and Chromium behavior pass and the artifact reproduces, but exact upstream SHA-256 provenance is incorrect (F1), the security build gate accepts executable `Math.random()` in the new cold module (F2), and fresh Firefox verification did not complete (F3). |

## 4. Findings

### F1 — Recorded upstream SHA-256 is not the SHA-256 of the pinned upstream bytes

**Severity:** blocking provenance
**Location:** `docs/05-development/dependencies.md` SLIP-39 row; ADR-0036; P2.1 packet; `src/cold/slip39.js` provenance header.
**Observed:** The branch records `21c15adf2a2f692662b469b609e6a3e42eb828d4fc5c13707f3a4beea027ea48` as the SHA-256 of upstream `src/slip39_helper.js`. A fresh download from exact commit `414a73b0a28f2fc966514b8f318eb6f68bf9979a` is 36,923 bytes and hashes to `2009d910cda84e81e3d75f78f6961a9b82258c66513f4160efc25c6a4f6a6c5e`. Its Git blob SHA-1 is the correctly recorded `8bcb9568c6c6191e686de2d34864088c644fc7b2`. The recorded `21c15...` value is the SHA-256 after LF→CRLF transformation, not the exact upstream bytes.
**Expected:** A value explicitly labeled as the SHA-256 of the upstream file must reproduce from the pinned upstream bytes without platform newline transformation.
**Required action:** Correct the SHA-256 assertions, define the exact-byte convention unambiguously, and add/re-run an independent exact-byte provenance check. Preserve the already-correct tag commit and Git blob identity unless new evidence requires otherwise.

### F2 — Build/lint gate fails open to executable `Math.random()` in the new cold security module

**Severity:** blocking security
**Location:** `scripts/lint.js` `scanSourceFile()`; exercised path `src/cold/slip39.js`.
**Observed:** In a disposable reviewer worktree I appended `void Math.random();` to `src/cold/slip39.js` and ran `npm run build`. The build exited **0** and emitted an artifact. The module was then restored byte-for-byte. `scripts/lint.js` defines an executable `Math.random` pattern but applies it only when `relative === 'src/capabilities.js'`; secret-handling `src/cold/**` files do not receive that rule.
**Expected:** The review protocol requires no `Math.random` anywhere in a security-relevant path and requires deliberate invalid mutations to fail with a non-zero exit.
**Required action:** Extend the security/forbidden-construct lint to reject executable `Math.random()` throughout the security-relevant cold source surface, add a regression test proving `npm run build` exits non-zero for this mutation, and rerun the independent negative gate.

### F3 — Fresh independent Firefox coverage is incomplete

**Severity:** blocking verification
**Location:** P2.1 Chromium/Firefox browser verification.
**Observed:** Chromium item assertions passed, but the run exited 1 on the Firefox `page.close`/`Browser.removeBrowserContext` protocol error above before a P2.1 Firefox item PASS was produced. Sibling-branch Firefox passes cannot verify this branch because those branches do not contain P2.1.
**Expected:** The exact reviewed P2.1 head must complete the requested real Chromium/Firefox `file://` harness.
**Required action:** Re-run the normal disposable-cache browser workflow on the remediated P2.1 head and obtain a clean Firefox item-specific PASS. If the lifecycle error reproduces, diagnose/fix that harness/browser lifecycle issue rather than substituting author-side evidence.

## 5. Verdict rationale

The earlier documentation remediation and build reproducibility are verified, but three blocking findings remain: exact-byte provenance is wrong, the build gate accepts executable `Math.random()` in a seed/share security module, and fresh Firefox coverage is incomplete. P2.1 remains `[~]` and must not merge. A fresh independent review is required after all three are addressed.

FAIL


## Re-review round 3 — 2026-08-12

**VERDICT: FAIL**

Findings: 1 (1 blocking, 0 advisory — all must be addressed)
Reviewed commit: `3ba149ed774a653a3276fdd31bf20c0ab28c6e9d`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `672d5ddb511b731798eadf30a14c7abee13d326b25b158e6b690072f1dd16c88`

### 1. What I verified this round

I freshly reviewed the exact PR #48 tip against `main@5f0fa5e442524033e91bb31006a0ca6288385dc0` from a full-history clone with separate detached primary and reproducibility worktrees.

The following independent gates passed:

- `git diff --check` for the full PR and the remediation-only delta.
- `npm ci`, `npm run verify-vendor`, `npm run lint`, and `npm run check-docs`.
- `node --test test/slip39.test.js`.
- The added lint/build security regression tests.
- The full suite: 330 tests passed.
- Three builds were byte-identical across two worktrees and different caller timezone/locale settings: `7151846e74ce1520746fdc7e6cf4748aca6d7adf713d3ca30a659fba68a6cb23`, 2,249,808 bytes.
- The complete `file://` browser harness exited 0 in Chromium and Firefox and emitted the P2.1 SLIP-39 item-specific PASS in both engines.
- A reviewer-only executable `Math.random()` mutation in `src/cold/slip39.js` made both `npm run lint` and `npm run build` exit non-zero; the file was then restored exactly.
- Deliberate vendor-manifest corruption made `npm run verify-vendor` exit non-zero and was restored.
- The pinned v0.1.9 tag, Git blob identity, and exact LF-byte upstream `slip39_helper.js` SHA-256 independently reproduce. The exact SHA-256 is `2009d910cda84e81e3d75f78f6961a9b82258c66513f4160efc25c6a4f6a6c5e`.

The prior round's three findings are therefore resolved: exact-byte SLIP-39 provenance is corrected, the cold-path `Math.random()` build gate now fails closed, and fresh Firefox item coverage completes successfully.

### 2. What I could not verify

The packet's round-3 "Current evidence" build hash does not reproduce from the exact committed head. This is R3-F1.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.1 SLIP-39` | ❌ | Functional, security, provenance, reproducibility, and both-browser behavior independently pass, but the packet's current exact-artifact evidence does not describe the reviewed committed head (R3-F1). |

### 4. Findings

#### R3-F1 — Round-3 packet build evidence does not reproduce from the exact committed head

**Severity:** blocking evidence
**Location:** `docs/05-development/packets/p2.1-slip39.md`, round-3 `Current evidence` build block.
**Observed:** The packet records two current builds as `bab40fbbdf3df49e01328d028e85901376f9beef277f86bf1c7218d917adca97` / 2,249,808 bytes. Three fresh independent builds from exact head `3ba149ed774a653a3276fdd31bf20c0ab28c6e9d`, including a second path/timezone/locale, all produced `7151846e74ce1520746fdc7e6cf4748aca6d7adf713d3ca30a659fba68a6cb23` / 2,249,808 bytes. The independent builds agree with each other; the packet claim does not.
**Expected:** The review protocol requires packet claims to reproduce on the exact committed tip being reviewed.
**Required action:** Rebuild after the build-affecting remediation is committed, from the exact committed branch state, and update the packet/self-review evidence to the resulting hash and size. Then request a fresh independent review; do not alter the already-resolved product/security behavior merely to chase the old digest.

### 5. Verdict rationale

The substantive P2.1 remediation is independently verified and the three prior findings are resolved. The current packet nevertheless asserts an artifact digest that does not reproduce from the exact reviewed head, which is a binary FAIL under the review protocol. P2.1 remains `[~]` and must not merge.

FAIL
