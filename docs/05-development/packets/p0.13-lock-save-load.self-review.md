# P0.13 author self-review gate

**VERDICT: PASS**

Findings: 0
Reviewed implementation commit: `ceab5a8`
Base: `p0.12-kdf-profiles` at `b24661f`
Branch: `p0.13-lock-save-load`
Date: 2026-08-03

This is the author gate for the final P0.13 implementation tip. The reserved independent-review path, `p0.13-lock-save-load.review.md`, is intentionally absent and remains available for the independent reviewer.

The roadmap item remains `[~]`, not `[x]`, because its explicit iOS Safari manual-path acceptance requires the P0.19 human device pass. This PASS records that the implementation is complete and clean for the available automated scope; it does not infer a physical-device result.

## Gate results

The implementation commit was reviewed after completion. The gate included the complete diff, the mode-gated vault opener, the warm/cold source boundary, typed protocol changes, deterministic build, negative vendor checks, and both browser engines.

```text
$ git diff --check b24661f..ceab5a8
PASS

$ npm.cmd run verify-vendor -- --offline
Local vendor verified: all seven pinned artifacts
Vendor verification passed in offline mode.

$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm.cmd test
tests 46
pass 46
fail 0

$ npm.cmd run build, then clean rebuild
19516552efb996adf27cae4f3b4bf47190564ce72edd8de706dbd4f17bb67e09
387110 bytes

$ npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (19516552efb996adf27cae4f3b4bf47190564ce72edd8de706dbd4f17bb67e09)
Browser harness passed in Chromium and Firefox.
```

The full suite also passed the deliberate corrupted-vendor, unmanifested-vendor, CSP lockdown, missing-randomness, tampered-build, protocol, vault-authentication, and public-only HKDF negatives.

## Acceptance review

| # | Criterion | Automated result | Evidence |
|---|---|---|---|
| 1 | Three save paths and symmetric load | PASS for available APIs | File System Access is implemented when exposed; blob download and complete manual base64 save/load are exercised in Chromium and Firefox. |
| 2 | At least one save path on every device-matrix platform | Unverified on physical devices | The universal manual path has visible copy/select fallback; iOS Safari and the remaining matrix are reserved for P0.19. |
| 3 | Idle auto-lock and `Esc Esc` panic hide | PASS for implementation/browser coverage | Cold timer and cleanup are present; Chromium/Firefox verify warm concealment, and the cold frame has its own typed panic request. |
| 4 | Secret compartment stays sealed online | PASS | `openPublic` returns no secret compartment, and the tracked HKDF test observes only `cbx/public/v1`; online-to-offline mode changes clear the session. |

## Security checklist

- The warm source and template contain no `__coldboxVault`, passphrase field, `openPublic`, or secret HKDF path.
- `vault.open` carries ciphertext only; the cold passphrase input is `spellcheck="false"` and is cleared on explicit lock, idle lock, mode transition, and panic hide.
- The online opener never derives `cbx/secret/v1`; full opener selection is cold-only and requires an offline mode signal.
- The warm shell receives only sanitized public data, encrypted bytes, typed status, or generic error messages. The new `panic.hide` message has an empty payload.
- Manual base64 parsing enforces the vault size ceiling and sends only the resulting encrypted bytes through the existing validator.
- The blob URL is revoked after download scheduling; File System Access errors and user cancellation are rendered generically.
- The opaque sandbox, CSP, network throw contract, global-message discard, and runtime airgap guard remained green in both browser engines.

## Findings

None. Findings: 0.

## What remains unverified

- No physical iOS Safari from the Files app was available. The iOS manual-path claim remains `[~]` until P0.19.
- Android Chrome, macOS Safari/Chrome, Linux Firefox, and Tor Browser remain part of the human matrix.
- Upstream online vendor re-download verification was not exercised in this author environment; local offline verification and corruption negatives passed.

## Verdict rationale

The P0.13 implementation has no author-gate findings and its browser-verifiable criteria pass in Chromium and Firefox. The roadmap remains `[~]` solely because the item itself names a physical iOS Safari acceptance criterion that an agent must not infer. No PR was merged.

PASS
