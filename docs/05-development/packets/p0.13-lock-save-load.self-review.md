# P0.13 author self-review gate

> <!-- P0.13-SELF-REVIEW-HISTORY-NOTICE -->
> **Evidence history notice:** Earlier author evidence below records prior branch tips and is preserved as history. It is superseded for the current tip by the exact-tip remediation supplement at the end of this file. Historical test counts and artifact hashes are not current-tip claims.
**VERDICT: PASS**

Findings: 0
Reviewed implementation commit: `3f9c283`
Base: `p0.12-kdf-profiles` at `164cb51`
Branch: `p0.13-lock-save-load`
Date: 2026-08-04

This is the author gate for the refreshed P0.13 implementation tip. The reserved independent-review path, `p0.13-lock-save-load.review.md`, is intentionally absent and remains available for a fresh independent reviewer.

The roadmap item remains `[~]`, not `[x]`. The authoritative `main` roadmap and [ADR-0010](../adr/0010-ios-local-html-execution.md) already place direct iOS local execution at P0.19 rather than in P0.13 acceptance; P0.14 remains blocked on P0.13 until P0.13 receives an independent PASS and completes its merge workflow. This historical author PASS does not treat Quick Look as execution evidence.

## Gate results

The complete remediation diff was reviewed after implementation. The gate covered the explicit mode-gated opener, session save nonce rules, opaque online secret preservation, `.cbx` naming, manual share/QR controls, QR sequence validation, the warm/cold source boundary, typed protocol changes, deterministic build, negative vendor checks, and both browser engines.

```text
$ git diff --check 164cb51..3f9c283
PASS

$ npm.cmd run verify-vendor -- --offline
Local vendor verified: all eight pinned artifacts
Vendor verification passed in offline mode.

$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm.cmd test
tests 48
pass 48
fail 0

$ npm.cmd run build
1108f91fc661cc3790d53740ef6f845dd3dbd023b6182ab4e951447496528ca4
463439 bytes

$ npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (1108f91fc661cc3790d53740ef6f845dd3dbd023b6182ab4e951447496528ca4)
Browser harness passed in Chromium and Firefox.
```

The full suite also passed the deliberate corrupted-vendor, unmanifested-vendor, CSP lockdown, missing-randomness, tampered-build, protocol, vault-authentication, and public-only HKDF negatives. The browser gate created an offline vault so the real output crossed multiple QR frames, rejected a missing middle frame, reassembled the complete ordered frame set, and then unlocked it.

## Acceptance review

| # | Criterion | Automated result | Evidence |
|---|---|---|---|
| 1 | Three save paths and symmetric load | PASS for available APIs | File System Access is implemented when exposed; blob download and manual raw-base64/multipart-QR save/load are exercised in Chromium and Firefox. All file surfaces use `.cbx`. |
| 2 | At least one save path on every supported device-matrix platform | PASS for automated scope; physical supported platforms remain | The manual path has visible copy/select fallback and optional share/QR enhancements; direct iOS local execution is a blocked target under ADR-0010, not a supported-platform pass. |
| 3 | Idle auto-lock and `Esc Esc` panic hide | PASS for implementation/browser coverage | Cold timer and cleanup are present; Chromium/Firefox verify warm concealment, cold-frame panic requests, locked session evidence, cleared working bytes, and cleared passphrase input. |
| 4 | Secret compartment stays sealed online and saves obey nonce rules | PASS | HKDF tracking observes only `cbx/public/v1` online; session tests prove offline public/secret nonce rotation and online opaque secret-byte preservation. |

## Security checklist

- The warm source and template contain no `__coldboxVault`, passphrase field, `openPublic`, `deriveSecretSubkey`, or `cbx/secret/v1` path.
- `vault.open` carries ciphertext only; the cold passphrase input is `spellcheck="false"` and is cleared on explicit lock, idle lock, mode transition, and panic hide.
- The online opener never derives `cbx/secret/v1`; full opener selection is cold-only and requires an offline mode signal.
- Every session save creates a fresh public nonce; offline saves create a fresh secret nonce; online saves copy the original secret nonce/ciphertext without deriving its key.
- Closing the session zeroes keys, plaintext, current encrypted bytes, pending bytes, and the passphrase field. Browser assertions inspect the locked/cleared cold state after both panic paths.
- The warm shell receives only sanitized public data, encrypted bytes, typed status, or generic error messages. The `panic.hide` message has an empty payload.
- Manual raw base64 and QR-frame parsing enforce the vault size ceiling, QR count/payload limits, sequence completeness, and duplicate rejection before the existing encrypted-byte validator runs.
- `.cbx` is the only P0.13 file extension in the picker, suggested name, download name, packet, and changelog.
- The local QR encoder is pinned and verified offline; no network dependency was added to the runtime bundle.
- The blob URL is revoked after download scheduling; File System Access errors and user cancellation are rendered generically.
- The opaque sandbox, CSP, network throw contract, global-message discard, runtime airgap guard, and browser lockdown negatives remained green in both browser engines.

## Findings

None. Findings: 0.

## What remains unverified

- A human iPhone test was attempted from the Files app. Files offered only Quick Look for `coldbox.html`; Safari execution was unavailable. [ADR-0010](../adr/0010-ios-local-html-execution.md) records this as a blocked portability target, not a pass. The device model and iOS version were not recorded.
- Android Chrome, macOS Safari/Chrome, Linux Firefox, and Tor Browser remain part of the human matrix.
- Upstream online vendor re-download verification was not exercised in this author environment; local offline verification and corruption negatives passed.
- Camera-based QR scanning is not claimed; the implementation accepts copied/scanned numbered frame text and renders frames locally.

## Verdict rationale

The historical P0.13 author gate had no author-gate findings. Its browser-verifiable criteria passed in Chromium and Firefox, including the multipart QR and cleanup cases added for the earlier independent-review findings. The roadmap remained `[~]`; authoritative `main` now records the iOS target at P0.19, and P0.14 remains blocked on P0.13 until independent PASS plus merge. No PR was merged by this author evidence.

PASS

<!-- P0.13-STACK-REFRESH -->
## Stacked-branch refresh

Prompted by the current-tip review of PR #21. Stacked-branch synchronization only; no implementation change, no roadmap status change, and no ADR change.

This branch now merges the remediated `p0.12-kdf-profiles` tip `bf44a8f`, which itself carries the remediated `p0.11-vault-format` tip `bb7996e`. Those parents corrected stale reproducibility evidence and repaired a UTF-8 double-encoding defect in the P0.11 self-review; neither touched implementation.

The merge conflicted only in `BATCH-2026-08-03.md`. This branch's copy already restates current P0.11, P0.12, and P0.13 figures, so the parents' historical paragraphs and their superseded notes were dropped in favour of this branch's current text. No other file conflicted.

**Preserved unchanged, and re-confirmed after the merge:** ADR-0010 (Accepted, Choice 3), the P0.13 roadmap status `[~]`, and the P0.14 roadmap status `[ ]`. Quick Look is still not recorded as execution evidence. P0.14 has not been started and no P0.14 branch exists.

Independently re-verified at the post-merge tip:

```text
$ git diff --check
PASS

$ npm run verify-vendor -- --offline
Vendor verification passed in offline mode.        (8 local artifacts)

$ npm run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm test
# tests 48
# pass 48
# fail 0

$ npm run build   (clean rebuild, twice)
Built build/coldbox.html (1108f91fc661cc3790d53740ef6f845dd3dbd023b6182ab4e951447496528ca4)
1108F91FC661CC3790D53740EF6F845DD3DBD023B6182AB4E951447496528CA4
463439
```

This reproduces the 463,439-byte artifact at `1108F91F…496528CA4` already recorded for P0.13, confirming the parent merges are artifact-neutral for this branch.

**Not re-run in this environment.** `npm run test:browser` (Playwright browsers are not installed here) — the Chromium/Firefox results recorded above stand from the authoring environment and are not re-asserted as fresh evidence. Node here is 22.22.3 against the pinned 24.16.0; the build proved byte-identical across both, but a confirming host build is recommended.

<!-- P0.13-POST-P012-MERGE-RECONCILIATION -->
## Post-P0.12 merge reconciliation -- historical author evidence for `f3eb4474...`

P0.12 had been independently reviewed, closed out, and merged into `main`. The author reconciliation merged exact `main` `9a4567a9e371a924b8e43810a759e2e81890b48d` into the prior PR #21 tip `4f56909dc696a20b96a72d67a3d94daecadbe105`; no rebase or force-push was used.

The merge had exactly three content conflicts:

- `docs/05-development/packets/BATCH-2026-08-03.md` -- resolved to current main's frozen historical snapshot.
- `src/cold/main.js` -- preserved both P0.13 `updateVaultControls()` and current-main `updateBenchmarkAvailability()`.
- `src/cold/styles.css` -- preserved one `--cold-pink` declaration and current-main `--cold-disabled-fill`.

The reconciliation also repaired the P0.13 session/API integration defect exposed by current main:

- session open passes the P0.11 cold-health gate and airgap snapshot checks;
- the DEK is zeroed before the session is returned rather than retained for the session lifetime;
- `save()` re-checks cold health and current airgap-derived network mode every time;
- health or mode drift closes/zeroes the session and fails serialization closed;
- online saves rotate the public nonce while preserving the secret nonce/ciphertext/tag region byte-for-byte opaque;
- offline saves rotate both public and secret nonces and re-encrypt the decrypted secret compartment;
- concurrent or post-close saves fail closed;
- P0.13 idle-lock, mode-change lock, explicit lock, and panic-hide paths call `session.close()`.

`test/vault.test.js` treats `openSession` as a P0.13 cold-only API, covers health gating, nonce rotation, online opaque-secret preservation, and session closure on mode/health drift. The browser harness requires the cold-only `openSession` surface while separately proving `deriveSecretSubkey` remains unavailable.

Historical author verification for `f3eb4474...`:

```text
npm ci
PASS

npm run verify-vendor -- --offline
PASS

npm run lint
PASS

node --test test\vault.test.js test\protocol.test.js
22/22 PASS

npm test
57/57 PASS

npm run build
PASS

npm run build
PASS (byte-identical repeat)

build/coldbox.html
SHA-256: f82b16efd894b43add0d8ead8148560ae9c172b5d4cf20cd8a2bae09f3826d59
Bytes: 582354

npm run test:browser
PASS

git diff --cached --check origin/main --
PASS
```

That exact tip received a fresh independent **FAIL** with three governance/documentation findings. The reviewer report content is preserved; only trailing spaces are normalized for repository diff hygiene in `p0.13-lock-save-load.review.md`.

<!-- P0.13-F3EB447-FAIL-AUTHOR-REMEDIATION -->
## Author remediation after the fresh `f3eb4474...` FAIL

The remediation follows the review without weakening the review standard:

1. **F1 -- iOS acceptance gate:** no device evidence is fabricated. A docs-only governance commit was landed directly on `main` at `cd9f60f0392f122651129aa143f8e4d26e7ad839`, making accepted ADR-0010 authoritative and moving direct iOS Files-to-Safari execution to the separately recorded P0.19 portability target.
2. **F2 -- criterion reinterpretation:** PR #21 no longer defines its own acceptance re-baseline. Its roadmap/ADR text is inherited from authoritative `main@cd9f60f0392f122651129aa143f8e4d26e7ad839`; this branch changes only the P0.13 status to `[~]` while the new exact tip awaits independent review.
3. **F3 -- stale/malformed evidence:** this self-review's malformed PowerShell-interpolated Markdown is repaired, historical evidence is explicitly labeled historical, and the packet now records the exact current test/build evidence.

Fresh author verification on the remediation candidate:

```text
npm ci
PASS

npm run verify-vendor
PASS

npm run verify-vendor -- --offline
PASS

npm run lint
PASS

node --test test\vault.test.js test\protocol.test.js
22/22 PASS

npm test
57/57 PASS

npm run build
PASS

npm run build
PASS (byte-identical repeat)

build/coldbox.html
SHA-256: f82b16efd894b43add0d8ead8148560ae9c172b5d4cf20cd8a2bae09f3826d59
Bytes: 582354

npm run test:browser
PASS

git diff --cached --check origin/main --
PASS
```

P0.13 remains `[~]`. P0.14 remains unchecked and unstarted. This is author evidence only; the new exact PR #21 tip requires a fresh independent binary review before merge.

<!-- P0.13-D6ECE-RUNTIME-HEALTH-AUTHOR-REMEDIATION -->
## Author remediation after the independent FAIL at `d6ece634...`

The independent reviewer reported three findings on exact commit `d6ece634b9597c7cfd3da126dd075cdfc69d0ade`.

1. **Runtime airgap/health session zeroization:** cold-realm airgap, capability, and crypto failure setters now clear and close the active vault session before exposing locked/failure state. A save rejection also fail-closes the cold session owner, so a session that internally closes after a health/mode failure cannot remain presented as unlocked. Chromium and Firefox create an unlocked vault, trigger a real runtime `fetch` violation through the neutered network guard, and verify locked session evidence, cleared working bytes, cleared passphrase, full lockdown, and locked warm/cold status. A second browser regression forces the save-time health predicate red while unlocked and verifies the same session cleanup.
2. **P0.19 handoff semantics:** both human-action and batch-resume examples now require every supported execution-matrix platform to pass while the separate iOS local-execution target is recorded as PASS, BLOCKED, or UNSUPPORTED with exact device/iOS build under ADR-0010. They no longer claim that iOS Safari itself must pass before P0.19 can complete.
3. **Mojibake:** the P0.13 packet/self-review UTF-8 punctuation is repaired. The pre-existing independent review history was not rewritten; its original bytes were preserved and the `d6ece634...` FAIL handoff was append-only.

Fresh author verification:

```text
npm ci
PASS

npm run verify-vendor
PASS

npm run verify-vendor -- --offline
PASS

npm run lint
PASS

node --test test\vault.test.js test\protocol.test.js
22/22 PASS

npm test
57/57 PASS

npm run build
PASS

npm run build
PASS (byte-identical repeat)

build/coldbox.html
SHA-256: b7d10476ede992d62b2e5d0773b3c4435c5542886a7fab80a0bb83698eccb147
Bytes: 582749

npm run test:browser
PASS - Chromium and Firefox, including unlocked runtime-airgap and save-time health-failure zeroization regressions

git diff --check
PASS
```

P0.13 remains `[~]`. P0.14 remains unchecked/unstarted. This is author evidence only and does not create an independent PASS. A fresh reviewer must append a new binary verdict to the preserved review history.

<!-- P0.13-BE485-R3-CONSISTENCY-AUTHOR-REMEDIATION -->
## Author remediation after the independent FAIL at `be485c36...`

The independent reviewer reported two current-tip consistency findings on exact commit `be485c36a6054a361d6a0c9779d88c997f1d4a68`.

1. **R3-F1 - vendor inventory:** the P0.13 packet now states that offline verification covers all 10 artifacts in `vendor/vendor-manifest.json`, including `qrcode-generator@1.4.4`. The manifest count is checked directly during this author gate; both upstream and offline verification pass.
2. **R3-F2 - portability guidance:** `release-checklist.md` now distinguishes the seven supported execution-matrix contexts from the separate iOS local-execution target, which is recorded PASS, BLOCKED, or UNSUPPORTED with exact device/iOS build under ADR-0010. The live manual-export UI no longer presents iOS Safari as a supported destination and explicitly states that direct iOS Files-to-Safari execution is not currently supported.
3. **Verification harness race hardening:** the existing CSP-stripped `warm-only`/`both` fixture now waits for the warm canary's already-required terminal `data-csp-canary="failed"` state before asserting it. This removes an ordering race where cold-side lockdown could make `data-lockdown-state="full"` visible while the asynchronous warm canary was still `checking`; it does not weaken or change the CSP product behavior being asserted.

Fresh author verification for the new exact tip:

```text
npm ci
PASS

vendor/vendor-manifest.json artifacts
10

npm run verify-vendor
PASS

npm run verify-vendor -- --offline
PASS

npm run lint
PASS

node --test test\vault.test.js test\protocol.test.js
22/22 PASS

npm test
57/57 PASS

npm run build
PASS

npm run build
PASS (byte-identical repeat)

build/coldbox.html
SHA-256: 070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0
Bytes: 582857

npm run test:browser
PASS - Chromium and Firefox

git diff --check
PASS
```

The built artifact contains the corrected portability copy and no longer contains the stale phrase `Export the encrypted bytes as text for iOS Safari`.

P0.13 remains `[~]`. P0.14 remains unchecked/unstarted. This is author evidence only; the new exact PR #21 tip requires a fresh independent binary review before merge.