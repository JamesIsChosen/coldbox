# P2.3 — Seed XOR cold-only backups

**Roadmap item:** [P2.3 Seed XOR](../ROADMAP.md)

**Branch:** `p2.3-seed-xor`

**Base:** `main` at `2375fbcfb76b179228bac60402b62bce8f379c74`

**Initial product commit:** `e6506588bb037c3cb56747a68a92cdd208f79d65`

**Reviewed product/integration tip:** `0918a6cdc1b63cd6cd657494f6b9683afe9cf179`

**Status:** R5-F1 EVIDENCE-ONLY REMEDIATION; CURRENT-MAIN BUILD EVIDENCE CORRECTED; READY FOR FRESH INDEPENDENT R6 RE-REVIEW.

**Current change:** documentation and build-evidence correction only; no Seed XOR product or security code changed.

**Pull request:** [#50](https://github.com/JamesIsChosen/coldbox/pull/50)

## 1. Summary

This item adds a cold-only, Coldcard-compatible Seed XOR implementation for
2–4 complete BIP-39 parts from 12-, 18-, or 24-word source phrases. It adds
deterministic and CSPRNG-derived masks, independent vector tests, and a masked
timed-reveal UI that never sends or persists the phrases. The user-facing guide
and ADR document the N-of-N property, entropy domain, Coldcard construction,
and separate BIP-39 passphrase boundary.

## 2. Scope

Included:

- `src/cold/seed-xor.js`: entropy-domain split/combine APIs, deterministic
  Coldcard masks, random masks, input validation, and transient zeroization.
- Cold-realm markup and wiring in `src/cold/index.html`, `src/cold/main.js`,
  and `src/cold/styles.css`, including masked inputs/outputs, 30-second reveal
  timers, count and wordlist selection, and lock/teardown clearing.
- Independent Node tests, browser-harness coverage, and build assembly.
- The P2.3 roadmap marker, changelog, dependencies note, user guide, ADR, and
  guide index entry.

Deliberately not included in the P2.3 product change: SLIP-39,
Shamir39/raw SSS, vault recovery records, threshold recovery, BIP-39
passphrase splitting, clipboard/download/storage, new message types, or any
warm-realm Seed XOR surface. The branch is integrated with current `main`, so
the other Phase-2 products are present in the base; this PR does not modify
their product or security code.

## 3. How to verify

Run from the repository root with `p2.3-seed-xor` checked out. The
build-affecting product/integration state is exact tip
`0918a6cdc1b63cd6cd657494f6b9683afe9cf179`, against current
`main@2375fbcfb76b179228bac60402b62bce8f379c74`. The review-history commit
and this evidence-only correction touch only `docs/`, so they do not change
the product artifact; the exact current artifact is recorded below.

```text
npm.cmd test
ℹ tests 357
ℹ pass 357
ℹ fail 0
```

```text
npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
...
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.
```

```text
npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 197 markdown file(s) checked, 0 warning(s).
```

The exact integrated product tip was built twice in a Git-capable detached
checkout. Both builds produced the same current artifact:

```text
npm.cmd run build
Built build/coldbox.html (ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d)

Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
hash=ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d size=2428951

npm.cmd run build
Built build/coldbox.html (ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d)

Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
hash=ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d size=2428951
```

The browser harness was run from `file://` with the locally installed
Playwright browsers:

```text
$env:PLAYWRIGHT_BROWSERS_PATH = 'C:\Users\semaj\AppData\Local\Temp\coldbox-playwright-browsers'
npm.cmd run test:browser
Chromium: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Firefox: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

The independent reproducibility check used a separate Git-capable checkout at
a different path with `TZ=Pacific/Kiritimati`, `LANG=fr-FR`, and `LC_ALL=fr-FR`:

```text
git checkout --detach 0918a6cdc1b63cd6cd657494f6b9683afe9cf179
$env:TZ = 'Pacific/Kiritimati'; $env:LANG = 'fr-FR'; $env:LC_ALL = 'fr-FR'; npm.cmd run build
Built build/coldbox.html (ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d)
hash=ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d size=2428951
```

The separate checkout was clean after the check. The R5 independent review
also confirmed that deliberate vendor corruption and executable `Math.random()`
mutations failed with non-zero status, while Chromium and Firefox `file://`
verification passed for the full Phase-2 surface and Seed XOR.

```text
vendor corruption: verify-vendor exit 1
src/cold/seed-xor.js Math.random mutation: lint exit 1; build exit 1
Chromium: PASS
Firefox: PASS
```

## 4. Acceptance criteria

| Criterion | How satisfied | Test |
|---|---|---|
| only 12-, 18-, and 24-word BIP-39 phrases are accepted | The cold API maps only 16-, 24-, and 32-byte entropy to 12, 18, and 24 words and rejects every other count or entropy length. | `test/seed-xor.test.js` rejects unsupported lengths; browser flow exercises the supported UI. |
| every generated part is independently valid BIP-39 | Every entropy part is passed through the vendored Seed Forge encoder, and the focused tests validate each generated part. | `Seed XOR random mode round-trips 12, 18, and 24 words for 2 through 4 parts`; deterministic vector tests validate generated parts. |
| deterministic output matches the published Coldcard construction and independent vectors | Deterministic masks double-hash `Batshitoshi ` + source entropy + zero-based `i of N parts`, then use the entropy-length prefix; the final part XORs the source with earlier masks. | The independent Coldcard three-part 24-word vector and an independent Node `crypto.createHash('sha256')` reference both pass. |
| random output refuses missing `crypto.getRandomValues` | Random mode checks for the CSPRNG API and throws before processing when it is absent; it never falls back to `Math.random`. | `Seed XOR rejects malformed sets, unsupported lengths, and missing randomness`; browser lockdown coverage also tests missing randomness. |
| combine accepts all parts in any order but rejects missing, malformed, mismatched-length, or invalid-checksum parts | The combine path validates each selected phrase, enforces 2–4 equal-length parts, and XORs in input order, which is commutative. | Focused tests cover arbitrary order, incomplete sets, invalid checksum, unsupported count, and mismatched length; browser UI covers incomplete-set rejection. |
| the source, parts, and combined phrase never cross the cold boundary or persist, are masked by default, and clear on teardown | The Seed XOR module is assembled only into the cold realm. No warm selector, protocol message, storage, clipboard, download, or passphrase field was added. UI inputs are cleared after use; result state, DOM, and reveal timers are cleared by `clearVaultSession`. | Focused source-surface test; browser assertions prove warm isolation, masked defaults, timed reveal, and teardown clearing. |
| docs state that Seed XOR is N-of-N and that any BIP-39 passphrase is separate | The three-depth guide, cold UI copy, ADR, and glossary link state the all-parts requirement and separate passphrase boundary. | `npm.cmd run check-docs`; browser help/UI text assertions and the documented external references. |

## 5. Security impact

- **Realm boundary:** Yes. Secret phrases, entropy buffers, generated parts, and
  the combined phrase are processed only in the cold realm. No new message type
  or warm-facing state was added. The browser harness asserts that
  `window.__coldboxSeedXor` is absent from the warm page and that teardown
  clears the cold state.
- **Message schema:** No change. Seed XOR has no message path and cannot carry a
  mnemonic, private key, xprv, passphrase, or other secret-compartment value.
- **CSP/network:** No new host or capability. The existing cold CSP remains
  `connect-src 'none'`; Chromium and Firefox runtime probes reported blocked
  fetch, XHR, WebSocket, EventSource, and `sendBeacon` calls.
- **Randomness:** Random masks require `crypto.getRandomValues` and fail closed
  when unavailable. The repository lint and focused source test reject
  `Math.random`, `eval`, and `new Function` in this module.
- **If this were wrong:** A bad entropy domain or mask construction could make
  Coldbox unable to recover or interoperate with a written backup. A boundary
  or teardown mistake could expose the original phrase or a generated part to
  the warm document or a later session. Those risks are addressed by the
  independent Coldcard vector, Node reference, source-surface checks, and
  browser teardown tests, but the external reviewer must re-check the exact
  algorithm and all cold/warm paths.

## 6. Test evidence

New focused tests in `test/seed-xor.test.js` prove:

- interoperability with Coldcard's published three-part 24-word example;
- deterministic masks against an independent Node SHA-256 implementation for
  all supported entropy lengths;
- entropy XOR and any-order reconstruction;
- random round trips for 12, 18, and 24 words with 2, 3, and 4 parts;
- invalid checksum, mismatched length, unsupported count/mode, and unavailable
  randomness rejection; and
- absence of warm-boundary and persistence surfaces.

The vector source is [Coldcard's published Seed XOR documentation](https://github.com/Coldcard/firmware/blob/master/docs/seed-xor.md);
the construction is cross-checked against the [published firmware implementation](https://github.com/Coldcard/firmware/blob/master/shared/xor_seed.py).
The deterministic reference in the test is independently implemented with
Node's built-in `crypto.createHash`, not imported from the product module.

The browser harness adds both-engine checks for the official vector, random
flow, masked output, explicit reveal, incomplete sets, warm isolation, and
teardown. The intentional forbidden-construct build failure exited with code
1, rather than merely printing an error.

Not tested: physical iOS local execution, Android Files, macOS Safari, macOS
Chrome, Linux Firefox, Tor Browser, or a hardware wallet. No production seed,
private key, or vault was used or committed.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | PASS | Chromium Playwright `file://` harness; Seed XOR and full regression matrix passed. |
| Windows Firefox | PASS | Firefox Playwright `file://` harness; Seed XOR and full regression matrix passed. |
| macOS Safari | UNTESTED | No macOS device in this session. |
| macOS Chrome | UNTESTED | No macOS device in this session. |
| Linux Firefox | UNTESTED | No Linux device in this session. |
| **iOS local-execution target** | UNTESTED | No physical iOS device or iOS build was available; Quick Look was not treated as a Safari result. |
| Android Chrome (Files) | UNTESTED | No physical Android device in this session. |
| Tor Browser | UNTESTED | No Tor Browser in this session. |

## 8. Assumptions made

- The Coldcard documentation and firmware source reviewed on 2026-08-12 are
  the authoritative external interoperability references for this roadmap
  item. If Coldcard changes the prefix, index encoding, hash count, supported
  lengths, or part-count semantics, the vector and ADR require a new review.
- Seed XOR's random construction uses double-SHA-256 of CSPRNG bytes before
  truncation, matching the published firmware shape; it is not claimed to be
  threshold recovery or a substitute for a separately backed-up BIP-39
  passphrase.
- All vendored BIP-39 wordlists may use the same entropy operation, while
  English is the documented Coldcard interoperability choice.
- Timed reveal is a usability control, not a guarantee against screenshots,
  shoulder surfing, browser memory forensics, or malicious host hardware.

## 9. What to scrutinise

Review the exact deterministic mask bytes in `src/cold/seed-xor.js` against the
Coldcard source, especially the zero-based index, ASCII suffix, double hash,
and final XOR part. Then inspect the cold-only wiring and teardown paths in
`src/cold/main.js`: source and part fields are cleared immediately after
processing, output strings remain only in cold-local state, and reveal timers
must not outlive a lock. Finally, verify that no future refactor turns the
returned entropy or mnemonic into a warm message, storage record, clipboard
payload, or passphrase surrogate.

## 10. Self-assessment

- The implementation is most sensitive to a future interpretation change in
  Coldcard's random-mask construction; the published deterministic vector does
  not by itself prove random-mode interoperability.
- Browser automation covers Windows Chromium and Firefox only. Physical mobile,
  Safari, Tor, and hardware-wallet execution were not performed.
- JavaScript strings cannot be explicitly zeroized; the implementation clears
  all DOM and object references on use/lock, but this is not a claim of perfect
  garbage-collector erasure.
- Future threshold-share work must not reuse this API or describe Seed XOR as
  SLIP-39/Shamir recovery. P2.4 is the separate threshold/raw-SSS item.

## 11. Bundle impact

The exact current-main baseline `2375fbcfb76b179228bac60402b62bce8f379c74`
was independently built twice in a Git-capable checkout. Both builds produced
SHA-256 `c724ec9c1a90590cdb42e3cbff82fa5b3cffa5999234f8f5aa05756299e4e79e`
at 2,383,563 bytes. The integrated P2.3 artifact at exact product/integration
tip `0918a6cdc1b63cd6cd657494f6b9683afe9cf179` is
`ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d` at
2,428,951 bytes. Against the exact current-main base, the P2.3 Bundle Impact
is therefore 45,388 bytes (+44.32 KiB; 45,388 / 1,024). This is the increase
from the cold UI, source module, and harness-facing assembly; no new runtime
dependency or network fetch was introduced.

## Historical artifact evidence (superseded)

The following records historical initial-product builds and is retained only
to explain the earlier review discrepancy. Neither digest below is a current
P2.3 artifact; both are superseded historical/pre-integration evidence.

The combined independent review reported that the historical packet's earlier
build hash (`2acc62bd...`, 2,223,968 bytes) did not reproduce in its Git-capable
fresh checkout. The discrepancy was explained and reproduced rather than
merely replaced:

```text
Git-capable build, exact product commit e6506588bb037c3cb56747a68a92cdd208f79d65:
9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778  2223951

Restricted build process, same checkout and source:
2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97  2223968
```

The historical packet evidence came from the restricted build process. In that
environment, `scripts/build.js` cannot spawn `git` for its informational
source-commit date and intentionally uses its documented
`unknown (no git commit metadata available)` fallback. That fallback is
embedded in the HTML, accounting for the 17-byte difference and the different
digest. A normal Git-capable checkout at that historical state reproduced the
reviewer's exact `9a6a0616...` hash in two successive builds and in the
alternate-path build recorded above. No product source or build-input change
was made for that historical finding; the packet distinguishes the restricted
fallback from the reviewable historical artifact.

## 12. Docs updated

- `docs/03-guides/backup-seed-xor.md` — plain, working, and technical help
  depths, limitations, recovery steps, and review date.
- `docs/03-guides/README.md` — guide index entry.
- `docs/05-development/adr/0039-seed-xor-cold-only.md` and ADR index — the
  entropy-domain and cold-boundary decision.
- `docs/05-development/dependencies.md` — inline implementation note.
- `docs/05-development/ROADMAP.md` — P2.3 set to `[~]`; the author does not
  mark it independently verified.
- `CHANGELOG.md` — P2.3 entry dated 2026-08-12.

## 13. Historical pre-integration remediation round 3 (superseded)

Everything in this section predates the current-main integration and is
retained only as review history. Its `d8b163...` artifact is not current.

The fresh independent review at product commit
`5d580ee5fc6d10026e57ccdf3942ede08446cba0` found one actionable issue:
`Math.random()` was rejected in `src/capabilities.js` but not in cold security
modules. The remediation is committed at
`23c73c4e34506af6be0b364da54c9eff5ac7bc61`.

The security gate now applies the existing `Math.random()` rejection to every
`src/cold/` module. A cold-module fixture proves the lint rejects executable
`Math.random()`, and a build-level mutation proves the build exits non-zero
and reports `Math.random` when `src/cold/seed-xor.js` is altered. The existing
warm capability gate remains covered as well.

Verification from this remediation checkout:

```text
npm.cmd ci
added 2 packages, and audited 3 packages in 889ms
found 0 vulnerabilities

node --test test/seed-xor.test.js test/lint.test.js
tests 10
pass 10
fail 0

node --test test/build.test.js
tests 14
pass 14
fail 0

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd test
tests 328
pass 328
fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run check-docs
Documentation hygiene check passed: 184 markdown file(s) checked, 0 warning(s).

npm.cmd run build (twice, Git-capable process)
d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6  2223951 bytes
d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6  2223951 bytes

npm.cmd run test:browser
Chromium: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Firefox: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

The browser run was a fresh full harness over `file://`, not only the
Seed-XOR item. No product source was changed; the remediation is limited to
the shared security lint and its negative coverage. This branch is ready for
another independent review. P2.3 remains `[~]`; the author has not marked it
independently verified.

## 14. Historical pre-integration remediation round 4 (superseded)

Everything in this section predates the current-main integration and is
retained only as review history. Its `d8b163...` artifact is not current.

The fresh independent review recorded in commit
`b5b3bbbd730096bd42ffae4e0628a670c098796c` reviewed the exact remediation
head `540a3f2ae0d7b026b5030d21387b01e3c9421da2`. The only finding was stale
round-3 build evidence. No product or security code was changed in this
round.

Two fresh Git-capable builds from that exact committed remediation state
produced:

```text
d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6  2223951
d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6  2223951
```

The packet's current evidence now identifies the exact committed-head
artifact. The round-3 security remediation remains unchanged and
independently passed; P2.3 remains `[~]` pending fresh independent review.
## 15. Historical candidate-tree current-main integration evidence (superseded)

This candidate-tree record predates the exact pushed integration-tip evidence
below. It is retained for audit history only; its candidate artifact is not
the current P2.3 artifact.

On 2026-08-13, the P2.3 branch was resolved against current `main` commit
`2375fbcfb76b179228bac60402b62bce8f379c74`. The resolved candidate preserved
the Seed XOR implementation blob
`9b773b2f5f99067db494c5727f02b057ce520906` and the prior independent-review
history blob `857eeb9c1077e7358a2aa3ff042aacd36b8940ef`.

Before the integration commit, the complete resolved candidate passed:

```text
npm.cmd run verify-vendor
PASS

npm.cmd run lint
PASS

npm.cmd run check-docs
PASS

npm.cmd test
PASS

npm.cmd run build
PASS

npm.cmd run build
PASS

candidate build SHA-256:
c2a16cce6df4d465f9c647be4a69aa551afb25d7887ef61ae822cdc047defb0c
candidate build size:
2428951 bytes

npm.cmd run test:browser
Chromium + Firefox PASS
```

The worktree initially lacked the declared Playwright development dependency.
It was restored with `npm.cmd ci --ignore-scripts` while browser downloading was
disabled; the existing local Playwright browser binaries were then used for the
Chromium and Firefox `file://` harness.

This is candidate-tree integration evidence, not a claim of independent
exact-tip review. The fresh reviewer must independently verify the pushed
integration commit under the review protocol. P2.3 remains `[~]` until that
review passes.

## 16. Current R5-F1 evidence-only remediation

R5-F1 identified stale present-tense artifact and Bundle Impact claims after
current-main integration. This remediation changes only this packet and the
matching self-review. It does not change `src/cold/seed-xor.js`, any product or
security source, the roadmap marker, or the preserved review history.

The exact build-affecting product/integration tip handed to R6 is
`0918a6cdc1b63cd6cd657494f6b9683afe9cf179`, a merge of P2.3 pre-integration
head `e294eae023ad6bc625fb45f2d80909f921306ccf` and current
`main@2375fbcfb76b179228bac60402b62bce8f379c74`. The protected source and
review-history blobs remain:

```text
src/cold/seed-xor.js
9b773b2f5f99067db494c5727f02b057ce520906

docs/05-development/packets/p2.3-seed-xor.review.md
e1f3dd8ba08130d4339930f55d087e0eeffaadcd
```

The exact current-main baseline was independently checked in a Git-capable
detached checkout:

```text
git checkout --detach 2375fbcfb76b179228bac60402b62bce8f379c74
HEAD is now at 2375fbc Merge pull request #49 from JamesIsChosen/p2.2-codex32

npm.cmd run build
Built build/coldbox.html (c724ec9c1a90590cdb42e3cbff82fa5b3cffa5999234f8f5aa05756299e4e79e)
BASE-GIT-A c724ec9c1a90590cdb42e3cbff82fa5b3cffa5999234f8f5aa05756299e4e79e 2383563

npm.cmd run build
Built build/coldbox.html (c724ec9c1a90590cdb42e3cbff82fa5b3cffa5999234f8f5aa05756299e4e79e)
BASE-GIT-B c724ec9c1a90590cdb42e3cbff82fa5b3cffa5999234f8f5aa05756299e4e79e 2383563
```

The exact integrated tip was independently built twice with Git metadata
available:

```text
git checkout --detach 0918a6cdc1b63cd6cd657494f6b9683afe9cf179
HEAD is now at 0918a6c merge(p2.3): integrate current main

npm.cmd run build
Built build/coldbox.html (ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d)
INTEGRATED-GIT-A ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d 2428951

npm.cmd run build
Built build/coldbox.html (ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d)
INTEGRATED-GIT-B ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d 2428951
```

The alternate-path build used the same exact integration tip with
`TZ=Pacific/Kiritimati`, `LANG=fr-FR`, and `LC_ALL=fr-FR`:

```text
ALT-A ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d 2428951
ALT-B ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d 2428951
```

Current verification rerun from the author worktree passed:

```text
npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 197 markdown file(s) checked, 0 warning(s).

node --test test/seed-xor.test.js test/build.test.js test/lint.test.js
tests 24
pass 24
fail 0

npm.cmd test
tests 357
pass 357
fail 0
```

The R5 independent review additionally passed the deliberate vendor-corruption
and executable-`Math.random()` non-zero gates and the complete Chromium and
Firefox `file://` harness. P2.1, P2.2, and P2.4 remain `[x]`; P2.3 remains
`[~]` pending the fresh independent R6 verdict. No merge is authorized by
this packet.
