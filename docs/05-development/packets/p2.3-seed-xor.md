# P2.3 — Seed XOR cold-only backups

**Roadmap item:** [P2.3 Seed XOR](../ROADMAP.md)

**Branch:** `p2.3-seed-xor`

**Base:** `main` at `5f0fa5e442524033e91bb31006a0ca6288385dc0`

**Implementation tip:** `e6506588bb037c3cb56747a68a92cdd208f79d65`

## 1. Summary

This item adds a cold-only, Coldcard-compatible Seed XOR implementation for
2–4 complete BIP-39 parts from 12-, 18-, or 24-word source phrases. It adds
deterministic and CSPRNG-derived masks, independent vector tests, and a masked
timed-reveal UI that never sends or persists the phrases. The user-facing guide
and ADR document the N-of-N property, entropy domain, Coldcard construction,
and separate BIP-39 passphrase boundary.

## 2. Scope

Included:

- `src/cold/seed-xor.js`: entropy-domain split/combine APIs, deterministic
  Coldcard masks, random masks, input validation, and transient zeroization.
- Cold-realm markup and wiring in `src/cold/index.html`, `src/cold/main.js`,
  and `src/cold/styles.css`, including masked inputs/outputs, 30-second reveal
  timers, count and wordlist selection, and lock/teardown clearing.
- Independent Node tests, browser-harness coverage, and build assembly.
- The P2.3 roadmap marker, changelog, dependencies note, user guide, ADR, and
  guide index entry.

Deliberately not included: SLIP-39, Shamir39/raw SSS, vault recovery records,
threshold recovery, BIP-39 passphrase splitting, clipboard/download/storage,
new message types, or any warm-realm Seed XOR surface. Those belong to other
roadmap items or are explicitly outside Seed XOR.

## 3. How to verify

Run from the repository root with the committed branch checked out.

```text
npm.cmd test
ℹ tests 327
ℹ pass 327
ℹ fail 0
```

```text
npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
...
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.
```

```text
npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 181 markdown file(s) checked, 0 warning(s).
```

The product build was run twice from the same committed tip:

```text
npm.cmd run build
Built build/coldbox.html (2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97)

Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
hash=2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97 size=2223968

npm.cmd run build
Built build/coldbox.html (2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97)

Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
hash=2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97 size=2223968
```

The browser harness was run from `file://` with the locally installed
Playwright browsers:

```text
$env:PLAYWRIGHT_BROWSERS_PATH = 'C:\Users\semaj\AppData\Local\Temp\coldbox-playwright-browsers'
npm.cmd run test:browser
Chromium: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Firefox: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

The independent reproducibility check used a detached worktree at a different
path with `TZ=Pacific/Kiritimati` and `LANG=fr-FR` in the build process:

```text
git worktree add --detach C:\Users\semaj\Projects\coldbox\.review-p2.3 e6506588bb037c3cb56747a68a92cdd208f79d65
$env:TZ = 'Pacific/Kiritimati'; $env:LANG = 'fr-FR'; npm.cmd run build
Built build/coldbox.html (2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97)
hash=2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97 size=2223968
```

The detached worktree was clean after the check and was removed. A deliberate
`eval('1')` insertion into `src/cold/seed-xor.js` produced exit code 1:

```text
npm.cmd run build
Forbidden construct "eval" in src/cold/seed-xor.js:4:31
Lint failed: Forbidden-construct lint failed with 1 finding(s)
Error: Build refused: source failed forbidden-construct lint
Node.js v24.16.0
Process exited with code 1
```

## 4. Acceptance criteria

| Criterion | How satisfied | Test |
|---|---|---|
| only 12-, 18-, and 24-word BIP-39 phrases are accepted | The cold API maps only 16-, 24-, and 32-byte entropy to 12, 18, and 24 words and rejects every other count or entropy length. | `test/seed-xor.test.js` rejects unsupported lengths; browser flow exercises the supported UI. |
| every generated part is independently valid BIP-39 | Every entropy part is passed through the vendored Seed Forge encoder, and the focused tests validate each generated part. | `Seed XOR random mode round-trips 12, 18, and 24 words for 2 through 4 parts`; deterministic vector tests validate generated parts. |
| deterministic output matches the published Coldcard construction and independent vectors | Deterministic masks double-hash `Batshitoshi ` + source entropy + zero-based `i of N parts`, then use the entropy-length prefix; the final part XORs the source with earlier masks. | The independent Coldcard three-part 24-word vector and an independent Node `crypto.createHash('sha256')` reference both pass. |
| random output refuses missing `crypto.getRandomValues` | Random mode checks for the CSPRNG API and throws before processing when it is absent; it never falls back to `Math.random`. | `Seed XOR rejects malformed sets, unsupported lengths, and missing randomness`; browser lockdown coverage also tests missing randomness. |
| combine accepts all parts in any order but rejects missing, malformed, mismatched-length, or invalid-checksum parts | The combine path validates each selected phrase, enforces 2–4 equal-length parts, and XORs in input order, which is commutative. | Focused tests cover arbitrary order, incomplete sets, invalid checksum, unsupported count, and mismatched length; browser UI covers incomplete-set rejection. |
| the source, parts, and combined phrase never cross the cold boundary or persist, are masked by default, and clear on teardown | The Seed XOR module is assembled only into the cold realm. No warm selector, protocol message, storage, clipboard, download, or passphrase field was added. UI inputs are cleared after use; result state, DOM, and reveal timers are cleared by `clearVaultSession`. | Focused source-surface test; browser assertions prove warm isolation, masked defaults, timed reveal, and teardown clearing. |
| docs state that Seed XOR is N-of-N and that any BIP-39 passphrase is separate | The three-depth guide, cold UI copy, ADR, and glossary link state the all-parts requirement and separate passphrase boundary. | `npm.cmd run check-docs`; browser help/UI text assertions and the documented external references. |

## 5. Security impact

- **Realm boundary:** Yes. Secret phrases, entropy buffers, generated parts, and
  the combined phrase are processed only in the cold realm. No new message type
  or warm-facing state was added. The browser harness asserts that
  `window.__coldboxSeedXor` is absent from the warm page and that teardown
  clears the cold state.
- **Message schema:** No change. Seed XOR has no message path and cannot carry a
  mnemonic, private key, xprv, passphrase, or other secret-compartment value.
- **CSP/network:** No new host or capability. The existing cold CSP remains
  `connect-src 'none'`; Chromium and Firefox runtime probes reported blocked
  fetch, XHR, WebSocket, EventSource, and `sendBeacon` calls.
- **Randomness:** Random masks require `crypto.getRandomValues` and fail closed
  when unavailable. The repository lint and focused source test reject
  `Math.random`, `eval`, and `new Function` in this module.
- **If this were wrong:** A bad entropy domain or mask construction could make
  Coldbox unable to recover or interoperate with a written backup. A boundary
  or teardown mistake could expose the original phrase or a generated part to
  the warm document or a later session. Those risks are addressed by the
  independent Coldcard vector, Node reference, source-surface checks, and
  browser teardown tests, but the external reviewer must re-check the exact
  algorithm and all cold/warm paths.

## 6. Test evidence

New focused tests in `test/seed-xor.test.js` prove:

- interoperability with Coldcard's published three-part 24-word example;
- deterministic masks against an independent Node SHA-256 implementation for
  all supported entropy lengths;
- entropy XOR and any-order reconstruction;
- random round trips for 12, 18, and 24 words with 2, 3, and 4 parts;
- invalid checksum, mismatched length, unsupported count/mode, and unavailable
  randomness rejection; and
- absence of warm-boundary and persistence surfaces.

The vector source is [Coldcard's published Seed XOR documentation](https://github.com/Coldcard/firmware/blob/master/docs/seed-xor.md);
the construction is cross-checked against the [published firmware implementation](https://github.com/Coldcard/firmware/blob/master/shared/xor_seed.py).
The deterministic reference in the test is independently implemented with
Node's built-in `crypto.createHash`, not imported from the product module.

The browser harness adds both-engine checks for the official vector, random
flow, masked output, explicit reveal, incomplete sets, warm isolation, and
teardown. The intentional forbidden-construct build failure exited with code
1, rather than merely printing an error.

Not tested: physical iOS local execution, Android Files, macOS Safari, macOS
Chrome, Linux Firefox, Tor Browser, or a hardware wallet. No production seed,
private key, or vault was used or committed.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | PASS | Chromium Playwright `file://` harness; Seed XOR and full regression matrix passed. |
| Windows Firefox | PASS | Firefox Playwright `file://` harness; Seed XOR and full regression matrix passed. |
| macOS Safari | UNTESTED | No macOS device in this session. |
| macOS Chrome | UNTESTED | No macOS device in this session. |
| Linux Firefox | UNTESTED | No Linux device in this session. |
| **iOS local-execution target** | UNTESTED | No physical iOS device or iOS build was available; Quick Look was not treated as a Safari result. |
| Android Chrome (Files) | UNTESTED | No physical Android device in this session. |
| Tor Browser | UNTESTED | No Tor Browser in this session. |

## 8. Assumptions made

- The Coldcard documentation and firmware source reviewed on 2026-08-12 are
  the authoritative external interoperability references for this roadmap
  item. If Coldcard changes the prefix, index encoding, hash count, supported
  lengths, or part-count semantics, the vector and ADR require a new review.
- Seed XOR's random construction uses double-SHA-256 of CSPRNG bytes before
  truncation, matching the published firmware shape; it is not claimed to be
  threshold recovery or a substitute for a separately backed-up BIP-39
  passphrase.
- All vendored BIP-39 wordlists may use the same entropy operation, while
  English is the documented Coldcard interoperability choice.
- Timed reveal is a usability control, not a guarantee against screenshots,
  shoulder surfing, browser memory forensics, or malicious host hardware.

## 9. What to scrutinise

Review the exact deterministic mask bytes in `src/cold/seed-xor.js` against the
Coldcard source, especially the zero-based index, ASCII suffix, double hash,
and final XOR part. Then inspect the cold-only wiring and teardown paths in
`src/cold/main.js`: source and part fields are cleared immediately after
processing, output strings remain only in cold-local state, and reveal timers
must not outlive a lock. Finally, verify that no future refactor turns the
returned entropy or mnemonic into a warm message, storage record, clipboard
payload, or passphrase surrogate.

## 10. Self-assessment

- The implementation is most sensitive to a future interpretation change in
  Coldcard's random-mask construction; the published deterministic vector does
  not by itself prove random-mode interoperability.
- Browser automation covers Windows Chromium and Firefox only. Physical mobile,
  Safari, Tor, and hardware-wallet execution were not performed.
- JavaScript strings cannot be explicitly zeroized; the implementation clears
  all DOM and object references on use/lock, but this is not a claim of perfect
  garbage-collector erasure.
- Future threshold-share work must not reuse this API or describe Seed XOR as
  SLIP-39/Shamir recovery. P2.4 is the separate threshold/raw-SSS item.

## 11. Bundle impact

The committed P2.3 build is 2,223,968 bytes with SHA-256
`2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97`.
The P2.2 product packet recorded a main-baseline build of 2,178,561 bytes;
the change is therefore approximately +45.3 KiB. This is a meaningful bundle
increase from the cold UI, source module, and harness-facing assembly, but no
new runtime dependency or network fetch was introduced.

## 12. Docs updated

- `docs/03-guides/backup-seed-xor.md` — plain, working, and technical help
  depths, limitations, recovery steps, and review date.
- `docs/03-guides/README.md` — guide index entry.
- `docs/05-development/adr/0037-seed-xor-cold-only.md` and ADR index — the
  entropy-domain and cold-boundary decision.
- `docs/05-development/dependencies.md` — inline implementation note.
- `docs/05-development/ROADMAP.md` — P2.3 set to `[~]`; the author does not
  mark it independently verified.
- `CHANGELOG.md` — P2.3 entry dated 2026-08-12.
