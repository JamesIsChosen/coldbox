# P2.6 PR packet: BackupRecords and verify-your-shares

## 1. Summary

This branch implements [P2.6](../ROADMAP.md), `BackupRecords and verify-your-shares`, after P2.5. It adds warm public BackupRecord metadata and a cold-only verification flow for SLIP-39, codex32, Seed XOR, Shamir39, and raw SSS. Only a bounded result enum and, on success, a canonical verification timestamp cross the realm boundary; the share material and reconstructed candidate never do.

## 2. Scope

In scope:

- Public BackupRecord CRUD, search, and unverified-by-default rendering in the warm shell.
- `backup.verifyRequest` and `backup.verifyResult` protocol messages with closed schemas.
- Cold-only reconstruction/checking, threshold/group binding, timestamp authority, and teardown clearing.
- Durable-save prompting after a successful cold verification.
- Tests, browser harness coverage, security documentation, four backup guides, ADR-0041, changelog, roadmap status, and this packet.

Deliberately out of scope:

- P2.7 scheduling, health dashboard, survivability scoring, or inheritance workflow.
- Physical backup-media inspection or proof that a user stored shares safely.
- Support for `seedqr`, `metal`, `paper`, or `encrypted-file` records; those return `unsupported`.
- A new vault binary-format field. `lastVerifiedAt` remains public registry data and is persisted only by the existing authenticated public-compartment save path.

## 3. How to verify

Run from the repository root on Node 24.16.0. The commands below are the commands run for this branch; the browser command used the installed Chromium and Firefox Playwright binaries.

```text
PS> npm.cmd test -- test/protocol.test.js
ℹ tests 19
ℹ pass 19
ℹ fail 0

PS> npm.cmd test -- test/registry.test.js
ℹ tests 10
ℹ pass 10
ℹ fail 0

PS> npm.cmd test -- test/vault.test.js
ℹ tests 34
ℹ pass 34
ℹ fail 0

PS> npm.cmd test -- test/p0.19-runtime-wiring.test.js
ℹ tests 13
ℹ pass 13
ℹ fail 0

PS> npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

PS> npm.cmd run check-docs
Documentation hygiene check passed: 202 markdown file(s) checked, 0 warning(s).

PS> npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Vendor verification passed in offline mode.
Vendor verification passed against local files and upstream releases.

PS> npm.cmd run test:browser
Chromium: public BackupRecord creation, cold-only SLIP-39 verification, warm isolation, and teardown passed
Firefox: public BackupRecord creation, cold-only SLIP-39 verification, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.

PS> npm.cmd test
ℹ tests 369
ℹ pass 369
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

The reproducible-build evidence is recorded after the documentation commit below. The final artifact was built twice from the same committed source and both runs reported:

```text
Built build/coldbox.html (ef825d95337a7c9802ab2318f565cc5cc2b06e37f8582e42075f2b9853e7249b)
SHA256 build/coldbox.html: ef825d95337a7c9802ab2318f565cc5cc2b06e37f8582e42075f2b9853e7249b
```

The previous P2.5 parent was archived and built for the size baseline:

```text
BASELINE_SHA256=0bd077cbf5e63b2902afc25641e9d7a2672c3f13f485947901561615b7d086bd
BASELINE_BYTES=2471651
BASELINE_KB=2413.7
```

## 4. Acceptance criteria

The roadmap item has no prose acceptance paragraph beyond the following exact text:

| Criterion | How satisfied | Test |
|---|---|---|
| `P2.6 BackupRecords and verify-your-shares` | Public BackupRecords are warm metadata; verification is requested by ID and runs in the cold realm. Supported formats reconstruct/check in the cold realm and return only `verified`, `invalid`, `unsupported`, `no-record`, or `vault-locked`, with a timestamp only for `verified`. | `test/protocol.test.js`, `test/registry.test.js`, `test/vault.test.js`, `test/p0.19-runtime-wiring.test.js`, and the Chromium/Firefox browser harness flow. |
| `*Deps: P2.5*` | P2.5 is marked `[x]` on the base history and the implementation reuses the existing cold vault/session and recovery libraries. | Full `npm.cmd test` (369/369) and `npm.cmd run test:browser`. |

The roadmap marker is `[~]`, not `[x]`, because independent review owns the final verification marker.

## 5. Security impact

| Surface | Impact | Evidence |
|---|---|---|
| Realm boundary | Yes. The flow deliberately crosses the existing warm/cold message channel, but only with `backupId`, a closed outcome enum, and an ISO timestamp on success. | Protocol allowlist/schema tests and static runtime-wiring tests reject share words, private keys, mnemonics, xprvs, passphrases, reconstructed bytes, and `shareMaterial` fields. |
| Message schema | Yes. Two new typed messages are added. `backup.verifyRequest` accepts only `backupId`; `backup.verifyResult` accepts only `backupId`, `outcome`, and the success-only `verifiedAt`. | `test/protocol.test.js`; `src/protocol.js`. |
| Cold CSP | No. No CSP directive or `connect-src` host changed. The cold realm remains `connect-src 'none'`. | `npm.cmd run lint`; build/static CSP tests in the full suite. |
| Vault format/crypto | No new cryptographic primitive or vault record. Verification uses the already-vendored cold combiners and the existing authenticated public replacement/save path. | `test/vault.test.js`; `npm.cmd run verify-vendor`. |

If this boundary were wrong, an attacker controlling the warm shell could learn the user's share material or reconstructed seed, or forge completion state without a valid cold reconstruction. The implementation prevents both: warm code cannot set or clear `lastVerifiedAt`, and the cold session owns the completion timestamp. No new network endpoint or host is introduced, so no new service learns anything about the backup.

## 6. Test evidence

New or changed tests include:

- Protocol tests for the BackupRecord schema, exact message whitelist, secret-shaped field rejection, success-only timestamp rules, and failure outcomes.
- Registry tests proving public records start unverified, warm callers cannot forge completion, and only the cold completion method can set the timestamp.
- Vault tests proving cold-only authority, preservation only across same-identity public replacement, reset on identity change, and invalid timestamp rejection.
- Runtime-wiring tests proving no secret-bearing boundary path, durable-save dirty-state behavior, and browser harness coverage.
- Browser harness coverage in Chromium and Firefox proving BackupRecord creation, cold-only SLIP-39 verification, cleared inputs, warm DOM isolation, and teardown.

Independent vector sources already exercised by the underlying cold combiners are preserved and reused by this orchestration layer:

- [Trezor's published SLIP-39 vectors](https://github.com/trezor/python-shamir-mnemonic/blob/master/vectors.json).
- [BIP-93 codex32 vectors](https://github.com/bitcoin/bips/blob/master/bip-0093.mediawiki).
- [Coldcard's Seed XOR documentation and implementation](https://github.com/Coldcard/firmware/blob/master/docs/seed-xor.md) and [source](https://github.com/Coldcard/firmware/blob/master/shared/xor_seed.py).
- Ian Coleman's Shamir39 reference and the published [secrets.js](https://github.com/grempe/secrets.js/) compatibility behavior, pinned in `test/fixtures/reference/`.

Negative cases deliberately exercised include insufficient/malformed/duplicate/mixed shares, threshold and group mismatches, forged warm timestamps, changed backup identities, invalid timestamps, unsupported formats, missing records, vault-locked requests, and attempts to place secret-shaped fields in protocol messages or warm registry data. These fail closed with non-success outcomes or rejected operations.

The physical/device matrix below is incomplete; no claim is made for mobile, macOS, Linux, or Tor Browser.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | UNTESTED | The automated run used Playwright Chromium, not a separately installed Google Chrome binary. |
| Windows Firefox | PASS | Playwright Firefox opened the built artifact from `file://` and passed the P2.6 browser flow. |
| macOS Safari | UNTESTED | No macOS/Safari device available. |
| macOS Chrome | UNTESTED | No macOS device available. |
| Linux Firefox | UNTESTED | No Linux device available. |
| **iOS local-execution target** | UNTESTED | No iOS device or iOS build was available; no Quick Look or localhost result is being inferred. |
| Android Chrome (Files) | UNTESTED | No Android device available. |
| Tor Browser | UNTESTED | No Tor Browser environment available. |

## 8. Assumptions made

- `subjectId` is a UUID supplied by the public record form because warm Seed CRUD does not yet exist. This is a temporary integration assumption; the record relationship must be wired to the eventual public Seed registry before the feature is considered complete in a user-facing release.
- `groupConfig` is public metadata and is used to bind a submitted share set to the recorded group/member thresholds. The record identity comparison is `subjectId`, `method`, `threshold`, and `groupConfig`; a change clears the cold-owned completion timestamp.
- A `verified` result means the supplied shares passed the format's parser/checksum/math and reconstructed under the recorded threshold/group configuration. It does not prove that the user copied the intended source seed, that physical storage is safe, or that a separate BIP-39 passphrase is present/correct.
- SLIP-39's share passphrase is entered only in the cold panel and is not stored in the BackupRecord. The BIP-39 passphrase remains a separate recovery input.
- The timestamp is generated in the cold realm with `toISOString()` and is only accepted in canonical UTC ISO form.
- The supported verification methods are the cold combiners already present in the repository. Physical-only formats are explicitly `unsupported` until their own safe cold workflow is designed.
- JavaScript strings and DOM values cannot be promised to be physically zeroized by this implementation. The code clears references and visible inputs on teardown, and the tests assert that behavior; this is not a claim about garbage-collector memory erasure.

## 9. What to scrutinise

- The threshold and nested group binding in `src/cold/main.js`: a parser-valid set must also match the public record's threshold/group configuration.
- The warm/cold timestamp lifecycle: the cold result updates the warm registry, marks the vault unsaved, and requires a subsequent authenticated save for durable persistence.
- `preserveColdVerificationAuthority` in `src/cold/vault.js`: same-identity public replacement preserves completion; any identity change resets it.
- The closed protocol schemas and the static no-secret assertions. In particular, check that future edits cannot add share text or reconstructed bytes to either message.
- Unsupported method behavior and the distinction between `invalid`, `unsupported`, `no-record`, and `vault-locked`.
- The UUID `subjectId` assumption and whether the future Seed registry should replace the temporary form input without weakening the public-only model.

## 10. Self-assessment

- This change does not validate physical share locations, custodians, or survivability; those remain P2.7 work.
- It verifies reconstruction consistency, not an independent comparison against a known seed. That limitation is documented and should remain visible in the UI.
- No physical devices were available, so the device gaps are real, especially iOS local execution and Android Files execution.
- The browser harness asserts cleared verification input and warm isolation. A timing-sensitive assertion about the cold panel being hidden after Escape was removed because the existing unsaved-lock warning legitimately keeps the warm shell visible; teardown code and input clearing remain directly tested.
- This item adds no independent test vector of its own; it composes already independently pinned combiner implementations. A reviewer should confirm that the orchestration checks do not accidentally bypass those combiners.
- Follow-up is P2.7 for health scheduling/dashboard and P2.8 for printable cards/worksheets.

## 11. Bundle impact

Measured on `build/coldbox.html`:

| Revision | Bytes | KB |
|---|---:|---:|
| P2.5 parent baseline | 2,471,651 | 2,413.7 |
| P2.6 final | 2,524,220 | 2,465.1 |
| Delta | +52,569 | +51.3 |

The increase is below the existing bundle budget and includes the public/cold verification UI, wiring, and compiled help content. The baseline was built from the P2.5 parent archive; its artifact hash is recorded in §3.

## 12. Docs updated

- `docs/01-spec/SPEC.md`, `architecture.md`, and `data-model.md`: workflow, boundary, and public record semantics.
- `docs/03-guides/backup-slip39.md`, `backup-codex32.md`, `backup-seed-xor.md`, and `backup-shamir.md`: plain/working/technical help for recording and verifying backups.
- `docs/05-development/adr/0041-backup-record-verification-boundary.md`: structural decision and alternatives.
- `docs/05-development/adr/README.md`: ADR index.
- `docs/05-development/ROADMAP.md`: P2.6 moved to `[~]` for independent review; it is intentionally not `[x]`.
- `CHANGELOG.md`: P2.6 entry.
- This packet: `docs/05-development/packets/p2.6-backup-records-verify-shares.md`.
