# P0.13 roadmap closeout - governance recovery

## 1. Summary

This is a governance-only continuation of already-merged P0.13. PR #21 independently passed, its reviewer-owned PASS was preserved and merged, and `main` is the exact merge commit `591bcefdcc4ff0fa41b7f1cff6fb9ddab45eb883`; this PR records P0.13 complete in the authoritative roadmap, repairs only the two P0.13-owned mojibake tokens in that same block, and removes the now-stale P0.13/P0.14 current-status sentence from the changelog.

No P0.14 implementation is included.

## 2. Scope

Changed paths are intentionally limited to:

- `docs/05-development/ROADMAP.md`
- `CHANGELOG.md`
- `docs/05-development/packets/p0.13-roadmap-closeout.md`

In scope:

- P0.13 `[~]` -> `[x]` after independent PASS and merge.
- Repair the `P0.13` heading punctuation to a real Unicode em dash.
- Repair the P0.13 browser-evidence marker to a real globe character.
- Replace the changelog sentence that still described P0.13 as pending review/merge with durable historical wording that points current status back to `ROADMAP.md`.
- Add this permanent audit packet.

Deliberately not in scope:

- P0.14 implementation or status changes.
- P0.19 status or inherited P0.19 encoding cleanup; P0.19 is a separate roadmap item.
- Runtime, source, tests, dependencies, vendor files, CSP, vault format, cryptography, or browser behavior.
- Rewriting the merged P0.13 implementation packet, self-review, or preserved independent review history.
- Merging this PR from the author session.

## 3. How to verify

Start-state identity:

```text
main / origin-main expected: 591bcefdcc4ff0fa41b7f1cff6fb9ddab45eb883
PR #21 reviewed implementation tip: 9f2fe771ca19bbf45a612ced500a7155a5373c67
PR #21 review-closeout head: e1a256f4b52ff0edce532f4007c1d21948f69c9f
PR #21 merge commit: 591bcefdcc4ff0fa41b7f1cff6fb9ddab45eb883
Final P0.13 review: 40791 bytes
Final P0.13 review SHA-256: a58ef4cbdbf308cf2cde006ca0b4576a3c6db86fec8a8f3202700a46ed82d43c
Final P0.13 review verdict: PASS
```

Author verification ran from the uncommitted documentation candidate copied into disposable clones, so `node_modules` and `build/` in the source checkout were not touched.

```text
npm ci (clone A): exit 0
npm run verify-vendor (clone A): exit 0
npm run lint (clone A): exit 0
npm test (clone A): exit 0
npm run build (clone A, path contains a space, TZ=UTC, LANG=C, LC_ALL=C): exit 0
npm ci (clone B): exit 0
npm run build (clone B, different path, TZ=Pacific/Auckland, LANG=fr_FR.UTF-8, LC_ALL=fr_FR.UTF-8): exit 0

Build A bytes: 582857
Build A SHA-256: 070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0
Build B bytes: 582857
Build B SHA-256: 070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0
Expected merged P0.13 artifact bytes: 582857
Expected merged P0.13 artifact SHA-256: 070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0
```

Reviewer commands:

```text
git status --porcelain=v1 -uall
git diff --check main...HEAD
git diff --name-status main...HEAD
git diff main...HEAD -- docs/05-development/ROADMAP.md
git diff main...HEAD -- CHANGELOG.md
git show HEAD:docs/05-development/packets/p0.13-lock-save-load.review.md
npm ci
npm run verify-vendor
npm run lint
npm test
npm run build
```

The reviewer must also follow `docs/05-development/review-protocol.md` independently, including a different-path/timezone/locale build and a deliberate non-zero failure fixture.

## 4. Acceptance criteria

Verbatim P0.13 criterion from the authoritative roadmap:

| Criterion | How satisfied | Test |
|---|---|---|
| **Accept:** the three save/load paths are complete browser flows, and the manual base64/QR path is usable without File System Access or blob-download support. At least one save path must work on every platform in the supported execution matrix. **Direct local execution from iOS Files into Safari is not a P0.13 acceptance gate under [ADR-0010](adr/0010-ios-local-html-execution.md); it remains a separately recorded portability target at P0.19.** | The implementation was independently reviewed at `9f2fe771ca19bbf45a612ced500a7155a5373c67`; the reviewer-owned PASS was committed at `e1a256f4b52ff0edce532f4007c1d21948f69c9f`; PR #21 merged as `591bcefdcc4ff0fa41b7f1cff6fb9ddab45eb883`. This closeout does not alter implementation semantics. | Re-check the final review at `docs/05-development/packets/p0.13-lock-save-load.review.md`, its exact SHA-256 `a58ef4cbdbf308cf2cde006ca0b4576a3c6db86fec8a8f3202700a46ed82d43c`, PR #21 merge metadata/ancestry, and the unchanged build artifact `070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0`. |

The criterion text itself is unchanged by this PR.

## 5. Security impact

- Realm boundary: no change.
- Message schema: no change.
- CSP: no change.
- Vault format: no change.
- Derivation: no change.
- Randomness: no change.
- `connect-src`: no change.
- Message types: no change.

If this governance closeout were wrong, the risk would be process/dependency truthfulness: P0.14 could be unblocked before P0.13 had actually passed and merged. The runner therefore requires the exact final PASS review, exact PR #21 merged state, exact merge commit, and exact merge ancestry before it edits the roadmap.

## 6. Test evidence

No new runtime tests are added because no runtime behavior changes.

Author-side regression verification in disposable clones:

- `npm ci`: PASS.
- `npm run verify-vendor`: PASS.
- `npm run lint`: PASS.
- `npm test`: PASS.
- Two deterministic builds from different temporary paths/timezone/locale settings: PASS.
- Both builds remain exactly `582857` bytes / `070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0`.

Negative runtime fixtures are not claimed as new evidence here. The independent reviewer must still perform the review protocol's deliberate failure check. The merged P0.13 review already preserves the implementation-level negative evidence.

## 7. Device matrix

Not applicable to this governance-only diff: it changes no bootstrap, CSP, storage, rendering, or execution behavior.

P0.19 remains a separate unchecked human-required portability/device item and is deliberately untouched by this PR.

## 8. Assumptions made

> **Assumed:** An already-complete but unmarked roadmap item must be marked complete before moving on.
> **Basis:** `AGENTS.md` says the roadmap is authoritative, dependencies must be `[x]`, and "If the item is already complete but unmarked, mark it and move to the next."
> **If wrong:** The reviewer should FAIL this closeout rather than allowing P0.14 to start.

> **Assumed:** The inherited P0.19 mojibake must not be repaired in this P0.13 closeout.
> **Basis:** P0.19 is a separate roadmap item, and the review protocol automatically fails a PR containing more than one roadmap item.
> **If wrong:** Handle P0.19 through its own governance/item scope rather than widening this PR.

## 9. What to scrutinise

- Confirm PR #21 really is MERGED at `591bcefdcc4ff0fa41b7f1cff6fb9ddab45eb883` and its second parent is `e1a256f4b52ff0edce532f4007c1d21948f69c9f`.
- Confirm the final P0.13 review is exactly `40791` bytes / `a58ef4cbdbf308cf2cde006ca0b4576a3c6db86fec8a8f3202700a46ed82d43c` and ends in PASS.
- Confirm the P0.13 acceptance criterion is byte-for-byte unchanged.
- Confirm only P0.13 status plus the two P0.13-owned encoding tokens changed in the roadmap.
- Confirm P0.14 and P0.19 remain unchecked and otherwise untouched.
- Confirm the changelog no longer presents the pre-review/pre-merge P0.13 state as current.
- Confirm no runtime/source/test/vendor/dependency path appears in the PR diff.

## 10. Self-assessment

The unusual part is that this is a governance-recovery PR after the implementation PR already merged and its branch was deleted. It exists because the repository forbids direct pushes to `main`, while the authoritative roadmap still needs the completed item recorded as `[x]`.

No implementation claim is being added or re-reviewed by the author. The independent reviewer must decide whether the exact merged PASS evidence justifies the status transition.

Known limitation outside this PR: P0.19 still contains inherited encoding corruption on current `main`; touching it here would widen the PR to another roadmap item.

Follow-up: only after this closeout independently PASSes and merges may P0.14 become eligible to start.

## 11. Bundle impact

Built HTML before this governance closeout: `582857` bytes / `070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0`.

Built HTML from this governance candidate in both disposable clones: `582857` bytes / `070963661de81ac3318a505065d20792b9e62e261526c1c97c3325290d620ac0`.

Bundle delta: 0 bytes.

## 12. Docs updated

- `docs/05-development/ROADMAP.md` - P0.13 completion marker plus two P0.13-owned encoding repairs only.
- `CHANGELOG.md` - converts the stale pre-review/pre-merge status sentence into durable historical wording and points live status back to the roadmap.
- `docs/05-development/packets/p0.13-roadmap-closeout.md` - permanent audit packet for this governance recovery.

No ADR is added because no structural/product decision changes.
