# P2.4 self-review — batch gate

**Branch:** `p2.4-shamir39`

**Reviewed tip:** `6ac1e71a0493a16aa0d623dd057c87a91b737377`

**Pull request:** [#51](https://github.com/JamesIsChosen/coldbox/pull/51)

**Base:** `main` at `5f0fa5e442524033e91bb31006a0ca6288385dc0`

**Round:** 1

**Verdict:** PASS — ready to push and open the P2.4 PR

## What I verified

I re-read `docs/05-development/batch-run.md`, the review protocol, and the
P2.4 acceptance scope. I read the complete product diff as a stranger and
checked that it contains exactly one roadmap item. The full unit suite, vendor
verification, lint, documentation hygiene, reproducible build, and both-engine
browser harness all passed:

```text
npm.cmd test
tests 328
pass 328
fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 182 markdown file(s) checked, 0 warning(s).

npm.cmd run build (twice)
62e5e380ca78c86f7261005833c8903fa86ee5592cf7caffbd24a823c125c40a  2262034 bytes
62e5e380ca78c86f7261005833c8903fa86ee5592cf7caffbd24a823c125c40a  2262034 bytes

npm.cmd run test:browser
Chromium: Shamir39 and raw SSS official-format UI, masking, reconstruction, negative case, warm isolation, and teardown passed
Firefox: Shamir39 and raw SSS official-format UI, masking, reconstruction, negative case, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

I built the exact reviewed tip in a separate detached worktree at
`C:\Users\semaj\Projects\coldbox\.review-p2.4` with
`TZ=Pacific/Kiritimati` and `LANG=fr-FR`. It produced the same
`62e5e380ca78c86f7261005833c8903fa86ee5592cf7caffbd24a823c125c40a` hash and
2262034-byte size. The detached full unit suite passed 328/328. Its detached
browser harness also passed in Chromium and Firefox from `file://`.

I inserted `eval('1')` into the temporary checkout’s `src/cold/shamir.js`.
`npm.cmd run build` exited 1 with:

```text
Forbidden construct "eval" in src/cold/shamir.js:4:34
Lint failed: Forbidden-construct lint failed with 1 finding(s)
Error: Build refused: source failed forbidden-construct lint
```

I restored the detached checkout with the same source text; `git status
--short` and `git diff --check` were clean. The primary checkout was not
modified by the mutation.

The independent compatibility vectors are the published Ian Coleman
Shamir39 current-format shares and the published secrets.js compatibility
shares. The focused tests compare deterministic output to those references,
then separately exercise arbitrary-order reconstruction and negative cases.

## Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | valid 12-, 15-, 18-, 21-, and 24-word BIP-39 phrases can be split using the selected official wordlist | ✅ | `test/shamir.test.js` validates the BIP-39 input boundary; the API enforces the supported counts and browser UI exposes all ten wordlists. |
| 2 | Shamir39 output matches the published `shamir39-p1` encoding and combines in arbitrary order | ✅ | Published five-share fixture and deterministic Ian Coleman-format fixture pass; arbitrary three-share order reconstructs the official phrase. |
| 3 | raw SSS matches the published secrets.js share format and supports GF(2^n) fields from 3 through 20 bits | ✅ | Published compatibility shares and deterministic formatting pass; configurable-field and invalid-field tests pass. |
| 4 | insufficient, malformed, duplicate, mixed-parameter, and wrong-threshold sets fail closed | ✅ | Focused negative tests and browser malformed-share coverage pass; combine inputs clear after the attempt. |
| 5 | both generators require `crypto.getRandomValues` and never substitute `Math.random` | ✅ | Missing-randomness tests, source assertions, lint, and browser lockdown coverage pass. |
| 6 | sources, shares, and reconstructed candidates stay cold-local, masked by default, and clear on teardown | ✅ | Warm binding is absent; both-engine UI tests cover masking, explicit reveal, input clearing, and Escape-Escape teardown. |
| 7 | docs distinguish Shamir39/raw SSS from SLIP-39 and keep passphrases separate | ✅ | Three-depth guide, UI warnings, ADR-0038, dependency provenance, changelog, and docs check pass. |
| 8 | build assembly remains deterministic with no runtime dependency or network path | ✅ | Explicit manifest entry, two identical hashes, detached locale/timezone reproduction, vendor verification, and the full CSP/airgap browser suite pass. |

## Findings

None. No advisory or cosmetic concern is being carried forward; under the
batch rule any such concern would be a FAIL requiring another round.

## Honest gaps

No physical iOS, Android, macOS Safari, Linux, Tor, or hardware-wallet
execution was available. Chromium and Firefox headless `file://` coverage is
not being represented as a physical mobile or hardware pass. The UI is
intentionally bounded to eight combine fields, and neither format claims share
authentication or durable storage. These are documented product limits, not
silent claims.

## Verdict rationale

The implementation meets the P2.4 scope, preserves the cold-only and
fail-closed constraints, matches independent published formats, reproduces
the committed build under a different environment, and rejects a deliberately
forbidden construct with a non-zero exit. **PASS.**
