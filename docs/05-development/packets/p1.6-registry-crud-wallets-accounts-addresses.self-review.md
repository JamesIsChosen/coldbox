# Self-review: P1.6 Registry CRUD

**VERDICT: PASS**

Findings: 0
Reviewed commit: `b8172aa`
Reviewed by: author self-review gate, round 1
Date: 2026-08-10

## What I verified

- Re-read `docs/05-development/review-protocol.md` before this gate.
- Read the complete P1.6 diff from `7be66e3` through `b8172aa`; `git diff --check` passed.
- `node --test test/protocol.test.js test/registry.test.js`: 16/16 passed.
- `node --test test/vault.test.js`: 23/23 passed.
- `npm.cmd test`: 277/277 passed after rerunning with temporary-Git fixture execution permitted.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 149 markdown files and 0 warnings after the packet was added.
- `npm.cmd run verify-vendor`: local files and all ten upstream releases passed.
- Production builds under `LC_ALL=de-DE, TZ=Pacific/Honolulu` and `LC_ALL=ja-JP, TZ=Asia/Tokyo` produced identical SHA-256 `919c5d888ba9897d80f384a676f6d00b866c79a72474680ce37b3a01c118a6a3` and size 1,867,651 bytes.
- A clean disposable copy under a different path and `LC_ALL=fr-FR, TZ=Europe/Paris` produced the same hash and size.
- Deliberately appending an `eval` construct to the disposable registry source made lint exit 1; the tampered copy was removed afterward.
- The message schema, cold session ID check, public-only registry store, relationship checks, clone isolation, soft-hide, and persistence round-trip were checked against the P1.6 scope.

## What I could not verify

The prescribed `npm.cmd run test:browser` harness could not start because Playwright's Chromium and Firefox binaries are absent. The in-app browser also rejects `file://` navigation. This is recorded as an untested device/browser matrix item in the PR packet; it is not silently promoted to a pass or used as evidence for browser behavior. The P1.6 roadmap line has no separate browser acceptance clause, so this gate does not treat the environment gap as an item-level finding; an independent reviewer must resolve it before merge.

## Acceptance criteria

The roadmap contains one exact P1.6 line and no separate `Accept:` block:

| Criterion | Result | Evidence |
|---|---|---|
| `P1.6 Registry CRUD: wallets, accounts, addresses` | Met | Warm UI/store CRUD, typed public replacement, and cold save/reopen tests |

## Security review

- No secret field is accepted by the two new message types.
- Unknown text fields fail closed; bounded registry text is validated by collection.
- The cold realm still uses `connect-src 'none'`; no host or runtime dependency was added.
- Public replacement cannot change the authenticated Vault ID and does not expose secret-compartment plaintext.
- UUID generation uses `crypto.getRandomValues` only and fails closed when unavailable.
- The registry source is warm-only and has no DOM or secret handling.

## Verdict rationale

All item-scope behavior and negative tests pass, the full suite and upstream vendor verification pass, builds are reproducible across locale/timezone/path changes, and the deliberate lint tamper exits non-zero. The browser harness gap is fully disclosed in the packet and device matrix; it is not claimed as coverage. PASS for the batch gate, with independent browser verification still required before merge.
