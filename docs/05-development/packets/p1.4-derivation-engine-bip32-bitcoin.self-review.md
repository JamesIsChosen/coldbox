# Review: P1.4 — Derivation engine

**VERDICT: PASS**

Findings: 0 in this author self-review gate. The consolidated independent stacked review remains authoritative and still records the P1.4 findings as unresolved until this remediation is independently reviewed.

Reviewed implementation commit: `d97e7cf` (`Remediate P1.4 watch-only validation`)

Reviewed by: Codex self-review gate (same session; not an independent approval)

Date: 2026-08-10

Review rounds: 1

## 1. Remediation scope checked

- The official BIP-32 `m/0'/1/2'` extended public key fixture now includes the upstream `Q` and is asserted byte-for-byte; the prior invalid-checksum exception is gone.
- Bitcoin watch-only derivation now requires a depth-3 account node with a hardened account child. Negative coverage rejects the master, shallow, depth-4, and depth-3 non-hardened cases.
- `addressFromPublicKey` parses the compressed bytes as a secp256k1 point before any P2PKH, P2SH-P2WPKH, P2WPKH, or P2TR address hash. The invalid-point negative runs across all four script types.
- The reference documentation, ADR-0029, changelog, and P1.4 packet describe the corrected contract. The roadmap remains `[~]`.

## 2. Verification evidence

```text
node --test test/derivation.test.js
ℹ tests 4
ℹ pass 4
ℹ fail 0

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 144 markdown file(s) checked, 0 warning(s).

npm.cmd test
ℹ tests 270
ℹ pass 270
ℹ fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.
```

The full suite used the elevated verification path because the sandbox could not spawn Git from its temporary reproducibility fixtures. The repository code and fixtures passed; no test was skipped.

The production build was run twice and produced the same result:

```text
Built build/coldbox.html (c407e12d68d2eb9c1523af2d8f7f166e3d00d82689beb5036e33a4d42bed64f9)
build/coldbox.html size=1798453
build/coldbox.html sha256=c407e12d68d2eb9c1523af2d8f7f166e3d00d82689beb5036e33a4d42bed64f9
```

The current-head build fixture also passed:

```text
node --test test/build.test.js
ℹ tests 13
ℹ pass 13
ℹ fail 0
```

The Chromium and Firefox `file://` browser harness passed in both engines, including the existing CSP, airgap, boundary, lock, zeroization, Seed Forge, and tamper checks.

## 3. Security and scope review

- No message type or warm/cold boundary changed.
- Seeds, private HDKey state, and private derivation remain cold-only.
- Watch-only inputs are constrained to a defensible account boundary and public results remain secret-free.
- Invalid compressed points fail closed before any script-family address construction.
- The cold CSP and no-runtime-network model remain unchanged.
- This branch contains only P1.4 work; P1.5–P1.9 remain stacked children.

## 4. Findings and remaining gaps

No P1.4 remediation finding remains in this self-review. This is not an independent review and does not authorize `[x]`, merge, or closure of the consolidated FAIL. Platform gaps remain as declared in the packet: no macOS/Safari, mobile, Tor, or physical hardware run was available, and P1.9 owns the final hardware gate.

The hardest points for the independent reviewer are the exact BIP-32 transcription, depth/index validation for all watch-only paths, and the secp256k1 point parse before every address hash.

## 5. Gate conclusion

PASS for the author self-review gate. The branch is ready to publish after this packet update is committed. An independent stacked review must still verify the complete P1.4–P1.9 remediation and decide whether the roadmap markers may change.
