# Review: P1.4 — Derivation engine

**VERDICT: PASS**

Findings: 0. No acceptance, security, reproducibility, documentation, or scope findings remain after one review round.

Reviewed commit: `8da9b15` (`Document P1.4 verification evidence`)

Reviewed by: Codex self-review gate (same session; independent-review filter applied)

Date: 2026-08-10

Review rounds: 1

## 1. What I verified

I re-read `docs/05-development/review-protocol.md` before this gate. The implementation commit is `059d140`; the reviewed commit adds only the required packet, so the code under review is unchanged by the packet commit.

The focused official-vector and negative suite passed:

```text
node --test test/derivation.test.js
ℹ tests 4
ℹ pass 4
ℹ fail 0
```

The repository-wide checks passed:

```text
npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 143 markdown file(s) checked, 0 warning(s).

npm.cmd test
ℹ tests 270
ℹ pass 270
ℹ fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.
```

The build was run under two different locale/time-zone combinations and produced the same SHA-256 both times:

```text
8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6
8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6
```

A separate disposable source copy was built from `C:\Users\semaj\AppData\Local\Temp\coldbox-p1.4-review-20260810`; it produced the same hash:

```text
review-hash=8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6
```

After the matching build, I deliberately tampered with the disposable copy. The build refused the tampered source with a non-zero status:

```text
Lint failed: CRLF line ending found in src/cold/derivation.js
Error: Build refused: source failed forbidden-construct lint
tampered-build-exit=1
```

The disposable copy was removed and the worktree was not modified by the tamper.

The Chromium and Firefox `file://` browser harness passed. It covered CSP/network blocking, warm/cold isolation, lock/zeroization, Seed Forge teardown, provider neutering, provenance, and the built artifact. There is no P1.4 UI flow to exercise because this item is the cold engine layer.

## 2. Acceptance criteria checked verbatim

The roadmap contains this exact P1.4 line and no separate acceptance bullet:

> P1.4 — Derivation engine: BIP-32 core plus Bitcoin script types

The title is met by the following checked outcomes:

| Wording in the roadmap title | Review evidence |
| --- | --- |
| BIP-32 core | Official BIP-0032 root/child extended-key vectors, independent public-key check, strict path parser, and fail-closed child/batch negatives. |
| Bitcoin script types | P2PKH, P2SH-P2WPKH, P2WPKH, and P2TR construction, with official BIP-0049/84/86 address and extended-key vectors. |
| Derivation engine | Cold-only API, seed/xpub inputs, account/change/index batches, network/script validation, public-only projections, and bundle integration. |

No roadmap acceptance criterion was reinterpreted or omitted. No later UI or verification workflow was pulled into this branch.

## 3. Security and scope review

- No secret-bearing message was added.
- The seed, private HDKey state, and private derivation operations remain inside the cold iframe.
- Results contain public addresses/xpub-family data only.
- Cold `connect-src 'none'` and the single-file build model remain unchanged.
- Vendored dependencies are pinned and verified against upstream release bytes.
- Invalid inputs, unsupported combinations, hardened public derivation, and unsafe batch ranges fail closed.
- The branch contains one roadmap item only.
- The roadmap status is `[~]`, not `[x]`.

## 4. Findings

None. Count: 0.

The known device/platform gaps are recorded in the packet as untested environments, not as hidden support claims. They do not block the P1.4 engine criteria, and P1.9 owns final hardware-wallet validation.

## 5. Verdict rationale

PASS. The implementation meets the literal roadmap scope, has independent official vectors and negative tests, preserves the two-realm security boundary, builds reproducibly, rejects a deliberate source tamper, and has a complete verification packet. A future reviewer must still apply the repository's independent review protocol; this self-review is not an independent approval and does not authorize marking the roadmap item `[x]` or merging the PR.
