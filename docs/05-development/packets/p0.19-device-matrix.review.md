# Review: P0.19 — Device matrix pass

**VERDICT: FAIL**

Findings: 4 (3 blocking, 1 advisory — all must be addressed)
Reviewed commit: `b3a6f067853d481dd5e8ac4d6e982bb808999e4e`
Reviewed base: `afb8de9e73c3a4fd3cbc6c71c96517f7339c3f3e` (`main`)
Reviewed branch: `p0.19-device-matrix`
PR: none — branch reviewed directly by design
Reviewed by: GPT-5.6 Thinking
Date: 2026-08-08

## 1. What I verified

I reviewed the exact effective range `afb8de9e73c3a4fd3cbc6c71c96517f7339c3f3e..b3a6f067853d481dd5e8ac4d6e982bb808999e4e`, independently of the author packet. Discovery confirmed that the requested tip is exactly five commits ahead of the requested base, the source checkout was clean, and Node matched the repository pin at `v24.16.0`. All execution and destructive checks ran in disposable clones; the source checkout stayed unchanged until this review report was written.

### Normal independent gates

The independent execution runner produced the following exact outcomes:

```text
npm ci
exit 0

npm run verify-vendor
exit 0
10/10 local vendored artifacts verified
10/10 corresponding upstream npm release artifacts downloaded and verified

npm run lint
exit 0

node scripts/check-docs.js
exit 0
Documentation hygiene check passed: 134 markdown file(s) checked, 0 warning(s).

npm test
exit 0
tests 252
pass 252
fail 0

npm run build  # first clean build
exit 0
SHA-256 39ca057aa76d5c6249623d544e1069c36d412064187dbdc62fe94ddb16fdf3cd
bytes 1266176

npm run build  # second clean build
exit 0
SHA-256 39ca057aa76d5c6249623d544e1069c36d412064187dbdc62fe94ddb16fdf3cd
bytes 1266176
```

A fresh second clone at the different path `C:\cbx19-4kqeho\b alt`, with `TZ=UTC`, `LANG=fr-FR`, and `LC_ALL=fr-FR`, produced the same `1266176`-byte artifact and the same SHA-256 `39ca057aa76d5c6249623d544e1069c36d412064187dbdc62fe94ddb16fdf3cd`.

`npm run test:browser` ran for real and exited 0 in both Chromium and Firefox. The existing harness re-verified the cold `connect-src 'none'` policy, opaque sandbox without `allow-same-origin`, warm/cold CSP canaries, all five cold network primitive blockers, runtime/provider neutering including a pre-existing injected provider, missing-randomness lockdown, tampered-build rejection, warm/cold normal and emergency lock paths, P0.19 named-vault/save/library behavior, live-transfer sender presentation, and online-transition zeroization.

### Deliberate negative/destructive checks

I deliberately flipped one byte in the vendored `@fontsource/bangers@5.3.0` tarball in a disposable clone. Both checks failed non-zero as required:

```text
node scripts/verify-vendor.js --offline
exit 1

npm run build
exit 1
```

Independent source/built-artifact inspection confirmed:

- active reachability hosts occur in the warm shell and not the cold source;
- the built cold policy retains `connect-src 'none'`;
- the cold iframe still omits `allow-same-origin`;
- `src/cold/vault.js` consumes the validated `data-warm-network-online` state and does not resample `navigator.onLine`, `navigator.connection`, or the airgap snapshot;
- the current runtime contains no `CBX-QR/1` vault-export path or downloadable vault-QR control.

Reviewer-written browser probes additionally exercised cases not established merely by reading the author harness:

- an invalid cold mode/state string did not unlock the vault;
- middle-byte-corrupted and truncated `.cbx` bytes never unlocked in either Chromium or Firefox;
- a live-transfer frame from a different Transfer ID was refused without advancing the active collector;
- a corrupted reconstructed transfer failed SHA-256 integrity and produced no loadable receipt;
- a receiving instance with the vault already in its granted library refused the transfer;
- a clean receiver still required the ordinary vault passphrase and, after successful authentication, started `Not saved`;
- a `Saved · unverified` sender could not start live QR, while a vault re-opened from durable local `.cbx` storage could;
- no live-transfer QR download control existed;
- with reviewer-supplied camera/`BarcodeDetector` shims, the camera-start control was enabled in a locked healthy state and called `getUserMedia()` in both engines. This synthetic result does not substitute for the unresolved physical Windows camera finding in F2.

The focused follow-up also resolved an ambiguity in the first corruption probe: Chromium and Firefox both remained non-unlocked for a middle-byte corruption and a 17-byte truncation. Firefox surfaced the generic authentication failure; Chromium failed closed into a locked state.

## 2. What I could not verify

The physical-device acceptance matrix is incomplete. `testing.md` explicitly says automated browser testing is insufficient for this item. I did not infer any physical-platform result from Windows, Chromium, Firefox, or Playwright.

The current evidence does not independently establish:

- macOS Safari;
- macOS Chrome;
- Linux Firefox;
- Android Chrome launched from Files;
- Tails / Tor Browser;
- a formal iOS local-execution result recorded as PASS, BLOCKED, or UNSUPPORTED with the exact device and iOS version.

The maintained Windows Edge/Chrome/Firefox non-camera observations are useful human evidence, but they do not establish the deferred platforms. Windows camera receive also remains unresolved; see F2.

Every unverified acceptance requirement is itself a finding under `review-protocol.md`, so this section alone prevents an item-level PASS.

## 3. Acceptance criteria

The current P0.19 ROADMAP acceptance criterion is:

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | “After remediation, every platform in the supported execution matrix passes the per-platform checks in `testing.md` and the results are recorded in the P0.19 evidence packet. In particular, the vault check uses at least two named vaults (creation phrase confirmation with visible mismatch feedback, immutable Vault ID, public-name collision refusal, one canonical `<name>--<id8>.cbx` with no visible generations, unchanged-save duplicate refusal, truthful Saved · verified / Saved · unverified status, normal-lock warning from both warm and cold visible controls, reload/library selection, one-phrase unlock, correct active identity). The network check exercises both loss and restoration of external reachability while verifying the cold realm remains sealed and unknown status fails online-safe. Where camera QR decode is available, live animated-QR transfer is tested device-to-device; where unavailable it is recorded honestly with `.cbx` fallback. No downloadable vault QR artifact is permitted. The iOS local-execution target is recorded separately as PASS, BLOCKED, or UNSUPPORTED with the exact device and iOS version. A BLOCKED or UNSUPPORTED iOS result does not fail P0.19 under ADR-0010, but remains visible portability debt. Quick Look, a third-party viewer, localhost, a renamed file, or another execution context is not a Safari-from-Files PASS unless a later accepted ADR explicitly qualifies it.” | ❌ | The physical matrix is incomplete (F1), Windows camera receive is unresolved (F2), and the stale/checking reachability path is not online-safe (F3). |

### Acceptance sub-checks

| Area | Result | Independent evidence |
|---|---|---|
| Two named vaults, creation-only confirmation, random immutable Vault ID, canonical filename, duplicate-name/unchanged-save refusal, truthful persistence status, warm/cold normal-lock warning, reload/select/one-phrase unlock | ✅ automated in Chromium/Firefox | `npm run test:browser` passed both engines; independent transfer/save probes also passed. Physical coverage remains governed by F1. |
| Warm-only active probes; cold `connect-src 'none'`; cold mode authority from validated `mode.set`; `navigator.onLine` cannot override it | ✅ for these boundaries | Source/built inspection plus browser harness and invalid-mode probe. |
| Unknown/stale/checking reachability fails online-safe | ❌ | F3: a previously `unreachable` classification remains `online:false` while a new active probe is in flight. |
| Live transfer contains encrypted `.cbx` bytes, Transfer ID prevents mixing, reconstruction hash is checked, normal passphrase still required, receiver starts Not saved, already-known vault refused | ✅ automated in Chromium/Firefox | Reviewer-written live-transfer probes. Physical camera acceptance remains F2/F1. |
| No downloadable vault QR artifact | ✅ | Runtime/source inspection plus browser checks found no QR download/export control. |
| Required physical device matrix complete | ❌ | F1. |
| Windows camera receive demonstrated or honestly recorded unavailable with working fallback | ❌ | F2. |
| iOS target has exact PASS/BLOCKED/UNSUPPORTED result with device/iOS version | ❌ | F1. |

## 4. Findings

### F1 — Required physical device matrix remains incomplete

**Severity:** blocking

**Location:** `docs/05-development/ROADMAP.md:188-192`; `docs/05-development/testing.md:150-176`; `docs/05-development/packets/p0.19-device-matrix.md:91-112`

**Observed:** The packet records Windows Edge/Chrome/Firefox non-camera hands-on results, but macOS Safari/Chrome, Linux Firefox, Android Chrome from Files, and Tails/Tor Browser remain deferred. No formal iOS PASS/BLOCKED/UNSUPPORTED result with exact device/iOS version is recorded. The item itself explicitly requires physical execution and forbids inferring one platform from another.

**Expected:** Every supported execution-matrix platform must receive the required physical `file://` pass from `testing.md`. The iOS portability target must separately record PASS, BLOCKED, or UNSUPPORTED with exact device and iOS version; third-party HTML-reader execution is not Safari-from-Files evidence.

**Required action:** Complete and record the remaining physical matrix without inference. Record the formal iOS result with exact device/iOS version. Keep P0.19 at `[~]` until a fresh independent review can verify the resulting evidence.

### F2 — Windows live-QR camera receiver has not passed its required physical path

**Severity:** blocking

**Location:** `docs/05-development/packets/p0.19-device-matrix.md:98-100`; `docs/05-development/testing.md:174`; receiver wiring at `src/main.js:1655-1657` and `src/main.js:2443-2498`

**Observed:** The recorded physical Windows result says the capability panel reports `Camera access: API available`, while the receiver remains at `Camera is off`, `Start camera scanner` is disabled/not clickable, and no receiving-device transfer was demonstrated. My synthetic Playwright camera/`BarcodeDetector` shim could start the scanner in Chromium and Firefox, but that does not reproduce or dismiss the physical `file://` behavior that P0.19 exists to test.

**Expected:** Where the platform exposes the required camera QR-decode path, a real receiving device must scan the live transfer, verify it, require the ordinary passphrase, and begin Not saved. If the required QR decode capability is unavailable in that browser/context, the product must report it honestly as unavailable and direct the user to the `.cbx` fallback rather than presenting an apparently available-but-inoperable receiver.

**Required action:** Diagnose the real Windows `file://` capability/control mismatch. Either make live receive work and demonstrate a device-to-device receive on the applicable Windows browsers, or accurately classify the decoder path as unavailable with the explicit `.cbx` fallback. Record the physical result per `testing.md`.

### F3 — A stale offline classification remains secret-capable while a new reachability probe is in flight

**Severity:** blocking security

**Location:** `src/main.js:1340-1351`, `src/main.js:1394-1461`; contradicted by `docs/05-development/adr/0024-warm-reachability-monitor.md` Decision 4, `docs/01-spec/SPEC.md` §network reachability, `docs/01-spec/architecture.md`, and `docs/02-security/threat-model.md`

**Observed:** After establishing `data-reachability-state="unreachable"` and opening an offline secret-capable vault, I forced the next warm reachability probe to remain in flight. In both Chromium and Firefox the focused diagnostic observed:

```text
during recheck:
  data-reachability-state = unreachable
  data-reachability-checking = true
  cold data-warm-network-online = false
  cold session state = unlocked
  warm vault state = unlocked
  cold vault state = unlocked
```

When that held probe was released as reachable, both engines changed to `reachable` / `data-warm-network-online=true` and locked the session.

The source explains the gap. `runReachabilityCheck()` sets only `data-reachability-checking="true"` when a new round begins; it does not move a prior `unreachable` classification to unknown/online-safe. `updateAirgapBanner()` later sends `mode.set { online: reachabilityState !== 'unreachable' }`, so the stale `unreachable` value continues to authorize offline mode throughout the check. The author source-pattern test named “online-safe on uncertainty” only asserts the final `sendColdMode(reachabilityState !== 'unreachable')` expression and does not exercise this in-flight stale-state transition.

**Expected:** ADR-0024 explicitly requires checking and stale states to be online-safe and to keep the secret compartment sealed. A recheck of a previously offline-classified device must not leave an unlocked offline session authorized while the current reachability result is unknown/stale.

**Required action:** On starting a new reachability round from `unreachable`, immediately move the authority to an online-safe classification and send the corresponding validated `mode.set` before waiting for probe completion, so an unlocked offline session locks/zeroizes at once. Add a real Chromium/Firefox regression that holds the probe unresolved and asserts the cold mode is online-safe and any offline secret-capable session is already locked during the in-flight interval.

### F4 — Current dependency documentation still uses the superseded numbered-vault-QR description

**Severity:** advisory

**Location:** `docs/05-development/dependencies.md:23`

**Observed:** The current dependency table still describes `qrcode-generator` as:

`Local SVG/data-URL generation for numbered encrypted-vault QR frames`

That is the pre-ADR-0026 vocabulary the P0.19 documentation reconciliation was supposed to retire. The current canonical docs distinguish the historical numbered save/export flow from the live-only `CBX-VT/1` transport, and the review scope explicitly requires that current documentation not continue to advertise the old numbered/downloadable vault-QR model.

**Expected:** Current dependency documentation should describe the actual live-only use, e.g. rendering ephemeral encrypted `CBX-VT/1` transfer frames, and should not read like the historical numbered vault-QR export feature.

**Required action:** Reconcile the dependency row with ADR-0026/current runtime terminology and re-run documentation checks plus a targeted stale-QR wording scan.

## 5. Verdict rationale

P0.19 is not eligible for closeout. The known physical matrix is incomplete, the Windows camera receiver has not demonstrated its required path, and independent browser testing found an additional security-relevant reachability defect: a stale offline classification remains authorized while a new active probe is checking, contrary to the accepted online-safe design and current documentation. The dependency documentation also retains stale numbered-vault-QR wording. A future PASS requires all four findings to be resolved, the physical matrix evidence completed without inference, all independent gates repeated on the new exact tip, and a fresh independent review with zero findings. P0.19 must remain `[~]`; do not merge, create a PR, or start P1.2.

FAIL

---

# Fresh independent re-review — P0.19 remediation

**VERDICT: FAIL**

Findings: 1 (1 blocking — physical acceptance remains incomplete)
Reviewed tip: `cd9d8f8ef963e3e6716e10261a59cb10f302a1b2`
Reviewed base: `afb8de9e73c3a4fd3cbc6c71c96517f7339c3f3e` (`main`)
Reviewed branch: `p0.19-device-matrix` (tip was checked out detached; no branch or parent checkout was modified)
Reviewed by: GPT-5 independent fresh-session reviewer
Date: 2026-08-08

## 1. Fresh verification

The checkout was clean at the exact requested tip. Node was `v24.16.0`. `npm.cmd ci` completed with exit 0 after using a disposable npm cache because the default host cache was inaccessible.

Normal gates, run against the exact tip, returned:

```text
npm.cmd test
exit 0
tests 252
pass 252
fail 0

npm.cmd run lint
exit 0
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run verify-vendor
exit 0
10 local vendored artifacts verified
10 corresponding upstream npm release artifacts downloaded and verified
Vendor verification passed against local files and upstream releases.

node scripts/check-docs.js
exit 0
Documentation hygiene check passed: 135 markdown file(s) checked, 0 warning(s).

npm.cmd run build
exit 0
Built build/coldbox.html (638722ee26f2640210fb7f85bbe32b88f2097070d13d65015eaced7e4a57825e)
bytes 1269833

second clean build
exit 0
SHA-256 638722ee26f2640210fb7f85bbe32b88f2097070d13d65015eaced7e4a57825e
bytes 1269833
```

A fresh local clone at `C:\Users\semaj\AppData\Local\Temp\coldbox-p019-alt-181c610764024466ba3a2f5ddb055768`, built with `TZ=Pacific/Honolulu` and `LC_ALL=fr-FR`, produced the same SHA-256 `638722ee26f2640210fb7f85bbe32b88f2097070d13d65015eaced7e4a57825e` and 1,269,833 bytes. The embedded build date was the product commit date `2026-08-08T21:04:35-07:00`, not the documentation-only tip commit date.

The required destructive check was run only in a disposable clone after flipping one byte in `vendor/npm/@fontsource/bangers/5.3.0/package.tgz`:

```text
node scripts/verify-vendor.js --offline
exit 1
Vendor verification failed: Vendor SHA-256 mismatch for @fontsource/bangers@5.3.0

npm.cmd run build
exit 1
Error: Build refused: vendored artifacts failed offline verification
```

`npm.cmd run test:browser` returned exit 0. The real `file://` harness passed in both Chromium and Firefox, including the held-probe stale-reachability regression, cold CSP/network guards, realm isolation, lock/zeroization paths, provider neutering, tampered-script rejection, vault-library/save flows, and all existing negative fixtures.

Independent source and built-artifact inspection confirmed the cold policy retains `connect-src 'none'`, the cold iframe has no `allow-same-origin`, the cold vault consumes only validated `data-warm-network-online`/`mode.set` state, and the current runtime contains no downloadable vault-QR artifact or `CBX-QR/1` live-transfer path.

## 2. F1–F4 disposition

### F1 — Required physical device matrix

**Disposition: OPEN; blocking.** The current packet still records only Windows Edge/Chrome/Firefox hands-on observations for candidate `eae0e4c2a700781ac87b037a952dba09c7275698`, dated 2026-08-08. No exact Windows device model or browser version is recorded. macOS Safari/Chrome, Linux Firefox, Android Chrome from Files, and Tails/Tor Browser remain deferred. The iOS target still has no formal `PASS`, `BLOCKED`, or `UNSUPPORTED` result with an exact device and iOS version/build; the packet explicitly says the observed third-party HTML-reader route is non-qualifying under ADR-0010. No physical result is inferred from Playwright or another platform.

The P0.19 acceptance criterion requiring every supported platform and a separately recorded exact iOS status is therefore unmet. P0.19 remains `[~]`.

### F2 — Windows camera receiver mismatch

**Disposition: Remediated at the product-classification level; no new physical Windows camera result.** The current receiver gates Start camera scanner on `getUserMedia`, `BarcodeDetector`, and `qr_code` support. A reviewer-written probe against the built `file://` artifact produced this exact result in both engines:

```text
Chromium: {"cameraState":"partial","cameraLabel":"Camera API only","cameraDetail":"Camera access exists, but live QR decoding is unavailable here. Use the canonical .cbx file instead.","receiverDisabled":true,"receiverStatus":"Live QR receive is unavailable in this browser. Use the canonical .cbx file instead."}
Firefox: {"cameraState":"partial","cameraLabel":"Camera API only","cameraDetail":"Camera access exists, but live QR decoding is unavailable here. Use the canonical .cbx file instead.","receiverDisabled":true,"receiverStatus":"Live QR receive is unavailable in this browser. Use the canonical .cbx file instead."}
```

This is truthful unavailable classification with the required canonical `.cbx` fallback, not physical camera evidence. The packet correctly makes no unsupported physical Windows claim; the remaining physical-camera/matrix gap is covered by F1.

### F3 — Stale/checking reachability

**Disposition: FIXED and independently verified.** `runReachabilityCheck()` now changes a prior `unreachable` state to `unknown` before awaiting probe I/O. `updateAirgapBanner()` consequently sends the online-safe `mode.set { online: true }`; the real Chromium and Firefox held-probe regression observed `unknown` + `checking`, online-safe cold mode, locked warm/cold session state, and cleared cold working bytes during the unresolved interval. The browser harness passed this regression in both engines. Source inspection also found no independent cold sampling of `navigator.onLine`, `navigator.connection`, or the airgap snapshot in `src/cold/vault.js`.

### F4 — Superseded dependency wording

**Disposition: FIXED.** `docs/05-development/dependencies.md` now describes `qrcode-generator` as ephemeral live encrypted `CBX-VT/1` transfer-frame rendering and explicitly says it is never durable QR export. `node scripts/check-docs.js`, the full test suite, source inspection, and the targeted browser checks passed; current runtime/source scans found no downloadable vault-QR path.

## 3. New findings

No new implementation, test-fixture, CSP/realm-boundary, zeroization, stale-packet, dependency-wording, or unsupported-physical-claim finding was found beyond the still-open F1 physical acceptance gap. The browser harness is not treated as physical evidence.

## 4. Verdict rationale

The remediation fixes F3, and F2 now fails honestly closed with a canonical `.cbx` fallback when the exact live receiver capability is absent; F4 is reconciled. However, the supported physical matrix remains incomplete and the required formal iOS status is absent. Under the roadmap and review protocol, an unverified human-required acceptance criterion is a blocking FAIL. P0.19 must remain `[~]`; no `[x]`, merge, or Phase-1 continuation is authorized.

**FAIL**
