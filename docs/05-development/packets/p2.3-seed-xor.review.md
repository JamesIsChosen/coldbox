# Review: P2.3 — Seed XOR

**VERDICT: FAIL**

Findings: 1 (1 blocking, 0 advisory — all must be addressed)
Reviewed commit: `5d580ee5fc6d10026e57ccdf3942ede08446cba0`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-12
Independent evidence bundle SHA-256: `31c56955b824d265deee00d7da2a1a7b2b831c1a56ce64335adea4f43571abe8`

## 1. What I verified

Fresh independent review used a full-history clone and separate detached worktrees. For this item I verified the exact product head and merge base, `git diff --check`, `npm ci`, `npm run verify-vendor`, `npm run lint`, `npm run check-docs`, the focused item test, the full `npm test` suite, two same-worktree builds, and a third build from a different detached path/timezone/locale. Vendor-manifest corruption was also exercised and correctly made `verify-vendor` exit non-zero. The cold CSP probe retained `connect-src 'none'`, found no `allow-same-origin`, and found no storage/network/warm-message surface in the item module.

The prior 17-byte reproducibility discrepancy is resolved. All three fresh builds — including the different path/TZ/locale build — are exactly `9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778`, 2,223,951 bytes, matching the packet.

The focused Seed XOR tests and full suite exited 0. The full browser harness exited 0 and produced item-specific PASS lines in both Chromium and Firefox for the Coldcard-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown, together with the existing security regression matrix.

Independent current Coldcard documentation/source was inspected for the XOR construction; no new interoperability finding was identified in this review.

## 2. What I could not verify

Nothing additional was omitted that would independently change the verdict. The item fails on F1.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.3 Seed XOR` — `Coldcard-compatible N-of-N splitting of BIP-39 entropy into 2, 3, or 4 independently checked phrases, with deterministic and CSPRNG-derived masks, cold-only UI, and local reconstruction.` | ❌ | Functional, interoperability, Chromium/Firefox, and reproducibility evidence passed, but the repository build gate accepts executable `Math.random()` in the Seed XOR secret-handling module (F1), violating the required randomness hard gate. |

## 4. Findings

### F1 — Build/lint gate fails open to executable `Math.random()` in Seed XOR security code

**Severity:** blocking security
**Location:** `scripts/lint.js` `scanSourceFile()`; exercised path `src/cold/seed-xor.js`.
**Observed:** In the disposable reviewer worktree, appending `void Math.random();` to `src/cold/seed-xor.js` followed by `npm run build` exited **0**. The original module was restored byte-for-byte. `scripts/lint.js` defines a `Math.random` detector but applies it only to `src/capabilities.js`, not `src/cold/**`.
**Expected:** The review protocol requires no `Math.random` anywhere in a security-relevant path, and deliberate invalid security mutations must make the build exit non-zero.
**Required action:** Extend the lint/security gate across cold secret-handling modules, add a regression test proving the build fails non-zero when executable `Math.random()` is introduced there, and rerun the independent negative gate.

## 5. Verdict rationale

P2.3 otherwise reproduced cleanly: the historical 17-byte discrepancy is resolved, focused/full tests pass, both browsers pass, and vendor/CSP/security-surface checks pass. The single hard-gate defect is sufficient for FAIL under the repository protocol. Leave P2.3 `[~]`; re-review after the security lint/build gate is fixed.

FAIL


## Re-review round 3 — 2026-08-12

**VERDICT: FAIL**

Findings: 1 (1 blocking, 0 advisory — all must be addressed)
Reviewed commit: `540a3f2ae0d7b026b5030d21387b01e3c9421da2`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `672d5ddb511b731798eadf30a14c7abee13d326b25b158e6b690072f1dd16c88`

### 1. What I verified this round

I freshly reviewed exact PR #50 tip `540a3f2ae0d7b026b5030d21387b01e3c9421da2` against `main@5f0fa5e442524033e91bb31006a0ca6288385dc0`.

Full/remediation diff checks, install/vendor/lint/docs gates, the focused Seed XOR suite, the new lint/build security regression tests, and the full 328-test suite all passed. The complete browser harness exited 0 and emitted Seed XOR item-specific PASS lines in Chromium and Firefox.

Three builds were byte-identical across different path/timezone/locale settings at `d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6`, 2,223,951 bytes.

A reviewer-only executable `Math.random()` mutation in `src/cold/seed-xor.js` made both lint and build exit non-zero; vendor-manifest corruption also failed non-zero; both were restored exactly. The prior round's sole security-gate finding is resolved.

### 2. What I could not verify

The packet's round-3 current artifact hash does not reproduce from the exact committed head. This is R3-F1.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.3 Seed XOR` | ❌ | Coldcard-compatible functional behavior, independent vectors, security gates, reproducibility, and both browsers pass, but the packet's current artifact evidence is stale relative to the exact reviewed head (R3-F1). |

### 4. Findings

#### R3-F1 — Round-3 packet build evidence does not reproduce from the exact committed head

**Severity:** blocking evidence
**Location:** `docs/05-development/packets/p2.3-seed-xor.md`, round-3 current build evidence.
**Observed:** The packet records `9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778` / 2,223,951 bytes. Three fresh builds of exact head `540a3f2ae0d7b026b5030d21387b01e3c9421da2` all produced `d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6` / 2,223,951 bytes.
**Expected:** The packet's current build claim must reproduce from the committed tip that is handed to independent review.
**Required action:** Capture the build evidence after the build-affecting remediation commit exists, update the packet/self-review to the exact committed-head digest, and request fresh re-review.

### 5. Verdict rationale

The previous P2.3 security finding is resolved, and all independent product/security/browser gates are green. The non-reproducing current packet digest still requires FAIL under the review protocol. Leave P2.3 `[~]`; do not merge.

FAIL


## Re-review round 4 — 2026-08-12

**VERDICT: FAIL**

Findings: 1 (0 product/security, 1 documentation/evidence — all findings block PASS)
Reviewed commit: `5d31874a768239640aed17e094f3f9a19710fe20`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `b924639296ec7e9d7f62717b1c5f11b786121beed38c37928701fcb1a3fe5238`

### 1. What I verified

I freshly reviewed PR #50 at exact head `5d31874a768239640aed17e094f3f9a19710fe20` against `main@5f0fa5e442524033e91bb31006a0ca6288385dc0`.

Independent gates passed:

- Full-PR and round-4-only diff checks.
- Clean install, vendor, lint, docs, focused Seed XOR tests, security regression tests, and the full suite.
- Three byte-identical builds across different worktrees/path/timezone/locale settings: `d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6`, 2,223,951 bytes.
- The packet's round-4 current evidence block matches that exact artifact.
- The complete Chromium/Firefox `file://` browser harness passed.
- Reviewer-injected executable `Math.random()` in `src/cold/seed-xor.js` made lint and build exit non-zero; vendor corruption also failed non-zero; both mutations were restored exactly.
- The prior P2.3 security-gate finding remains resolved.

### 2. What I could not verify

I could not treat the packet as internally current because its primary “How to verify” section and Bundle Impact still identify the older `9a6a0616…` artifact as the build of the committed branch/tip, while later remediation sections correctly identify the exact current artifact as `d8b16363…`. This is R4-F1.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.3 Seed XOR` | ❌ | Product behavior, independent compatibility evidence, security gates, reproducibility, and both browsers pass; the current packet still gives contradictory committed-build evidence (R4-F1). |

### 4. Findings

#### R4-F1 — Primary verification and Bundle Impact still identify an older artifact as the committed P2.3 build

**Severity:** documentation/evidence
**Location:** `docs/05-development/packets/p2.3-seed-xor.md`, `## 3. How to verify` and `## 11. Bundle impact`.
**Observed:** `## 3` tells the reviewer to run from “the committed branch checked out,” says the product was built twice from “the same committed tip,” and records SHA-256 `9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778`. `## 11` likewise says “The committed P2.3 build” has that hash. Fresh A1/A2/B builds of exact reviewed head `5d31874a768239640aed17e094f3f9a19710fe20` all produce `d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6` at the same 2,223,951-byte size, and later round-3/round-4 sections correctly record `d8b16363…`.
**Expected:** Historical artifact evidence may be retained when clearly scoped to the historical commit that produced it, but current “committed branch/tip” instructions and present-tense Bundle Impact claims must identify the exact current reviewed state.
**Required action:** Rewrite or relabel the older `9a6a0616…` blocks so they are unambiguously historical, and make the primary current verification/Bundle Impact point to `d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6`. Do not alter Seed XOR product/security code for this finding.

### 5. Verdict rationale

The Seed XOR implementation and its prior security remediation independently pass. The only remaining issue is contradictory current artifact documentation, but that is a finding under the binary review protocol. Leave P2.3 `[~]`; do not merge.

FAIL

## Re-review round 5 - 2026-08-13

**VERDICT: FAIL**

Findings: 1 (0 product/security, 1 documentation/evidence - all findings block PASS)
Reviewed commit: `0918a6cdc1b63cd6cd657494f6b9683afe9cf179`
Reviewed by: GPT-5.6 Sol
Independent evidence bundle SHA-256: `d7c3bbaafa003ee61771efa7e19f475063d4c9234aa41a2b7639d351897e01c9`

### 1. What I verified

I freshly reviewed PR #50 at exact integration head
`0918a6cdc1b63cd6cd657494f6b9683afe9cf179` against
`main@2375fbcfb76b179228bac60402b62bce8f379c74`. The reviewed commit has the
expected two parents: P2.3 pre-integration head
`e294eae023ad6bc625fb45f2d80909f921306ccf` and current main
`2375fbcfb76b179228bac60402b62bce8f379c74`.

Independent execution used disposable fresh clones and Node v24.16.0.
The following gates passed:

- `npm ci`
- online `npm run verify-vendor`
- `npm run lint`
- `npm run check-docs`
- focused `node --test test/seed-xor.test.js`
- full `npm test`
- two fresh builds after removing the build directory between runs
- a third build from a different clone path with `TZ=Pacific/Kiritimati`,
  `LANG=fr-FR`, and `LC_ALL=fr-FR`
- the full Chromium and Firefox `file://` browser harness, including
  item-specific Seed XOR PASS markers in both engines

All three builds were byte-identical:

`ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d`
at 2,428,951 bytes.

Deliberate fail-closed checks also passed. Corrupting a vendored artifact made
`verify-vendor` exit 1. Injecting executable `Math.random()` into
`src/cold/seed-xor.js` made both lint and build exit 1. Each mutation was
restored before continuing.

I also independently checked Coldcard's current published `shared/xor_seed.py`.
Its deterministic masks use double-SHA-256 over `Batshitoshi ` plus source
entropy plus the zero-based ASCII label `i of N parts`; random mode draws
`vlen` random bytes, double-hashes them, and truncates to `vlen`. The reviewed
P2.3 construction matches those semantics.

### 2. What I could not verify

Nothing additional was omitted that would independently change the verdict.
The review fails on R5-F1 below.

### 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.3 Seed XOR` - Coldcard-compatible N-of-N splitting of BIP-39 entropy into 2, 3, or 4 independently checked phrases, with deterministic and CSPRNG-derived masks, cold-only UI, and local reconstruction. Acceptance requires only 12-, 18-, and 24-word BIP-39 phrases; independently valid generated parts; published Coldcard deterministic compatibility and independent vectors; hard failure without `crypto.getRandomValues`; any-order combine with malformed/incomplete/mismatched/checksum rejection; no secret crossing/persistence, masked defaults and teardown clearing; and N-of-N/separate-passphrase documentation. | NO | Product behavior, independent interoperability checks, fail-closed security mutations, reproducibility, and Chromium/Firefox all pass. The tracked packet still gives a stale current artifact claim for the exact branch handed to review (R5-F1). |

### 4. Findings

#### R5-F1 - Primary current artifact and Bundle Impact are stale after current-main integration

**Severity:** documentation/evidence

**Location:** `docs/05-development/packets/p2.3-seed-xor.md`, especially
`## 3. How to verify` and `## 11. Bundle impact`.

**Observed:** The packet's current verification still identifies build state
`5d31874a768239640aed17e094f3f9a19710fe20` and SHA-256
`d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6`
at 2,223,951 bytes. It also states that "The committed P2.3 build" has that
digest and size. Three independent fresh builds of the exact reviewed
integration tip `0918a6cdc1b63cd6cd657494f6b9683afe9cf179` instead reproduce
`ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d`
at 2,428,951 bytes.

The packet does append candidate-tree current-main integration evidence, but
that section does not correct the primary current verification or Bundle Impact
claims and does not record the exact pushed-tip artifact.

**Expected:** Historical pre-integration evidence may remain when clearly
labeled historical, but the packet's present-tense current verification and
Bundle Impact must identify the exact pushed integration tip handed to
independent review and its reproducible artifact.

**Required action:** Update the packet and self-review so their primary current
artifact evidence and Bundle Impact are explicitly scoped to
`0918a6cdc1b63cd6cd657494f6b9683afe9cf179`, using the exact reproducible
artifact
`ff914a7016bb38b2fc6573c5173ee2b72bb5e5e2d2576013ec4102b3c762820d`
at 2,428,951 bytes. Retain older evidence only as clearly historical. Then
request a fresh independent re-review. No Seed XOR product/security change is
required by this finding.

### 5. Verdict rationale

P2.3's product, interoperability, fail-closed security gates, reproducible
build, and Chromium/Firefox behavior independently pass at the integrated tip.
However, the repository review protocol treats stale documentation and
non-reproducing packet claims as findings, and any finding forces FAIL. Correct
the current packet/self-review artifact labeling and Bundle Impact, keep P2.3
at `[~]`, and request a fresh review.

FAIL