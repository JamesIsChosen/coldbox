# P1.10 self-review — batch gate

- Branch: `p1.10-qr-generation`
- Base: `main`
- Product commit reviewed: `856602810205baadb7a81de8115860e4a01ba672`
- Review round: 1
- Verdict: **FAIL**

## Findings

1. `npm.cmd test` is not green: 295 passed and 1 failed. The failure is the build assembly test at `test/build.test.js:157`. The `NO_MACHINE_PATHS` scan sees `e:\` in the literal `license:\n` comment contained by the newly injected vendored QR source. This is a reproducible repository test failure and must be resolved before another gate.
2. `npm.cmd run test:browser` is blocked because the local Playwright Chromium and Firefox binaries are not installed. This is an additional verification gap.

## Passing checks

- `npm.cmd run lint` — passed.
- `npm.cmd run check-docs` — passed, 165 files and 0 warnings.
- `npm.cmd run build` — passed; artifact SHA-256 `ee20dede9f407629bc7dc6e56187cd4f14ae1dc569c9104d36b74cb3a70f5beb`.
- Alternate clean-path/TZ/locale build — byte-identical to the primary artifact.
- Focused build suite — 12 passed, 1 failed; the failure is the same gate failure, while the negative machine-path regression passed.
- QR unit tests are included in the full run; the only reported full-suite failure is the build assembly test.

## Gate decision

FAIL. The batch stop condition applies. No P1.11, P1.12, or P1.13 branch may be started from this session until P1.10 receives a fresh self-review after the blocking test collision is fixed. The branch is pushed with this packet for independent inspection; no roadmap item is marked `[x]`, and nothing is merged.

## Fresh self-review — round 2

The round-1 finding was fixed in `test/build.test.js`: the path guard now scans the decoded `coldRealmDocument` separately and excludes its JSON-encoded representation from the surrounding-document scan. Real paths remain detected in both representations.

Verification:

- `npm.cmd test` — 296 passed, 0 failed.
- `npm.cmd run verify-vendor` — passed for all 10 local files and upstream releases, including `qrcode-generator@1.4.4`.
- `npm.cmd run lint` — passed.
- `npm.cmd run check-docs` — passed, 167 markdown files and 0 warnings.
- Two primary builds — both SHA-256 `ee20dede9f407629bc7dc6e56187cd4f14ae1dc569c9104d36b74cb3a70f5beb`.
- Alternate clean-path/TZ/locale build — byte-identical to the primary artifact.
- `npm.cmd run test:browser` — completed successfully; Chromium file://, cold-boundary, registry, lock, and responsive checks passed.

Fresh verdict: **PASS — ready for independent review.** The round-1 failure is resolved, all software gates pass, and the branch remains limited to P1.10 plus its packet and test-harness correction. The roadmap marker remains `[~]` for the independent reviewer.
