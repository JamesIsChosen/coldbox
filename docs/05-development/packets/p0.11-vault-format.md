# PR packet: P0.11 - Vault format v1

## 1. Summary

P0.11 adds the format-v1 serializer and parser inside the sealed cold realm. It writes the authenticated 65-byte header, a length-delimited multi-record wrapped-DEK block, separate AES-GCM public and secret compartments, HKDF-SHA-512 domain-separated subkeys, and random 64 KiB padding. The warm shell receives no vault API or secret-subkey derivation function.

This branch is `p0.11-vault-format`, PR #19, based on `main`. P0.10 has merged into `main` at `edd690b`; `8811754` is the historical stacked merge-base, not the current PR base.

## 2. Scope

### In scope

- Format-v1 header encoding and strict parsing for magic, version, KDF, cipher, salt, and compartment lengths
- AAD equal to header bytes 0-64 for both wrapped-DEK and compartment authentication
- Length-delimited wrapped-DEK record parsing, with passphrase records implemented and future method records rejected safely
- Random 256-bit DEK, passphrase KEK derivation through the P0.10 layer, AES-256-GCM DEK wrapping, and 12-byte nonces
- HKDF-SHA-512 `cbx/public/v1` and `cbx/secret/v1` subkeys
- JSON length prefix plus random padding to 64 KiB boundaries for both compartments
- Public-only vaults with a zero-length secret ciphertext and no secret-subkey derivation
- Explicit online/offline opening modes: online opening never derives `cbx/secret/v1`; offline opening is fail-closed unless the shared airgap snapshot reports offline and the cold CSP/runtime/randomness/crypto health gate is proven
- Generic authentication failure for wrong passphrases, malformed files, header tampering, and corrupted ciphertext; the public 64 MiB implementation limit is a distinct refusal rather than a false authentication failure
- Cold-only build wiring, protocol-boundary tests, browser assertions, security documentation, roadmap, changelog, and packet evidence

### Deliberately out of scope

- Keyfile and recovery-share record creation (the parser preserves the record-list structure; later items own additional methods)
- KDF benchmark UI and profile selection UX (P0.12)
- Lock/unlock session state, save/load paths, verify-after-save, generational filenames, or rollback detection (P0.13-P0.14). The premature P0.13 session/save primitive identified in independent-review F4 has been removed from this branch.
- Warm-shell vault operations or any new warm-to-cold message carrying vault bytes or passphrases
- Physical device claims; P0.19 owns the manual matrix

## 3. How to verify

Run from the repository root:

```text
$ npm.cmd run verify-vendor -- --offline
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Vendor verification passed in offline mode.

$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ node --test test\vault.test.js
tests 10
pass 10
fail 0

$ npm.cmd test
tests 49
pass 49
fail 0

$ npm.cmd run build
Built build/coldbox.html (dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553)
size=344415

$ npm.cmd run test:browser
Browser harness passed in Chromium and Firefox.
```

The Chromium/Firefox lines above are from the fresh post-review remediation run on the Windows authoring host. The earlier evidence-integrity pass performed in a separate Node 22 environment explicitly did **not** re-run Playwright and remains historical only.

The post-review remediation runner also rebuilds from an alternate temporary root and verifies byte identity. The current candidate artifact is **344415 bytes** with SHA-256 **`dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553`**.

## 4. Acceptance criteria

| Criterion | How satisfied | Test |
|---|---|---|
| Serializer and parser per [vault-format.md](../../01-spec/vault-format.md): header, AAD, multi-record wrapped-DEK block, two compartments, HKDF subkeys, 64 KiB padding. | `src/cold/vault.js` encodes all v1 fields, authenticates the header as AAD, parses the length-delimited record list, wraps one random DEK, derives distinct public/secret HKDF subkeys, and pads each JSON compartment to a 64 KiB boundary. KDF header parameters come from the derivation result and the single canonical `cryptoLayer.profiles` table. | `test/vault.test.js` performs real and format-isolated round-trips, checks both ciphertext lengths, checks a zero-secret vault, validates KDF provenance/profile strictness, and proves the P0.13 session API is absent. |
| **Accept:** round-trips; tampering with any header byte fails authentication; wrong passphrase and corrupted file are indistinguishable in the error; padding always lands on a 64 KiB boundary; **the secret subkey has no derivation path reachable while online.** | The parser returns the original public and secret objects only for an offline opener. Online opening returns public data only and never invokes `cbx/secret/v1`; offline opening requires both the proven cold-health gate and the canonical airgap snapshot to report offline. Wrong credentials and corruption use the same generic error, the 64 MiB public size refusal is distinct, and the warm browser context has no `window.__coldboxVault`. | `test/vault.test.js` covers all 65 header bytes, wrong/corrupt equivalence, 64 KiB lengths, online HKDF exclusion, health-gate failures, airgap-snapshot mode control, size-limit refusal, strict KDF profile handling, and static warm-source absence. `scripts/run-browser-harness.js` verifies the warm/cold API split in Chromium and Firefox. |

## 5. Security impact

- Realm boundary: Yes. The serializer/parser is embedded only in the cold document. The warm shell does not load `src/cold/vault.js`, expose `__coldboxVault`, or receive a secret-subkey function.
- AAD: Yes. Header bytes 0-64 are passed as AAD to DEK wrapping and both compartment encrypt/decrypt operations. KDF parameters, cipher id, salt, and lengths therefore cannot be changed without authentication failure.
- Derivation: Yes. A random DEK is wrapped by a passphrase-derived KEK. HKDF-SHA-512 uses distinct `cbx/public/v1` and `cbx/secret/v1` info strings; the secret subkey is created only inside the cold module and only after the cold health gate is proven and the shared airgap snapshot reports offline. P0.11 contains no session/save primitive; Warm-Mode save behavior remains P0.13 work.
- Compartment separation: Yes. Public and secret JSON are padded and encrypted independently with fresh nonces. A public-only vault has no secret ciphertext and does not derive the secret subkey.
- Error handling: Yes. After the public file-size precheck, parse, KDF, unwrap, AEAD, padding, and JSON failures from `open` collapse to `Vault authentication failed.`; the caller cannot distinguish wrong passphrase from damaged bytes through the authentication error. The documented over-size refusal is intentionally distinct because file size is already public metadata.
- Randomness: Yes. DEK, salt, nonces, and padding use the P0.10 `crypto.getRandomValues`-only path. No fallback was introduced.
- Network/message surface: No new network host, message type, or secret-bearing field. The vault mode gate treats an unknown network state as online for default opens; only an explicit offline state may use the secret key.
- If this implementation is wrong, an attacker could exploit unauthenticated metadata, confuse compartment boundaries, or obtain a derived secret subkey from the online shell. The header-as-AAD checks, distinct HKDF labels, cold-only build wiring, and generic failure contract are the controls tested here; full unlock/save workflow review remains later work.

## 6. Test evidence

### New and changed tests

- `test/vault.test.js` uses real P0.10 crypto for a fast-profile public+secret round-trip and a public-only round-trip.
- The online-boundary regression proves `open` returns no secret data, records only `cbx/public/v1`, and rejects an explicit offline open while the shared airgap snapshot reports online.
- Health-gate regressions independently break cold-state, CSP-canary, runtime-neutering, required-randomness, AES-GCM crypto state, airgap, lockdown, and vault-operation state and prove `create`, `open`, `openPublic`, and `inspectHeader` fail closed.
- Mode, KDF, and size regressions prove the vault consumes the airgap snapshot instead of reading `navigator.onLine` directly, rejects unknown requested/derived profiles, uses the crypto-layer profile table including fallback `memoryKiB: 0`, and reports an over-64-MiB file distinctly.
- A scope regression proves `openSession`, `createVaultSession`, and `openVaultSession` are absent from P0.11.
- The same test mutates each of the 65 header bytes and expects the exact generic authentication failure. It also compares wrong-passphrase and corrupted-ciphertext errors.
- The test statically checks that warm source and warm template contain no vault-layer or secret-HKDF path, and that the cold API does not export `deriveSecretSubkey`.
- `scripts/run-browser-harness.js` verifies `window.__coldboxVault` is undefined in the warm document, the cold realm reports guarded vault operations, the v1 API exists there, and the P0.13 `openSession` API is absent in both engines.
- The P0.10 `deriveKey` API now accepts the explicit `fallback` profile so a fallback-encoded header is not reinterpreted as standard Argon2id on a machine where Argon2 is available.

### Independent sources

- Format field layout and padding rules: repository specification [vault-format.md](../../01-spec/vault-format.md).
- AES-GCM KAT inherited from [NIST SP 800-38D](https://csrc.nist.gov/pubs/sp/800/38/d/final).
- Argon2id KAT and KDF parameters inherited from [RFC 9106](https://www.rfc-editor.org/rfc/rfc9106#section-5.3).
- HKDF-SHA-512 is the pinned `@noble/hashes` implementation already verified in P0.10; domain-separation strings are fixed by the repository specification.

### Negative tests

- All 65 header-byte mutations fail authentication.
- Wrong passphrase and one-byte ciphertext corruption both return exactly `Vault authentication failed.`.
- Unsupported/malformed record or header shapes fail closed through the same parser boundary.
- Existing CSP, runtime network, protocol, tamper, missing-randomness, and browser lockout fixtures remain green.

### Not tested

No physical iOS Safari, Android Chrome from Files, macOS Safari, macOS Chrome, Linux Firefox, or Tor Browser was available. P0.12 owns production-profile timing and P0.13-P0.14 own real save/load and verify-after-save flows. Keyfile/recovery-share records are structurally reserved but not implemented.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | Automated pass | Bundled Chromium over `file://`; cold-only vault API, CSP, opaque frame, and existing security matrix passed |
| Windows Firefox | Automated pass | Bundled Firefox over `file://`; same cold-only and security assertions passed |
| macOS Safari | Untested | Requires P0.19 manual matrix |
| macOS Chrome | Untested | Requires P0.19 manual matrix |
| Linux Firefox | Untested | Requires P0.19 manual matrix |
| **iOS Safari (Files)** | Untested | Highest-risk platform; requires P0.19 |
| Android Chrome (Files) | Untested | Requires P0.19 manual matrix |
| Tor Browser | Untested | Requires P0.19 manual matrix |

## 8. Assumptions made

> **Assumed:** A zero-length secret compartment still carries its 12-byte nonce because the v1 layout lists a nonce separately from the ciphertext length.
> **Basis:** The specification says `L2` may be zero while showing the secret nonce as a separate field; retaining the field keeps offsets unambiguous and avoids a format branch.
> **Not verified on:** An independent v1 reader, because no other implementation exists in this repository yet.
> **If wrong:** the format needs an explicit interoperability clarification before save/load is exposed.

> **Assumed:** P0.11 can reserve future record methods by parsing the full record list and ignoring unsupported method ids until a later item implements their KDFs.
> **Basis:** The v1 wrapped-DEK block is length-delimited and method-specific; unsupported records must not be guessed or treated as passphrase records.
> **Not verified on:** Keyfile/recovery-share records, which are P0.15/Phase 2 work.
> **If wrong:** the parser must reject the entire file rather than attempt an unsafe migration.

> **Assumed/implementation limit:** The current v1 implementation may refuse complete vault files above 64 MiB without changing the v1 wire format.
> **Basis:** The bound limits parser/allocation exposure and is now canonical in [vault-format.md](../../01-spec/vault-format.md); file size is public metadata, so a distinct refusal leaks no secret.
> **Not verified on:** Vaults above 64 MiB, because P0.11 intentionally refuses them before authentication.
> **If wrong:** the limit must be revised deliberately in the canonical format document and implementation together; it must never be disguised as a wrong-passphrase/corruption result.

## 9. What to scrutinise

- Recheck every header offset and the fact that the exact 65-byte header, not a reconstructed object, is used as AAD.
- Recheck wrapped-record length accounting: each record length covers method data, nonce, and 48-byte wrapped DEK; the block itself is bounded and can contain multiple records.
- Verify that zero-secret vaults do not call `hkdf(..., "cbx/secret/v1")`, and that the warm shell cannot obtain the cold API through a message or global.
- Review the generic error boundary so no underlying AEAD, KDF, JSON, or parser message leaks through `open`.
- Confirm that later save/load work does not move serializer calls into `src/main.js` or add passphrases/secret bytes to the protocol schema.

## 10. Self-assessment

- This is a format library, not a user-facing vault workflow. No UI can yet create, save, load, lock, or verify a vault.
- Only passphrase records are serialized; the parser is structurally multi-record but unsupported methods are not implemented.
- The test uses the fast Argon2 profile to keep the real round-trip lightweight; standard/paranoid allocation and timing remain P0.12/device-matrix concerns.
- The zero-secret nonce interpretation is explicitly disclosed as an interoperability assumption.
- The warm shell can hold the serialized cold document string, but it has no callable vault object or secret-subkey path; the browser assertion checks the actual runtime global boundary. No session/save API remains in P0.11; that lifecycle is deferred intact to P0.13.

## 11. Bundle impact

Measured against the completed P0.10 artifact (322,054 bytes at its merged P0.10 tip):

| Artifact | Before | After | Delta |
|---|---:|---:|---:|
| `build/coldbox.html` | 322,054 bytes / 314.5 KiB | 344415 bytes / 336.3 KiB | +22361 bytes / +21.8 KiB (+6.9%) |

The result remains below the 3 MB target and 4.5 MB hard cap.

## 12. Docs updated

- `docs/05-development/ROADMAP.md` - marked P0.11 complete and recorded automated coverage with physical-device deferral.
- `docs/05-development/build.md` - documented cold-only vault-layer assembly and warm-shell exclusion.
- `docs/05-development/testing.md` - recorded P0.11 round-trip, header-tamper, padding, and secret-boundary tests.
- `docs/02-security/threat-model.md` - recorded the v1 AAD, wrapped-DEK, HKDF compartment, and generic-error controls.
- `CHANGELOG.md` - recorded the P0.11 format layer and tests.
- This packet - permanent implementation, security, test, and handoff evidence.
- The author gate is recorded at `docs/05-development/packets/p0.11-vault-format.self-review.md`; the independent FAIL review is preserved at `p0.11-vault-format.review.md` and must not be edited by remediation.
- No new ADR was added; the implementation follows ADR-0002, ADR-0003, the vault-format specification, and the existing opaque cold-realm architecture.

## 13. Independent-review remediation — 2026-08-04

The independent review at `p0.11-vault-format.review.md` remains unchanged and records the original FAIL. This candidate addresses every finding at the review tip. User decision on F4: **Option A — remove the session primitive from P0.11.**

| Finding | Disposition |
|---|---|
| F1 | **Fixed before this remediation.** Header KDF fields are built from `deriveKey()`'s returned `profileId`, not mutable module-global KDF state. The existing fallback/provenance regression remains. |
| F2 | **Fixed.** All vault entry points are gated on the established cold-realm health state: cold ready, CSP canary passed, runtime network guard installed, required randomness present, green airgap, no lockdown, and vault operations marked guarded. Any failed/unfinished gate refuses before cryptographic work. |
| F3 | **Removed with F4.** The P0.13 session state that could retain secret material across an offline→online transition no longer exists in this branch. |
| F4 | **Fixed by user decision A.** `createVaultSession`, `openVaultSession`, public `openSession`, the `P0.13` session test, and packet claims about session saves are removed. |
| F5 | **Fixed.** Bundle figures below are regenerated from the remediation candidate rather than retaining the stale 340,000-byte value. |
| F6 | **Fixed.** PR #19 is documented against `main`; the batch record no longer says the PR is pending/stacked on P0.10. Browser evidence is explicitly labeled as fresh remediation-host output rather than conflated with the later Node 22 evidence-integrity environment. |
| F7 | **Fixed completely.** Unknown requested profile names now fail closed instead of becoming Standard; unknown derived profile ids also fail closed. |
| F8 | **Fixed.** `src/cold/vault.js` consumes `cryptoLayer.profiles`; the duplicate profile table is gone and fallback now explicitly carries `memoryKiB: 0` in `src/cold/crypto.js`. |
| F9 | **Fixed.** Vault mode reads the canonical `__coldboxAirgap.getNetworkSnapshot()` result instead of reimplementing `navigator.onLine`, and secret-capable mode cannot be reached unless the CSP/airgap health gate has already passed. |
| F10 | **Fixed.** [vault-format.md](../../01-spec/vault-format.md) documents the 64 MiB implementation limit and over-size inputs return the distinct public error `Vault exceeds the 64 MiB size limit.` rather than `Vault authentication failed.`. |

### Fresh remediation gate

```text
Base candidate before remediation: 573353066af438aaf6ae57dfcd667c434387eded
PR: #19
Base: main at edd690ba6f7a7d0eab989dbb071c166ebc440682
Focused vault tests: 10 passed
Full Node suite: 49 passed
Browser harness: PASS - Chromium and Firefox
Final artifact: 344415 bytes
SHA-256: dfd88b32004c7b3c4b21e9d71f08e5d0bf4adcc968447f3436f55dcb0eaf9553
Alternate-root reproducibility: PASS - byte-identical across alternate path and locale/timezone inputs
Corrupted-vendor negative control: PASS - verifier and build both refused a one-byte corrupted vendor tarball
git diff --check: PASS
```

The remediation runner creates the full command logs and diff evidence separately. No commit, push, merge, history rewrite, dependency change, or P0.12/P0.13 implementation is performed by that runner.
