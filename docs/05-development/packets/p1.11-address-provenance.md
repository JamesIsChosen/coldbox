# P1.11 - Address verification state PR packet

## Identity

- Roadmap item: P1.11 - Address verification state in the data model
- Branch: `p1.11-address-provenance`
- PR: #44, base `main`
- Base reconciled: `main@a0edef3b271c2ae0d61fec9718ba6a8577d19e6b`
- Reconciliation commit: `428ac2a1733c869816cc5928527ce0b57a6945cc`
- Product/remediation commit: `03a2e3b32f9175b67bf0a2b02e5de9dc5062babe`
- Packet closeout: this packet records the evidence for the pushed R2 remediation head and the fresh independent-review request
- Status: `[~]`; the author does not mark roadmap items `[x]`.

PR #43 was merged by the maintainer before this remediation, so PR #44 was explicitly reconciled with the resulting `main`. PRs #45 and #46 were not modified.

## Scope and acceptance

P1.11 adds the public schema-2 provenance fields `addressOrigin`, `verificationState`, `lastColdVerifiedAt`, and `verifiedAgainstXpub`. Legacy schema-1 or schema-less public compartments migrate in the cold vault layer on open to schema 2 with explicit `manual` and `unverified` defaults. No verification state is inferred.

The R1 and R2 independent FAILs identified five defects. This remediation addresses the two fresh R2 defects while retaining the prior R1 fixes:

1. The cold session now reconciles warm `publicData.replace` mutations against its existing address provenance and the account xpub relationship. A warm replacement cannot create `cold-verified` or `cold-verified-stale` from an unverified/unverifiable record, cannot rewrite authenticated timestamp/xpub evidence, and an account xpub change forces `cold-verified-stale` even when the warm request leaves `cold-verified` in place.
2. Cold vault `open`, `openSession`, and session replacement migrate schema 1/missing schema to schema 2. A successful save/reopen persists the migrated schema; a failed migration rejects without mutating the source bytes.
3. The normal Chromium/Firefox browser harness opens a real stale-state `.cbx` fixture and asserts the rendered Registry label is exactly `Cold verification stale` and is not `Cold verified`.
4. The roadmap contains the exact P1.11 marker `- [~] **P1.11 Address verification state in the data model**` with its existing dependency, description, and acceptance text.

The xpub-change transition remains automatic: an existing authenticated `cold-verified` address becomes `cold-verified-stale` while retaining its prior timestamp and xpub evidence. Actual cold re-derivation remains outside this item and is the next verification workflow surface.

## Security boundary

`publicData.replace` is still a warm-origin message. The cold-owned session is the authority for whether persisted provenance already exists; no warm-supplied timestamp or xpub is treated as proof. The protocol continues to validate closed public schemas and reject secret-shaped content. No message type carries mnemonic, private-key, xprv, passphrase, or secret-compartment plaintext.

## Review-history preservation

The complete accumulated independent R1+R2 report remains byte-for-byte unchanged:

```text
File: docs/05-development/packets/p1.11-address-provenance.review.md
SHA-256: C166A0DB9EEB1C6DCFDBADFD7C351ECC82BE3E42EDC6724C01A4ED09886ED253
Git blob: c579db7735d400c9b6175616ed694731e49d82ef
R1 reviewed head: 49e7796a822c26ef687807f4d7596d34d09b5d3c
R1 review commit: ad5a2e02299cf3f8802ad46003915301d56d9c4c
R2 reviewed head: 1aade101e11d4d867a88ca657812adfe05f5c6ba
R2 review commit: 050867784295bc4455ae2014b3c2bfebd5a6a5b8
Verdicts recorded in report: FAIL (R1 F1-F3; R2 F1-F2)
```

## Verification evidence

Commands were run from `C:\Users\semaj\Projects\coldbox`.

```text
git diff --check
PASS

npm.cmd test
tests 303
pass 303
fail 0

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run check-docs
Documentation hygiene check passed: 172 markdown file(s) checked, 0 warning(s).

npm.cmd run build  (run twice)
FIRST=C17B20E496E08E7176163F6672B6B20977EF82DF64CBCB065DB242AB379336AE
SECOND=C17B20E496E08E7176163F6672B6B20977EF82DF64CBCB065DB242AB379336AE

npm.cmd run test:browser
Browser harness passed in Chromium and Firefox.
Chromium: cold-verified-stale persisted state rendered as stale, never as cold verified
Firefox: cold-verified-stale persisted state rendered as stale, never as cold verified
```

Focused remediation tests are included in `test/vault.test.js`:

- a warm replacement cannot manufacture or rewrite cold verification authority;
- a direct cold vault replacement forces an authenticated address to `cold-verified-stale` when its account xpub changes, even when the warm caller requests `cold-verified`, while preserving the old timestamp and xpub evidence;
- schema-1 open migrates to schema 2, save/reopen preserves schema 2, and failed migration preserves the original input bytes.

## Assumptions and scrutiny points

- Missing public schema is treated as legacy schema 1, as required by the migration contract.
- Legacy addresses always become `manual` and `unverified`; schema-1 verification-shaped fields are discarded rather than trusted.
- The warm holder retains the original encrypted bytes; the cold layer migrates copied decrypted state, and the new schema becomes durable only through a newly produced save result.
- Scrutinize the authority reconciliation in `src/cold/vault.js`, especially the requirement that authenticated evidence and both account records agree on xpub before a replacement can remain `cold-verified`, plus same-address evidence preservation versus address-identity changes. The real browser fixture remains in `scripts/run-browser-harness.js`.

## Known limits

P1.11 does not implement the actual cold re-derivation event or clipboard verdict. Those belong to P1.12. Physical hardware-wallet validation remains the P1.9/P1.12 gate. No mobile browser or physical hardware test was claimed here.

## Handoff

The branch is ready for a fresh independent R3 review of the exact pushed head. Do not merge PR #44 in this authoring session. Its base is `main`; PR #45 remains stacked on this unresolved parent and must not be independently closed until P1.11 settles.
