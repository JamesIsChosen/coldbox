# Review: P1.11 — Address verification state in the data model

**VERDICT: FAIL**

Findings: 3 (3 blocking, 0 advisory — all must be addressed)
Reviewed commit: `49e7796a822c26ef687807f4d7596d34d09b5d3c`
Base commit: `a3283ba2609cea9efb3f75732d2cd3708b7acfad`
PR: #44 (`p1.11-address-provenance`)
Reviewed by: OpenAI GPT-5.6 Sol — independent reviewer
Date: 2026-08-11

## 1. What I verified

I reviewed the complete effective PR diff from `main@a3283ba2609cea9efb3f75732d2cd3708b7acfad` to exact PR head `49e7796a822c26ef687807f4d7596d34d09b5d3c` rather than trusting the packet.

Reviewer-owned evidence:

- Runner: `pr44-p111-fresh-independent-review-04`
- Evidence ZIP SHA-256: `5a69b11fcd84541ea20f4138118c4e847a5c94871a18d129ac71f72ff3e861f2`
- Sidecar: independently matched the ZIP digest
- Artifact SHA-256: `37422e8bf0e2f87c19150b48befa7eb54c9cae0de3062283370d7b826ec6fc21`
- Artifact size: `1,992,876` bytes
- Alternate path/TZ/locale artifact: byte-identical
- Evidence secret scan: clean
- PR #45 and PR #46: heads remained unchanged and correctly stacked during evidence collection

Fresh gates:

```text
git diff --check
PASS

npm ci
PASS

npm run verify-vendor
PASS

npm run lint
PASS

npm run check-docs
PASS

node --test test/protocol.test.js test/registry.test.js test/vault.test.js --test-concurrency=1
PASS

npm test
tests 296
pass 296
fail 0

primary build
path: short disposable clone
TZ=UTC
LANG=C
SHA-256 37422e8bf0e2f87c19150b48befa7eb54c9cae0de3062283370d7b826ec6fc21
1,992,876 bytes

alternate build
path: disposable path containing spaces
TZ=Pacific/Honolulu
LANG=de_DE.UTF-8
SHA-256 37422e8bf0e2f87c19150b48befa7eb54c9cae0de3062283370d7b826ec6fc21
1,992,876 bytes

deliberate qrcode-generator corruption
verify-vendor exit 1
build exit 1

deliberate stale-transition regression
exit 1 as expected
actual cold-verified != expected cold-verified-stale

npm run test:browser
Chromium PASS
Firefox PASS

separate reviewer-owned stale/unverified Registry display probe
Chromium PASS
Firefox PASS
```

The independent invariant probe also exercised the P1.11-specific security and migration behavior with real branch code:

```json
{
  "warmForgedColdVerifiedAccepted": true,
  "staleTransitionState": "cold-verified-stale",
  "staleEvidencePreserved": true,
  "rawSchemaAfterOpen": 1,
  "rawSchemaAfterSaveReopen": 1,
  "protocolProjectedSchema": 2
}
```

The stale-transition implementation itself is sound: changing an account xpub moves an existing `cold-verified` address to `cold-verified-stale` and preserves the old verification timestamp/xpub as evidence.

The current Registry renderer also displays the states correctly. A separately constructed vault containing one `cold-verified-stale` address and one `unverified` address rendered `Cold verification stale` and `Never verified` respectively in both Chromium and Firefox.

## 2. What I could not verify

Nothing required by P1.11 was inaccessible to this review. The three failures below are verified implementation/acceptance gaps, not unverifiable criteria.

Physical hardware-wallet validation belongs to P1.9/P1.12 flows and is not required to decide this P1.11 data-model review.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `a vault written by the previous schema version still opens (the migration test data-model.md requires)` | ❌ | A real schema-1 vault opens, but the documented migration does not run on open: the cold vault session remains schema 1, and save/reopen remains schema 1. Only protocol projection reports schema 2. See F2. |
| 2 | ``verificationState` is never inferred — an address reaches `cold-verified` only via an actual cold re-derivation` | ❌ | A reviewer-owned warm→cold `publicData.replace` containing caller-supplied `cold-verified` plus timestamp/xpub is accepted. The committed protocol test explicitly expects this warm-originated elevation to pass. See F1. |
| 3 | `changing an account's xpub moves its verified addresses to cold-verified-stale automatically, and a test proves a stale entry is never displayed as verified` | ❌ | The automatic transition itself passes and a reviewer-owned browser probe confirms the present UI label is safe, but the branch contains no committed test/harness assertion proving the stale display requirement. See F3. |

## 4. Findings

### F1 — Warm public-data replacement can manufacture `cold-verified`

**Severity:** blocking security / authority-boundary violation

**Location:** `src/protocol.js` (`cleanRegistryRecord`, `cleanPublicDataReplace`); `test/protocol.test.js` (`address verification fields accept only the documented state model`)

**Observed:** `publicData.replace` is a warm-to-cold message. The P1.11 schema accepts `verificationState: "cold-verified"` as an ordinary enum value whenever `lastColdVerifiedAt` and `verifiedAgainstXpub` are also supplied. The reviewer-owned invariant probe submitted that state from the warm side and received an accepted sanitized message. The committed protocol test likewise constructs a warm-to-cold `publicData.replace` with `cold-verified` and asserts that the result remains `cold-verified`.

This means the warm side can create the exact state whose meaning is “re-derived from the seed, offline, with the vault unlocked” without any re-derivation event occurring.

**Expected:** Per the verbatim P1.11 acceptance criterion, an address reaches `cold-verified` **only via an actual cold re-derivation**. Timestamp/xpub fields are evidence metadata, not proof that the cold event occurred.

**Required action:** Enforce verification-state authority in the cold-owned mutation path. A warm replacement must not be able to elevate an unverified/unverifiable/stale address to `cold-verified` merely by supplying self-asserted evidence fields. Preserve existing authenticated `cold-verified` state when appropriate, but require a cold-authoritative transition for any elevation. Replace the current positive warm-forgery test with negative/transition tests that prove the authority rule.

### F2 — Schema 1 is only projected as schema 2; the vault is not migrated on open

**Severity:** blocking data-model / migration defect

**Location:** `src/protocol.js` (`cleanPublicCompartment`); cold vault/session open/save path; `docs/01-spec/data-model.md` Migration section

**Observed:** `cleanPublicCompartment()` always emits `schema: 2` and supplies `addressOrigin: manual` / `verificationState: unverified`, so protocol messages make legacy data *look* migrated.

The reviewer independently created a real schema-1 vault, opened it through the actual cold vault API, saved it without an unrelated Registry mutation, and reopened the saved bytes. Results:

```text
raw schema after open:        1
raw schema after save/reopen: 1
protocol projected schema:    2
```

The PR does not modify the cold vault open/session layer. The documented migration contract says migrations run **on open**, in sequence, with a backup copy before applying them and with failures leaving the original untouched. That does not happen here.

**Expected:** Opening a previous-schema vault must execute the documented schema migration in the cold-owned vault/session state, not merely sanitize a cross-realm projection. The migrated state must carry schema 2 plus the explicit legacy defaults, and the documented backup/original-untouched failure contract must be honored.

**Required action:** Implement the schema-1→schema-2 migration in the actual vault-open migration path and add a real previous-schema vault test that proves: open succeeds; the cold session is schema 2 with `manual`/`unverified` defaults; migrated save/reopen remains schema 2; and migration failure preserves the original according to the documented backup contract.

### F3 — No committed test proves a stale address is never displayed as verified

**Severity:** blocking acceptance/test-coverage defect

**Location:** `test/`; `scripts/run-browser-harness.js`

**Observed:** The roadmap explicitly requires “a test proves a stale entry is never displayed as verified.” The effective PR changes `test/protocol.test.js` and `test/registry.test.js` but does not change the browser harness. A reviewer-owned repository search for committed assertions covering `cold-verified-stale` display / `Cold verification stale` returned no match.

The reviewer-owned Chromium/Firefox probe proves the **current behavior** is correct: a stale record is rendered as `Cold verification stale`, not `Cold verified`. That independent evidence does not substitute for the required committed regression test.

**Expected:** The branch itself contains a regression test that fails if stale state is rendered as verified.

**Required action:** Add committed integration/browser coverage for the Registry address verification display. It must construct or reach `cold-verified-stale`, assert the stale state/label, and explicitly prove it is not displayed as `Cold verified`. Run it in the normal Chromium/Firefox harness or an equivalently committed display-level test.

## 5. Verdict rationale

P1.11 has working pieces: the new public fields are schema-bounded, legacy projections receive safe non-verified defaults, xpub changes correctly stale existing verified records, the present Registry labels stale/unverified records correctly, all 296 unit tests pass, both browser engines pass, the build is reproducible, and deliberate failure probes fail closed.

It still cannot pass. The warm side can manufacture `cold-verified`, which violates the item's core cold-authority acceptance rule; schema 1 is only projected as schema 2 rather than migrated in the vault on open; and the explicit stale-display regression test required by the roadmap is absent.

A fresh review can PASS only after all three findings are addressed and independently re-verified. Leave P1.11 at `[~]`. Do not merge PR #44. PR #45 remains stacked on this unresolved parent and must not be independently closed out until P1.11 is remediated and settled.

FAIL

---

# Review R2: P1.11 â€” Address verification state in the data model

**VERDICT: FAIL**

Findings: 2 (2 blocking, 0 advisory â€” all must be addressed)
Reviewed commit: `1aade101e11d4d867a88ca657812adfe05f5c6ba`
Base commit: `a0edef3b271c2ae0d61fec9718ba6a8577d19e6b`
PR: #44 (`p1.11-address-provenance`)
Reviewed by: OpenAI GPT-5.6 Sol â€” independent reviewer
Date: 2026-08-11

## 1. What I verified

I reviewed the effective PR diff from `main@a0edef3b271c2ae0d61fec9718ba6a8577d19e6b` to exact PR head `1aade101e11d4d867a88ca657812adfe05f5c6ba`, the preserved R1 review, the P1.11 packet, the roadmap acceptance text, and the current cold-vault/Registry authority paths rather than trusting the remediation packet.

Reviewer-owned evidence:

- Runner: `pr44-p111-fresh-independent-rereview-05-2`
- Evidence ZIP SHA-256: `bfc79679d5401838707e7944fe84f3e8546270272b654e11f43ea001375e4436`
- Sidecar: independently matched the ZIP digest
- Evidence secret scan: clean
- Source checkout: clean and read-only during collection
- PR #44: OPEN / DRAFT / UNMERGED at the exact reviewed head
- PR #45 and PR #46: heads remained unchanged during collection
- Preserved R1 review SHA-256 before this append: `878AD9842E21AD973DD6610ED7DFB32A131058B4FA917CA084A55011ED68E7A7`
- Preserved R1 review Git blob before this append: `9e86b5c9bea12e30a70a5a0957ca9ed142cd93d2`

Fresh gates completed before the decisive invariant failure:

```text
git diff --check
PASS

npm ci
PASS

npm run verify-vendor
PASS

npm run lint
PASS

npm run check-docs
PASS

node --test test/protocol.test.js test/registry.test.js test/vault.test.js --test-concurrency=1
tests 52
pass 52
fail 0

npm test
tests 302
pass 302
fail 0

security-path Math.random scan
PASS
(the only dot-form text was the intentional fail-closed diagnostic saying Math.random is never substituted)

independent upstream vendor downloads
PASS
all 10 vendored npm tarballs byte-matched independently downloaded registry.npmjs.org releases
```

The reviewer then generated real legacy vault fixtures using the exact pre-P1.11 base implementation at `a0edef3b271c2ae0d61fec9718ba6a8577d19e6b` and opened them with the exact current head. Before the later authority assertion failed, the probe independently established:

- schema-1 vault opens as schema 2 in the cold vault layer;
- migrated legacy addresses become explicit `manual` / `unverified`;
- schema-1 verification-shaped fields are discarded rather than trusted;
- open does not mutate the original encrypted bytes;
- save/reopen persists schema 2;
- missing-schema legacy data migrates to schema 2;
- malformed legacy migration rejects and leaves the source bytes unchanged;
- warm replacement cannot elevate an unverified record to `cold-verified`;
- warm replacement cannot mint `cold-verified-stale` from an unverified record;
- warm replacement cannot rewrite authenticated verification timestamp/xpub evidence;
- changing an address's public identity drops attempted forged authority.

The same reviewer probe then changed only the account xpub in a fresh cold session while leaving an existing authenticated address marked `cold-verified`. The exact observed assertion was:

```text
Expected: cold-verified-stale
Actual:   cold-verified
```

The effective PR diff and current GitHub head were then inspected independently. `src/registry.js` does perform the stale transition in the ordinary warm Registry helper, but `src/cold/vault.js::preserveColdVerificationAuthority()` never compares the old/new account xpub or the address's `verifiedAgainstXpub` against the next account xpub. The cold session therefore trusts the warm caller to request the stale downgrade instead of enforcing it itself.

A separate exact-head diff inspection also found that PR #44 deletes the entire roadmap item line:

```text
- [ ] **P1.11 Address verification state in the data model**
```

while leaving its dependency, description, and acceptance text behind. The packet and self-review claim that P1.11 remains `[~]`, but the exact branch contains no P1.11 marker at all.

## 2. What I could not verify

Nothing required for this FAIL verdict was inaccessible.

The collector is intentionally fail-fast. Once the reviewer-owned invariant probe reproduced the blocking cold-authority failure, later reproducible-build, browser, and deliberate-regression stages were not executed in R2 and are not claimed as fresh R2 evidence. Those later gates cannot change this verdict because P1.11 already fails a verbatim acceptance criterion and the branch also has a roadmap-governance defect.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `a vault written by the previous schema version still opens (the migration test data-model.md requires)` | âœ… | Reviewer-generated vault bytes from the exact pre-P1.11 base opened through the current cold vault layer as schema 2; save/reopen remained schema 2; missing schema migrated; malformed migration rejected without changing the source bytes. R1-F2 is remediated. |
| 2 | ``verificationState` is never inferred â€” an address reaches `cold-verified` only via an actual cold re-derivation` | âœ… | Reviewer-owned cold-session replacement probes blocked warm attempts to mint `cold-verified` / `cold-verified-stale`, blocked evidence rewriting, and dropped attempted authority after address-identity change. R1-F1 is remediated for the reviewed replacement path. |
| 3 | `changing an account's xpub moves its verified addresses to cold-verified-stale automatically, and a test proves a stale entry is never displayed as verified` | âŒ | The ordinary warm Registry helper performs the transition, but a direct warm-origin `publicData.replace` that changes the account xpub while retaining `cold-verified` is accepted by the cold authority boundary and remains `cold-verified`. See R2-F1. The committed stale-display browser regression is present, but R2 did not need to execute it after the first half of this composite criterion independently failed. |

## 4. Findings

### R2-F1 â€” Cold authority does not enforce xpub-change staleness

**Severity:** blocking security / authority-boundary and acceptance defect

**Location:** `src/cold/vault.js` (`preserveColdVerificationAuthority`, `replacePublicData`); related warm helper in `src/registry.js`

**Observed:** The reviewer created an authenticated schema-2 vault containing an address in `cold-verified` with `verifiedAgainstXpub` equal to the account's current xpub. In a fresh offline cold session, the reviewer changed only that account xpub in the replacement public compartment and left the address state as `cold-verified`.

`replacePublicData()` accepted the replacement and returned the address still as `cold-verified`, preserving the old timestamp/xpub evidence. The reviewer assertion failed with actual `cold-verified` versus expected `cold-verified-stale`.

The ordinary warm Registry update helper does proactively change matching addresses to `cold-verified-stale`, but that is not an authority boundary: `publicData.replace` is explicitly warm-originated. The cold reconciliation currently matches addresses by ID/address/accountId/index and protects their evidence, but it never resolves the corresponding previous/next account xpub. A compromised or nonconforming warm caller can therefore suppress the required stale transition by changing the account xpub without also requesting the downgrade.

This produces the exact invalid state the data model says `verifiedAgainstXpub` exists to prevent: an address remains displayed/recorded as `cold-verified` even though its verification xpub no longer matches the account's current xpub.

**Expected:** Per the verbatim P1.11 acceptance criterion and data model, changing an account's xpub must move its verified addresses to `cold-verified-stale` automatically. Because the replacement is warm-originated, the cold-owned reconciliation must enforce that transition from authenticated prior state; it cannot rely on the warm Registry helper to request it.

**Required action:** In the cold-owned reconciliation path, compare each authenticated verified address's evidence/account relationship against the next account xpub. When the relevant account xpub changes or no longer matches `verifiedAgainstXpub`, force `cold-verified-stale` while preserving the authenticated `lastColdVerifiedAt` and `verifiedAgainstXpub`. Add a vault-level regression that calls `replacePublicData` directly with a changed account xpub and an unchanged `cold-verified` address, proving the cold session forces stale even when the warm caller does not.

### R2-F2 â€” PR #44 deletes the P1.11 roadmap item line

**Severity:** blocking documentation / review-governance defect

**Location:** `docs/05-development/ROADMAP.md`; contradicted by `docs/05-development/packets/p1.11-address-provenance.md` and `.self-review.md`

**Observed:** The effective PR diff deletes:

```text
- [ ] **P1.11 Address verification state in the data model**
```

and does not add a replacement `[~]` line. The dependency, scope description, and acceptance text remain immediately below P1.10, so the roadmap no longer contains a P1.11 item/marker at all.

This directly contradicts the remediation packet's `Status: [~]` claim and the self-review statement that â€œP1.11 remains exactly one roadmap item on this branch, and the marker remains `[~]`.â€

**Expected:** P1.11 remains an explicit roadmap item throughout review, with the author leaving it at `[~]` until an independent PASS. On a FAIL the reviewer must leave that marker `[~]`; there must be a marker to preserve.

**Required action:** Restore the P1.11 roadmap item line as:

```text
- [~] **P1.11 Address verification state in the data model**
```

with its existing dependency, description, and acceptance text remaining beneath it. Reconcile the packet/self-review only if needed so they accurately describe the resulting branch state.

## 5. Verdict rationale

R1-F1 and R1-F2 show substantive remediation: the reviewer independently confirmed cold-session schema migration/persistence and blocked warm attempts to manufacture or rewrite verification authority. The branch still cannot pass. The cold authority boundary fails to derive mandatory staleness when the warm replacement changes an account xpub but leaves the address marked verified, directly violating P1.11's automatic-staleness acceptance rule. Separately, the PR deletes the P1.11 roadmap item line while claiming the item remains `[~]`, breaking the review-governance state. Both findings must be addressed before another fresh independent review. Do not merge PR #44. Do not independently close out PR #45 while its P1.11 parent remains unresolved.

FAIL
---

# Review R3: P1.11 â€” Address verification state in the data model

**VERDICT: FAIL**

Findings: 1 (1 blocking, 0 advisory â€” all must be addressed)
Reviewed commit: `2e3ba254c2f2364adb49b30756bcd96808c087a6`
Base commit: `a0edef3b271c2ae0d61fec9718ba6a8577d19e6b`
PR: #44 (`p1.11-address-provenance`)
Reviewed by: OpenAI GPT-5.6 Sol â€” independent reviewer
Date: 2026-08-11

## 1. What I verified

I reviewed the complete effective PR diff from `main@a0edef3b271c2ae0d61fec9718ba6a8577d19e6b` to exact PR head `2e3ba254c2f2364adb49b30756bcd96808c087a6`, the accumulated R1+R2 review history, the R2 remediation-only diff from review commit `050867784295bc4455ae2014b3c2bfebd5a6a5b8`, the roadmap acceptance text, the data-model state definitions, and the current cold-vault authority path.

Reviewer-owned evidence:

- Runner: `pr44-p111-r3-fresh-independent-rereview-06`
- Evidence ZIP SHA-256: `baf46a9445785a031e221304ba2ac0b10680fa8c8c2365b694392e524cdd8458`
- Uploaded sidecar: exact digest match
- Evidence secret scan: clean
- Source checkout: clean and read-only during collection
- PR #44: OPEN / DRAFT / UNMERGED at the exact reviewed head at start and end
- PR #45 head remained `4a110cc0b6178de8c8619bba9a977f2fa4c27467`
- PR #46 head remained `339e5dfb4e9512716fed538bb1642aaf2b6b36aa`
- Preserved R1+R2 review SHA-256 before and after evidence collection:
  `C166A0DB9EEB1C6DCFDBADFD7C351ECC82BE3E42EDC6724C01A4ED09886ED253`
- Preserved R1+R2 review Git blob:
  `c579db7735d400c9b6175616ed694731e49d82ef`

Fresh independent gates:

```text
git diff --check
PASS

P1.11 ROADMAP marker
PASS â€” exact `- [~] **P1.11 Address verification state in the data model**`
present exactly once

npm ci
PASS

npm run verify-vendor
PASS

npm run lint
PASS

npm run check-docs
PASS

node --test test/protocol.test.js test/registry.test.js test/vault.test.js --test-concurrency=1
tests 53
pass 53
fail 0

npm test
tests 303
pass 303
fail 0

security-path Math.random scan
PASS
(the only dot-form occurrence is the intentional fail-closed diagnostic stating
that Coldbox never substitutes Math.random)

independent upstream vendor comparison
PASS
all 10 vendored npm tarballs byte-matched independently downloaded
registry.npmjs.org release bytes
```

The reviewer generated legacy vault fixtures with the exact pre-P1.11 base implementation and opened them with the exact R3 head. Before the later R3-specific assertion, the reviewer-owned cold-vault probe independently established:

- schema-1 vault opens in the actual cold vault layer as schema 2;
- migrated legacy addresses become explicit `manual` / `unverified`;
- legacy verification-shaped fields are discarded rather than trusted;
- opening/migration does not mutate the source encrypted bytes;
- save/reopen persists schema 2;
- missing-schema legacy data migrates to schema 2;
- malformed migration rejects and preserves source bytes;
- warm replacement cannot mint `cold-verified` from `unverified`;
- warm replacement cannot mint `cold-verified-stale` from `unverified`;
- warm replacement cannot rewrite evidence while an address remains `cold-verified`;
- warm replacement cannot promote an existing stale address back to verified;
- address-identity changes drop attempted cold authority;
- changing the account xpub forces `cold-verified-stale` even when warm leaves the address `cold-verified`;
- changing the account xpub while supplying forged replacement evidence still preserves the prior authenticated timestamp/xpub;
- removing the account xpub also prevents the address from remaining `cold-verified`.

The final adversarial invariant then reproduced R3-F1: with the account xpub unchanged and still equal to the authenticated `verifiedAgainstXpub`, warm input requested `cold-verified-stale` and supplied a replacement timestamp/xpub. The cold session accepted the stale transition instead of preserving `cold-verified`.

Exact reviewer assertion:

```text
AssertionError: warm replacement manufactured cold-verified-stale even though
account xpub still matches authenticated evidence

actual:   cold-verified-stale
expected: cold-verified
```

The collector deliberately continued after that product finding so the rest of the required independent evidence could still be gathered in the same run.

Reproducible builds:

```text
primary build 1
SHA-256 c17b20e496e08e7176163f6672b6b20977ef82df64cbcb065db242ab379336ae

primary build 2
SHA-256 c17b20e496e08e7176163f6672b6b20977ef82df64cbcb065db242ab379336ae

alternate fresh clone
path contains spaces
TZ=Pacific/Honolulu
LANG=de_DE.UTF-8
SHA-256 c17b20e496e08e7176163f6672b6b20977ef82df64cbcb065db242ab379336ae
```

The independent hash therefore reproduces the author's exact-head build claim.

Browser evidence:

```text
npm run test:browser
PASS

Chromium: cold-verified-stale persisted state rendered as stale,
never as cold verified

Firefox: cold-verified-stale persisted state rendered as stale,
never as cold verified

Browser harness passed in Chromium and Firefox.
```

The reviewer then deliberately changed the production stale label to `Cold verified` in the disposable clone. The normal browser harness failed non-zero in Chromium at the committed P1.11 stale-label assertion. The product file was restored afterward.

Fail-closed dependency evidence:

```text
deliberately corrupt vendored @fontsource/bangers tarball

npm run verify-vendor
exit 1 â€” expected

npm run build
exit 1 â€” expected; build refused because vendor verification failed

restore exact vendor bytes
npm run verify-vendor
PASS
```

Final guards confirmed the disposable clone had no tracked diff, the source checkout remained clean at the exact reviewed head, the accumulated review report remained unchanged, and PR #44/#45/#46 retained their expected heads/states.

## 2. What I could not verify

Nothing required for this R3 verdict was inaccessible.

Physical hardware-wallet validation belongs to P1.9/P1.12 and is not required to decide P1.11. The R3 product finding was reproduced directly at the cold vault authority boundary with the exact reviewed code.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `a vault written by the previous schema version still opens (the migration test data-model.md requires)` | âœ… | Reviewer-generated vault bytes from the exact pre-P1.11 base open through the current cold vault layer as schema 2; explicit legacy defaults are applied; save/reopen remains schema 2; missing schema migrates; malformed migration rejects without changing source bytes. |
| 2 | ``verificationState` is never inferred â€” an address reaches `cold-verified` only via an actual cold re-derivation` | âœ… | Reviewer-owned direct cold-session attacks cannot mint `cold-verified` from unverified/stale state, cannot retain authority across an address-identity change, and cannot rewrite evidence while retaining `cold-verified`. |
| 3 | `changing an account's xpub moves its verified addresses to cold-verified-stale automatically, and a test proves a stale entry is never displayed as verified` | âœ… | A direct warm-origin replacement that changes the account xpub while leaving the address `cold-verified` is forced stale by the cold session with authenticated evidence preserved. The committed normal browser harness proves the stale label in both Chromium and Firefox; deliberately changing the production label to `Cold verified` makes that harness fail non-zero. |

## 4. Findings

### R3-F1 â€” Warm replacement can manufacture `cold-verified-stale` and rewrite authenticated evidence without an xpub mismatch

**Severity:** blocking security / provenance-integrity defect

**Location:** `src/cold/vault.js` â€” `preserveColdVerificationAuthority()`, specifically the transition handling after `xpubEvidenceChanged`

**Observed:** Start with a cold-authenticated address whose state is `cold-verified`, whose `verifiedAgainstXpub` equals the current account xpub, and whose timestamp/xpub evidence came from the prior authenticated state. Submit a warm-origin `publicData.replace` in which:

- the account xpub is unchanged;
- the address public identity is unchanged;
- warm changes `verificationState` from `cold-verified` to `cold-verified-stale`;
- warm supplies a different `lastColdVerifiedAt`;
- warm supplies a different `verifiedAgainstXpub`.

The cold-owned `xpubEvidenceChanged` check is false because both account records still match the prior authenticated xpub. The later state checks reject stale minting only when the previous state is neither verified nor stale. They do not handle `previousState === 'cold-verified' && requestedState === 'cold-verified-stale'`. That combination therefore falls through and accepts the warm-provided stale state and warm-provided evidence.

The reviewer reproduced exactly that behavior:

```text
actual:   cold-verified-stale
expected: cold-verified
```

This is not merely a conservative UI downgrade. `cold-verified-stale` is provenance-bearing state: the data model defines it as an address that **was verified** but whose `verifiedAgainstXpub` no longer matches the account's current xpub. Here no mismatch exists. More importantly, the accepted transition lets warm input replace the timestamp/xpub evidence that describes the prior cold verification event. That directly contradicts the P1.11 packet's security-boundary claim that no warm-supplied timestamp or xpub is treated as proof and that authenticated verification evidence cannot be rewritten.

**Expected:** A `cold-verified` address may become `cold-verified-stale` only when the cold-owned reconciliation itself establishes the required account/evidence mismatch. If the authenticated evidence still matches both the previous and replacement account xpub, a warm request for stale is unauthoritative. The cold session must retain `cold-verified` and preserve the prior authenticated `lastColdVerifiedAt` / `verifiedAgainstXpub`.

**Required action:** Close the `previousState === 'cold-verified'` / `requestedState === 'cold-verified-stale'` fall-through. After the cold-owned mismatch branch, if the authenticated evidence still matches, preserve `cold-verified` and its existing timestamp/xpub regardless of a warm-requested stale downgrade. Add a direct vault regression using an unchanged matching account xpub plus forged stale timestamp/xpub and prove the cold result remains `cold-verified` with the original evidence. Keep the existing xpub-change regression proving that a real cold-detected mismatch still forces stale.

## 5. Verdict rationale

R2-F1 and R2-F2 are independently remediated in R3. The exact P1.11 roadmap marker is restored; real previous-schema migration works in the cold vault; warm input cannot mint `cold-verified`; real account-xpub changes are forced stale by the cold authority boundary; the stale Registry display has committed Chromium/Firefox regression coverage; all 303 tests and normal gates pass; all upstream vendor bytes match; deliberate vendor corruption and deliberate stale-display breakage both fail closed; and the exact author build hash reproduces across an alternate path, timezone, and locale.

The branch still cannot pass because one provenance-bearing transition remains warm-authorable. With no xpub mismatch, warm input can convert an authenticated `cold-verified` address to `cold-verified-stale` and replace the authenticated timestamp/xpub evidence. That contradicts both the state model and the branch's stated cold-authority guarantee. Fix R3-F1, preserve the accumulated review history, and request a fresh R4 review. Leave P1.11 at `[~]`. Do not merge PR #44 or independently close out PR #45.

FAIL