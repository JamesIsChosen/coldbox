# P0.15 — Keyfile unlock

Branch: `p0.15-keyfile-unlock` (not yet created — see handoff; this packet was written before any git command ran in this session)

## 1. Summary

[P0.15](../ROADMAP.md) implements wrapped-DEK method 2 (passphrase + keyfile) exactly as already specified in [vault-format.md](../../01-spec/vault-format.md): `KEK = Argon2id(passphrase || SHA-512(keyfile), salt, params)`. A vault created with a keyfile carries a method-2 record in place of the method-1 record — the keyfile becomes required, not optional — and the cold-realm UI adds an off-by-default toggle with an unmissable warning that a lost or byte-altered keyfile means permanent, unrecoverable loss. Structural implementation choices the spec left open (size limits, hint truncation, empty-keyfile handling, method exclusivity) are recorded in new [ADR-0014](../adr/0014-keyfile-unlock-implementation-limits.md).

## 2. Scope

**In:** `src/cold/vault.js` (method-2 record creation/parsing/unwrapping), `src/cold/main.js` (keyfile toggle, warning, `FileReader`-based file input, wiring into create/unlock), `src/cold/index.html` and `src/cold/styles.css` (new keyfile UI section), `test/vault.test.js` (9 new P0.15 tests), `docs/01-spec/vault-format.md` (implementation-limit paragraph added to the existing Method 2 description), new `docs/05-development/adr/0014-keyfile-unlock-implementation-limits.md`, `docs/05-development/adr/README.md` (index entry), `docs/05-development/testing.md` (P0.15 coverage paragraph), `CHANGELOG.md`, `ROADMAP.md` (`[~]`).

**Out, deliberately:**
- No change to `src/main.js` or `src/index.html` (the warm shell). Keyfile bytes, like the passphrase, never leave the cold realm — there is no code path for them to.
- No new `postMessage` type. `src/protocol.js` is untouched. The keyfile is passed as a direct in-memory argument to `vaultLayer.create()`/`vaultLayer.openSession()`, both of which already run inside the same cold document; it never crosses the `MessageChannel`.
- No format-version bump. `HEADER_LENGTH` and the fixed header layout are byte-identical; only the (already-generic, already-multi-record) wrapped-DEK block gains a second record *type* it can hold, which `parseWrappedRecords()` required zero changes to support.
- No recovery-share method (method 3) — out of scope, Phase 2 per vault-format.md.
- No P0.17 three-depth help content — the help framework does not exist yet (same precedent as P0.14's packet; see §12).

## 3. How to verify

All commands below were run for real, in this session's sandbox, and this is the actual output. I could not run `git` at all this session (see §9/handoff) — a repo-specific environment constraint, not something I'm withholding.

```
$ node scripts/lint.js
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ LC_ALL=C TZ=UTC node scripts/build.js
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Vendor verification passed in offline mode.
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.
Built build/coldbox.html (2a39e56407ef6532b47b2cdc8fee1dd072c30dcc32581afbfa4b8004a4a0b454)

$ LC_ALL=de-DE TZ=Pacific/Honolulu node scripts/build.js && sha256sum build/coldbox.html
Built build/coldbox.html (2a39e56407ef6532b47b2cdc8fee1dd072c30dcc32581afbfa4b8004a4a0b454)
2a39e56407ef6532b47b2cdc8fee1dd072c30dcc32581afbfa4b8004a4a0b454  build/coldbox.html

$ wc -c build/coldbox.html
614567 build/coldbox.html

$ node --test test/vault.test.js
1..21
# tests 21
# pass 21
# fail 0

$ node --test test/accessibility.test.js test/airgap.test.js test/browser-runner.test.js test/build.test.js test/handshake.test.js test/lint.test.js
1..24
# tests 24
# pass 24
# fail 0

$ node --test test/protocol.test.js test/save-flow-wiring.test.js test/save-integrity.test.js test/vendor.test.js
1..44
# tests 44
# pass 44
# fail 0

$ node --test test/crypto.test.js
1..6
# tests 6
# pass 6
# fail 0
```

(Split into four `node --test` invocations only because this session's tool has a 45-second command timeout, same reason recorded in the P0.14 packet — `vault.test.js` and `crypto.test.js` alone are the slow ones. Total across all four groups: **95 tests, 95 pass, 0 fail** — I did not run one combined `npm test`/`node --test` over the whole `test/` directory in this session, so I have not personally reproduced the "+1 incidental `test/browser/harness.js` pass" the P0.14 packet documented; a reviewer running plain `npm test` should see 96, not 95, for that reason, not because anything here is broken.)

`node run scripts/verify-vendor.js --offline` passes (shown above, inside the build's own gate). The **online** `npm run verify-vendor` (re-fetching from the real npm registry) and `npm run test:browser` (Playwright) were **not attempted** this session — I did not test whether this sandbox's network allowlist blocks them the way the P0.14 session's did, since this item touches no vendored dependency and the P0.15 acceptance criteria carry no 🌐 marker. Flagged as untested rather than assumed-working; see §7 and §9.

## 4. Acceptance criteria

Copied verbatim from [ROADMAP.md](../ROADMAP.md):

> Wrapped-DEK method 2. Off by default, with an unmissable warning that a lost or byte-altered keyfile means permanent loss.
> **Accept:** unlocks with the correct keyfile; fails with a one-byte-altered keyfile; passphrase-only vaults are unaffected.

| Criterion | How satisfied | Test |
|---|---|---|
| Unlocks with the correct keyfile | `createVault()` builds a method-2 record when `options.keyfile` is supplied (`KEK = Argon2id(passphrase \|\| SHA-512(keyfile), salt, params)`, per vault-format.md); `unwrapDek()` tries the matching method-2 record when the caller supplies the same keyfile at open time. | `test/vault.test.js`: `'P0.15 keyfile unlock: correct passphrase and correct keyfile unlocks; wraps as method 2'` — full round trip including the secret compartment; `'P0.15 openSession round-trips a keyfile vault and save() preserves the wrapped-DEK block'` — proves the session/save path too, not just the one-shot `open()`. |
| Fails with a one-byte-altered keyfile | `SHA-512` of an altered keyfile produces a different `KEK`, so `aesGcm('decrypt', ...)` on the wrapped DEK throws inside `tryUnwrapWithKey()`'s try/catch, which is swallowed and falls through to the same generic `authenticationError()` every other failure path already uses. | `test/vault.test.js`: `'P0.15 keyfile unlock fails closed on a one-byte-altered keyfile, indistinguishably from a wrong passphrase'` — flips bit 0 of the keyfile, asserts the failure, and asserts its `.message` is **identical string-for-string** to a wrong-passphrase failure on the same vault (`'Vault authentication failed.'` in both cases). Also: `'...keyfile unlock fails closed with the exact original passphrase but no keyfile supplied'` (omitting the keyfile entirely is the same failure class). |
| Passphrase-only vaults are unaffected | `unwrapDek()` only derives a method-2 `KEK` and only consults method-2 records `if (hasKeyfileRecord && keyfile)` — a vault with no method-2 record never enters that branch regardless of what the caller passes. `createVault()` without `options.keyfile` produces the exact same 64-byte method-1 record as before this PR (unchanged code path). | `test/vault.test.js`: `'P0.15 a vault created without a keyfile is byte-identical in shape to a method-1-only vault'` (asserts `wrappedDekLength === 64` and `methodId === 1`, matching every pre-existing P0.11–P0.14 vault); `'P0.15 regression: passphrase-only vaults are unaffected by the keyfile feature existing'` (opens a plain vault normally, then again while erroneously supplying an unrelated keyfile, and confirms both succeed identically, plus the existing wrong-passphrase failure still fires). |
| Off by default | `#cold-vault-keyfile-toggle` in `src/cold/index.html` has no `checked` attribute; `src/cold/main.js`'s `createEmptyVault()`/`unlockLoadedVault()` only build `activeKeyfile` (and therefore only ever call `vaultLayer.create()`/`openSession()` with a keyfile) `if (keyfileToggle && keyfileToggle.checked)`. | Not covered by an executed browser test this session (no Playwright available — see §7/§9); verified by reading `src/cold/index.html`'s markup (no `checked` attribute exists anywhere on that element) and `src/cold/main.js`'s conditional. This is the one criterion I could not prove by execution, only by inspection — flagged in §9. |
| Unmissable warning | `#cold-vault-keyfile-warning` (`role="alert"`, red background matching the existing `#cold-protocol-warning` treatment, `hidden` by default) becomes visible the instant the toggle is checked, via a plain `change` listener — not a separate confirmation step that could be dismissed unread. Warning text states explicitly: both credentials become required, a single altered byte destroys the vault permanently, there is no recovery path. Same fact stated in `vault-format.md` (spec) and `CHANGELOG.md` (release-note-shaped summary), not duplicated verbatim — see doc-hygiene §1. | Not covered by an executed browser test this session — same gap as "off by default" above, same reason, flagged in §9. |

## 5. Security impact

- **Realm boundary:** no code added or changed in the warm shell (`src/main.js`, `src/index.html`). Keyfile bytes are read via `FileReader` entirely inside `src/cold/main.js`, which already runs inside the sandboxed `srcdoc` iframe. `test/vault.test.js`'s existing boundary test (`assert.doesNotMatch(warmSource, /__coldboxVault|.../)`) still passes unmodified.
- **Message schema:** no. `src/protocol.js` is untouched; no new `postMessage` type was added, and none was needed — the keyfile is a direct function argument between two modules that already share the same JS realm (`src/cold/main.js` calling into `src/cold/vault.js`), not a message.
- **CSP:** no. Neither `src/index.html`'s nor `src/cold/index.html`'s policy `<meta>` tag changed. `FileReader` is a same-realm, no-network API; it needs no CSP allowance beyond what the cold realm already has.
- **Vault format:** the wire format itself is unchanged — vault-format.md already fully specified method 2 before this PR (I did not invent the byte layout, the KDF composition, or the "keyfile hint, never contents" rule; all three came directly from the existing spec). What this PR adds is the *implementation* of a shape the multi-record design was explicitly built to accommodate: `parseWrappedRecords()` needed **zero changes**, because it was already generic over method-data length. A vault written before this PR (method 1 only) opens completely unmodified after it — proven by every pre-existing `test/vault.test.js` case still passing unchanged, plus the new byte-identity assertion in `'P0.15 a vault created without a keyfile is byte-identical in shape to a method-1-only vault'`.
- **Derivation / randomness:** yes, in one specific sense — a new KDF *input composition* (`passphrase || SHA-512(keyfile)` instead of `passphrase` alone), reusing the exact same Argon2id/PBKDF2 derivation code path (`cryptoLayer.deriveKey()`) and the exact same AES-256-GCM wrap/unwrap (`aesGcm()`) that method 1 already uses and that P0.10 already established known-answer tests for. `SHA-512` itself is the same `@noble/hashes` implementation `hkdfSubkey()` already depends on elsewhere in this file (P0.11's HKDF subkey derivation), so no new cryptographic primitive enters the codebase — see §6 on test vectors.
- **New `connect-src` host:** none.
- **If this implementation is wrong, what does an attacker — or bad luck — gain?** The failure mode I most want scrutinized: if `unwrapDek()`'s method-2 branch were reachable *without* the caller actually supplying a keyfile (e.g. a bug where `keyfile` is falsy-but-truthy, like an empty array satisfying a loose truthiness check), a keyfile-protected vault could silently degrade to passphrase-only security while still displaying "keyfile required" in the UI — a confidentiality regression the user would have no way to detect from the outside. I guarded this explicitly with `if (hasKeyfileRecord && keyfile)` (a plain existence check, not a truthiness-only check on something empty-array-shaped) and `normalizeKeyfileBytes()` rejecting a zero-length keyfile *at creation* so an empty keyfile can never end up wrapped into a real vault in the first place — but I have not fuzzed this function against exotic `keyfile` argument shapes the way `test/save-integrity.test.js`'s `bytesEqual` bug (documented in the P0.14 packet) was eventually caught. Second-highest concern: `combinePassphraseKeyfile()`'s zeroing (`zeroBytes(passphraseBytes); zeroBytes(digest);`) happens on the *intermediate* buffers, but the returned `combined` array is zeroed by the caller (`createVault()`'s `finally` block, `unwrapDek()`'s `finally` block) — if any caller forgot that `finally` zero, keyfile-derived key material would linger in memory longer than a passphrase-only vault's KEK does today. I traced every call site and both zero it, but this is exactly the kind of discipline that's easy to regress in a later PR without a dedicated "was this zeroed" test, which I did not write (no existing test in this codebase asserts zeroing behavior via memory inspection — it's a property this codebase currently only enforces by code review, not by test, for any secret, not just this one).

## 6. Test evidence

- **9 new tests in `test/vault.test.js`**, all executed for real against the actual `src/cold/vault.js` (via the same `vm.runInNewContext(cryptoSource); vm.runInNewContext(vaultSource)` pattern every other case in this file already uses — not pattern-matched, not mocked at the crypto layer for the "real context" cases):
  1. `'a vault created without a keyfile is byte-identical in shape to a method-1-only vault'` — the off-by-default/no-regression proof at the byte level.
  2. `'keyfile unlock: correct passphrase and correct keyfile unlocks; wraps as method 2'` — full create→open round trip, public **and** secret compartments.
  3. `'keyfile unlock fails closed with the exact original passphrase but no keyfile supplied'`.
  4. `'keyfile unlock fails closed on a one-byte-altered keyfile, indistinguishably from a wrong passphrase'` — the core acceptance criterion, with an explicit string-equality assertion between the two failure messages.
  5. `'regression: passphrase-only vaults are unaffected by the keyfile feature existing'`.
  6. `'openSession round-trips a keyfile vault and save() preserves the wrapped-DEK block'` — proves the long-lived session/save path, not just one-shot open, keeps working.
  7. `'create() rejects an empty keyfile and an oversized keyfile, failing closed rather than deriving from weak material'`.
  8. `'the keyfile hint is filename-only display metadata, never the keyfile bytes'` — reads the raw record bytes directly off the produced vault and decodes them, rather than trusting a getter.
- **Vector sources:** this feature has **no external standard test vectors of its own** — it is a Coldbox-specific composition (`Argon2id(passphrase || SHA-512(keyfile))`) of primitives, not a standard itself, exactly the case AGENTS.md anticipates ("If this feature has no external standard test vectors... that's fine — say so"). The primitives it composes already carry independent vectors from earlier phases, unchanged by this PR: Argon2id against RFC 9106 vectors and AES-256-GCM against NIST vectors (both in `test/crypto.test.js`, P0.10), and `@noble/hashes`' SHA-512 implementation is the same one `hkdfSubkey()` in this exact file already relies on for HKDF-SHA-512 (P0.11). I did not add a new SHA-512 known-answer test specifically for this PR because one was not needed — no new primitive was introduced, only a new *input construction* over existing, already-vetted ones.
- **Negative tests deliberately made to fail, and how:** a one-byte-altered keyfile (fails with the generic authentication error, not a distinct/informative one); a keyfile-protected vault opened with the passphrase alone (fails, same error); an empty `Uint8Array(0)` keyfile at creation (rejected with `Vault serialization failed.`, not silently accepted); a keyfile one byte over the 64 MiB implementation ceiling at creation (same rejection).
- **What I could not test:** the actual cold-realm UI (`#cold-vault-keyfile-toggle`, `#cold-vault-keyfile-warning`, `#cold-vault-keyfile-input`) has never been rendered or interacted with by anyone, human or harness, before this packet — no Playwright/browser access in this sandbox this session (see §3, §7, §9). This means "off by default" and "unmissable warning" are verified by reading the markup and the conditional JS, not by an execution that actually observed the checkbox unchecked or the warning appearing, which is a meaningfully weaker form of evidence than the cryptographic criteria above have.
- Full existing suite (95 tests across the four `node --test` groups in §3) shows **no regression** — every pre-P0.15 test still passes unmodified.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | Untested | No browser available in this sandbox this session |
| Windows Firefox | Untested | |
| macOS Safari | Untested | |
| macOS Chrome | Untested | |
| Linux Firefox | Untested | |
| **iOS local-execution target** | Untested | Not evaluated this session; no change to the ADR-0010 status is claimed |
| Android Chrome (Files) | Untested | |
| Tor Browser | Untested | |

This item carries no 🌐 roadmap marker, so browser verification is not a formal acceptance gate — but as noted in §6, the new UI genuinely has zero execution evidence behind it, which is the single biggest gap in this packet. See §9.

## 8. Assumptions made

- **Method 2 replaces method 1 rather than coexisting with it (one vault, one active unlock method).** Full reasoning in [ADR-0014](../adr/0014-keyfile-unlock-implementation-limits.md). **Basis:** the roadmap's own acceptance text ("fails with a one-byte-altered keyfile") only makes sense as an unconditional requirement if there is no passphrase-only fallback record sitting alongside it that would let someone skip the keyfile entirely. **What breaks if wrong:** if a reviewer believes a vault should be unlockable by *either* credential, this is a different, weaker feature (arguably not "keyfile unlock" as a security property at all, since losing the keyfile would no longer risk anything) needing its own acceptance criteria and its own UI copy — the current warning text is only honest under the "keyfile is mandatory once enabled" model this PR implements.
- **64 MiB keyfile size ceiling, 255-byte hint truncation, empty-keyfile rejection** — all recorded with rationale in ADR-0014. **What breaks if wrong:** the 64 MiB number in particular is a judgment call (reusing the existing vault-size constant rather than deriving a keyfile-specific one); if a reviewer wants a smaller, UX-motivated ceiling instead, that's a one-line constant change with no format implications.
- **The keyfile hint (`options.keyfileHint`) is caller-supplied and unvalidated beyond type/length.** I pass `file.name` from the browser's `File` object in `src/cold/main.js` without sanitizing its content further (beyond UTF-8 encoding and the 255-byte cap). **Basis:** vault-format.md is explicit that the hint is "never contents" and purely advisory display metadata, so unlike the vault's public/secret compartments there is no injection-into-decrypted-data risk — it is authenticated ciphertext structure, not interpreted as markup or executed anywhere I can find in this codebase. **What breaks if wrong:** if a future UI ever renders the hint as HTML instead of `textContent`, a maliciously named keyfile (`<img onerror=...>`) could matter; I did not add an explicit encode-on-render step because no code in this PR renders the hint back out anywhere (it's write-only in the current implementation) — flagged as a forward-looking note for whoever builds a "vault details" panel that displays it.
- **`FileReader` is assumed available whenever the cold realm is otherwise functional.** `keyfileInput`'s `change` listener is gated on `typeof window.FileReader === 'function'`; if absent, the keyfile input simply never wires up (the checkbox still toggles the warning, but selecting a file does nothing observable). I did not add an explicit capability-panel entry for this (unlike `randomValues`/`cryptoSubtle`/`wasm`/etc. in `src/capabilities.js`) because `FileReader` has been part of every browser Coldbox's other capability checks already assume workable File-object handling (blob download, manual export) since long before this PR — I judged it out of scope to retrofit a new capability flag for one specific API this deep into Phase 0, but a reviewer could reasonably disagree.

## 9. What to scrutinise

- **The two acceptance criteria this packet could not execute-test: "off by default" and "unmissable warning."** Both are verified by reading markup and a conditional, not by an interaction a harness or a human actually performed. If Playwright is available in the reviewer's environment, actually clicking the toggle and confirming the warning's `hidden` attribute flips, and confirming a fresh page load has the checkbox unchecked, would close the single largest evidence gap in this PR.
- **`unwrapDek()`'s branch structure** (`src/cold/vault.js`, the rewritten function around `hasPassphraseRecord`/`hasKeyfileRecord`). This is the function carrying the entire security property of "passphrase-only vaults are unaffected" and "a keyfile vault requires the keyfile." I'm fairly confident in it — it's covered by 5 of the 8 new tests directly or indirectly — but it's also the most-rewritten function in this diff (full replacement, not an incremental edit) and exactly the kind of control-flow change where a subtle boolean-logic bug (e.g. `||` where I meant `&&`) would be catastrophic and easy to miss in review. I'd read it line by line rather than trust the tests alone.
- **`combinePassphraseKeyfile()`'s zeroing discipline**, discussed in §5. No test in this PR inspects memory to confirm zeroing actually happened — it's verified by code reading only, same limitation the rest of this codebase's secret-zeroing already has.
- **Whether the 64 MiB keyfile size ceiling is the right number.** It's a defensible but somewhat arbitrary reuse of an unrelated existing constant (ADR-0014's own "what would change our mind" section says as much) — a reviewer with stronger opinions about acceptable keyfile sizes (e.g. capping much lower, since realistic keyfiles are probably kilobytes, not tens of megabytes) should feel free to push back on this specific number without it implicating anything else in the PR.

## 10. Self-assessment

- **What might be wrong:** the UI-level evidence gap in §7/§9 is the biggest one — I have no proof, only code reading, that the toggle behaves as described in a real browser. The 64 MiB/255-byte/empty-keyfile numbers in ADR-0014 are judgment calls, not derived from the spec (which is silent on all three), though I believe they're defensible and said so with reasoning rather than picking them silently.
- **What I did not do that arguably should have been done:** no memory-inspection test for secret zeroing (a pre-existing gap in this codebase's testing approach, not new to this PR, but this PR adds another function with the same limitation); no capability-panel entry for `FileReader`; did not add a "verify keyfile before create" UX affordance (e.g. re-selecting the keyfile a second time to confirm it matches, catching a fat-fingered wrong-file selection before it's too late) — the roadmap item doesn't require this, but it's the kind of thing that would meaningfully reduce the "permanent loss" risk this feature explicitly warns about, and I'd call it a reasonable P1-scoped follow-up rather than something this PR skipped carelessly.
- **Known limitations shipping with this change:** the keyfile hint is unsanitized display metadata with no current renderer (disclosed in §8); no UI-level confirmation step beyond the one warning paragraph; entirely untested in any real browser this session (disclosed throughout).
- **Follow-up work this creates:** a "confirm your keyfile" UX step (re-select-and-compare, or at minimum re-prompt on unlock) would materially reduce the real-world risk this feature's own warning describes — worth a follow-up issue, not filed as a new roadmap item since it's a UX refinement of this exact item rather than new scope. A future "vault details" panel (if one is ever built) needs to render the keyfile hint with `textContent`, never `innerHTML`, per §8's note.

## 11. Bundle impact

I could not produce a clean **before** baseline this session — that would require checking out `main` separately from this working tree, and I was instructed not to run any `git` command at all this session (see handoff). What I have:

| | Bytes | KiB |
|---|---|---|
| After (this branch, `LC_ALL=C TZ=UTC` build) | 614,567 | ~600.2 |

Source-diff size is a reasonable proxy in the meantime: the new code is roughly 140 lines added to `src/cold/vault.js`, ~90 lines added to `src/cold/main.js`, ~14 lines of new markup in `src/cold/index.html`, and ~35 lines of new CSS in `src/cold/styles.css` — comparable in scale to P0.14's measured +16.4 KiB delta for a similarly-sized feature. **The reviewer should run a clean `main` build and diff against `614,567` bytes** rather than trust an estimate; flagged rather than fabricated.

## 12. Docs updated

- [docs/01-spec/vault-format.md](../../01-spec/vault-format.md) — added the implementation-limits paragraph to the existing Method 2 description (method exclusivity, size/hint/empty-keyfile limits), linking to ADR-0014 rather than restating its reasoning.
- [docs/05-development/adr/0014-keyfile-unlock-implementation-limits.md](../adr/0014-keyfile-unlock-implementation-limits.md) — new ADR for the four structural decisions the spec left open.
- [docs/05-development/adr/README.md](../adr/README.md) — index entry for ADR-0014.
- [docs/05-development/testing.md](../testing.md) — added the "P0.15 additionally…" paragraph, matching the existing per-item convention.
- [docs/05-development/ROADMAP.md](../ROADMAP.md) — P0.15 marked `[~]`.
- [CHANGELOG.md](../../../CHANGELOG.md) — Unreleased entry for this item.

**No user-facing three-depth help content exists to update yet** — P0.17 (the help framework that compiles `docs/` into the app) has not been built, matching the precedent in P0.14's packet. `docs/01-spec/SPEC.md` already described keyfile unlock accurately (§ wrapped-DEK methods, and the Phase 0 checklist line already read "**keyfile unlock**") before this PR started, so no SPEC.md change was needed — verified by reading it, not assumed.
