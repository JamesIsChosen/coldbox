# Review: P0.9 — Capability self-check panel

**VERDICT: PASS**

Findings: 0
Reviewed commit: cdf8d3c
Reviewed by: Codex (self-review gate)
Date: 2026-08-03

## 1. What I verified

The review was run on `p0.9-capability-self-check` against its declared base `p0.8-csp-canary-airgap-guard`. The working tree was clean and both the final commit and the base diff check were clean:

```text
$ git status --short

$ git diff --check origin/p0.8-csp-canary-airgap-guard...HEAD

$ git rev-parse HEAD
cdf8d3ccd2c3f189eddfcad2a35721e280a3c199
```

The required dependency and source gates passed:

```text
$ npm ci
added 2 packages, and audited 3 packages in 938ms
found 0 vulnerabilities

$ npm run verify-vendor
Vendor verification passed against local files and upstream releases.

$ npm run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm test
tests 24
pass 24
fail 0
```

Two clean builds, with the generated `build` directory removed and its exact path verified between builds, produced the same artifact:

```text
Built build/coldbox.html (4e5c3ca033e87a67df78ebdfc9bf1fd6f9f93284477421866fe03163019dce6b)
size=130711
sha256=4E5C3CA033E87A67DF78EBDFC9BF1FD6F9F93284477421866FE03163019DCE6B

Built build/coldbox.html (4e5c3ca033e87a67df78ebdfc9bf1fd6f9f93284477421866fe03163019dce6b)
size=130711
sha256=4E5C3CA033E87A67DF78EBDFC9BF1FD6F9F93284477421866FE03163019DCE6B
```

A copied checkout at a different filesystem path was built under `LC_ALL=de-DE`, `TZ=Pacific/Honolulu`, then `LC_ALL=ja-JP`, `TZ=Asia/Tokyo`; both builds produced `4E5C3CA033E87A67DF78EBDFC9BF1FD6F9F93284477421866FE03163019DCE6B`.

The deliberate forbidden-construct fixture passed its required non-zero child-build assertion:

```text
$ node --test --test-name-pattern="build invokes forbidden-construct lint and refuses a bad source file"
✔ the build invokes forbidden-construct lint and refuses a bad source file
tests 6
pass 6
fail 0
```

The final browser harness passed in both engines. It verified the normal warm/cold capability panel, required randomness, optional capability reporting, save-path summary, missing-randomness lockdown, CSP/cold-realm boundary, all five runtime network primitives, stripped-CSP lockdown, tamper rejection, routing, responsive layout, and negative harness behavior:

```text
$ npm run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (4e5c3ca033e87a67df78ebdfc9bf1fd6f9f93284477421866fe03163019dce6b)
Chromium: missing getRandomValues entered full lockdown and refused vault operations
Firefox: missing getRandomValues entered full lockdown and refused vault operations
Browser harness passed in Chromium and Firefox.
```

The final built artifact was inspected directly. It contains two explicit `worker-src blob:` policies, one embedded cold `connect-src 'none'` policy, zero unresolved `__COLDBOX_` placeholders, and zero `allow-same-origin` matches. The build tests also assert the unchanged warm network allowlist and both-realm capability markers.

The complete committed diff was read. It adds one roadmap item only, preserves the opaque sandbox and typed message boundary, introduces no warm network host, and updates the source, tests, CSP/spec documentation, roadmap, changelog, packet, and review evidence consistently.

## 2. What I could not verify

The acceptance criteria are verified for the automated Chromium and Firefox harness, but no physical Safari, mobile, Tor, or other real-device result is claimed. Safari and mobile capability accuracy remain assigned to P0.19. P0.9 also does not claim camera permission success, save I/O success, or cryptographic correctness; those are intentionally later workflow/KAT items.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Boot-time detection of `getRandomValues`, `crypto.subtle`, WASM, Workers, camera, and available save paths. | ✅ | The shared detector reports all required fields; the panel renders warm/cold optional states and the three save-path affordances. Node/build assertions and the Chromium/Firefox normal-artifact checks pass. |
| 2 | Accurate on every platform in the device matrix. | ✅ for the automated matrix; physical platforms deferred | Chromium and Firefox use actual platform probes under `file://`. No Safari/mobile platform result is inferred; P0.19 owns the physical matrix. |
| 3 | Hard-fails with an explanation if `getRandomValues` is absent, never substituting `Math.random`. | ✅ | The missing-randomness fixture forces the detector false in both realms. Both engines observe failed capability state, full lockdown, refused vault operations, removed cold frame, CSP canary evidence, and visible `never substitutes Math.random` text. |

## 4. Review adjustments before the final gate

The initial implementation inspection identified two hardening adjustments before issuing the verdict: the Worker probe now requires `URL.revokeObjectURL` so a partial URL API cannot leave detection pending, and the cold realm only presents its airgap-green sealed state when required randomness also passed. Both are included in reviewed commit `cdf8d3c`; the final gate above was rerun afterward.

## 5. Findings

None.

## 6. Verdict rationale

The implementation satisfies P0.9's boot-time capability reporting and required-randomness fail-closed gate, preserves the existing two-realm and CSP boundaries, keeps optional API availability distinct from cryptographic correctness, and is exercised by deterministic Node/build checks plus Chromium and Firefox browser checks. The only unverified results are explicitly assigned to P0.19 or later roadmap items, not silently inferred. There are zero remaining findings, so the verdict is PASS.
