# P1.12 self-review — batch gate

- Branch: `p1.12-address-verification`
- Base: `p1.11-address-provenance`
- Product commit reviewed: `485f6f7`
- Review round: 1
- Verdict: **PASS**

## Acceptance review

- Whole-string mismatch reports the first divergence index, including a middle-only difference that shares the first and last characters.
- Bech32 compares case-insensitively; Base58 remains case-sensitive.
- Mixed-case EVM checksum failure is reported as `checksum-invalid`.
- Round-trip, inbound, and batch warm flows are present and keep registry matching separate from cold authority.
- `address.verifyRequest` and `address.verifyResult` are whitelisted, sanitized, and enum-only; free-form result prose is rejected.
- A locked cold session returns `vault-locked`, never `no-record`.
- Cold verification updates the address only after an actual derived address matches the stored public record.

## Tests and gates

- Focused comparison/protocol tests: 22 passed, 0 failed.
- Full `npm.cmd test`: exit 0, green.
- `npm.cmd run verify-vendor`: passed locally and against upstream releases.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 0 warnings.
- Two builds: identical SHA-256 `3fe14b4066ba98e0397bc6179c596af8fced6611f78ed9a4fbd6ba4c334fac47`.
- Browser harness: existing Chromium file://, cold-boundary, Registry, lock, and responsive checks passed.

## Self-assessment

The remaining verification gap is an end-to-end browser assertion specifically driving the new P1.12 form controls. The source-level contracts and build/browser regression harness pass, but this new interaction should receive explicit independent browser review. The Keccak implementation and the cold-to-warm two-message update ordering deserve deliberate scrutiny.

## Gate decision

**PASS — ready for independent review.** Open the PR against `p1.11-address-provenance`, then continue to P1.13 only after this branch is pushed and the PR is open. Do not mark the roadmap item `[x]` and do not merge.
