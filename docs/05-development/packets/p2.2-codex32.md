# PR packet: P2.2 codex32

**Roadmap item:** P2.2 codex32
**Branch:** `p2.2-codex32`
**Base:** `main`
**PR:** [#49](https://github.com/JamesIsChosen/coldbox/pull/49)
**Author self-review:** recorded in `p2.2-codex32.self-review.md`

**Initial product commit:** `260d65f0def3eb37fe00f463e006c9899e6762aa`
**Remediation commit:** `72bd1d58a41cd8cf92015a8ec0f26ffaab8cc24a`
**Current security-gate remediation commit:** `91a788ede31e1e5291ad109490ca79e81203f339`

**Status:** REMEDIATION ROUND 3 APPLIED; SECURITY AND BROWSER GATES PASS; READY FOR INDEPENDENT RE-REVIEW.

## 1. Summary

P2.2 adds a cold-only, inline BIP-93 codex32 implementation for direct 16-to-64-byte BIP-32 master seeds. It supports regular and long checksums, GF(32) threshold generation/recovery, official interoperability vectors, and a bounded confirmation-gated single-character correction helper. The cold UI masks generated/recovered values, provides timed reveals, and clears all codex32 state on the existing cold-session teardown path.

## 2. Scope

In scope:

- `src/cold/codex32.js`, assembled in a fixed order by `scripts/build.js`.
- Cold-only codex32 generation, recovery, masking, correction confirmation, and teardown UI.
- Official BIP-93 vector and negative tests plus Chromium/Firefox `file://` harness coverage.
- ADR, guide, dependency provenance, changelog, roadmap split/marker, and this packet.

Deliberately out of scope:

- SLIP-39, Seed XOR, Shamir39/raw SSS, vault recovery records, health dashboards, and printable worksheets; those are P2.1/P2.3–P2.8 items.
- BIP-93's full multi-error correction search. P2.2 offers one bounded single-character candidate and requires explicit confirmation; broader arithmetic repair is a later roadmap item.
- Physical paper, printer, mobile, iOS, hardware-wallet, and manual worksheet verification.

## 3. How to verify

The focused vector run completed as follows:

```text
$ node --test test/codex32.test.js
✔ BIP-93 vector 1 decodes a 128-bit unshared secret
✔ BIP-93 vector 2 derives and recovers the published values
✔ BIP-93 vectors 3, 4, and 5 decode and recover
✔ generation round-trips 16, 32, and 64-byte secrets with threshold enforcement
✔ invalid BIP-93 vectors fail closed and one transcription error is confirmation-gated
✔ generation refuses missing secure randomness and rejects invalid configurations
ℹ tests 7
ℹ pass 7
ℹ fail 0
```

The source/build gates completed as follows:

```text
$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: @fontsource/bangers@5.3.0
Upstream release verified: @fontsource/comic-neue@5.3.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Upstream release verified: argon2-browser@1.18.0
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.

$ npm.cmd run check-docs
Documentation hygiene check passed: 182 markdown file(s) checked, 0 warning(s).

$ npm.cmd run build
Built build/coldbox.html (dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7)
```

The browser harness completed in both engines. These are the codex32 and final lines from the real run:

```text
Chromium: codex32 BIP-93 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, and teardown passed
Firefox: codex32 BIP-93 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

The final full-suite result, after the packet was present and the required local Git/browser permissions were available, is recorded here before the PR is opened:

```text
$ npm.cmd test
ℹ tests 328
ℹ pass 328
ℹ fail 0
```

Reproducibility evidence for the final source commit:

```text
$ npm.cmd run build
Built build/coldbox.html (dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7)
$ Get-FileHash -Algorithm SHA256 build/coldbox.html
dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7
$ (Get-Item build/coldbox.html).Length
2227044

$ Remove-Item -LiteralPath build/coldbox.html
$ npm.cmd run build
$ Get-FileHash -Algorithm SHA256 build/coldbox.html
dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7
```

The dependency-free browser preflight also rebuilt without `node_modules` and reported a byte-for-byte match to the normal build: `dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7`.

The remediation self-review used a Git-capable build under a different caller locale/timezone and produced the same `dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7` hash and `2227044`-byte size. A temporary `eval('')` inserted into a disposable worktree made `npm.cmd run build` exit `1` with `Forbidden construct "eval"`; the worktree was restored and removed.

## 4. Acceptance criteria

The roadmap currently names this item as:

> P2.2 codex32

The implementation satisfies that item through the BIP-93 surface described by the existing codex32 guide and ADR-0036. The focused suite proves the five independent published vectors, regular/long decoding, interpolation/recovery, generated threshold round-trips, invalid inputs, and missing-randomness refusal. The browser harness proves the user-facing cold-only workflow and teardown.

## 5. Security impact

Yes. This touches secret generation, secret recovery, randomness, the cold-realm assembly, and masked rendering. It does not change the message schema or add any cold-to-warm message.

If interpolation or the polynomial anchor is wrong, the application could produce valid-looking checksummed shares that reconstruct the wrong seed. The official BIP-93 interpolation/recovery vectors and generated 16/32/64-byte round-trips are the principal evidence against that failure. If the masking/teardown path is wrong, a secret could remain visible in the cold DOM after a session lock; the Chromium/Firefox browser test checks both the default masks and teardown clearing.

The cold CSP remains `connect-src 'none'`; no host was added. `codex32.js` has no storage, network, warm DOM, or message-channel path. The warm parent sees no codex32 API and the browser test asserts that isolation.

All random polynomial coefficients use `crypto.getRandomValues`. Missing randomness raises an error and the focused negative test proves generation refuses to proceed. No `Math.random`, `eval`, `new Function`, module import, or runtime dependency was added.

## 6. Test evidence

New tests:

- `test/codex32.test.js`: five official BIP-93 vectors, regular and long checksums, direct decode/recovery, generated threshold round-trips, invalid vectors, correction, missing randomness, and invalid configuration.
- `scripts/run-browser-harness.js`: cold API isolation, masked generation, timed reveal, threshold recovery, masked recovered output, explicit correction confirmation, insufficient-share failure, warm isolation, and lock teardown in Chromium and Firefox.

Vector authority:

- [Bitcoin BIP-93](https://github.com/bitcoin/bips/blob/master/bip-0093.mediawiki), including its reference Python algorithms and published test vectors. The vectors were not generated by this implementation.

Negative cases deliberately exercised:

- Invalid checksums and wrong data-size families.
- Mixed/invalid codex32 text, one altered character, one missing share, repeated/invalid share conditions, and thresholds/counts outside the allowed range.
- Missing `crypto.getRandomValues`.
- Parent-frame API absence and post-lock secret-state clearing.

The full existing `npm.cmd test`, vendor verification, lint, docs check, build, and Chromium/Firefox browser gates also ran. Physical/mobile/browser-device execution and manual checksum calculation remain untested.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chromium | PASS | Playwright `file://` harness, including codex32 and existing regressions |
| Windows Firefox | PASS | Playwright `file://` harness, including codex32 and existing regressions |
| macOS Safari | UNTESTED | No macOS device in this session |
| macOS Chrome | UNTESTED | No macOS device in this session |
| Linux Firefox | UNTESTED | No Linux device in this session |
| iOS local-execution target | UNTESTED | No iOS device/build available; no Quick Look inference made |
| Android Chrome (Files) | UNTESTED | No Android device in this session |
| Tor Browser | UNTESTED | No Tor Browser in this session |

## 8. Assumptions made

- The roadmap's one-line P2.2 title is the acceptance surface; the existing BIP-93 guide and architecture documents supply the format and realm requirements because the roadmap does not enumerate separate bullet criteria.
- The current BIP-93 draft is the interoperability authority. Regular and long forms are both implemented because the standard's published vectors include both and BIP-32 allows 64-byte seeds.
- The UI accepts a direct hex master seed rather than a mnemonic because BIP-93 explicitly encodes BIP-32 master-seed bytes, not BIP-39 entropy. It has no passphrase field.
- Share generation uses random polynomials expressed around the secret coordinate `s`; this is necessary for an existing secret to be recovered at the codex32 secret index rather than at zero.
- The P2.2 branch is a sibling cut from `main`, as required by `batch-run.md`; it does not include P2.1's unmerged SLIP-39 code. The roadmap is split into individual markers on this branch so only P2.2 is `[~]`.
- The correction helper is intentionally bounded to a single candidate substitution. P4.3e owns broader codex32 correction and requires its own independent vectors.

## 9. What to scrutinise

First scrutinise `gfLagrange`, the `s`-anchored polynomial generation, and the regular/long checksum constants in `src/cold/codex32.js`. A bug there can preserve checksums while changing the recovered secret. Then scrutinise the build manifest ordering and the main.js guard/teardown calls to ensure the API remains cold-only and state cannot survive a double-Escape lock.

## 10. Self-assessment

- The highest residual risk is the small hand-transcribed GF(32)/BIP-93 adaptation; the official vectors cover interoperability but cannot prove every possible share-set shape.
- The single-character correction search is deliberately narrower than BIP-93's recommended maximum correction capability. The UI and docs state that limitation rather than presenting it as full repair.
- No independent human paper checksum, physical printer, mobile local-file, or iOS execution test was possible.
- The final independent review must inspect the exact standard constants and verify that no generated/recovered codex32 string reaches a message or warm DOM path.

## 11. Bundle impact

The main-branch baseline artifact was 2,178,561 bytes. The P2.2 artifact is 2,227,044 bytes: a delta of 48,483 bytes (47.35 KiB). This remains below the documented 3 MiB target and 4.5 MiB hard cap.

## 12. Docs updated

- `docs/05-development/ROADMAP.md` — split Phase 2 items and marked only P2.2 `[~]`.
- `docs/05-development/adr/0036-codex32-cold-hand-verifiable.md` and ADR index.
- `docs/03-guides/backup-codex32.md` — direct-seed and confirmation-gated UI guidance.
- `docs/05-development/dependencies.md` — inline standard-defined implementation provenance.
- `CHANGELOG.md` — P2.2 entry.
- `docs/05-development/packets/p2.2-codex32.md` and its self-review companion.

## 13. Independent-review remediation

The attached combined independent review reported FAIL for PRs #48-#52. This
P2.2 branch addresses its P2.2 findings as follows:

- The guide now describes direct BIP-32 master-seed recovery accurately and
  explicitly says not to reinterpret recovered bytes as BIP-39 words.
- Claims about current worksheet printing, completion records, fingerprints,
  and the Health dashboard are removed or identified as external/later
  processes; the shipped P2.2 surface is the source of truth.
- The identifier input is blank by default. Blank generation omits the
  identifier option so the API uses a fresh `crypto.getRandomValues`-backed
  identifier; a typed identifier remains an explicit compatibility choice.
- Focused and browser tests cover the blank default, successive fresh
  identifiers, and the absence of the old fixed `cash` default.

The P2.2 branch does not define P2.5's method-3 vault format or any later
recovery-share composition rule.

## 14. Independent-review remediation round 3

The fresh independent review of commit `3219685bef8f52af0e0abe160ea8ba1b6acfb35f`
reported two blocking findings: executable `Math.random()` in cold codex32
code was accepted by the build gate, and the exact P2.2 Firefox run ended with
a Playwright context-close error. This round addresses both findings without
changing codex32 format behavior.

`scripts/lint.js` now applies the executable `Math.random()` rejection to the
complete `src/cold/**` security surface. The lint fixture suite includes a
cold-module `Math.random()` fixture, and `test/build.test.js` mutates the
codex32 module and requires both lint and build to exit non-zero.

The exact remediated head completed the full browser harness in Chromium and
Firefox using the test-only `MOZ_DISABLE_CONTENT_SANDBOX=1` setting. Both
engines emitted the codex32 item-specific PASS and the harness exited 0; the
previous Firefox lifecycle failure did not recur.

Current evidence:

```text
node --test test/codex32.test.js test/lint.test.js
tests 11
pass 11
fail 0

node --test test/build.test.js
tests 14
pass 14
fail 0

npm.cmd test
tests 329
pass 329
fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 183 markdown file(s) checked, 0 warning(s).

npm.cmd run build (twice)
dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7  2227044
dcceeababf290c0812d86e14ceac6708f18072a2e9e282acf3811cbd5d342cd7  2227044

npm.cmd run test:browser
Chromium: codex32 BIP-93 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, and teardown passed
Firefox: codex32 BIP-93 vectors, threshold generation/recovery, masking, confirmation-gated correction, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

The roadmap marker remains `[~]`; independent re-review must set `[x]` only
after verifying this remediation.
