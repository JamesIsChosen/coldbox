# PR packet — P2.5 Vault recovery shares

**Roadmap item:** [P2.5 Vault recovery shares](../ROADMAP.md)
**Branch:** `p2.5-vault-recovery-shares`
**Pull request:** [#52](https://github.com/JamesIsChosen/coldbox/pull/52)
**Dependency base:** current `main` at `a6ceb3a`; this branch includes the
sync merge `cc4df9e` so the P2.5 diff is reviewable against the current
roadmap.

## 1. Summary

P2.5 adds an offline-only SLIP-39 recovery route for the vault's 32-byte DEK.
It stores exact public recovery metadata in a fixed method-3 record beside the
normal passphrase or keyfile record, binds that metadata to both encrypted
compartments, and keeps share words in the cold session/UI only. The existing
normal unlock route remains valid, and pre-P2.5 readers reject marked files.

## 2. Scope

Included:

- Accepted [ADR-0040](../adr/0040-vault-recovery-share-record.md) and the
  normative [vault-format contract](../../01-spec/vault-format.md).
- Fixed binary method-3 parsing, generation, offline recovery, replacement,
  metadata binding, marker compatibility boundary, and fail-closed negatives.
- Cold-only controls for configuring, masking, transcribing, and unlocking
  with recovery shares; no share words enter the warm realm or the vault file.
- `vault.dirty` as a boolean-only cold-to-warm persistence signal.
- Independent Trezor SLIP-39 coverage, a byte-stable pre-P2.5 vault fixture,
  and deterministic method-data coverage.
- Serialized save/share configuration operations; on-demand re-authentication
  for share generation; exact threshold-set validation without silently
  trimming surplus shares; and stale UI-operation guards.
- Three-depth user guidance, FAQ/quick-start updates, CHANGELOG, roadmap, and
  this packet.

Not included: BackupRecords, share verification records, health scoring,
printable cards, or mobile/physical-device validation. Those remain later
roadmap work or explicitly untested environments.

## 3. How to verify

All commands below were run from `C:\Users\semaj\Projects\coldbox`.

### Focused P2.5 and boundary checks

```text
$ node --test --test-name-pattern="P2.5|pre-P2.5" test/vault.test.js test/p0.19-runtime-wiring.test.js
✔ P2.5 recovery shares stay cold-only and the offline mode refreshes their controls
✔ P0.11 current readers open the byte-stable pre-P2.5 vault fixture
✔ P2.5 recovery shares use the fixed method-3 fixture, preserve the normal route, and recover offline
✔ P2.5 rejects malformed or unknown recovery records rather than ignoring them
✔ P2.5 method-1 recovery keeps exact group thresholds and rejects foreign surplus groups
✔ P2.5 save and recovery-share configuration are mutually exclusive operations
✔ P2.5 aborts recovery-share reconfiguration if the session closes during rewrapping
ℹ tests 7
ℹ pass 7
ℹ fail 0

$ node --test --test-name-pattern="256-bit extendable" test/slip39.test.js
✔ SLIP-39 validates and recovers the official 256-bit extendable vector used by vault recovery
ℹ tests 1
ℹ pass 1
ℹ fail 0

$ node --test test/protocol.test.js test/p0.19-runtime-wiring.test.js
ℹ tests 30
ℹ pass 30
ℹ fail 0
```

### Repository gates

```text
$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm.cmd run check-docs
Documentation hygiene check passed: 200 markdown file(s) checked, 0 warning(s).

$ npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

$ npm.cmd test
ℹ tests 365
ℹ pass 365
ℹ fail 0
```

The first sandboxed `npm.cmd test` attempt could not spawn the Git subprocesses
used by existing provenance tests (`EPERM`); the same command was rerun with
the required process permission and passed 365/365. The final elevated run
above passed 365/365.
Vendor verification was also rerun with upstream access after the sandbox
initially reported `fetch failed`.

### Reproducible build evidence

The exact baseline was built in a clean detached worktree at current `main`
`a6ceb3a` and then removed.

```text
$ node scripts/build.js                         # current main baseline
Built build/coldbox.html (e56a8fb8d3dbba2a6c2d3afb7689073826702fc1a2c1203dd5e5568ca41d6b97)
$ Get-FileHash -Algorithm SHA256 build/coldbox.html
SHA256  E56A8FB8D3DBBA2A6C2D3AFB7689073826702FC1A2C1203DD5E5568CA41D6B97
Length  2428968

$ npm.cmd run build                         # P2.5 build, first run
Built build/coldbox.html (0bd077cbf5e63b2902afc25641e9d7a2672c3f13f485947901561615b7d086bd)
$ Get-FileHash -Algorithm SHA256 build/coldbox.html
SHA256  0BD077CBF5E63B2902AFC25641E9D7A2672C3F13F485947901561615B7D086BD
Length  2471651

$ npm.cmd run build                         # P2.5 build, second run
Built build/coldbox.html (0bd077cbf5e63b2902afc25641e9d7a2672c3f13f485947901561615b7d086bd)
$ Get-FileHash -Algorithm SHA256 build/coldbox.html
SHA256  0BD077CBF5E63B2902AFC25641E9D7A2672C3F13F485947901561615B7D086BD
Length  2471651
```

Bundle delta from current `main`: `+42,683` bytes. The two P2.5 builds are
byte-identical.

### Browser evidence

```text
$ npm.cmd run test:browser
Chromium: P2.5 recovery generation, stale-completion lockout, explicit replacement, saved-vault reload, online refusal, stale-share rejection, and threshold recovery passed
Firefox: P2.5 recovery generation, stale-completion lockout, explicit replacement, saved-vault reload, online refusal, stale-share rejection, and threshold recovery passed
Browser harness passed in Chromium and Firefox.
```

The harness exercised both engines from `file://`, including CSP/network
lockdown, cold/warm boundary, the P2.5 generation/replacement/save/reload
flow, deterministic stale-completion lockout, online refusal, stale-share
rejection, threshold recovery, SLIP-39 UI isolation, and teardown.
The final run required the installed Playwright browser binaries and process
permission to access their user-level cache.

Physical mobile browsers and the device matrix were not available in this
session; see §7 and §10.

### Review remediation status

The independent review findings are tracked explicitly here:

| Finding | Status | Remediation/evidence |
|---|---|---|
| F1 save/configuration race | REMEDIATED | Session-wide `operationInFlight` serialization, immutable save snapshots, and deterministic mutual-exclusion test; the browser save/reload flow also passes. |
| F2 surplus/mixed shares | REMEDIATED | Recovery requires exactly the recorded threshold groups and members; checksum-valid foreign surplus member/group and duplicate negatives pass. |
| F3 exact member-count overclaim | REMEDIATED | ADR, vault-format, roadmap, guide, and packet now distinguish encoded mnemonic fields from the Coldbox member-index bound; surplus supplied inputs fail closed without claiming standard SLIP-39 encodes total count. |
| F4 retained root keys | REMEDIATED | Session state retains neither root DEK nor wrapping key; reissue re-authenticates and zeroes local keys. |
| F5 stale UI completion | REMEDIATED | UI captures the session/generation and ignores stale completions after lock/mode changes; the browser harness holds a real cold AES rewrap, forces online lock, releases it, and verifies the UI stays locked. |
| F6 compatibility/vector coverage | REMEDIATED | Official Trezor vector, direct method-1 positive recovery, correct old-share refusal, and a byte-stable fixture generated from pre-P2.5 commit `a6ceb3a`. |
| F7 browser coverage | REMEDIATED | Chromium and Firefox `file://` harness now exercise generation, deterministic stale-completion lockout, explicit replacement, save, reload, online refusal, stale-share rejection, exact-threshold recovery, and teardown. |
| F8 hardware/mobile | DEFERRED | Explicitly deferred by the user; no physical/mobile result is claimed. |
| F9 packet artifact provenance | IN PROGRESS UNTIL FINAL HEAD | Local evidence is refreshed to the final source state; exact-head CI artifact/hash matching is recorded below after the final push. |
| F10 independent clean checkout | REVIEWER GATE | The author cannot supply independent verification; a fresh reviewer must clone the final pushed head and run the review protocol. |

P2.5 remains `[~]`; the author does not mark a roadmap item independently
verified.

## 4. Acceptance criteria

Copied from P2.5 in [ROADMAP.md](../ROADMAP.md):

> method 3 has a versioned fixed binary record with exact bounds, one normal
> record, and a recovery marker that makes pre-P2.5 readers reject
> recovery-enabled files; generation and recovery are cold-only and always use
> the empty SLIP-39 share passphrase; supplied shares must match every
> recorded SLIP-39 field encoded by each mnemonic and member-index bound, with
> exactly the required threshold groups/members supplied; malformed, duplicate,
> insufficient, surplus, mixed, tampered, unknown, or unsupported inputs failing closed;
> recovery metadata is authenticated with both encrypted compartments; the
> normal unlock route continues to work; replacing an existing set requires an
> explicit choice; share material never crosses the realm boundary or persists
> outside the cold session; and tests include an independent official vector
> plus a deterministic byte-exact method-data fixture.

| Criterion | How satisfied | Evidence |
|---|---|---|
| Versioned fixed binary record with exact bounds; one normal record; marked files reject older readers | Method-data version 1 is fixed-length, bounded to 16 groups; parser requires exactly one normal record and the high-bit header marker; the old length ceiling rejects the marked block | `test/vault.test.js` method-data hex/record-length assertions and malformed/unknown-record test; ADR-0040 compatibility section |
| Cold-only generation/recovery with empty share passphrase | UI enables recovery only offline; API rejects non-empty share passphrases and calls SLIP-39 with `''`; online recovery is rejected | P2.5 vault test, P2.5 runtime-wiring test, `src/cold/main.js` controls |
| Exact encoded metadata/member matching and fail-closed malformed/duplicate/insufficient/surplus/mixed/tampered/unknown cases | Decoder checks every SLIP-39 field the mnemonic encodes, bounds member indices with the recorded generation policy, requires exactly the threshold groups/members, and rejects surplus inputs instead of trimming them; parser and AEAD reject malformed, modified, or unsupported records | P2.5 vault tests and `test/protocol.test.js` |
| Metadata authenticated with both compartments | Recovery AAD is header bytes plus exact method-3 method data for public and secret AES-GCM; changing metadata causes authentication failure | P2.5 tampered-metadata test; `src/cold/vault.js` `compartmentAad()`; vault-format.md |
| Normal route remains usable | Method 1 or 2 is retained and rewrapped under the recovery-marked header; both passphrase and keyfile normal unlock are exercised | P2.5 method-1 and keyfile tests open normally and through shares |
| Replacement is explicit | Existing metadata requires `replace: true`; UI exposes a replacement checkbox and states old shares stop working after save | P2.5 replacement and refusal assertions; cold UI markup |
| Share material stays cold-only and non-persistent | Method 3 stores metadata and zero reserved bytes only; no warm message carries shares; cold UI masks and clears them on teardown | P2.5 runtime-wiring test, protocol secret-field regression, cold UI, and parser fixture |
| Independent vector and compatibility fixtures | Trezor 256-bit extendable vector, a byte-stable pre-P2.5 vault, and method data `01201234010001010203` are asserted | `test/slip39.test.js`, `test/vault.test.js`, `test/fixtures/pre-p2.5-vault-v1.fixture` |

## 5. Security impact

This changes the vault format, cold vault unlock logic, AAD, and the cold-to-warm
message whitelist. It does not add a network host, weaken the cold CSP, or add
a warm-to-cold credential message.

The only new message is `vault.dirty` with the bounded payload `{ dirty: true }`.
The protocol maps it to the existing boolean-only cleaner; extra fields such as
`shareMaterial` are rejected. The warm handler only marks persistence state
unsaved and contains no recovery-share identifiers or storage.

If the format binding were wrong, an attacker could attempt to substitute a
valid share set from another vault or alter the recorded threshold/metadata.
The implementation limits that risk by exact metadata comparison and by
including the method-data bytes in both compartment AEAD AAD values. If either
the header or metadata changes, normal and recovery opens fail at the
appropriate authentication check. This is the area requiring the most hostile
independent review.

Recovery bypasses the passphrase KDF by reconstructing the DEK directly. It is
available only in offline mode. Sessions do not retain the root DEK or normal
wrapping key: generating or replacing shares requires the normal passphrase
and, when applicable, keyfile to be supplied again, and those local keys are
zeroed when the operation ends. Save uses the already-decrypted compartments
and serialized header/wrapped-block snapshots, so it does not need a retained
root key.

## 6. Test evidence

New tests:

- `test/vault.test.js`: fixed record bytes, header marker, pre-P2.5 byte-stable
  compatibility fixture, normal passphrase/keyfile unlock, direct method-1
  recovery, exact two-share recovery, surplus foreign member/group rejection,
  duplicate rejection, online refusal, replacement, old-share refusal, AAD
  tamper rejection, malformed version/tail/unknown method rejection, explicit
  replacement refusal, save/configuration mutual exclusion, and
  close-during-rewrap race.
- `test/slip39.test.js`: official 256-bit extendable vector from the Trezor
  `python-shamir-mnemonic` `vectors.json` fixture:
  [official vectors.json](https://raw.githubusercontent.com/trezor/python-shamir-mnemonic/master/vectors.json).
- `test/protocol.test.js`: whitelist and secret-bearing-field rejection for
  `vault.dirty`.
- `test/p0.19-runtime-wiring.test.js`: cold-only UI, re-authentication input,
  no warm share surface, operation serialization, exact-share validation,
  offline control refresh, stale-completion guards, and browser-flow wiring.

Deliberate negative cases include: insufficient shares, online recovery,
wrong/replaced shares, changed metadata, invalid method-data version, non-zero
reserved tail, unknown method ID, malformed metadata, duplicate shares,
unsupported/unknown records, no explicit replacement, and a session close
during rewrapping.

No real seed, private key, or production share was used or committed. The
historical compatibility fixture is an encrypted byte file made from
throwaway literals by the pre-P2.5 implementation; its passphrase and
contents are test-only. Generated share words are held only in test memory and
are not printed in this packet.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chromium | PASS | Full Playwright `file://` harness |
| Windows Firefox | PASS | Full Playwright `file://` harness |
| macOS Safari | UNTESTED | No device/browser available |
| macOS Chrome | UNTESTED | No device/browser available |
| Linux Firefox | UNTESTED | No Linux environment available |
| **iOS local-execution target** | DEFERRED | User explicitly deferred physical/mobile testing; no iOS device or iOS build available |
| Android Chrome (Files) | DEFERRED | User explicitly deferred physical/mobile testing; no Android device available |
| Tor Browser | UNTESTED | No Tor Browser available; not part of the deferred hardware/mobile set |

The required desktop `file://` coverage is complete. Physical/mobile testing is
explicitly deferred by the user and is not claimed here; it remains a release
gate for the roadmap item.

## 8. Assumptions made

- The roadmap's existing v1 wrapped-block ceiling of 65,535 bytes makes the
  high-bit marker an unambiguous fail-closed boundary for pre-P2.5 readers.
- A recovery-enabled vault must retain exactly one normal record so an owner
  does not lose the established passphrase/keyfile route.
- The pinned SLIP-39 implementation's rule that a 1-member threshold cannot
  advertise multiple members is part of the method-3 metadata bounds.
- No share passphrase is intentional: the printed threshold set is the full
  recovery authority. Seed-backup SLIP-39's optional passphrase remains a
  separate feature and guide.
- Sessions intentionally do not retain the root DEK or normal wrapping key.
  Share generation/replacement re-authenticates with the normal unlock phrase
  and selected keyfile inside the cold realm, then zeroes the local keys. A
  recovery-only session may save normal public edits, but it must re-enter the
  normal credentials before creating or replacing a share set.
- Standard SLIP-39 mnemonics do not encode a total member count. The recorded
  count is therefore a Coldbox generation bound; recovery compares every
  encoded field and rejects surplus supplied groups/members, but does not claim
  to prove that no additional printed members ever existed.

## 9. What to scrutinise

- Confirm the recovery AAD is constructed from the exact serialized method
  data in both `openSession()` and `save()`, and that the normal wrapped-DEK
  record uses the marked header bytes after configuration.
- Confirm the parser cannot accept a method-3-only block, duplicate members,
  an extra group, a non-zero reserved tail, a marker mismatch, or an unknown
  method without a non-zero failure.
- Confirm save and share configuration cannot overlap, and that asynchronous
  close/mode/health changes cannot resurrect state or leave a newly built
  wrapped block alive; the regression tests deliberately hold AES and close
  during rewrapping.
- Confirm recovery remains unreachable online even when valid shares and a
  public-only vault are supplied.
- Confirm no method-3 words or generated share strings appear in the compiled
  warm realm or in any protocol payload, and that the pre-P2.5 fixture is
  genuinely from the historical unmarked reader rather than generated by the
  current implementation.

## 10. Self-assessment

The implementation passes the full Node suite, vendor provenance, reproducible
build, lint, documentation, and Chromium/Firefox `file://` harness, including
the real P2.5 browser scenario. Physical/mobile testing was explicitly
deferred and is not claimed. A fresh independent reviewer must still run the
clean-checkout review gate before this roadmap item can move from `[~]` to
`[x]`.

The subtle remaining risk is format interoperability: the method-3 record is
an internal Coldbox v1 contract, not a pre-existing external vault standard.
ADR-0040 and the byte fixture make the contract reviewable, but an independent
reviewer must still check every offset and boundary against the implementation.

The next dependent work is P2.6 BackupRecords and verify-your-shares; this PR
does not implement it.

## 11. Bundle impact

| Build | Bytes | SHA-256 |
|---|---:|---|
| Current `main` baseline | 2,428,968 | `e56a8fb8d3dbba2a6c2d3afb7689073826702fc1a2c1203dd5e5568ca41d6b97` |
| P2.5 | 2,471,651 | `0bd077cbf5e63b2902afc25641e9d7a2672c3f13f485947901561615b7d086bd` |
| Delta | +42,683 | — |

The output remains one self-contained HTML file; no runtime dependency or
network fetch was added.

## 12. Docs updated

- `docs/01-spec/vault-format.md`
- `docs/01-spec/SPEC.md`
- `docs/01-spec/architecture.md`
- `docs/03-guides/inheritance-planning.md`
- `docs/03-guides/backup-slip39.md`
- `docs/00-overview/faq.md`
- `docs/00-overview/quick-start.md`
- `docs/05-development/ROADMAP.md`
- `docs/05-development/adr/README.md`
- `docs/05-development/adr/0040-vault-recovery-share-record.md`
- `docs/05-development/packets/BATCH-2026-08-12.md` (historical-link/status clarification)
- `CHANGELOG.md`

User-facing help has plain, working, and technical depth blocks in the
inheritance and SLIP-39 guides.
