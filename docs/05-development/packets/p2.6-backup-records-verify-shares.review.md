# Review: P2.6 â€” BackupRecords and verify-your-shares

**VERDICT: FAIL**

Findings: 3 (3 blocking, 0 advisory â€” all must be addressed)
Reviewed commit: `727e62d2b8d2f535f4e90dddb24ce2184c763652`
Reviewed against: `main@79d39ab4d98cd11fdf821fc182b27fd6b0ef875c`
Reviewed by: GPT-5.6 Sol â€” independent review session
Date: 2026-08-13

## 1. What I verified

I reviewed PR #53 independently from two fresh clones rather than reusing the authorâ€™s runners or working tree.

### Exact revision and scope

```text
branch: p2.6-backup-records-verify-shares
head:   727e62d2b8d2f535f4e90dddb24ce2184c763652
base:   79d39ab4d98cd11fdf821fc182b27fd6b0ef875c
changed paths: 26
```

The roadmap entry at the reviewed tip is still `[~] P2.6 BackupRecords and verify-your-shares`, with dependency `P2.5`.

### Required checks

Fresh clone A ran under a different review environment (`Pacific/Auckland`, `fr_FR.UTF-8`):

```text
npm.cmd ci
exit 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.
exit 0

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.
exit 0

npm.cmd run check-docs
Documentation hygiene check passed: 203 markdown file(s) checked, 0 warning(s).
exit 0

npm.cmd test
tests 369
pass 369
fail 0
exit 0
```

### Reproducible build across fresh paths / timezone / locale

Clone A:

```text
Built build/coldbox.html (df828583a3421f9274b8ff68e6484ab1cc0dde1cc2b4fa06344798c68fd80b9d)
CLONE_A_SHA256=df828583a3421f9274b8ff68e6484ab1cc0dde1cc2b4fa06344798c68fd80b9d
CLONE_A_BYTES=2524203
```

Fresh clone B ran separately under `America/New_York`, `de_DE.UTF-8`:

```text
Built build/coldbox.html (df828583a3421f9274b8ff68e6484ab1cc0dde1cc2b4fa06344798c68fd80b9d)
CLONE_B_SHA256=df828583a3421f9274b8ff68e6484ab1cc0dde1cc2b4fa06344798c68fd80b9d
CLONE_B_BYTES=2524203
```

The exact reviewed tip is therefore reproducible across the two independent paths/environments. This does not rescue the stale packet evidence described in F3.

### Deliberate fail-closed vendor corruption

I altered one byte of the vendored `@scure/bip39@2.2.0` archive inside the disposable clone only.

```text
npm.cmd run verify-vendor
Vendor verification failed: Vendor size mismatch for @scure/bip39@2.2.0
exit 1

npm.cmd run build
Error: Build refused: vendored artifacts failed offline verification
exit 1
```

The corruption was then restored with Git and the clone returned clean.

### Reviewer-owned P2.6 protocol probes

Using an official Trezor SLIP-39 vector without printing the share material:

```text
Official SLIP-39 vector word count: 20
Secret detector flagged official SLIP-39 share: False
Warm->cold public text accepted official SLIP-39 share: True
Cold->warm public text accepted official SLIP-39 share: True
backup.verifyRequest strips unknown secret field: True
backup.verifyResult strips unknown secret fields: True
Impossible group configuration accepted: True
```

Those probes produced F1 and F2 below.

### Browser verification

The complete `file://` browser harness ran against the reviewed build:

```text
Chromium: public BackupRecord creation, cold-only SLIP-39 verification, warm isolation, and teardown passed
Firefox: public BackupRecord creation, cold-only SLIP-39 verification, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
exit 0
```

The broader harness also reconfirmed the cold CSP/network blocking, opaque sandbox behavior, panic teardown, runtime air-gap lockdown, and built-artifact CSP tamper rejection.

## 2. What I could not verify

None of the P2.6 roadmap acceptance text requires a physical-device result. The packetâ€™s physical-device gaps were not promoted into acceptance claims, so they are not additional findings in this review.

The three findings below are independently reproduced defects/evidence mismatches, not unverified questions.

## 3. Acceptance criteria

Roadmap text checked verbatim:

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.6 BackupRecords and verify-your-shares` | âŒ | The normal verification UI works in Chromium/Firefox, but the implementation violates the repositoryâ€™s hard no-secret realm-boundary constraint because an official SLIP-39 share can be carried through allowed public BackupRecord text (F1). The group schema also accepts an impossible configuration that cannot be satisfied (F2). |
| 2 | `*Deps: P2.5*` | âœ… | P2.5 is complete on the reviewed base and its recovery-share tests/browser flows pass in the full suite. |

Repository hard constraints checked for this security-relevant change:

- Cold CSP / network isolation: no new finding.
- `backup.verifyRequest` and `backup.verifyResult` closed result/request fields: no new finding; unknown secret fields are stripped.
- No secret may cross the realm boundary: **violated â€” F1**.
- Fail-closed vendor/build behavior: verified.
- Reproducible current-tip build: verified independently, but the packetâ€™s claimed final hash/size is stale â€” F3.

## 4. Findings

### F1 â€” An official SLIP-39 share can cross the realm boundary through public BackupRecord text

**Severity:** blocking security  
**Location:** `src/protocol.js`, `isSecretContent()` and BackupRecord public text-field projection (`shareLabel`, `location`, `custodian`, `notes`)  
**Observed:** The reviewer probe loaded an official 20-word Trezor SLIP-39 share. `isSecretContent()` returned `false`. The same share was then accepted inside an allowed public BackupRecord text field by both the warmâ†’cold `publicData.replace` projection and the coldâ†’warm public-compartment projection. The dedicated `backup.verifyRequest` / `backup.verifyResult` messages correctly stripped unknown secret fields; the leak is through the generic public BackupRecord text path.  
**Expected:** Per `AGENTS.md`, the review protocol, ADR-0041, architecture documentation, and the PRâ€™s own security claim, share material must never cross the warm/cold boundary or be accepted as public BackupRecord metadata.  
**Required action:** Add a real share-material guard for public text carriers that rejects supported backup-share formats, including standard 20-word and 33-word SLIP-39 shares, rather than relying only on BIP-39 mnemonic word-count heuristics. Add regression tests using official independent SLIP-39 vectors proving that create/update/public projection and both message directions reject the share without logging it. Re-check the other supported share encodings against all public free-text carriers as part of the same boundary audit.

### F2 â€” Backup group schema accepts an impossible group threshold

**Severity:** blocking functional/schema  
**Location:** `src/protocol.js`, `cleanBackupGroupConfig()`  
**Observed:** The reviewer probe supplied a group configuration whose `groupThreshold` exceeds the number of configured groups. The schema accepted it (`Impossible group configuration accepted: True`). The function validates each member groupâ€™s `threshold <= count`, but does not validate the top-level `groupThreshold` against `groups.length`.  
**Expected:** Public threshold/group metadata must be a realizable configuration. A BackupRecord must not accept a configuration for which no valid SLIP-39 share set can ever satisfy the recorded group threshold.  
**Required action:** Reject `groupThreshold > groups.length` whenever `groups` is present, and add positive/negative schema tests around the boundary (`1-of-1`, `N-of-N`, and `N+1-of-N`). Ensure the cold verification binding consumes only configurations that passed this realizability check.

### F3 â€” The packetâ€™s claimed final build hash and byte size do not reproduce at the reviewed tip

**Severity:** blocking evidence provenance  
**Location:** `docs/05-development/packets/p2.6-backup-records-verify-shares.md` Â§3 and Â§11 / PR #53 body  
**Observed:** The packet claims the final committed artifact is:

```text
ef825d95337a7c9802ab2318f565cc5cc2b06e37f8582e42075f2b9853e7249b
2524220 bytes
```

Two fresh clones of exact reviewed head `727e62d2b8d2f535f4e90dddb24ce2184c763652`, built under different paths/timezones/locales, independently produced:

```text
df828583a3421f9274b8ff68e6484ab1cc0dde1cc2b4fa06344798c68fd80b9d
2524203 bytes
```

The two fresh builds match each other, so current-tip determinism is good; the packetâ€™s asserted â€œfinalâ€ evidence is not evidence for the actual PR tip.  
**Expected:** The packetâ€™s exact build hash/size claims must reproduce from the commit being reviewed. The review protocol explicitly treats packet claims that do not reproduce as a FAIL.  
**Required action:** After F1/F2 remediation is committed, regenerate the build evidence from the exact new committed tip and update every packet/PR-body hash, byte-size, and delta claim to that tip. Do not reuse a pre-final-commit artifact as â€œfinalâ€ evidence.

## 5. Verdict rationale

P2.6 cannot pass at `727e62d2b8d2f535f4e90dddb24ce2184c763652`. F1 violates the repositoryâ€™s central no-secret realm-boundary rule and is an automatic FAIL. F2 permits invalid BackupRecord group metadata that can never be verified. F3 shows the packetâ€™s final artifact provenance is stale even though the current tip itself builds reproducibly. A PASS requires author-side remediation of all three findings, new exact-tip evidence, and a fresh independent re-review. The reviewer must not fix these findings.

FAIL
---

# Re-review R2: P2.6 - BackupRecords and verify-your-shares

**VERDICT: FAIL**

Findings: 3 (3 blocking, 0 advisory - all must be addressed)
Reviewed commit: `f594d1f619f59c6900c17d84b0b852be2cfa3fd2`
Reviewed against: `main@79d39ab4d98cd11fdf821fc182b27fd6b0ef875c`
Prior review commit in ancestry: `b5525c3d8a31bd9ecd218df9b6007ab7fbd5b64f`
Reviewed by: GPT-5.6 Sol - fresh independent R2 review session
Date: 2026-08-14

## 1. What I verified

I re-reviewed PR #53 independently at the exact remediation head. The source checkout was not used as evidence; the review ran in two fresh temporary clones.

### Exact revision and remediation ancestry

```text
branch: p2.6-backup-records-verify-shares
head:   f594d1f619f59c6900c17d84b0b852be2cfa3fd2
base:   79d39ab4d98cd11fdf821fc182b27fd6b0ef875c
prior R1 review commit: b5525c3d8a31bd9ecd218df9b6007ab7fbd5b64f
PR changed paths: 27
R1 remediation changed paths: 8
```

The R1 review commit is an ancestor of the reviewed remediation tip. The R1 review history remained present and P2.6 remained `[~]`.

### Required checks

Fresh clone A ran under `Pacific/Auckland`, `fr_FR.UTF-8`.

```text
npm.cmd ci
exit 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.
exit 0

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.
exit 0

npm.cmd run check-docs
Documentation hygiene check passed: 204 markdown file(s) checked, 0 warning(s).
exit 0

npm.cmd test
tests 371
pass 371
fail 0
exit 0
```

### Exact-tip build reproducibility

Clone A:

```text
SHA-256: b79dc9a181b00206a94b324c87ce20bd3d03618118bc90e301cebaf4d96e2c86
Bytes:   2543160
```

Fresh clone B ran separately under `America/New_York`, `de_DE.UTF-8`:

```text
SHA-256: b79dc9a181b00206a94b324c87ce20bd3d03618118bc90e301cebaf4d96e2c86
Bytes:   2543160
```

The current tip is reproducible across the two independent paths/environments. The packet's claimed exact-tip artifact does not match it; see R2-F2.

### Deliberate fail-closed vendor corruption

I appended one byte to the disposable clone's vendored `@scure/bip39@2.2.0` archive.

```text
npm.cmd run verify-vendor
exit 1

npm.cmd run build
exit 1
```

The vendor file was restored with Git and the clone returned clean.

### R1 F1 recheck - independent upstream SLIP-39 coverage

The reviewer downloaded Trezor's current official `python-shamir-mnemonic` word list and vectors directly rather than trusting the remediation's embedded list or its new tests.

```text
official upstream SLIP-39 words: 1024
upstream words missing from remediation detector: 3
missing words: modern, phrase, similar
official valid vector found: yes
official valid share word count: 20
official valid share classified secret: false
warm->cold BackupRecord text carriers accepting the share: 4 of 4
cold->warm BackupRecord text carriers accepting the share: 4 of 4
```

The valid official share itself was never printed into the review evidence.

The direct `backup.verifyRequest` and `backup.verifyResult` schemas continued to strip unknown secret-bearing fields correctly. The remaining leak is the generic public BackupRecord text projection.

### R1 F2 recheck - group realizability

```text
impossible group configuration accepted: false
realizable group configuration accepted: true
```

R1 F2 is fixed.

### R1 F3 recheck - packet evidence

The packet claims:

```text
55368cfae6f30034dc60518874d2b5a8d81d8a668b18819ae782e0a31e84e277
2543177 bytes
```

Both fresh clones of the exact reviewed tip produced:

```text
b79dc9a181b00206a94b324c87ce20bd3d03618118bc90e301cebaf4d96e2c86
2543160 bytes
```

R1 F3 is not fixed at the reviewed tip.

### Browser verification

The complete built-artifact `file://` browser harness passed in Chromium and Firefox, including the P2.6 normal BackupRecord verification flow and the broader realm/CSP/air-gap assertions.

```text
Browser harness passed in Chromium and Firefox.
exit 0
```

The final fresh clone remained at the exact reviewed head and had a clean tracked-and-untracked status.

## 2. What I could not verify

Nothing was left unverified for the P2.6 roadmap text. The verdict is FAIL because the three concrete findings below were reproduced.

## 3. Acceptance criteria

Roadmap text checked verbatim:

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.6 BackupRecords and verify-your-shares` | FAIL | The normal UI/tests/browser flow pass, and R1 F2 is fixed, but a valid official SLIP-39 share still crosses the public BackupRecord realm boundary (R2-F1). Exact-tip packet evidence is also stale (R2-F2), and the packet's docs count is stale (R2-F3). |
| 2 | `*Deps: P2.5*` | PASS | P2.5 remains complete on the base history; the full test/browser suites pass. |

Hard constraints:

- No secret may cross the realm boundary: **violated - R2-F1**.
- Cold CSP/network isolation: no new finding.
- Unknown backup verification fields are stripped: verified.
- Group threshold realizability: R1 F2 fixed.
- Current-tip build reproducibility: verified between two fresh clones.
- Packet claims reproduce: **violated - R2-F2 and R2-F3**.
- Deliberately corrupted vendor/build failure exits non-zero: verified.

## 4. Findings

### R2-F1 - Valid official SLIP-39 share still crosses all public BackupRecord text carriers

**Severity:** blocking security
**Location:** `src/protocol.js`, embedded `SLIP39_WORD_SET`, `containsSlip39WordSequence()`, and public BackupRecord text validation
**Observed:** The remediation introduced an embedded public SLIP-39 word set, but independent comparison against Trezor's official 1,024-word list found three omissions: `modern`, `phrase`, and `similar`. A valid official 20-word Trezor vector containing an omitted word was not classified as secret material. The reviewer then placed that valid share in each BackupRecord public text carrier (`shareLabel`, `location`, `custodian`, `notes`); all four carriers were accepted in both warm->cold and cold->warm public projections.
**Expected:** Supported share material must never cross the realm boundary. The remediation's own ADR/architecture claim says all supported SLIP-39 shares are rejected before either public projection direction.
**Required action:** Do not maintain a hand-copied/incomplete SLIP-39 word list. Source the public 1,024-word format list from one canonical reviewed artifact or add a mechanically verified exact copy with a test that compares all 1,024 words against an independent upstream fixture. Add the valid official vector that exposed this gap to the boundary regression suite and assert rejection through all four text carriers in both directions. Keep the actual share out of logs/review bundles.

### R2-F2 - Refreshed final build evidence still does not reproduce from the exact reviewed tip

**Severity:** blocking evidence provenance
**Location:** `docs/05-development/packets/p2.6-backup-records-verify-shares.md` section 3 / section 11 and PR #53 body
**Observed:** The packet claims `55368cfae6f30034dc60518874d2b5a8d81d8a668b18819ae782e0a31e84e277` at 2,543,177 bytes. Two fresh builds from exact head `f594d1f619f59c6900c17d84b0b852be2cfa3fd2`, under different paths/timezones/locales, both produced `b79dc9a181b00206a94b324c87ce20bd3d03618118bc90e301cebaf4d96e2c86` at 2,543,160 bytes.
**Expected:** The packet's final artifact claim must be generated from and reproduce at the exact committed tip submitted for review.
**Required action:** After the next remediation is fully committed, build the exact final tip and update every final SHA-256, byte count, and bundle-delta claim to that artifact. Verify again after the last packet commit instead of relying on an earlier remediation commit's artifact.

### R2-F3 - Packet documentation-check count is stale

**Severity:** blocking documentation/evidence
**Location:** `docs/05-development/packets/p2.6-backup-records-verify-shares.md` section 3 / PR #53 body
**Observed:** The packet says `check-docs` checked 202 markdown files. The exact reviewed tip reports `Documentation hygiene check passed: 204 markdown file(s) checked, 0 warning(s).`
**Expected:** Recorded verification output in the packet must match the exact reviewable tree. Documentation is explicitly in review scope, and stale numeric evidence is a defect under the review protocol.
**Required action:** Regenerate the packet's command evidence from the final committed remediation tree and update the markdown-file count to the actual exact-tip result. Re-run it after the reviewer-history file and any other tracked documentation expected in the PR are present.

## 5. Verdict rationale

P2.6 still cannot pass at `f594d1f619f59c6900c17d84b0b852be2cfa3fd2`. R1 F2 is genuinely fixed, and the implementation passes 371/371 tests plus the complete Chromium/Firefox harness, but R2-F1 is an automatic FAIL because a valid official SLIP-39 share still crosses the realm boundary. R2-F2 and R2-F3 independently show that the refreshed packet evidence is not exact-tip evidence. A PASS requires author-side remediation of all three R2 findings, regenerated final-tip evidence after the last author commit, and another fresh independent re-review.

FAIL
---

# Re-review R4: P2.6 - BackupRecords and verify-your-shares

**VERDICT: PASS**

Findings: 0
Reviewed commit: `b2c2d04ccb3fd4565a4ea281faf0d5fad68799a3`
Reviewed against: `main@79d39ab4d98cd11fdf821fc182b27fd6b0ef875c`
Reviewed by: GPT-5.6 Sol - fresh independent R4 review session
Date: 2026-08-14

## 1. What I verified

### Exact revision and closeout state

```text
PR:     #53
branch: p2.6-backup-records-verify-shares
head:   b2c2d04ccb3fd4565a4ea281faf0d5fad68799a3
base:   79d39ab4d98cd11fdf821fc182b27fd6b0ef875c
prior reviewer-history blob: c4839ea417f646be1c4d82be34f9a486153e74ff
```

PR #53 remained open and unmerged at the exact reviewed head. P2.6 remained `[~]`, as required before an independent PASS, and P2.5 remained complete as its dependency.

### R3-F1 - subject binding before completion

The R3 remediation closes the wrong-subject verification flaw. The cold completion authority now accepts the BackupRecord ID, method, reconstructed candidate, and verification timestamp; resolves the BackupRecord `subjectId` to one cold-stored seed, either directly or through the public wallet-to-seed relation; compares the reconstructed candidate to that resolved subject inside the cold realm; and only then writes `lastVerifiedAt`.

Wrong-subject, unresolved, ambiguous, and secretless subject resolution fail closed before timestamp mutation. The supported reconstruction paths for SLIP-39, codex32, Seed XOR, Shamir39, and raw SSS all converge on the same cold `markBackupVerified` gate before a `verified` result can be returned.

The negative regression uses two independent valid SLIP-39 subjects with the same method/threshold shape: subject B cannot verify subject A; the correct subject succeeds; and an unresolved subject stays unverified.

### Exact-head automated evidence

GitHub Actions checked out exact SHA `b2c2d04ccb3fd4565a4ea281faf0d5fad68799a3` under Node 24.16.0 and passed:

```text
upstream vendor verification: PASS
lint:                         PASS
documentation hygiene:        204 markdown files, 0 warnings
unit/vector tests:             373/373 PASS
Chromium file:// harness:      PASS
Firefox file:// harness:       PASS
```

The browser harness specifically passed the unresolved-BackupRecord-subject fail-closed flow in both Chromium and Firefox, including cold-only share input and teardown.

The test suite also retains the earlier review remediations: exact canonical 1,024-word SLIP-39 public-detector coverage, rejection of all supported share encodings from every public BackupRecord text carrier in both projection directions, realizable group-threshold boundaries, cold-only timestamp authority, and vendor-corruption/build-refusal regressions.

### Exact artifact provenance

I independently checked the exact-head Ubuntu and Windows CI artifacts. They are byte-identical:

```text
SHA-256: dbe0201bdf7f45c3b6e420836ba9459d565405c1cee4e62484150d991d8631ff
bytes:   2551484
```

This matches the final packet claim for `b2c2d04ccb3fd4565a4ea281faf0d5fad68799a3`.

## 2. What I could not verify

None of the P2.6 roadmap acceptance criteria remain unverified.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.6 BackupRecords and verify-your-shares` | PASS | Public BackupRecords remain warm metadata; supported reconstruction stays cold-only; every supported candidate is subject-bound before completion; failure cases stay incomplete; only the bounded result and success timestamp cross the boundary. Exact-head tests and Chromium/Firefox `file://` verification pass. |
| 2 | `*Deps: P2.5*` | PASS | P2.5 is complete on the reviewed base history and its recovery paths remain green in the full test/browser suites. |

## 4. Findings

None.

## 5. Verdict rationale

P2.6 passes at exact reviewed commit `b2c2d04ccb3fd4565a4ea281faf0d5fad68799a3`. R3-F1 is fixed: reconstruction success alone is no longer sufficient to set completion; the cold realm uniquely resolves and binds the intended subject before `lastVerifiedAt` can change, with wrong-subject and unresolved cases failing closed. The prior R1/R2 findings remain remediated, the exact-tip packet evidence reproduces, all acceptance criteria are met, and I have no findings of any severity.

PASS
