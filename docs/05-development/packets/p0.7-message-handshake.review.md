# Review: P0.7 - MessageChannel handshake and schema validator

**VERDICT: PASS**

Findings: 0
Reviewed commit: 0d01e69
Reviewed by: Codex self-review gate
Date: 2026-08-03

## 1. What I verified

The review protocol was reread in full. The committed branch was clean before review, `git diff --check origin/p0.6-cold-realm-bootstrap...HEAD` passed, and the complete dependency-base diff contains one roadmap item: P0.7. The P0.7 acceptance wording was checked verbatim against `docs/05-development/ROADMAP.md`, and the complete diff was read.

Fresh independent verification from commit `0d01e69`:

- `npm ci` - 2 packages installed, 0 vulnerabilities.
- `npm run verify-vendor` - all six local vendor artifacts and all six upstream releases verified.
- `npm run lint` - passed.
- `npm test` - 22 passed, 0 failed.
- Two builds, with the generated `build/` directory removed between them, both produced `25798ab6cd40edd34dd73ad2b7b3f02a2d38369c786ca45548b2a160237d047a`.
- Final artifact SHA-256 - `25798AB6CD40EDD34DD73AD2B7B3F02A2D38369C786CA45548B2A160237D047A`; final size 75,654 bytes.
- A clean copy at a different temporary path with `LC_ALL=ja-JP` and `TZ=Asia/Tokyo` produced the same artifact hash and size.
- A deliberate temporary `eval` source fixture made the build exit with status `1` and report `Forbidden construct "eval"` plus `Build refused: source failed forbidden-construct lint`.
- `npm run test:browser` passed in Chromium and Firefox. It verified the exact opaque sandbox, cold `connect-src 'none'` policy, blocked cold network primitives, parent opacity, route/theme/responsive behavior, fail-closed iframe creation, and script/CSP tamper checks.
- The same browser run waited for `#app[data-handshake-state="ready"]`, injected a global message into the warm shell and another into the cold frame after handshake, and observed one fixed warning plus one anomaly counter in each realm. No injected message was routed through the private protocol.
- Protocol unit tests enumerated every documented warm-to-cold and cold-to-warm type, rejected unknown types, stripped unknown fields, and recursively confirmed that secret-bearing fields did not survive any cold-to-warm type.
- The built artifact retains `sandbox="allow-scripts allow-downloads"`, omits `allow-same-origin`, and exposes the cold `connect-src 'none'` policy as asserted by both browser engines.

## 2. What I could not verify

None for the P0.7 automated acceptance criteria. Physical-device behavior on iOS Safari, Android Chrome from Files, macOS Safari, Linux Firefox, and Tor Browser remains untested and is explicitly owned by P0.19; no result is inferred from the headless runs.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Handshake transferring a port; typed whitelist schema per [architecture.md](../../01-spec/architecture.md); global `message` handler ignored after handshake. | ✅ | The warm shell creates a `MessageChannel`, transfers `port2` in one exact handshake message after the frame's payload-free ready signal, and accepts the typed ready response only on `port1`. Both realms discard and log later global traffic; Chromium and Firefox reached the private-channel ready state. |
| 2 | Schema rejects unknown types and strips unknown fields. | ✅ | `test/protocol.test.js` rejects unknown types and verifies that unknown envelope, payload, collection, and public-record fields are absent from the sanitized result. |
| 3 | A test asserts **no message type can carry a mnemonic, private key, xprv, passphrase, or secret-compartment plaintext**. | ✅ | The protocol test supplies prohibited fields to every cold-to-warm message type and recursively asserts that none survive; the allowlist contains only documented public values and ciphertext bytes. |
| 4 | Messages injected on the global handler post-handshake are discarded and logged. | ✅ | The browser harness injects one global message into each realm after handshake and asserts each realm's anomaly marker and fixed warning text in Chromium and Firefox. |

## 4. Findings

None.

## 5. Verdict rationale

The implementation meets every P0.7 roadmap criterion, preserves the opaque cold boundary and `connect-src 'none'` policy, rejects unknown or secret-bearing protocol shapes, strips unknown fields, and proves post-handshake global-message discard behavior in both available browser engines. The committed artifact is reproducible across clean builds and a different path/locale/timezone, the build fails non-zero on a deliberate forbidden construct, all dependency and unit checks pass, and no finding of any severity remains. The verdict is PASS.
