# P1.11 self-review — batch gate

- Branch: `p1.11-address-provenance`
- Base: `main`
- Product commit reviewed: `241b170`
- Review round: 1
- Verdict: **PASS**

## Acceptance review

- Previous schema and schema-less public compartments open through the protocol sanitizer and migrate to schema 2.
- Legacy addresses become explicitly `manual` and `unverified`; no state is inferred from an address string or record shape.
- New registry addresses default to `manual` and `unverified`.
- An account xpub change automatically moves related `cold-verified` records to `cold-verified-stale`.
- Stale records retain their evidence and the Registry renders them as `Cold verification stale`, never `Cold verified`.
- The Registry visibly lists never-verified addresses with a count and per-record `Never verified` label.

## Tests and gates

- Focused protocol/registry tests: 26 passed, 0 failed.
- Full `npm.cmd test`: exit 0, green.
- `npm.cmd run verify-vendor`: passed locally and against upstream releases.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 0 warnings.
- Two builds: identical SHA-256 `ef5f453895c20fa92d3200be4cd980fa235beb4b1e0b2522bd8f50cb8691c55f`.
- Browser harness: completed successfully for the existing Chromium file://, cold-boundary, Registry, lock, and responsive flows.

## Self-assessment

The main risk is that future cold verification code must preserve the invariant that `cold-verified` is only written with actual re-derivation evidence. This branch enforces the data shape and stale transition but intentionally does not implement that event. No known acceptance gap remains for P1.11.

## Gate decision

**PASS — ready for independent review.** Open the PR against `main`, then continue to P1.12 only after this branch is pushed and the PR is open. Do not mark the roadmap item `[x]` and do not merge.
