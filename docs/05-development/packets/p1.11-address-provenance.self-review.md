# P1.11 self-review - R3 remediation gate

- Branch: `p1.11-address-provenance`
- Base: `main@a0edef3b271c2ae0d61fec9718ba6a8577d19e6b`
- Reconciliation commit: `428ac2a1733c869816cc5928527ce0b57a6945cc`
- Product/remediation commit: `68ad6411739e685e8e4676d5bcd1af1bc70ca26a`
- Independent R1 review commit: `ad5a2e02299cf3f8802ad46003915301d56d9c4c`
- Independent R2 review commit: `050867784295bc4455ae2014b3c2bfebd5a6a5b8`
- Independent R3 review commit: `a1cebd8317e3855a289c8fe368b7b02256704e63`
- Accumulated R1+R2+R3 report SHA-256: `F25CF604EA1F509278C1A72F8A89CFCD70E8F0C9821198BD82FA7D3320CEEC16`
- Review round: R3 remediation
- Verdict: **PASS candidate**; independent review is still required
- Roadmap status: `[~]`

## R1 findings addressed

- F1: `publicData.replace` remains warm-originated, and the cold session now refuses warm elevation to `cold-verified`/`cold-verified-stale` while preserving authenticated evidence and allowing the existing xpub-change stale transition.
- F2: cold vault `open`, `openSession`, and session replacement perform actual schema-1/missing-schema migration; save/reopen persists schema 2; invalid migration rejects without mutating the source bytes.
- F3: the committed Chromium/Firefox browser harness opens a real stale-state fixture and asserts `Cold verification stale` is not `Cold verified`.

## R2 findings addressed

- R2-F1: the cold-owned `publicData.replace` reconciliation now compares the authenticated address evidence with both the current and replacement account xpub. Any changed or mismatched xpub forces `cold-verified-stale` and preserves the authenticated `lastColdVerifiedAt` and `verifiedAgainstXpub`; the direct vault regression leaves the warm request at `cold-verified` to prove the cold side owns the transition.
- R2-F2: the exact roadmap marker `- [~] **P1.11 Address verification state in the data model**` is restored above the existing dependency, description, and acceptance text.

## R3 findings addressed

- R3-F1: the cold-owned reconciliation now rejects a warm `cold-verified-stale` request when authenticated evidence still matches the current and replacement account xpub. It preserves `cold-verified`, `lastColdVerifiedAt`, and `verifiedAgainstXpub`; the paired vault regression also proves a genuine xpub change still forces stale.

## Acceptance review

- Legacy public compartments open as schema 2 with `manual`/`unverified` address defaults.
- Verification is never inferred from warm public replacement or self-supplied evidence.
- Existing authenticated verification evidence is retained; xpub changes stale it automatically.
- The cold authority enforces xpub staleness even when a warm replacement omits the stale transition.
- The cold authority rejects warm stale downgrades when no xpub mismatch exists and retains the authenticated evidence.
- Stale display behavior has a committed cross-browser regression assertion.
- P1.11 remains exactly one roadmap item on this branch, and the marker remains `[~]`.

## Gates

- `git diff --check`: PASS.
- `npm.cmd test`: 303 passed, 0 failed.
- `npm.cmd run lint`: PASS.
- `npm.cmd run verify-vendor`: PASS locally and against upstream releases.
- `npm.cmd run check-docs`: PASS, 172 Markdown files, 0 warnings.
- Two builds: identical SHA-256 `c14cb437fb6d057e748bbaa0cfd19707dc627c58ba3371a559c65f351e0f86b4`.
- `npm.cmd run test:browser`: PASS in Chromium and Firefox, including the stale-label regression.

## Review-history integrity

The accumulated R1+R2+R3 FAIL report was not edited. Its SHA-256 is `F25CF604EA1F509278C1A72F8A89CFCD70E8F0C9821198BD82FA7D3320CEEC16`; its Git blob is `fc876ab9d80d3ab43362b22dcab6f5d375f2b24c`.

## Remaining scrutiny

The next reviewer should independently inspect the cold authority reconciliation ordering: genuine xpub mismatch must force stale, while a matching-xpub warm stale request must preserve authenticated verified state and evidence. The actual cold re-derivation event is intentionally not part of P1.11 and remains for P1.12.

## Gate decision

**PASS candidate - push normally, synchronize PR #44 to the exact new head, and request a fresh independent R4 review. Do not merge. Do not begin PR #45.**
