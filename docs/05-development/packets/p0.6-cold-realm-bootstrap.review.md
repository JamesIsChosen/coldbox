# Review: P0.6 — Cold realm bootstrap

**VERDICT: PASS**

Findings: 0
Reviewed commit: 70eaeef
Reviewed by: Codex self-review gate
Date: 2026-08-03

## 1. What I verified

The review protocol was reread in full. The branch was clean before and after the review, and `git diff --check origin/p0.5-warm-shell-skeleton...HEAD` passed. The complete branch contains one roadmap item: P0.6.

Fresh independent verification from commit `70eaeef`:

- `npm ci` — 2 packages installed, 0 vulnerabilities.
- `npm run verify-vendor` — all six local vendor artifacts and all six upstream releases verified.
- `npm run lint` — passed.
- `npm test` — 18 passed, 0 failed.
- Two builds, with the generated `build/` directory removed between them, both produced `29075398cd57c8f2e3448a23aff5ea6674880647a6f6cdcf32686d787479e71d`.
- Final artifact SHA-256 — `29075398CD57C8F2E3448A23AFF5EA6674880647A6F6CDCF32686D787479E71D`; final size 45,928 bytes.
- `npm run test:browser` — Chromium and Firefox passed cold `fetch`, XHR, and WebSocket blocking, child CSP hash checks, parent opacity, responsive shell checks, and explicit iframe-creation lockdown.
- A clean copy at a different temporary path with `LC_ALL=ja-JP` and `TZ=Asia/Tokyo` produced the same artifact hash.
- A deliberate temporary eval source break made the build return exit code `1` and report `Forbidden construct "eval"` plus `Build refused: source failed forbidden-construct lint`.
- The built artifact contains `sandbox="allow-scripts allow-downloads"`, the cold `connect-src 'none'` policy, and no `allow-same-origin` token.
- The only message sender in the changed source is the payload-free `{ type: 'cold.ready' }` bootstrap signal; no mnemonic, private key, xprv, passphrase, or secret-compartment field is carried by it.

## 2. What I could not verify

Nothing required for the P0.6 automated acceptance remained unverifiable in the available environment. Physical-device confirmation is explicitly deferred to P0.19 and is not inferred from this review.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `srcdoc` iframe with `sandbox="allow-scripts allow-downloads"` and its own CSP including `connect-src 'none'`. | ✅ | The built artifact and both browser engines confirm the exact sandbox, child CSP, and child inline hashes. |
| 2 | **Accept:** iframe instantiates; `fetch`, `XHR`, and `WebSocket` inside it throw; warm shell cannot read its DOM or variables; **app fails closed with an explanation if the iframe cannot be established.** | ✅ | Chromium and Firefox report all three primitives blocked, reject parent DOM access, and show the locked-down explanation when iframe creation is deliberately made to throw. Browser-specific CSP blocking may surface as a rejected promise or error event; no request is allowed. |

## 4. Findings

None.

## 5. Verdict rationale

The implementation meets both roadmap criteria, preserves the documented warm allowlist, establishes the tested opaque cold boundary with `connect-src 'none'`, rejects secret-bearing message shapes by construction because the only provisional message is payload-free, remains reproducible across clean builds and environment variation, and fails with a non-zero status when a forbidden construct is introduced. The fresh review has zero findings, so the verdict is PASS. P0.7 remains the required next security item for replacing the provisional readiness signal with the typed MessageChannel protocol.
