# Review: P2.4 — Shamir39 and raw SSS

**VERDICT: FAIL**

Findings: 4 (4 blocking, 0 advisory — all must be addressed)
Reviewed commit: `3d346a1161b5ce6f410b70a9dcb12edc4f7510ff`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-12
Independent evidence bundle SHA-256: `31c56955b824d265deee00d7da2a1a7b2b831c1a56ce64335adea4f43571abe8`

## 1. What I verified

Fresh independent review used a full-history clone and separate detached worktrees. For this item I verified the exact product head and merge base, `git diff --check`, `npm ci`, `npm run verify-vendor`, `npm run lint`, `npm run check-docs`, the focused item test, the full `npm test` suite, two same-worktree builds, and a third build from a different detached path/timezone/locale. Vendor-manifest corruption was also exercised and correctly made `verify-vendor` exit non-zero. The cold CSP probe retained `connect-src 'none'`, found no `allow-same-origin`, and found no storage/network/warm-message surface in the item module.

The prior build-reproduction mismatch is resolved. All three fresh builds are exactly `a0f42eda650c8f7fdb4d9e4fd13e4e8671ae6d6d8380e65e2ec64f3c89f0ebba`, 2,262,395 bytes, matching the packet across the different path/TZ/locale build.

The focused Shamir suite and full suite exited 0. The complete browser harness exited 0 and produced item-specific PASS lines in Chromium and Firefox for Shamir39/raw SSS formatting, masking, reconstruction, negative cases, warm isolation, teardown, and the existing security matrix.

I independently re-fetched the exact pinned Ian Coleman and secrets.js sources. Their Git blob identities match the packet:
- Ian `specification.md`: `8be52ecd5d7700bf68086dccff7851dee3670074`
- Ian `src/js/shamir39.js`: `4b0aae2cc63ac588326037e1718f7d888c21d269`
- secrets.js `README.md`: `a4f1b45a96de9ab9c6a86f6927d3657b417cb643`
- `secrets.js`: `2eb1360d61d99f5cee46ebb2aaf1f938b065069c`
- `spec/secrets/SecretsSpec.js`: `8986699144de4d25623217ac4377f85a4042f945`

The packet itself correctly preserves the coefficient-distribution issue as an unresolved cryptographic stop; I independently reproduced it mathematically as described in F1.

## 2. What I could not verify

I could not verify the claimed information-theoretic below-threshold secrecy because the implemented zero-rejecting coefficient distribution contradicts that claim (F1). I also could not reproduce the packet's stated SHA-256 values from the exact pinned upstream bytes (F3).

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.4 Shamir39 and raw SSS` | ❌ | Functional/vector tests, reproducible builds, and both browsers pass, but the coefficient distribution does not support the intended below-threshold secrecy claim (F1), the guide still states that claim (F2), exact-byte provenance hashes are wrong (F3), and the build gate accepts executable `Math.random()` in the cold module (F4). |

## 4. Findings

### F1 — Zero-rejecting nonconstant coefficients leak below-threshold information

**Severity:** blocking cryptographic design
**Location:** `src/cold/shamir.js:195-198` and coefficient construction around `src/cold/shamir.js:253-256`.
**Observed:** `randomFieldElement()` repeatedly samples until the field value is non-zero, and every nonconstant polynomial coefficient uses it. For threshold 2 over GF(2^3), with observed `x=1`, `y=5`, restricting `a` to 1..7 makes possible secrets `0,1,2,3,4,6,7`; secret `5` is impossible because it would require `a=0`. One share therefore excludes one field value. The information reduction is `log2(8/7) ≈ 0.192645` bits per GF(8) segment in this minimal demonstration. The packet independently acknowledges that the distribution is not the full uniform coefficient space and that intended information-theoretic secrecy cannot honestly be claimed.
**Expected:** If P2.4 claims threshold secrecy in the ordinary information-theoretic sense, fewer than threshold shares must not narrow the secret distribution through an avoidable coefficient-space restriction.
**Required action:** Obtain the maintainer cryptographic design decision already identified in the packet, record it in the ADR, implement the chosen compatibility/secrecy policy, and add independent vectors/tests for that exact policy. Do not casually change the sampler without resolving compatibility and degree semantics.

### F2 — User guide still makes unconditional below-threshold secrecy claims contradicted by the current implementation

**Severity:** blocking documentation/security
**Location:** `docs/03-guides/backup-shamir.md:11` and `:104`.
**Observed:** The guide says “fewer than the threshold reveal nothing useful” and “The threshold property protects the secret below threshold,” while the packet correctly states that the current zero-rejecting distribution cannot honestly claim the intended information-theoretic secrecy.
**Expected:** User-facing security documentation must not promise a secrecy property the current implementation does not establish.
**Required action:** Resolve F1 first, then make the guide accurately state the resulting verified property and limitations. Until the cryptographic decision is made, remove or qualify the unconditional secrecy language.

### F3 — Pinned upstream SHA-256 provenance does not reproduce from the exact upstream bytes

**Severity:** blocking provenance
**Location:** P2.4 packet provenance table and corresponding dependency/ADR provenance statements.
**Observed:** Fresh exact-byte downloads at the pinned commits produce:
- Ian `specification.md`: `979d15d588adf80b27d515dbaf97a8a9f97766395289632142bef78614f77c62` (recorded: `86ca41da920644d09857c15cd00faba3824917cf6475391474bf12df861da55a`)
- Ian `src/js/shamir39.js`: `a1f822fe010d5ddbf9b33bda0eaf5152388e8700d5e35893fb8f85116ed4233c` (recorded: `0d20b48116ee4a41fbf13b323c72237252b9ac74b2a0da2e8124a222d4420564`)
- secrets.js `README.md`: `56d52d02a32735a5858bf7e6ffb2b95544c6a761906a4594cd438ffbbf125914` (recorded: `40d9f61be4495516e378bd832da48da98227eb319ec73ceddf4755a73ef1ff1e`)
- `secrets.js`: `6c90ec0b0d88a8c90d08f8657448c72db6592fcec5096306c70c815e2404eee9` (recorded: `c790e06716c7608eea706d96c63dd1c665dd3578d71a9162ebe3e28ce69db411`)
- `SecretsSpec.js`: `b6f843bc4c40f268c175b0c49564fb5be43e1187e4216c33efd6b3559040db0f` (recorded: `870cbd1be4495516e378bd832da48da98227eb319ec73ceddf4755a73ef1ff1e`)

The recorded values for the Ian files, `secrets.js`, and `SecretsSpec.js` reproduce after LF→CRLF transformation, not from the raw pinned GitHub bytes. The recorded secrets.js README value matches neither the raw bytes nor that CRLF normalization. The Git blob SHA-1 identities listed above are correct.
**Expected:** Exact SHA-256 provenance must identify the exact upstream bytes and be reproducible without an undocumented platform newline rewrite.
**Required action:** Correct all SHA-256 provenance fields to a documented exact-byte convention, fix the README hash independently rather than copying a neighboring value, and add/re-run exact-byte provenance verification while preserving the already-correct Git blob identities.

### F4 — Build/lint gate fails open to executable `Math.random()` in Shamir security code

**Severity:** blocking security
**Location:** `scripts/lint.js` `scanSourceFile()`; exercised path `src/cold/shamir.js`.
**Observed:** Appending `void Math.random();` to `src/cold/shamir.js` and running `npm run build` exited **0**. The original bytes were restored exactly. The `Math.random` rule in `scripts/lint.js` is scoped only to `src/capabilities.js`, not the cold secret-handling tree.
**Expected:** Executable `Math.random()` in any seed/share security path must make the build fail non-zero.
**Required action:** Extend the lint/security gate over all relevant cold secret-handling modules, add a non-zero regression test, and rerun the independent negative gate.

## 5. Verdict rationale

P2.4's functional tests, both browsers, build reproducibility, and Git blob provenance identities are solid, and the packet correctly acknowledges the central crypto stop. It nevertheless has four blocking findings: the zero-rejecting coefficient distribution leaks below-threshold information, the guide still overclaims secrecy, the exact-byte SHA-256 provenance is wrong, and the build gate accepts executable `Math.random()` in the Shamir security module. P2.4 remains `[~]`; the Phase 2 batch must remain stopped before P2.5–P2.8.

FAIL
