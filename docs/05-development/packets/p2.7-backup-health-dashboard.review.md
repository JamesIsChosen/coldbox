# Review: P2.7 - Backup Health dashboard

**VERDICT: FAIL**

Findings: 5
Reviewed commit: `e25a41437d0823b536e38b48e76d6064673300dc`
Reviewed by: GPT-5.6 Sol
Date: 2026-08-14

## 1. What I verified

PR #54 is based on `main@cf476a1d18c123094cb71d6bd6b49f0b9e787822` and the
reviewed implementation tip is `e25a41437d0823b536e38b48e76d6064673300dc`.
The PR is scoped to P2.7 and does not modify `src/protocol.js`, cold-realm
source, or the vault-format implementation.

I read the complete PR diff, the focused Backup Health evaluator and tests,
dashboard/browser wiring, ADR-0042, the new Backup Health guide, the related
security/specification documentation, `AGENTS.md`, and
`docs/05-development/review-protocol.md`.

The conservative placement boundary is correct. Public location/custodian
labels are not treated as proof that individual shares are physically
independent or that a threshold survives a loss. Different recorded
locations are described as `distributed, threshold-unproven`, consistent with
the public data-model limitation.

The exact-head GitHub Actions run for `e25a41437d0823b536e38b48e76d6064673300dc`
succeeded and reported:

- vendor verification against upstream passed;
- lint passed;
- documentation hygiene passed with 207 Markdown files and 0 warnings;
- full tests passed 378/378;
- Chromium and Firefox `file://` harness passed;
- Ubuntu and Windows builds matched byte-for-byte;
- exact-head CI artifact SHA-256 was
  `7173ea3c380ceb139313b17473c9053294140fea06f68db469d478b5e17e51c2`;
- Ubuntu reported 2,597,137 bytes.

Two evaluator edge cases were independently reproduced using the JavaScript
date semantics used by `src/backup-health.js`.

## 2. What I could not independently verify

The mandatory fresh-checkout command sequence could not run because the review
container could not resolve `github.com`; the attempted clone failed with
`Could not resolve host: github.com`.

Therefore the reviewer could not independently execute `npm ci`, the complete
vendor/lint/test sequence, required builds under another checkout/path/timezone/
locale, deliberate build-wiring corruption, or the real Chromium/Firefox
harness from a fresh checkout.

Physical/mobile `file://` execution was also unverified. `AGENTS.md` required
at least one desktop and one mobile browser, while the packet marked iOS and
Android untested.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | NO | Dashboard and conservative placement language exist, but valid time metadata could become `current` or throw; mobile and independent execution were incomplete. |
| 2 | `*Deps: P2.6*` | YES | PR was based directly on the P2.6 merge and retained the BackupRecord/cold-verification boundary. |

## 4. Findings

### F1 - Exact-tip reproducible-build evidence in the packet is stale

**Severity:** blocking
**Location:** `docs/05-development/packets/p2.7-backup-health-dashboard.md` section 11 and PR body

The packet reported `2e3d73d6116ee9dfb221a96454648144b94bed165af951ed15f43aebdb14b9b9`
and 2,597,154 bytes, while exact-head CI for the reviewed commit reported
`7173ea3c380ceb139313b17473c9053294140fea06f68db469d478b5e17e51c2` and
2,597,137 bytes. P2.7 guide content is compiled into the artifact, so
pre-final-doc evidence was not evidence for the reviewed tip.

**Required action:** After all remediation is complete, rerun final-tip builds
and update the packet with the exact SHA-256 and byte count produced from the
exact commit handed to the next reviewer.

### F2 - A future cold-verification timestamp is classified as `current`

**Severity:** blocking
**Location:** `src/backup-health.js` - `evaluate()`

The evaluator calculated `dueAt = verifiedAt + interval * DAY_MS` and classified
`current` whenever `now < dueAt`, without rejecting `verifiedAt > now`.

**Required action:** Detect `lastVerifiedAt > now` before current/overdue
calculation and fail closed to `invalid`/needs-review, or introduce an
explicitly designed clock-skew state. Add future-time and exact-boundary tests.

### F3 - Protocol-valid timestamp data can throw the evaluator

**Severity:** blocking
**Location:** `src/backup-health.js` - `isoDate()` / `evaluate()`

The canonical maximum JavaScript Date,
`+275760-09-13T00:00:00.000Z`, passed protocol validation, but adding a
one-day interval produced a finite number outside the representable Date range
and `toISOString()` threw `RangeError: Invalid time value`.

**Required action:** Check calculated due-date representability before
formatting or comparison. Overflow must return invalid/needs-review without an
exception, with a regression test.

### F4 - Required mobile `file://` testing is not complete

**Severity:** blocking
**Location:** `AGENTS.md` section 5 and the P2.7 packet device matrix

The packet recorded Playwright Chromium and Firefox on Windows but marked iOS
and Android untested. Responsive viewport emulation is not physical-mobile
evidence, and no explicit P2.7 waiver or deferment had been recorded.

**Required action:** Run P2.7 on a supported mobile browser through the local
file path, or make an explicit repository/user policy decision that defers or
alters this requirement before treating the deferral as non-blocking.

### F5 - Mandatory independent execution was unavailable

**Severity:** blocking
**Location:** `docs/05-development/review-protocol.md`

The required fresh checkout, command reruns, environment variation, deliberate
breakage, and independent build reproduction could not run because the review
environment could not clone GitHub. CI corroboration was not a substitute.

**Required action:** The next independent review must run in an environment
with checkout, dependency, and browser access and perform the complete review
protocol. No production-code change is required specifically for F5.

## 5. Verdict rationale

P2.7 could not PASS at `e25a41437d0823b536e38b48e76d6064673300dc`: F2 and F3
were functional defects, F1 made the artifact evidence stale, F4 left a
repository DoD requirement unresolved, and F5 prevented independent
reproduction.

**FAIL**

---

# Review round 2: P2.7 - Backup Health dashboard

**VERDICT: FAIL**

Findings: 4 open (F1, F4, F5, and the missing review-history artifact)
Reviewed commit: `0639697153b21b0c8e8220471e33d5dc19a7eda9`
Reviewed by: independent reviewer (verdict supplied by maintainer)
Date: 2026-08-14

## 1. What I verified

The remediation round closed F2 and F3. The supplied verdict stated that F1,
F4, F5, and the missing `.review.md` history artifact remained open. The
reviewer required the packet's section 11 evidence to be refreshed against the
final exact tip after all documentation was complete.

## 2. What I could not independently verify

The supplied verdict did not include a command transcript for a usable fresh
checkout. A subsequent review must run the complete independent protocol from
an environment that can clone the repository, install/use dependencies, run
the browser harness, and perform deliberate-breakage checks.

The physical mobile run was still absent at this round. The prior packet's
user statement that testing was deferred was not yet an explicit repository
policy exception.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | NO | F1, F4, F5, and the missing review-history artifact remained open. |
| 2 | `*Deps: P2.6*` | YES | No finding reopened the P2.6 dependency or public/cold verification boundary. |

## 4. Findings

### F1 - Section 11 evidence was not refreshed against the final exact tip

**Required action:** Complete all policy, packet, and review-history changes,
run the build twice from the exact final tip, and replace section 11 and the PR
body evidence with the resulting hash and byte count.

### F4 - Mobile deferral had no repository-policy basis

**Required action:** Either perform physical mobile `file://` testing or add an
explicit, narrowly scoped repository policy that makes P2.7's deferral
non-blocking while keeping the release device matrix and P0.19 gate intact.

### F5 - The next review still needs an independent execution environment

**Required action:** Request a fresh review from an environment that can run
the full checkout, dependency, browser, environment-variation, and deliberate-
breakage command sequence. No authoring change can substitute for reviewer
independence.

### F6 - The review-history artifact was missing

**Required action:** Commit this `.review.md` file alongside the packet,
preserving the earlier FAIL and appending each later verdict instead of
overwriting history.

## 5. Verdict rationale

The remediation round closed the two evaluator defects, but the packet still
needed exact-tip evidence, a policy basis for the maintainer's mobile
deferral, a persisted review history, and a fresh independent review
environment. The next reviewer must append a new verdict; this file is not a
PASS and does not authorize a merge.

**FAIL**

---

# Review round 3: P2.7 - Backup Health dashboard

**VERDICT: FAIL**

Findings: 2 open (F1 and F5)
Reviewed commit: `11b29f45ffa775c24a5ef1618d35892b7aa391f3`
Reviewed by: independent reviewer (handoff supplied by maintainer)
Date: 2026-08-14

## 1. What I verified

F2, F3, F4, and F6 were closed. The clean CI build for the exact tip produced
SHA-256 `73ce748f871166f717de4c22d31dcb4c6b8d048337a0eea78f1e4a7b676aafc1`
and 2,597,939 bytes, while section 11 and the PR evidence still claimed
`4d1a9235...` and 2,597,956 bytes.

## 2. What I could not independently verify

F5 remained open: the independent reviewer execution still needed a genuinely
usable fresh checkout and the complete command sequence, including dependency
access, browser execution, environment variation, deliberate breakage, and
independent build reproduction.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | NO | F1 exact-tip artifact evidence and F5 independent execution remained open. |
| 2 | `*Deps: P2.6*` | YES | The P2.6 dependency and public/cold verification boundary remained intact. |

## 4. Findings

### F1 - Exact-tip artifact evidence does not match clean CI

**Required action:** Reproduce the build from a genuinely clean checkout of
exact tip `11b29f45ffa775c24a5ef1618d35892b7aa391f3`, determine the source of
the mismatch, correct section 11 and the PR evidence, and show matching builds.

### F5 - Independent reviewer execution remains unavailable

**Required action:** Request another fresh independent review from an
environment that can run the full review protocol. No author-side change can
substitute for reviewer independence.

## 5. Verdict rationale

The exact-tip artifact evidence was still stale relative to clean CI, and the
independent reviewer could not yet perform the required execution sequence.
P2.7 remains `[~]` and the PR must not merge until a fresh review passes.

**FAIL**

---

# Review round 4: P2.7 - Backup Health dashboard

**VERDICT: FAIL**

Findings: 1 open (F5)
Reviewed commit: `6a9c9ab985d72c889b804b5eb18ed4b1a31bbc81`
Reviewed by: independent reviewer
Date: 2026-08-14

## 1. What I verified

F1 was closed by the clean Git-enabled artifact correction. The corrected
artifact was SHA-256
`73ce748f871166f717de4c22d31dcb4c6b8d048337a0eea78f1e4a7b676aafc1`,
2,597,939 bytes. F2, F3, F4, and F6 remained closed.

Hosted exact-head CI also passed its Ubuntu and Windows builds, cross-OS hash
comparison, and Chromium/Firefox browser harness.

## 2. What I could not independently verify

F5 remained open in this review environment. A separate fresh clone attempt
failed before checkout because `github.com` could not be resolved, so the
reviewer could not personally execute the mandatory fresh-checkout dependency,
vendor, lint, test, alternate-environment build, deliberate-breakage, and
browser sequence.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | NO | F5 still prevented the required independent execution. |
| 2 | `*Deps: P2.6*` | YES | The P2.6 dependency and public/cold verification boundary remained intact. |

## 4. Findings

### F5 - Independent reviewer execution remains unavailable

**Severity:** blocking
**Location:** `docs/05-development/review-protocol.md`

The required fresh checkout and command sequence could not execute in the
reviewer's own environment because GitHub DNS resolution failed.

**Required action:** Run the complete independent protocol through the
browser-runner workflow from a usable fresh checkout/browser environment and
route the resulting evidence back to the independent reviewer.

## 5. Verdict rationale

The implementation findings were closed, but the repository review protocol
still required independent fresh-checkout execution before P2.7 could pass.
P2.7 therefore remained `[~]` and merge remained prohibited.

**FAIL**

---

# Review round 5: P2.7 - Backup Health dashboard

**VERDICT: FAIL**

Findings: 2 open (F7 and F8)
Reviewed commit: `6a9c9ab985d72c889b804b5eb18ed4b1a31bbc81`
Reviewed by: independent reviewer
Date: 2026-08-14

## 1. What I verified

The browser-runner workflow closed F5 from a genuinely fresh independent
checkout at exact commit
`6a9c9ab985d72c889b804b5eb18ed4b1a31bbc81` with Node `v24.16.0`.

The independent run verified:

- the remote PR branch still resolved to the exact reviewed commit;
- the fresh checkout started clean and `cf476a1d18c123094cb71d6bd6b49f0b9e787822`
  was an ancestor of the reviewed head;
- `git diff --check` passed and the full PR diff was captured and reviewed;
- `npm ci` passed;
- `npm run verify-vendor` matched the real upstream packages;
- `npm run lint` passed;
- `npm run check-docs` passed with **209 Markdown files, 0 warnings**;
- focused Backup Health tests passed 5/5;
- the complete test suite passed 378/378;
- two primary builds were byte-identical at SHA-256
  `73ce748f871166f717de4c22d31dcb4c6b8d048337a0eea78f1e4a7b676aafc1`,
  2,597,939 bytes;
- Chromium and Firefox passed the real built `file://` browser harness;
- the canonical vendored package
  `vendor/npm/@fontsource/bangers/5.3.0/package.tgz` was deliberately mutated,
  its SHA-256 changed, and `npm run verify-vendor` exited non-zero with a vendor
  SHA-256 mismatch;
- restoring that package restored its exact original SHA-256 and upstream
  verification passed again;
- a second fresh checkout in a different path, with `TZ=Pacific/Kiritimati`,
  `LANG=fr_FR.UTF-8`, and `LC_ALL=fr_FR.UTF-8`, produced two byte-identical
  builds with the same expected artifact hash and size;
- both temporary clones ended clean;
- PR #54 remained open and unmerged and hosted checks were green.

The prior findings F1, F2, F3, F4, F5, and F6 are therefore closed.

## 2. What I could not independently verify

No additional item-level execution gap remains. Physical iOS/Android execution
is still recorded as `DEFERRED`, not PASS, under the explicit P2.7-only
ADR-0043 policy; that deferral does not close the release device matrix or
P0.19.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | NO | Runtime behavior and independent execution pass, but F7 and F8 leave the tracked packet/PR evidence stale and incorrectly labelled. |
| 2 | `*Deps: P2.6*` | YES | Fresh-clone ancestry and the full regression suite preserve the P2.6 dependency and public/cold verification boundary. |

## 4. Findings

### F7 - Verification transcript and PR body still claim 207 Markdown files

**Severity:** advisory, must fix
**Location:** `docs/05-development/packets/p2.7-backup-health-dashboard.md`
section 3 and PR #54 body

**Observed:** The exact reviewed checkout reports:

`Documentation hygiene check passed: 209 markdown file(s) checked, 0 warning(s).`

The tracked packet and mirrored PR body still report **207** Markdown files.

**Expected:** Evidence text must match the exact current repository state.
`review-protocol.md` explicitly treats a stale number in documentation as a
defect.

**Required action:** Refresh section 3 and the PR body to the exact current
`check-docs` result. Re-run the command after remediation and use that result
rather than carrying forward the older 207-file transcript.

### F8 - The packet calls parent commit 11b29f45 an "exact branch tip"

**Severity:** advisory, must fix
**Location:** `docs/05-development/packets/p2.7-backup-health-dashboard.md`
sections 11 and 13, and mirrored PR #54 evidence

**Observed:** The reviewed branch tip is
`6a9c9ab985d72c889b804b5eb18ed4b1a31bbc81`, but section 11 says it was
"Measured from a genuinely clean clone of the exact branch tip" and labels
`11b29f45ffa775c24a5ef1618d35892b7aa391f3` as the "exact tip". Section 13
repeats the same description.

The artifact hash itself is independently reproduced and correct. The defect is
the provenance label: `11b29f45...` is the parent candidate whose evidence was
then committed by `6a9c9ab...`, not the exact current branch tip.

`docs/05-development/browser-runner-flow.md` expressly says a tracked packet
must not call a parent-commit run "current-tip" evidence and that final
exact-tip provenance is established by closeout verification and fresh
independent re-review.

**Expected:** The packet must distinguish author candidate/product-input
evidence from independent exact-reviewed-tip evidence without making a
self-referential SHA claim.

**Required action:** Rewrite sections 11 and 13, and the mirrored PR body, so
`11b29f45...` is accurately described as the committed candidate/product-input
revision used for the author's clean evidence, not as the current/exact branch
tip. Keep the independently reproduced `73ce748f...` artifact evidence, and
leave current-tip verification to the independent review/closeout record as
the repository workflow requires.

## 5. Verdict rationale

F5 is closed and the implementation/runtime evidence is independently clean,
but the tracked packet and PR body still contain one stale numeric claim and
one prohibited parent-as-current-tip provenance label. Under this repository's
binary review rule, either documentation defect is sufficient for FAIL.
P2.7 remains `[~]` and PR #54 must not merge. A fresh review is required after
F7 and F8 are corrected.

**FAIL**

---

# Review round 6: P2.7 - Backup Health dashboard

**VERDICT: PASS**

Findings: 0
Reviewed commit: `2cf42d19b87064a908154803e9abf594aaf78a15`
Reviewed by: GPT-5.6 Sol (independent reviewer)
Date: 2026-08-14

## 1. What I verified

I performed a fresh independent review of PR #54 at exact commit
`2cf42d19b87064a908154803e9abf594aaf78a15`.

The Round-6 remediation lineage is exact: the reviewed commit is one commit
after reviewer-history head `ed28c24350ee2038011c1916d3e370dfac115f1f`,
the author remediation changes only
`docs/05-development/packets/p2.7-backup-health-dashboard.md`, and the
reviewer-owned `.review.md` history is unchanged by the author remediation.

F7 and F8 are closed. The packet and synchronized PR body now record
`209 markdown file(s) checked, 0 warning(s)`, and revision
`11b29f45ffa775c24a5ef1618d35892b7aa391f3` is correctly described as the
committed candidate/product-input revision used for author clean-build
evidence, not as the current branch tip.

From a fresh temporary checkout of exact head with Node `v24.16.0`, I verified:

- remote branch head matched the reviewed commit;
- the checkout started clean;
- P2.6 base `cf476a1d18c123094cb71d6bd6b49f0b9e787822` is an ancestor;
- `git diff --check` passed and the complete PR diff was captured and reviewed;
- `npm ci` passed;
- upstream vendor verification passed;
- lint passed;
- documentation hygiene passed with 209 Markdown files and 0 warnings;
- focused P2.7 tests passed 5/5;
- the full suite passed 378/378;
- two primary builds were byte-identical;
- the exact reviewed-tip artifact is SHA-256
  `73ce748f871166f717de4c22d31dcb4c6b8d048337a0eea78f1e4a7b676aafc1`,
  2,597,939 bytes;
- Chromium and Firefox passed the built `file://` browser harness;
- deliberately mutating
  `vendor/npm/@fontsource/bangers/5.3.0/package.tgz` changed its SHA-256 and
  made `npm run verify-vendor` fail non-zero with a vendor SHA-256 mismatch;
- restoring the package restored the exact original bytes and vendor
  verification passed again;
- the primary clone returned to a clean tracked state;
- a second fresh checkout in a different path with `TZ=Pacific/Kiritimati`,
  `LANG=fr_FR.UTF-8`, and `LC_ALL=fr_FR.UTF-8` produced two byte-identical
  builds matching the same expected artifact;
- the alternate clone ended clean;
- PR #54 remained open and unmerged, and the captured hosted checks were green.

The exact F7/F8 remediation patch contains only the intended evidence updates:
207 -> 209 for the current documentation count and the corrected
candidate/product-input provenance wording. No runtime, test, vendor, roadmap,
or reviewer-history content was changed by that author remediation.

## 2. What I could not independently verify

None of the evidence required for P2.7 item-level acceptance remains
unverified.

Physical iOS/Android execution remains explicitly `DEFERRED`, not PASS, under
the accepted P2.7-only ADR-0043 policy. That named deferral is non-blocking for
this item-level review and does not close the release device matrix or P0.19.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P2.7 Backup Health dashboard` | YES | Fresh exact-tip source/diff inspection, focused 5/5 tests, full 378/378 suite, Chromium/Firefox `file://` execution, conservative placement semantics, and corrected packet/PR evidence. |
| 2 | `*Deps: P2.6*` | YES | Fresh-checkout ancestry confirms the P2.6 base; the complete regression suite passes and the public/cold BackupRecord verification boundary remains intact. |

## 4. Findings

None.

All prior findings F1-F8 are closed.

## 5. Verdict rationale

Every P2.7 acceptance criterion is met and independently verified at
`2cf42d19b87064a908154803e9abf594aaf78a15`. The previously open F7/F8
documentation defects are corrected, the product tree remains independently
clean, reproducibility and deliberate fail-closed vendor corruption are
confirmed, both required desktop browser engines pass over `file://`, and
there are no findings of any severity. P2.7 is ready for reviewer closeout.

**PASS**
