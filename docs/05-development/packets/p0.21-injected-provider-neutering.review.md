# P0.21 — Cold-realm injected-provider neutering — Independent Review

**Verdict: FAIL (1 finding)**

Reviewed PR #32 at implementation head `a59afce9b198c41568e835502f84df9007157a7a` against `main@25773adddc08b36a667aaa989d3a73de5af7a90f`.

This file is recorded by the implementation session on the reviewer's behalf: the reviewing session's GitHub write authorization was unavailable, so it could not push this report or file a `REQUEST_CHANGES` review itself. Content below is transcribed verbatim from the reviewer's report.

## Evidence that passed

- Hosted CI checked out the exact implementation SHA, installed Playwright Chromium and Firefox, and successfully ran `npm run test:browser`. `verifyProviderNeutering()` passed in both engines: redefine/delete resistance, post-install `window.ethereum` assignment causing full lockdown, and `eip6963:announceProvider` causing full lockdown. This closes the browser-harness gap the implementation packet disclosed.
- 165/165 unit tests passed at the exact head.
- Lint, doc hygiene, and upstream vendor verification all passed.
- Double-build determinism confirmed on both Ubuntu and Windows, with matching hashes across OSes.
- The repository's timezone/locale determinism test passed.

## F1 (blocking) — pre-existing provider is silently erased instead of triggering lockdown

`defineProviderBlocked()` in `src/airgap.js` replaces an existing **configurable** `window.ethereum` property with the blocking accessor. The accessor reports *future* assignment attempts via `onAttempt`, but installation itself never reports that an actual provider object was already present at install time.

This conflicts with [ADR-0020](../adr/0020-injected-providers-rejected-and-neutered.md)'s explicit decision that provider presence inside the cold realm is an isolation failure, and with the P0.21 roadmap item's acceptance criterion that an observed provider object enters full lockdown.

The existing tests miss this timing:

- the unit fixture predefines `ethereum` with `value: undefined`, not an actual provider object;
- the browser harness waits until Coldbox reaches `ready`, then assigns a provider *afterward* — it never establishes provider presence *before* cold bootstrap runs.

The reviewer independently reproduced the underlying behavior with the same property-descriptor logic:

```
before present = true
installed = true
after = undefined, attempts = []
```

A configurable provider existing before guard installation is neutered (good — it can no longer be used), but no isolation violation is reported and installation is reported as a plain success. Since bootstrap only checks `providerNeutering.installed`, not whether a provider was present, cold bootstrap can reach `ready`/green with a provider having been silently wiped rather than routing through the mandated full-lockdown path.

## Required remediation

1. Detect an actual pre-existing `window.ethereum` provider during guard installation and route it through the isolation-violation/full-lockdown path — without invoking extension-controlled methods on it (do not call into the provider object; a presence check must not execute attacker-controlled code).
2. Still neuter it, and fail closed if safe inspection/replacement is impossible.
3. Add a unit regression where an actual provider object exists before `neuterProviders()` runs.
4. Add a real browser fixture that establishes provider presence before cold bootstrap, then verify Chromium and Firefox enter full lockdown rather than reaching healthy readiness.
5. Rerun the complete verification suite and update the packet.

Per [review-protocol.md](../review-protocol.md), any finding of any severity is a FAIL. **P0.21 stays `[~]`. PR #32 must not be merged as-is.**
---

# Re-review: P0.21 - Cold-realm injected-provider neutering

**VERDICT: PASS**

Findings: 0
Reviewed commit: `84cbe846495243b26def3643e5adcdf9557f14a1`
Reviewed by: GPT-5.6 Thinking, independent reviewer
Date: 2026-08-07

## 1. What I verified

- Confirmed PR #32 remains a single-roadmap-item branch and that remediation commit `84cbe846495243b26def3643e5adcdf9557f14a1` is exactly one commit ahead of the previously reviewed failing implementation `a59afce9b198c41568e835502f84df9007157a7a`.
- Re-read the F1 remediation in `src/airgap.js`. `inspectExistingValue()` uses `Object.getOwnPropertyDescriptor()` and inspects only the descriptor shape. For a data descriptor it compares the descriptor's raw `value` with `undefined`; for an accessor descriptor it treats the property as present without invoking its getter. It never reads a property from the provider object and never invokes a provider method.
- Re-read the cold bootstrap wiring in `src/cold/main.js`. `neuterProviders(recordProviderIsolationViolation)` reports the pre-existing provider synchronously; `providerNeuteringInstalled` is then `providerNeutering.installed && !providerNeutering.preexisting`, so a pre-existing provider cannot reach cold readiness. The generic install-failure message is emitted only for a genuine installation failure, so it does not overwrite the isolation-specific lockdown message.
- Re-checked the new unit regression. It places a real object at `window.ethereum` before `neuterProviders()` runs, gives that object a spy `request()` method, and proves all required behavior: the isolation callback fires, `preexisting` is true, the guard installs, the provider reads back as `undefined`, and `requestCalls` remains exactly zero. The negative counterpart proves explicit/default `undefined` does not false-positive.
- Independently inspected the new browser fixture. `createPreexistingProviderFixture()` places `window.ethereum` at the first statement inside the cold IIFE, before Coldbox's guard installs. Its `request()` method throws if invoked. `verifyPreexistingProviderLockdown()` requires the warm handshake to fail, vault operations to be refused, lockdown to be full, and the cold state not to be ready.
- Independently inspected the exact-head hosted browser execution rather than relying on the packet. GitHub Actions checked out `84cbe846495243b26def3643e5adcdf9557f14a1`, installed Playwright Chromium and Firefox, built the artifact, and actually ran `npm run test:browser` (`node scripts/run-browser-harness.js`). The new scenario printed, in both engines:

```text
Chromium: a provider present before cold bootstrap triggered full lockdown instead of reaching ready
Firefox: a provider present before cold bootstrap triggered full lockdown instead of reaching ready
Browser harness passed in Chromium and Firefox.
```

- In that same execution, the previously passing P0.21 cases also passed in both engines: redefine/delete resistance, post-install `window.ethereum` assignment causing isolation lockdown, and `eip6963:announceProvider` causing isolation lockdown. The rest of the existing browser harness passed as well.
- Checked the exact-head build/test jobs. On repository-pinned Node `v24.16.0`, upstream vendor verification passed against all pinned releases, lint passed, documentation hygiene passed with 0 warnings, and `npm test` finished 167 tests / 167 pass / 0 fail.
- Checked reproducibility independently from the remediation packet. Exact-head Ubuntu built twice to the same SHA-256, Windows produced the same artifact, and the cross-OS comparison passed:

```text
ae38a21904de9794e6626c83dd27848882c0e88c0436dda192abe067f9c8dea3  build/coldbox.html
```

- Re-checked security regressions relevant to this item: the browser harness still confirms the cold frame sandbox lacks `allow-same-origin`, the built cold CSP retains `connect-src 'none'`, and existing typed-protocol/secret-field rejection tests remain green.
- I attempted to trigger an additional reviewer-owned rerun of the already-successful exact-head browser job. GitHub returned `403 Resource not accessible by integration`. This does not leave an acceptance criterion unverified: the exact remediation commit already has a complete hosted execution of the required command, and I inspected its full logs directly rather than accepting the author's statement.

## 2. What I could not verify

None.

## 3. Acceptance criteria

| # | Criterion | Met? | Independent evidence |
|---|---|---|---|
| 1 | An announcement or provider object observed inside the cold realm enters full lockdown and refuses vault operations, exactly as a network-primitive call does. | PASS | F1 source path now reports pre-existing presence and blocks readiness; unit regression passes without invoking `request()`; real Chromium and Firefox pre-bootstrap fixture both enter full lockdown. Existing post-install assignment and EIP-6963 browser cases remain green. |
| 2 | The alarm text distinguishes an isolation failure from a policy failure. | PASS | Existing browser assertions for both Chromium and Firefox still pass after remediation; source still uses isolation-specific provider warning text distinct from CSP/runtime-network policy alarms. |
| 3 | The blockers survive an attempt to redefine or delete them, proven by a negative test that tries. | PASS | Unit test remains green, and the exact-head browser harness reports redefine/delete survival in Chromium and Firefox. |
| 4 | A fixture dispatching `eip6963:announceProvider` inside the cold realm is detected. | PASS | Unit fixture remains green; exact-head real-browser harness detects and locks down on the event in Chromium and Firefox. |
| 5 | Verified by the P0.3a harness in Chromium and Firefox, alongside the existing prototype-restoration and network-primitive checks. | PASS | Exact remediation commit ran the complete P0.3a browser harness with Playwright Chromium and Firefox and finished `Browser harness passed in Chromium and Firefox.` The new `verifyPreexistingProviderLockdown()` executed successfully in both engines. |

## 4. Findings

None.

## 5. Verdict rationale

The sole prior finding, F1, is resolved. A provider already present before guard installation is now treated as an isolation violation and cannot reach readiness; detection is descriptor-only and the regression proves no method on the provider object is invoked. The new timing-sensitive browser scenario executes successfully in both Chromium and Firefox at the exact remediation SHA, and all previously passing provider, security, build, vendor, lint, documentation, and regression checks remain green. I found no new defect or advisory concern.

PASS
