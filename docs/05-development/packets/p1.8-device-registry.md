# PR packet: P1.8 Device registry

## 1. Summary

P1.8 adds a public Device collection and a warm Devices workspace for recording
hardware-wallet inventory, lifecycle, tamper/PIN/purchase context, public seed
fingerprints, location, notes, and the boolean fact that a passphrase wallet is
used. The remediation also fixes the stacked-review F6 defect: blank optional
Device fields in an edit are now sent as explicit schema-safe clears, rather
than being omitted and preserved by merge-on-update semantics. Unit and
Chromium/Firefox workflows prove that the cleared fields remain absent after a
durable save and reopen. The roadmap marker remains `[~]` until an independent
reviewer verifies the item.

Roadmap: [P1.8 Device registry](../ROADMAP.md).

Branch: `p1.8-device-registry`
Dependency/base: `p1.7-notes-tags-concealment` at `3d100b5cc2ac05dba840e4eae255fd8fbf1a8b79`
Final product/remediation commit: `7647002`

The original stacked review remains preserved byte-for-byte in commit
`1d2049894fa003cc6dc2d2f041b91bd096cac303`; the append-only F15/F16 review is
`4ad879f0c699ff9ac5db0bffc2aa7dd6fa10cee4`. The combined review digest is
`bbcdd2b3ca0c9f8c42af1d7ece8d8a8e218b4c1484c922185e10346d53c43d53`, and the
safety tag `runner/pr37-42-append-f15-f16-fail-addendum-01/pre` is retained.

## 2. Scope

In scope:

- Closed `devices` public-projection schema with the canonical data-model
  fields, bounded ISO dates/text/fingerprints, and the lifecycle enum
  `in-use | retired | lost | destroyed | rma`.
- Warm create, edit, search, soft-hide, session reveal, and lock teardown for
  device records. The page explicitly says that it does not connect to,
  unlock, derive from, or sign with hardware.
- Secure UUID generation and device CRUD through the existing complete
  `publicData.replace` / `publicData.updated` boundary.
- Schema-safe optional-field clearing for Device edits. Required fields remain
  required; `passphraseUsed` remains an explicit boolean, so clearing unrelated
  optional fields does not turn `true` into an omitted or ambiguous value.
- Protocol, Registry, browser-harness, and warm-source boundary tests,
  including unknown-field, invalid-status/date/fingerprint, secret-shaped,
  missing-randomness, required-clear, and durable-reopen negatives.
- ADR-0033, three-depth user help, CHANGELOG, the `[~]` roadmap state, and this
  packet/self-review.

Deliberately out of scope:

- Hardware transport or screen reading; P1.9 owns verification workflows.
- Fingerprint, receive-address, xpub, backup, or passphrase verification
  workflows; those are P1.9.
- QR generation (P1.10), address verification-state migration (P1.11), or
  clipboard work (P1.12/P1.13).
- Secret phrases, private keys, xprvs, secret-note plaintext, or any other
  secret-compartment value in a Device record. `passphraseUsed` is a boolean
  public fact only.

## 3. How to verify

The commands below were run from `p1.8-device-registry` in
`C:\Users\semaj\Projects\coldbox` against product commit `7647002`.

```text
$ git diff --check p1.7-notes-tags-concealment...HEAD
(no output; exit 0)

$ node --test test/protocol.test.js test/registry.test.js test/vault.test.js
ℹ tests 47
ℹ pass 47
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0

$ npm.cmd test
ℹ tests 287
ℹ pass 287
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0

$ npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: @fontsource/bangers@5.3.0
Upstream release verified: @fontsource/comic-neue@5.3.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Upstream release verified: argon2-browser@1.18.0
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.

$ npm.cmd run lint
Lint passed: forbidden-construct, JavaScript syntax, and LF source line endings are valid.

$ npm.cmd run check-docs
Documentation hygiene check passed: 156 markdown file(s) checked, 0 warning(s).

$ npm.cmd run build
Built build/coldbox.html (404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5)

$ Get-FileHash build/coldbox.html -Algorithm SHA256; (Get-Item build/coldbox.html).Length
Hash: 404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5
Size: 1942658 bytes
```

The reproducibility check cloned the current checkout, including Git metadata,
to a different temporary path and built twice after deleting the first output.
The first run used `TZ=Pacific/Honolulu`, `LC_ALL=C`; the second used `TZ=UTC`,
`LC_ALL=de_DE.UTF-8`:

```text
repro root: C:\Users\semaj\AppData\Local\Temp\coldbox-p1.8-repro-22dee76134dd46770ac631c3df582ce15
build 1: 404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5 (1942658 bytes)
build 2: 404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5 (1942658 bytes)
reproducible build result: PASS (hashes and sizes identical)
temporary root removed: True
```

The full suite includes deliberate forbidden-construct/build failures and
vendor/path mismatch negatives; all passed with the expected non-zero result
inside their disposable fixtures. The browser harness also deliberately
tampered a built artifact and observed CSP rejection in both engines.

The prescribed dependency-free `file://` browser harness passed in both
available desktop engines. The P1.8-specific output was:

```text
Playwright is dev-only; dependency-free build matches byte-for-byte (404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5)
Chromium: registry wallet/account/address CRUD, explicit clear, durable reopen, and lock state passed
Chromium: device registry optional clear, durable reopen, lifecycle status, search, hidden reveal, and lock teardown passed
Firefox: registry wallet/account/address CRUD, explicit clear, durable reopen, and lock state passed
Firefox: device registry optional clear, durable reopen, lifecycle status, search, hidden reveal, and lock teardown passed
Browser harness passed in Chromium and Firefox.
```

## 4. Acceptance criteria

The roadmap contains one exact P1.8 criterion and no separate `Accept:` block:

| Criterion | How satisfied | Test |
|---|---|---|
| `P1.8 Device registry` | The public `devices` collection is schema-validated and persisted through the existing public projection. The Devices page performs CRUD, search, reversible hiding, session reveal, lifecycle updates, lock teardown, and explicit clearing of optional edit fields without hardware transport or secret material. | `test/protocol.test.js`, `test/registry.test.js`, `test/vault.test.js`, full suite, and the Chromium/Firefox harness |

## 5. Security impact

Yes. This item extends the public projection schema and warm rendering, but it
does not add a cross-realm message type or a new vault compartment. Device
mutations use the existing typed `publicData.replace` request and
`publicData.updated` acknowledgement; the protocol strips unknown fields,
bounds all accepted fields, rejects secret-shaped content, and accepts
`passphraseUsed` only as a boolean. The new `clearFields` option is interpreted
only by the Registry schema; it cannot clear required fields or introduce an
unknown field.

Device fields are intentionally public metadata. No mnemonic, passphrase,
private key, xprv, secret-note plaintext, or secret-compartment bytes are read
or sent by the warm Device page. The warm source contract still rejects a
standalone secret passphrase identifier; its explicit exception is the
documented `passphraseUsed` boolean. The cold realm policy is unchanged,
including `connect-src 'none'` and the existing sandbox without
`allow-same-origin`.

Device IDs use the existing `crypto.getRandomValues` UUID path and fail closed
when secure randomness is unavailable. No runtime dependency, network host,
cryptographic primitive, derivation path, or chain was added.

If this boundary were wrong, a public registry edit could become a path for
secret-shaped text to reach the warm shell or for a future developer to mistake
an inventory record for a hardware verification result. Closed schemas,
negative tests, explicit clear semantics, UI copy, and the deliberate lack of
hardware transport address those risks.

## 6. Test evidence

New or remediation-specific tests prove:

- Device schema acceptance for every canonical field, unknown-field stripping,
  bounded fingerprints, valid lifecycle values, and ISO dates.
- Rejection of invalid lifecycle values, malformed dates, malformed
  fingerprints, arbitrary secret-shaped fields, and missing secure randomness.
- Device CRUD, UUID shape, update, soft-hide, include-hidden listing, and
  clone isolation through `Registry`.
- Schema-safe `clearFields` removes every optional Device field while rejecting
  a required-field clear and preserving the explicit `passphraseUsed` boolean.
- Warm source absence of secret-compartment APIs and absence of standalone
  secret passphrase handling, while the documented public boolean remains
  explicit.
- Browser create, edit, optional-field clear, durable save/reopen, search,
  lifecycle update, hide, cold re-authentication reveal, and lock teardown over
  `file://` in Chromium and Firefox.

No new cryptographic primitive or chain was introduced, so P1.8 has no new
cryptographic vector claim. The full suite still runs the independent NIST,
BIP-32/BIP-39/BIP-49/BIP-84/BIP-86, and ERC-55 vectors already established by
earlier roadmap items. Vendor verification matched all ten pinned upstream
releases.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome / Chromium | PASS | Prescribed Playwright file-based harness, including optional clear and durable reopen. |
| Windows Firefox | PASS | Prescribed Playwright file-based harness, including optional clear and durable reopen. |
| Windows Edge | Untested | No separate Edge session was available. |
| macOS Safari | Untested | No macOS device/session was available. |
| macOS Chrome | Untested | No macOS device/session was available. |
| Linux Firefox | Untested | No Linux device/session was available. |
| **iOS local-execution target** | Untested | No qualifying iOS device/session or iOS build was available; Quick Look is not inferred as Safari. |
| Android Chrome (Files) | Untested | No Android device/session was available. |
| Tor Browser | Untested | No Tor Browser session was available. |

## 8. Assumptions made

- The `Device` field set and lifecycle enum in [data-model.md](../../01-spec/data-model.md)
  are canonical. Wallet records already have `deviceIds`, so P1.8 does not
  invent a second device-to-wallet relation field.
- Device inventory is public metadata by design. `passphraseUsed` records only
  yes/no configuration metadata; it is not a passphrase hint or input.
- A date-only browser input is normalized to midnight UTC as an ISO timestamp,
  matching the existing protocol's exact ISO validation. Device transport and
  hardware-derived values are intentionally not implied by a typed record.
- On a Device edit, blank optional text, date, and fingerprint-list controls
  are explicit clears. Required `vendor`, `model`, `firmware`, `status`, and
  `id` cannot be cleared; boolean controls remain explicit booleans. This
  matches the closed schema and the user expectation that deleting visible
  optional metadata is durable.
- Existing vaults that omit `devices` remain readable because the Registry
  fills the missing public collection with an empty list; the next accepted
  public write persists the expanded projection through the existing vault
  format.

## 9. What to scrutinise

- `validateClearFields`, `clearFieldsForUpdate`, and `prepareRecord` in
  `src/registry.js`: verify required-field protection, unknown-field rejection,
  clone isolation, and complete-projection relationship checks still apply to
  Device updates.
- `handleDeviceFormSubmit` in `src/main.js`: verify every blank optional
  control is represented in `clearFields`, while `passphraseUsed: false` is
  retained as an explicit boolean and required fields remain validated.
- `cleanRegistryField` and the closed `devices` field map in `src/protocol.js`:
  fingerprints, dates, bounded text, and the boolean must not become arbitrary
  text carriers.
- `renderRegistry`, Device lock teardown, and the browser-harness save/reopen
  path: the workspace must never remain visible after lock, and the cold
  re-authentication phrase must never enter the warm Device page.

## 10. Self-assessment

The stacked review identified F6: Registry merge-on-update semantics could not
clear optional fields, and the same defect was exposed by Device editing. The
remediation adds explicit schema-safe clear options, wires all Device optional
controls to them, and adds unit plus browser save/reopen coverage. The focused
and full suites, reproducible build, vendor verification, docs gate, and both
desktop browser runs were rerun after the remediation commit.

No physical hardware wallet was connected or validated. The item deliberately
does not claim that a listed vendor/model/fingerprint is genuine; that work is
P1.9. Mobile, Edge, Safari, Tor, and Android execution remain untested as shown
in the matrix. No real seed phrase, private key, keyfile, or vault file was
created or committed.

## 11. Bundle impact

P1.7 final baseline: 1,922,741 bytes.
P1.8 final artifact: 1,942,658 bytes.
Delta: +19,917 bytes (approximately +19.5 KiB).

The increase is the Device workspace, public schema/CRUD surface, explicit
clear/reopen coverage, and compiled three-depth guidance. The artifact remains
a single file; no runtime dependency was added.

## 12. Docs updated

- `docs/03-guides/first-wallet.md` — plain, working, and technical Device help.
- `docs/05-development/adr/0033-device-registry.md` and the ADR index.
- `docs/05-development/ROADMAP.md` — P1.8 changed to `[~]`, never `[x]`.
- `CHANGELOG.md`.
- `docs/05-development/packets/p1.8-device-registry.md` and the self-review.
