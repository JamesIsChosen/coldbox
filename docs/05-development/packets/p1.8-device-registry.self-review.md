# Self-review: P1.8 Device registry

**VERDICT: PASS**

Findings: 0
Reviewed commit: `4a59998`
Reviewed by: author self-review gate, final round after remediation
Date: 2026-08-10

## What I verified

- Re-read `docs/05-development/review-protocol.md` immediately before the
  gate and read the complete P1.8 diff from the P1.7 dependency tip
  `a715c51` through `4a59998`.
- `git diff --check p1.7-notes-tags-concealment...HEAD`: passed with no output.
- `npm.cmd ci`: installed 2 packages, audited 3, found 0 vulnerabilities.
- Focused P1.8/projection/vault tests: 44/44 passed.
- `npm.cmd test`: 284/284 passed.
- `npm.cmd run verify-vendor`: all ten local vendor files and all ten upstream
  releases passed.
- `npm.cmd run lint`: passed forbidden-construct, syntax, and LF checks.
- `npm.cmd run check-docs`: passed with 156 Markdown files and 0 warnings,
  including this packet and self-review.
- Final `npm.cmd run build`: SHA-256
  `2fec6d202481638ee42ba842d9e1ce7b25aecbcf734e31c617e27f11e0bd0211`, size
  1,936,948 bytes.
- A separate copied checkout with copied Git metadata, a different path,
  `TZ=Pacific/Honolulu`/`LC_ALL=C`, then `TZ=UTC`/`LC_ALL=de_DE.UTF-8`, produced
  the same hash and size twice.
- A disposable tampered checkout appended `eval('self-review-tamper');` and
  made `node scripts/lint.js --root .` exit 1 with one forbidden-construct
  finding. The disposable copy was removed.
- The prescribed file-based browser harness passed in Chromium and Firefox,
  including the new Device create/search/hide/reveal/update/lock flow and the
  inherited CSP, airgap, responsive, zeroization, and teardown checks.

## What I could not verify

Physical mobile/iOS/Edge/Safari/Tor/Android Files sessions and real hardware
wallet transport were unavailable. They are explicitly marked untested in the
packet's matrix. P1.8 has no separate hardware-verification acceptance clause;
P1.9 owns that validation, and the desktop harness does not stand in for those
sessions.

## Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `P1.8 Device registry` | ✅ | Closed public Device schema, warm CRUD/search/soft-hide/reveal/lock UI, focused tests, full suite, and Chromium/Firefox harness. |

## Findings

None. The first full-suite gate exposed two broken internal anchors in the new
documentation; they were fixed in `b5b78fb` and the complete suite/docs gate
was rerun successfully. The final diff audit also replaced the warm-source
passphrase string split with an explicit boolean and tightened its negative
assertion in `4a59998`; the final full suite, build, reproducibility, tamper,
and browser checks all ran after that commit.

## Security review

- The new collection is public-only and enters the same closed protocol map as
  the other Registry collections.
- Unknown device fields, malformed dates/fingerprints/statuses, secret-shaped
  values, and arbitrary text carriers fail closed. `passphraseUsed` is typed as
  boolean and the warm source has no secret-compartment API.
- No new message type, CSP permission, network host, runtime dependency,
  derivation path, key material, or cryptographic primitive was introduced.
- Device IDs require `crypto.getRandomValues`; the negative test and the
  browser lockdown matrix cover unavailable randomness and inherited runtime
  failures.
- Locking resets the device form, hides the workspace, clears the registry
  store, and clears the session hidden-reveal state.

## Verdict rationale

The single P1.8 criterion is met; the full test suite, independent vendor
verification, lint, docs hygiene, final reproducible build, deliberate
non-zero tamper failure, and both available file-based browser engines pass.
The only unavailable checks are disclosed platform/hardware sessions outside
this item's acceptance scope. **PASS** for the batch self-review gate; an
independent reviewer must still verify the branch before merge.
