# PR packet: P1.2 — Entropy Health Meter and Bias Analyzer

## 1. Summary

Implements the next available roadmap item, [P1.2](../ROADMAP.md#phase-1--core-wallet), inside the sealed cold realm. Entropy Health now analyzes physical/manual dice, discard-dice, coin, card, and hex observations with claimed bits, an observed empirical min-entropy estimate, per-symbol frequencies, chi-square evidence, a runs test, lag-one serial correlation, and deterministic pattern warnings. Device-RNG simulations are counted separately and excluded from every diagnostic. Cards are treated as without-replacement permutations, so iid tests and measured min-entropy are explicitly unavailable for them.

The analyzer is advisory. P1.1's integer possibility-space accounting remains the authority for independent-source credit and mixing. No entropy-health value or message crosses the cold/warm boundary.

## 2. Scope

**In scope:**

- `src/cold/entropy-health.js` — DOM-free, network-free statistical analyzer
- `src/cold/main.js`, `src/cold/index.html`, and `src/cold/styles.css` — cold-realm wiring and UI
- `scripts/build.js` — vendored inline cold component assembly
- `scripts/run-browser-harness.js` — Chromium/Firefox file-based assertions for simulations, manual samples, runs/pattern warnings, and card-permutation handling
- `test/entropy-health.test.js` — independent-vector and negative coverage
- [ADR-0027](../adr/0027-entropy-health-statistical-diagnostics.md) and its index entry
- [SPEC.md](../../01-spec/SPEC.md), [entropy-and-strength.md](../../04-reference/entropy-and-strength.md), [first-wallet.md](../../03-guides/first-wallet.md), and the three-depth [glossary](../../00-overview/glossary.md)
- `CHANGELOG.md` and `ROADMAP.md` (`[ ]` → `[~]`)

**Deliberately out of scope:**

- A confidence-bound or bias-corrected entropy estimator. P1.2 labels the maximum-frequency result as an empirical observed estimate; a stronger estimator needs a separate specification and sampling protocol.
- Using diagnostics as a P1.1 security gate or as proof that a platform CSPRNG is sound.
- Applying iid frequency, runs, or serial-correlation tests to card permutations.
- P1.3 Seed Forge, BIP-39 generation, or any secret-bearing hand-off.
- The physical device matrix. This packet records the automated file-based browser result and marks physical/mobile platforms untested.

## 3. How to verify

Commands below were run from `C:\Users\semaj\Projects\coldbox` on the P1.2 branch. PowerShell uses the `.cmd` shims for npm.

### Upstream vendor verification

```text
> npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: @fontsource/bangers@5.3.0
Upstream release verified: @fontsource/comic-neue@5.3.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Upstream release verified: argon2-browser@1.18.0
Upstream release verified: qrcode-generator@1.4.4
Vendor verification passed against local files and upstream releases.
```

### Reproducible build

The clean `main` baseline was built in an explicit detached worktree at `25ba569`, then removed. The product build had Git metadata available, so the provenance date was the same in both runs.

```text
Baseline command: node scripts/build.js  (detached main worktree, 25ba569)
Built build/coldbox.html (638722ee26f2640210fb7f85bbe32b88f2097070d13d65015eaced7e4a57825e)
SHA256 638722ee26f2640210fb7f85bbe32b88f2097070d13d65015eaced7e4a57825e
Length 1269833 bytes
```

First product build:

```text
> $env:LC_ALL = 'C'; $env:TZ = 'UTC'; npm.cmd run build
Built build/coldbox.html (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)
SHA256 BD02286F4BBF0C93DE92A79F130501437BE4881394A1AEAF4A441F39EE592A66
Length 1318116 bytes
Embedded build date: 2026-08-09T21:24:38-07:00
```

Second product build, with a different caller locale and timezone:

```text
> $env:LC_ALL = 'en_US.UTF-8'; $env:TZ = 'Pacific/Honolulu'; npm.cmd run build
Built build/coldbox.html (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)
SHA256 BD02286F4BBF0C93DE92A79F130501437BE4881394A1AEAF4A441F39EE592A66
Length 1318116 bytes
Embedded build date: 2026-08-09T21:24:38-07:00
```

The two product hashes match exactly. The current artifact is 1,318,116 bytes. The baseline is 1,269,833 bytes, for a delta of 48,283 bytes (≈47.2 KiB), inside the [SPEC §3](../../01-spec/SPEC.md) bundle budget.

### Tests and static gates

```text
> node --test test/entropy-health.test.js
ℹ tests 9
ℹ pass 9
ℹ fail 0

> npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

> npm.cmd run check-docs
Documentation hygiene check passed: 138 markdown file(s) checked, 0 warning(s).

> npm.cmd test
ℹ tests 261
ℹ suites 0
ℹ pass 261
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

### Browser harness

After installing the pinned Playwright Chromium and Firefox binaries, the exact remediation head passed the real `file://` harness in Chromium and Firefox. The unchanged harness was run with Git/process access because the local sandbox cannot launch Firefox's tab subprocess. The final run ended:

```text
> npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)
Chromium: Entropy Lab normal-vs-fallback strength, provenance, bulk input, grid/undo/reset, CSPRNG burn, mix, and touch targets passed
Firefox: Entropy Lab normal-vs-fallback strength, provenance, bulk input, grid/undo/reset, CSPRNG burn, mix, and touch targets passed
Browser harness passed in Chromium and Firefox.
```

The new assertions also exercised: generated dice excluded from analysis; six manual base-6 dice rows; twenty-two alternating manual coin flips with `runs=22` and an alternating-pattern warning; and a manual card whose health state is `not-applicable`, with iid diagnostics unavailable and measured min-entropy shown as unavailable.

## 4. Acceptance criteria

The roadmap entry is a one-line item with no expanded `Accept:` block. The criterion copied verbatim is:

> P1.2 Entropy Health Meter and Bias Analyzer

The concrete behavior is defined by [SPEC.md §11.1a](../../01-spec/SPEC.md) and resolved operationally in [ADR-0027](../adr/0027-entropy-health-statistical-diagnostics.md).

| Criterion | How satisfied | Test/evidence |
|---|---|---|
| Entropy Health Meter | Cold-realm panel stays live during collection and shows health state, samples, claimed bits, observed measured estimate, and source frequencies. | Chromium and Firefox `verifyEntropyLab()` assertions; build contains the panel. |
| Bias Analyzer frequency and claimed/measured values | Finite-alphabet frequency table and empirical maximum-frequency estimate; simulation provenance is excluded. | `analyze() reports exact frequencies and claimed-versus-measured min-entropy`; browser simulation/manual assertions. |
| Chi-square and p-value | Pearson statistic, upper-tail p-value, `alpha=0.01`; unavailable until every expected bin has at least five observations. | NIST-derived statistic/critical-value test; skew and small-expected-count negative test; browser output. |
| Runs test | NIST binary Wald-Wolfowitz formulation, midpoint projection for multi-symbol sources, explicit sample/proportion preconditions. | Alternating/grouped independent vectors; browser `runs=22` warning. |
| Serial correlation | NIST lag-one correlation and 95% advisory band; constant and card cases are unavailable. | `[1,2,3,4,5]` vector and zero-variance negative test; browser card assertion. |
| Pattern warnings | Long runs, ordered sequences, alternation, and repeated blocks are deterministic advisory warnings. | Four-pattern unit vector and browser alternating-pattern assertion. |
| Honest source handling | Device-RNG simulations are excluded; cards are without-replacement and do not receive iid tests or a fabricated measured value. | Card/simulation unit tests and final Chromium/Firefox harness. |
| Fail closed | Malformed alphabets, symbols, options, and empty observations throw; underpowered valid tests return explicit unavailable reasons. | Negative analyzer tests for invalid symbols, empty input, ambiguous claims, and non-boolean replacement mode. |

## 5. Security impact

- **Randomness:** yes, in the sense that this code reports diagnostics about recorded physical/manual source values. It does not generate key material, change P1.1's `BigInt` accounting, or change `mix()`.
- **Realm boundary:** touched only by adding a cold-realm component. The analyzer is assembled inside the sealed realm and no new message type or field was added.
- **CSP/network:** no new `connect-src` host; the analyzer has no network API and the cold policy remains `connect-src 'none'`.
- **Secrets:** no mnemonic, private key, xprv, passphrase, or decrypted vault data is read or carried. No analyzer result crosses to the warm shell.
- **If wrong:** a user could be misled about evidence in a physical recording, especially by treating an empirical estimate or passing test as proof. The UI and ADR explicitly state that finite-sample diagnostics are advisory and cannot establish platform CSPRNG integrity. The stronger P1.1 entropy accounting is unchanged.

## 6. Test evidence

### New tests

`test/entropy-health.test.js` loads the raw cold module in a DOM-free VM and covers:

- exact frequencies and claimed-versus-observed empirical min-entropy;
- chi-square statistic, degrees of freedom, upper-tail p-value interval, and alpha decision;
- strong skew detection and refusal when expected counts are below five;
- alternating and grouped binary runs;
- lag-one serial correlation and zero-variance unavailability;
- all four deterministic pattern warning classes;
- without-replacement card handling; and
- malformed inputs and ambiguous options failing closed.

### Independent references

The chi-square statistic and critical-value boundary are checked against the [NIST chi-square goodness-of-fit reference](https://itl.nist.gov/div898/handbook/eda/section3/eda35f.htm). The runs formula is checked against [NIST's runs-test definition](https://www.itl.nist.gov/div898/handbook/eda/section3/eda35d.htm), and the lag-one correlation/band against [NIST autocorrelation material](https://www.itl.nist.gov/div898/handbook/eda/section3/eda35c.htm) and its [confidence-band reference](https://www.itl.nist.gov/div898/handbook/eda/section3/autocopl.htm). The broader statistical-test context is [NIST SP 800-22 Rev. 1a](https://nvlpubs.nist.gov/nistpubs/legacy/sp/nistspecialpublication800-22r1a.pdf).

These are diagnostics rather than cryptographic vectors; the tests intentionally assert independently computed statistics and published preconditions, not a claim that the implementation can prove randomness.

### Negative evidence

- Invalid symbols outside the configured alphabet throw.
- Empty observations throw.
- Ambiguous `claimedBits` plus `claimedBitsPerSymbol` throws.
- Non-boolean `withoutReplacement` throws.
- Expected-counts-below-five, too-small runs samples, skewed runs proportions, constant correlation, and cards return explicit unavailable reasons.

## 7. Device matrix

The item changes a cold-realm rendering surface. The automated file-based harness ran on Windows in Playwright's Chromium and Firefox binaries; that is not a claim about native Chrome, Safari, iOS, Android, or Tor behavior.

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | Untested | Playwright Chromium passed against `file://`; native Chrome not run. |
| Windows Firefox | Untested | Playwright Firefox passed against `file://`; native desktop Firefox not run. |
| macOS Safari | Untested | No macOS device available. |
| macOS Chrome | Untested | No macOS device available. |
| Linux Firefox | Untested | No Linux device available. |
| **iOS local-execution target** | Untested | No iOS device or iOS build available; Quick Look was not substituted. |
| Android Chrome (Files) | Untested | No Android device available. |
| Tor Browser | Untested | No Tor Browser run. |

## 8. Assumptions made

- The roadmap's one-line P1.2 item is accepted against SPEC §11.1a because it contains no detailed acceptance block; ADR-0027 records the missing operational definitions rather than silently inventing them in code.
- P1.2 is advisory and does not block P1.1 mixing. Seed Forge does not exist yet, so there is no generation gate to wire; P1.3 must consume the existing P1.1 strength/accounting contract and decide how any future user acknowledgement is represented.
- Internal finite-alphabet symbols remain zero-based because that is the P1.1 session representation; UI labels preserve physical notation such as die faces `1`–`6` and discard outcomes `1`–`4`.
- The multi-symbol runs test uses the alphabet midpoint projection specified in ADR-0027. It is one clearly labeled diagnostic, not a claim that it is the only valid multi-symbol test.
- The empirical maximum-frequency estimate is intentionally not a lower confidence bound. This is why the code calls it an estimate and the documentation warns that finite samples can miss a high-probability outcome.
- Cards can span multiple shuffles, so the analyzer conservatively treats the selected card stream as without-replacement and leaves iid tests unavailable across the whole selected stream.

## 9. What to scrutinise

- The regularized-gamma implementation used for chi-square upper-tail p-values, especially extreme tails and large alphabets.
- The exact mapping from each P1.1 source's stored representation to analyzer symbols, including the zero-based dice/discard values and the card pool-size claimed-bit calculation.
- The distinction between an empirical observed estimate and a true min-entropy lower bound; a future edit must not turn the displayed number into a guarantee.
- The decision that a chi-square flag alone makes the health state marginal, while runs/correlation/pattern warnings remain advisory evidence and do not alter P1.1.
- The absence of physical/mobile device verification noted in §7.

## 10. Self-assessment

- **What might be wrong:** The gamma-tail approximation has not been compared against a second numerical library at every extreme value. The multi-symbol midpoint runs projection and deterministic pattern thresholds are engineering choices, not universal standards.
- **What was not done:** No physical iOS, Android, macOS, or Tor run; no native desktop Chrome/Firefox run; no confidence-bound entropy estimator; no P1.3 Seed Forge integration.
- **Known limitations:** A passing statistical test remains absence of detected evidence. The empirical estimate can overstate true source entropy for a finite sample.
- **Follow-up:** P1.3 must connect the existing P1.1 meter/gate contract to BIP-39 generation. Any attempt to use a confidence-bound estimator or statistical result as a generation gate should first amend ADR-0027.

## 11. Bundle impact

| Build | Bytes |
|---|---:|
| Clean `main` at `25ba569` | 1,269,833 |
| P1.2 branch | 1,318,116 |
| Delta | **+48,283 (≈+47.2 KiB)** |

The resulting artifact remains inside the SPEC bundle budget.

## 12. Docs updated

- [ADR-0027](../adr/0027-entropy-health-statistical-diagnostics.md) and [ADR index](../adr/README.md)
- [SPEC.md §11.1/§11.1a](../../01-spec/SPEC.md), including the empirical-estimate limitation and review date
- [Entropy and strength reference](../../04-reference/entropy-and-strength.md)
- [First wallet guide](../../03-guides/first-wallet.md), all three depth blocks
- [Glossary](../../00-overview/glossary.md), new three-depth Bias Analyzer term
- [Threat model](../../02-security/threat-model.md), current advisory diagnostic boundary
- [ROADMAP.md](../ROADMAP.md), P1.2 now `[~]`
- `CHANGELOG.md`

No dated outside-world reference document was changed; the SPEC review date was updated because its normative P1.2 wording changed.

## 13. Remediation response to the independent FAIL

The independent review report is preserved unchanged at [p1.2-entropy-health-meter-bias-analyzer.review.md](p1.2-entropy-health-meter-bias-analyzer.review.md). Its SHA-256 before and after remediation is `6e8734f5b11ce3d509e3f280c0615d0d82c90a398c7673f8c0936fe238e2f020`. The reviewed product head was `70f5211a0de04c9a512268693c63343b8354a97b`; remediation is built from the branch including review-history commit `47963f278d0b8ea59d6983ca85a4b76079af3940` and product remediation commit `edc91fe4a97e94fe54121d14fb20b552fd8de946`.

| Finding | Remediation response | Evidence |
|---|---|---|
| F1 | Added `#cold-vault-passphrase-health`, visible only after a new-vault creation prepare message. It reports the specification's qualitative unknown range/no numeric estimate and Diceware guidance; it is hidden for ordinary unlock. | Browser harness creates a vault, checks live creation guidance and entered-state copy, then checks the panel is hidden during unlock. |
| F2 | Chose the advisory-only contract. SPEC, ADR-0027, reference, guide, packet, and cold UI now say P1.2 does not block Entropy Lab mixing or ask for acknowledgement; P1.3 owns any future generation boundary. | Static documentation search; browser assertion for `data-enforcement="advisory"` and the no-mixing-block disclosure; existing Mix path remains covered. |
| F3 | The normal-approximation runs test now requires `n1 > 10` and `n2 > 10`, in addition to existing sample/proportion preconditions. Smaller projected counts return `symbol-counts-too-small`. | Unit vectors prove 20 balanced symbols are unavailable and 22 balanced symbols use the normal approximation; browser uses 22 alternating flips. |
| F4 | Replaced the first-two-symbol shortcut with a whole-block constant check, so `[0,0,1,0,0,1]` is recognized as a repeated non-constant block. | Regression vector in `test/entropy-health.test.js`. |
| F5 | Made the selected-target state machine explicit and non-overlapping: insufficient below target, marginal at/above target with chi-square flag, adequate at/above target without that flag and below 256, strong at/above 256. | Unit test covers targets 128, 160, 192, 224, and 256; SPEC, reference, and guide use the same definitions. |
| F6 | Updated SPEC status to Phase 1 in progress while Phase 0 device-matrix sign-off remains human-required. | `npm.cmd run check-docs`; reviewed SPEC header. |
| F7 | Removed claims that Shannon entropy is displayed. The UI and docs now state that claimed bits and measured min-entropy are displayed; Shannon entropy is explanatory only. | Static documentation search and browser panel assertions. |

### Remediation verification

The following evidence is final for product remediation commit `edc91fe4a97e94fe54121d14fb20b552fd8de946`; the packet-only commit that follows does not change product inputs or the reproducible artifact hash.

```text
> node --test test/entropy-health.test.js
ℹ tests 9
ℹ pass 9
ℹ fail 0

> npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

> npm.cmd run check-docs
Documentation hygiene check passed: 138 markdown file(s) checked, 0 warning(s).
```

Additional final integrity output:

```text
> Get-FileHash docs/05-development/packets/p1.2-entropy-health-meter-bias-analyzer.review.md -Algorithm SHA256
6E8734F5B11CE3D509E3F280C0615D0D82C90A398C7673F8C0936FE238E2F020

> npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

> $env:LC_ALL = 'C'; $env:TZ = 'UTC'; npm.cmd run build
Built build/coldbox.html (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)
> $env:LC_ALL = 'en_US.UTF-8'; $env:TZ = 'Pacific/Honolulu'; npm.cmd run build
Built build/coldbox.html (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)

> npm.cmd test
261 tests; 261 passed; 0 failed; 0 cancelled; 0 skipped.

> npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (bd02286f4bbf0c93de92a79f130501437be4881394a1aeaf4a441f39ee592a66)
Browser harness passed in Chromium and Firefox.

> git diff --check
```

The FAIL report remains `[~]`-compatible and is not edited, deleted, squashed, or replaced. A fresh independent review is required at the exact post-remediation head; this author must not merge PR #35 or mark P1.2 `[x]`.
