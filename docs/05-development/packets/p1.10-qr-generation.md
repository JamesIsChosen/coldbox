# P1.10 — QR generation PR packet

## Identity

- Roadmap item: P1.10 — QR generation
- Branch: `p1.10-qr-generation`
- Base: `main` at `a3283ba2609cea9efb3f75732d2cd3708b7acfad`
- Product commit: `856602810205baadb7a81de8115860e4a01ba672`
- Status: `[~]`; the author does not mark roadmap items `[x]`.

## Scope

This branch adds the QR Studio flow for public addresses and secret SeedQR artifacts. Standard SeedQR uses zero-padded four-digit BIP-39 word indices. Compact SeedQR uses the original entropy bytes. Secret QR output remains in the cold realm; public address QR output remains in the warm realm. The QR encoder is read from the existing pinned vendor tarball during the deterministic build.

Changed product areas include `src/cold/qr.js`, the cold QR Studio UI and styles, the warm public-address QR UI, the build injection points, Seed Forge word-index conversion, the QR guide, glossary wording, changelog, roadmap, and QR unit tests.

## Acceptance evidence

The roadmap acceptance text is the P1.10 line in `docs/05-development/ROADMAP.md`; the detailed format and security requirements are in SPEC §11.1. The implementation and tests cover:

- standard QR numeric encoding for valid 12/15/18/21/24-word phrases;
- compact QR binary encoding for valid 16/20/24/28/32-byte entropy;
- rejection of invalid lengths and out-of-range word indices;
- cold-realm-only secret QR generation, download, print, and session clearing;
- warm public Bitcoin and EVM address QR generation with optional URI payloads;
- user-facing warnings and the secret-confirmation gate;
- single-file build assembly and placeholder resolution.

## Verification commands and results

Commands were run from `C:\Users\semaj\Projects\coldbox` using the PowerShell npm shims.

```text
npm.cmd run lint
Lint passed.

npm.cmd run check-docs
Documentation hygiene check passed: 165 markdown file(s) checked, 0 warning(s).

npm.cmd run build
Built build/coldbox.html (ee20dede9f407629bc7dc6e56187cd4f14ae1dc569c9104d36b74cb3a70f5beb)

npm.cmd run test:browser
Playwright browser harness completed successfully; Chromium file://, cold-boundary, registry, lock, and responsive checks passed.
```

The round-1 failure was fixed in `test/build.test.js`: the JSON-embedded cold document is now decoded and scanned independently, while its encoded representation is excluded from the surrounding-document scan. This preserves real absolute-path detection and avoids treating the vendored QR source's encoded comment newline as a path.

The fresh full suite result was:

```text
tests 296
pass 296
fail 0
```

The focused build suite is also green, including the negative machine-path regression.

An alternate clean archive build was also run under a different path, `TZ=Pacific/Honolulu`, and `LANG=de_DE.UTF-8`; it produced the same SHA-256 and byte length as the primary build: `ee20dede9f407629bc7dc6e56187cd4f14ae1dc569c9104d36b74cb3a70f5beb`, 2,089,328 bytes.

## Security and assumptions

- The QR encoder remains the existing pinned vendored dependency; no runtime fetch was added.
- Compact SeedQR follows SPEC §11.1 and the corrected glossary wording: the payload is original entropy bytes, not raw word indices.
- No scanner or camera input is introduced in P1.10.
- Secret QR generation is gated by explicit confirmation and is cleared with the vault session.

The least-confidence areas remain browser-only print/download behavior; the browser harness covers the surrounding file://, cold-boundary, and responsive flows but does not replace physical printer/scanner testing.

## Handoff

P1.10 passed its fresh self-review. Proceed to P1.11 from `main`; do not merge this PR.
