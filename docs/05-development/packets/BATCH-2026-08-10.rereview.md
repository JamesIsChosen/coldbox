# Fresh stacked independent re-review: P1.4-P1.9

**VERDICT: FAIL**

Findings: 1 blocking finding affecting PRs #37, #38, and #40
Reviewed stack:
- PR #37 / P1.4: `8f99771d1b66825f99fbd4e4b495869b809d1406` against `main@00da58e04f4d015352b43e435fa2332c5f093d65`
- PR #38 / P1.5: `05ca0026c132d3cb22e21e6f8c99f0a78c7a5b05` against `p1.4-derivation-engine-bip32-bitcoin@8f99771d1b66825f99fbd4e4b495869b809d1406`
- PR #39 / P1.6: `cc4a2f97d5b4faa824cbdf12b8764dabacd92688` against `p1.5-derivation-evm-arbitrary-path@05ca0026c132d3cb22e21e6f8c99f0a78c7a5b05`
- PR #40 / P1.7: `3d100b5cc2ac05dba840e4eae255fd8fbf1a8b79` against `p1.6-registry-crud-wallets-accounts-addresses@cc4a2f97d5b4faa824cbdf12b8764dabacd92688`
- PR #41 / P1.8: `7ae8df6e4afe8f370a2cde60f99719f361d008b9` against `p1.7-notes-tags-concealment@3d100b5cc2ac05dba840e4eae255fd8fbf1a8b79`
- PR #42 / P1.9: `818d65b0f207345a027ef2ae37b7c0a5209b37e6` against `p1.8-device-registry@7ae8df6e4afe8f370a2cde60f99719f361d008b9`

Reviewed by: independent stacked reviewer
Date: 2026-08-10
Evidence bundle: `coldbox-runner-pr37-42-fresh-stacked-rereview-03-20260810-141425.zip`
Evidence bundle SHA-256: `0af1b368f0c7c1b478697e32241247670b0f1d8a981cb76fb6257ed4bac01e74`
Preserved historical review SHA-256: `bbcdd2b3ca0c9f8c42af1d7ece8d8a8e218b4c1484c922185e10346d53c43d53`

## 1. What I verified

The collector operated read-only against the source checkout and performed all mutations only in disposable clones. The source tree was clean before and after collection. No PR was merged and P1.10-P1.13 were not started.

The six PRs were independently resolved from GitHub and verified open, non-draft, unmerged, and exactly stacked in dependency order. Each remote base and head SHA matched the expected stack.

Independent primary sources were downloaded for BIP-32, BIP-39, BIP-49, BIP-84, BIP-86, ERC-55, ethereumjs HD-key, go-ethereum HD paths, and Trezor BIP-39 vectors.

For every PR, the reviewer independently ran:
- fresh exact-head clone/checkout;
- `git diff --check`;
- `npm ci`;
- `npm run verify-vendor`;
- `npm run lint`;
- `npm run check-docs`;
- focused item tests;
- the full `npm test` suite;
- two production builds from different paths, time zones, and locales;
- the Chromium/Firefox `file://` browser harness;
- deliberate upstream-vendor corruption, requiring both verifier and build to fail non-zero;
- deliberate source tampering, requiring lint and build to fail non-zero;
- final clone-cleanliness checks.

All six exact heads built reproducibly:

| PR | Exact reviewed head artifact SHA-256 | Bytes |
|---|---|---:|
| #37 / P1.4 | `fdbdf4a012ee75108fe43be7ee392a0cea0eca41fb00a9135624c4ddb475ff48` | 1,798,436 |
| #38 / P1.5 | `58fb1603065c3fc2dc34996e61164ff0775f625fa6e3921509fa26bf7d1dedec` | 1,817,765 |
| #39 / P1.6 | `baba0a304e440feadf9677d42bf29a02fb990d34577c59fc0417e39db053b337` | 1,872,745 |
| #40 / P1.7 | `a7c00c4ab3202531c9bf4edcaa0c45eb70a135f0d1f863ec72e1002cd13e637e` | 1,922,741 |
| #41 / P1.8 | `404804f296cf39238c312d5da2dcd31f6f12c1bf371ff36fff0796f7f5c3e2f5` | 1,942,658 |
| #42 / P1.9 | `03452808ec72a27c0d75d3a833a6d8d3f98f09807d4a4f55525d3e72badd8b46` | 1,987,610 |

The built artifacts retained `connect-src 'none'` in the cold realm. No `Math.random` call was found under `src/cold`.

### Prior finding re-check

The preserved historical review was independently hashed at the current top branch and still matches:
`bbcdd2b3ca0c9f8c42af1d7ece8d8a8e218b4c1484c922185e10346d53c43d53`.
It still contains F15/F16 and ends in `FAIL`.

The previous 16 findings were re-checked:

- **F1:** PASS. The independently downloaded BIP-32 source contains the exact official `m/0'/1/2'` xpub, and the repository fixture now copies and asserts it exactly with no invalid-checksum bypass.
- **F2:** PASS. Reviewer-owned negatives reject master, shallow, depth-4, and depth-3 non-hardened Bitcoin xpubs as account imports while accepting the depth-3 hardened account xpub.
- **F3:** PASS. Reviewer-owned negatives reject invalid compressed secp256k1 points for every Bitcoin script family.
- **F4:** PASS. Reviewer-owned EVM probe rejects a depth-3 non-hardened account xpub.
- **F5:** PASS. Registry construction/replacement rejects orphan relationships while preserving prior valid state.
- **F6:** PASS. Wallet/Account/Address optional fields and Device optional fields have explicit schema-safe clearing, including durable browser reopen coverage.
- **F7:** PASS. The item-specific P1.6 and P1.7 Chromium/Firefox acceptance paths executed successfully.
- **F8:** PASS. The four collector failures for P1.7 were reviewer-probe false positives. The source assigns `record.hidden = Boolean(<checkbox> && <checkbox>.checked)` for Wallet, Account, Address, and Note, so unchecked edits explicitly submit `hidden:false`. The browser harness independently hides a wallet, reveals it, edits it, unchecks concealment, saves it, and confirms the concealed marker is removed in Chromium and Firefox.
- **F9:** PASS. `git diff --check` passed on P1.9.
- **F10:** PASS. The remediated Verify Bench browser workflow passed in Chromium and Firefox, including collapsed-panel interaction and lock teardown.
- **F11:** PASS. Reviewer-owned tests prove leading, trailing, and internal BIP-39 passphrase spaces remain significant.
- **F12:** PASS. Reviewer-owned and committed tests reject compatibility characters, whitespace-corrupted public values, invalid xpub checksums, mixed-case Bech32, and bad address checksums.
- **F13:** PASS. The human-required evidence records a SeedSigner at firmware 0.8.0, Windows Microsoft Edge over `file://`, Wi-Fi and Ethernet physically disconnected, a throwaway test seed, and successful fingerprint, receive-address, xpub, backup-fingerprint, and passphrase-derived-identity comparisons. The exact Edge version was not recorded; that does not change the roadmap's real-hardware acceptance result. The collector's hardware-parent failure was a reviewer-probe false positive: `818d65b` follows documentation-only remediation-verification commit `79d20b6`, which in turn follows final product commit `8a359a1`; the hardware commit itself is packet-only documentation.
- **F14:** PARTIAL / FAIL. P1.6 is corrected, but exact-head reproducibility evidence remains stale or internally incoherent in P1.4 and P1.5; the same evidence defect is now present in P1.7. See R2-F1 below.
- **F15:** PASS. Verify Bench has the explicit `Use current Seed Forge wallet` cold-local handoff, and the verification module has no warm-channel primitive.
- **F16:** PASS. The standalone BIP-39 passphrase shell is absent, and `verification.js` exposes no duplicate mnemonic/passphrase/private-key entry API.

## 2. What I could not verify

None of the non-delegated acceptance criteria remain unverifiable.

P1.9 explicitly delegates final real-device validation to a human. The maintainer supplied and committed that human evidence; this reviewer verified the recorded device/firmware/offline setup, the five required comparison results, the documentation-only commit scope, and the independently tested software path. The reviewer did not claim to physically operate the SeedSigner.

## 3. Acceptance criteria

| PR | Roadmap criterion | Met? | Evidence |
|---|---|---|---|
| #37 / P1.4 | Derivation engine: BIP-32 core plus Bitcoin script types | **NO** | Cryptographic/product behavior passes, but the submitted exact-head reproducibility evidence is stale. R2-F1. |
| #38 / P1.5 | Derivation: EVM and generic arbitrary-path mode | **NO** | Product behavior passes, but the packet simultaneously claims an older production artifact and the current browser artifact. R2-F1. |
| #39 / P1.6 | Registry CRUD: wallets, accounts, addresses | **YES** | Whole-compartment relationship checks, explicit clear, durable reopen, protocol boundary, full tests, reproducibility, and both browsers pass. |
| #40 / P1.7 | Notes, tags, and concealment levels | **NO** | Product behavior, reversible concealment, secret-note boundary, and both browsers pass, but the packet's claimed exact build evidence is stale. R2-F1. |
| #41 / P1.8 | Device registry | **YES** | Device clear/reopen, lifecycle/search/hide/reveal/lock paths, full tests, reproducibility, and both browsers pass. |
| #42 / P1.9 | Verification workflows: fingerprint, receive address, xpub, backup, passphrase; final validation needs real hardware wallets | **YES** | Cold-local Seed Forge handoff, strict independent public comparisons, teardown, full tests, both browsers, and recorded SeedSigner 0.8.0 human hardware validation pass. |

Because this is one dependency-stacked review, no PR in #37-#42 may merge while an earlier stacked PR has an unresolved finding.

## 4. Findings

### R2-F1 — Exact-head reproducibility evidence is still stale/incoherent in P1.4, P1.5, and P1.7 packets

**Severity:** blocking documentation/evidence accuracy
**PRs:** #37 / P1.4, #38 / P1.5, #40 / P1.7
**Locations:** each PR packet's reproducibility/browser evidence

**Observed:**

**P1.4 / PR #37**
- Packet claims the current production/browser artifact is
  `c407e12d68d2eb9c1523af2d8f7f166e3d00d82689beb5036e33a4d42bed64f9`,
  1,798,453 bytes.
- Fresh exact-head reviewer builds at
  `8f99771d1b66825f99fbd4e4b495869b809d1406`
  both produced
  `fdbdf4a012ee75108fe43be7ee392a0cea0eca41fb00a9135624c4ddb475ff48`,
  1,798,436 bytes.
- The exact-head hash is absent from the packet.

**P1.5 / PR #38**
- The reproducibility section calls
  `780afd40755ab16c3ca520ebbe3eb3331571d1889fc24644b7e740ed1cab8f81`,
  1,817,782 bytes, the **final production artifact**.
- The same packet's browser section instead reports
  `58fb1603065c3fc2dc34996e61164ff0775f625fa6e3921509fa26bf7d1dedec`.
- Fresh exact-head reviewer builds at
  `05ca0026c132d3cb22e21e6f8c99f0a78c7a5b05`
  both produced the latter hash,
  `58fb1603065c3fc2dc34996e61164ff0775f625fa6e3921509fa26bf7d1dedec`,
  at 1,817,765 bytes.
- The packet therefore remains internally inconsistent even though the collector's simple "exact hash appears somewhere" probe marked F14 PASS.

**P1.7 / PR #40**
- The packet says its evidence was run at product commit
  `0ce59e42e8973cf1eade1c207f9f8f2f16afaa48` and states that the packet/self-review follow-ups "do not change build inputs."
- It reports
  `01970aa0fc07c0c4ed949f0bfaf7cbbd5a1b25e78b829ac3969fb51c26c0cf76`,
  1,922,741 bytes, for both reproducibility and browser evidence.
- Fresh exact-head reviewer builds at
  `3d100b5cc2ac05dba840e4eae255fd8fbf1a8b79`
  instead both produced
  `a7c00c4ab3202531c9bf4edcaa0c45eb70a135f0d1f863ec72e1002cd13e637e`,
  also 1,922,741 bytes.
- The statement that the follow-up commits do not affect the built artifact is therefore not supported by the exact-head result.

**Expected:** The packet submitted for independent review must describe one coherent artifact identity for the exact final head being reviewed. A prior product-commit hash may be preserved as historical evidence, but it must not be called the current/final artifact when a fresh exact-head build produces different bytes.

**Required action:**
1. Remediate #37 first, preserving all historical review files and commits.
2. Restack #38-#42 bottom-up after #37 changes.
3. At the **final restacked exact head** of every affected PR, rerun the two-path/timezone/locale reproducibility check and browser harness.
4. Update #37, #38, and #40 packets so their current/final artifact claim contains exactly the final exact-head SHA-256 and byte size. If older product-commit hashes are retained, label them explicitly as historical and explain why they differ.
5. Re-run `git diff --check`, full tests, vendor verification, lint/docs, browser harness, and the consolidated exact-head review after the final restack.

No product-code remediation is requested by this finding.

## 5. Collector-failure adjudication

The collector reported six evidence-level failures. Five are not product/review findings:

- Four P1.7 `F8-reversible-hidden` failures were caused by a reviewer probe that searched for the literal text `hidden: registry...checked`. The implementation uses the semantically correct `record.hidden = Boolean(registry... && registry....checked)` form, and the item-specific browser workflow independently proves `hidden:true -> hidden:false` reversal.
- The P1.9 `F13-hardware-parent` failure assumed the hardware evidence commit had to be the direct child of product commit `8a359a1`. In fact, documentation-only commit `79d20b6` records the software remediation verification between them. The hardware commit is also documentation-only. There is no product drift between software validation and the human evidence record.

The remaining collector failure, P1.4 stale packet evidence, is real and is broadened by independent packet inspection to include P1.5 and P1.7 as R2-F1.

## 6. Verdict rationale

The implementation/security remediation is substantially successful: all six exact heads passed the full tests, vendor provenance, lint/docs, deterministic two-path builds, Chromium/Firefox harnesses, deliberate vendor corruption, deliberate source tampering, cold-CSP checks, and the targeted prior-finding probes. The human SeedSigner gate is recorded and sufficient for the roadmap's delegated hardware criterion.

However, this repository's review protocol makes the verdict binary and explicitly treats packet claims that do not reproduce as FAIL. PRs #37, #38, and #40 still submit stale or internally inconsistent final artifact evidence. Because #38-#42 are stacked above #37, none of PRs #37-#42 may merge until R2-F1 is corrected at the final restacked heads and a fresh consolidated re-review returns zero findings.

FAIL
