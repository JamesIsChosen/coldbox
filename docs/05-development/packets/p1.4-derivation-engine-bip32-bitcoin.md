# P1.4 — Derivation engine: BIP-32 core plus Bitcoin script types

## Summary

- Roadmap item: [P1.4 — Derivation engine: BIP-32 core plus Bitcoin script types](../ROADMAP.md#phase-1--core-wallet)
- Branch: `p1.4-derivation-engine-bip32-bitcoin`
- Dependency/base: `main`
- Implementation commit: `059d140` (`Add cold-only Bitcoin derivation engine`)
- Scope: the cold-realm derivation engine and its independent test vectors. This item does not add a warm-realm UI or a new message type.

The implementation adds a vendored, cold-only derivation layer for BIP-32 secp256k1 keys and the Bitcoin script families currently specified by Coldbox: P2PKH, P2SH-P2WPKH, P2WPKH, and P2TR. It supports mainnet/testnet, account/change/index derivation, account-level watch-only xpub derivation, SLIP-132 public version bytes, strict path parsing, bounded batches, and public-only result projections.

## Verification commands and evidence

All commands below were run from `C:\Users\semaj\Projects\coldbox` unless another working directory is stated.

### Focused derivation vectors

```text
node --test test/derivation.test.js
✔ BIP32 vector 1 matches official xprv/xpub data and an independent public-key check
✔ BIP49/BIP84/BIP86 official vectors derive the expected account keys and addresses
✔ watch-only derivation matches seed derivation and exposes no secret material
✔ malformed paths, invalid keys, hardened public derivation, mismatches, and batch overflow fail closed
ℹ tests 4
ℹ pass 4
ℹ fail 0
```

### Repository checks

```text
npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 142 markdown file(s) checked, 0 warning(s).

npm.cmd test
ℹ tests 270
ℹ pass 270
ℹ fail 0
```

The full test suite was rerun after the implementation commit, so the provenance checks saw committed Git metadata rather than an uncommitted working tree.

### Vendored dependency verification

```text
npm.cmd run verify-vendor
Local vendor verified: @fontsource/bangers@5.3.0
Local vendor verified: @fontsource/comic-neue@5.3.0
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Local vendor verified: qrcode-generator@1.4.4
Upstream release verified: all listed packages
Vendor verification passed against local files and upstream releases.
```

### Reproducible builds

The production build was run twice with different locale/time-zone settings:

```text
LC_ALL=de-DE TZ=Pacific/Honolulu npm.cmd run build
Built build/coldbox.html (8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6)

LC_ALL=ja-JP TZ=Asia/Tokyo npm.cmd run build
Built build/coldbox.html (8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6)
```

The SHA-256 hashes were identical. The current built file is 1,798,185 bytes. The P1.3 packet records the previous baseline as 1,780,682 bytes, so this change adds 17,503 bytes (approximately 17.1 KiB). The build output is ignored and is not committed.

### Browser and file:// verification

```text
npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (3ba60c26460014d1d44b5d010bb94df4f5946b568bd74d69697e7b735a7e8c11)
Chromium: all browser harness checks passed
Firefox: all browser harness checks passed
Browser harness passed in Chromium and Firefox.
```

The existing browser harness covered the built `file://` application, cold-realm network blocking, CSP probes, warm/cold boundary, vault locking and zeroization, Seed Forge vectors and teardown, provider neutering, provenance, and documentation/help checks. P1.4 has no new UI controls, so there is no new UI flow to exercise.

### Independent-path and fail-closed review checks

A fresh disposable copy of the committed source was created at:
`C:\Users\semaj\AppData\Local\Temp\coldbox-p1.4-review-20260810`.

The build from that copy produced:

```text
review-hash=8b8c24e80816ff9b3be1b2b0f6ce617c7855549547fd8a89ece77ac9046c7bc6
```

The hash matched the production build. Afterward, a deliberate source-integrity tamper was appended to the disposable copy. The build rejected it:

```text
Lint failed: CRLF line ending found in src/cold/derivation.js
Error: Build refused: source failed forbidden-construct lint
tampered-build-exit=1
```

The disposable copy was removed after the check. No tampered file entered the worktree.

## Acceptance mapping

The roadmap entry is exactly:

> P1.4 — Derivation engine: BIP-32 core plus Bitcoin script types

There is no separate `Accept:` block on this roadmap line. The implementation maps the title to these concrete, testable outcomes:

| Roadmap scope | Evidence |
| --- | --- |
| BIP-32 core | Official BIP-0032 vector 1 xprv/xpub matches; an independent Node/OpenSSL ECDH public-key calculation checks the derived public key; strict path parsing and child derivation negatives are covered. |
| Bitcoin script types | P2PKH, P2SH-P2WPKH, P2WPKH, and P2TR address construction are implemented. Official BIP-0049, BIP-0084, and BIP-0086 account/address vectors cover testnet, native SegWit, and Taproot paths. |
| Public/watch-only derivation | Account-level xpub/ypub/zpub/tpub/upub/vpub inputs derive public addresses without a seed or private key; the focused tests assert that serialized results contain no secret fields. |
| Cold single-file integration | The derivation layer is bundled into the cold iframe only. The reproducible build, vendor verification, lint, and Chromium/Firefox `file://` harness passed without adding a message carrying secret material or changing the cold CSP. |

## Security impact

This is a security-sensitive change. A derivation error can cause a user to send funds to an address they cannot spend from, or fail to recognize funds at an address they own.

- Seed bytes and private HDKey state are created and used only inside `src/cold/derivation.js` in the cold iframe.
- The exported API returns public addresses, xpub-family strings, and fingerprints only. It does not return seeds, mnemonics, passphrases, private keys, or xprvs.
- Intermediate HDKey private state is wiped after derivation, and seed input is copied so the caller's input is not mutated.
- No new warm/cold message type was added. The existing message boundary remains public-only.
- The cold realm keeps its existing `connect-src 'none'` policy and no runtime dependency or network fetch was added.
- Invalid paths, invalid seeds, malformed extended keys, unsupported version/network/script combinations, hardened public derivation, and unsafe batch ranges reject rather than degrade.
- Taproot uses tagged TapTweak hashing and normalizes the internal point to even Y before constructing the output key.

## Independent test vectors

The vector sources are independent of this implementation:

- [BIP-0032 official test vectors](https://github.com/bitcoin/bips/blob/master/bip-0032.mediawiki) — seed `000102030405060708090a0b0c0d0e0f`, root and child xprv/xpub values.
- [BIP-0049 official test vectors](https://github.com/bitcoin/bips/blob/master/bip-0049.mediawiki) — `abandon` × 11 plus `about`, testnet account xpub and P2SH-P2WPKH address.
- [BIP-0084 official test vectors](https://github.com/bitcoin/bips/blob/master/bip-0084.mediawiki) — the same mnemonic, zpub, and native SegWit addresses.
- [BIP-0086 official test vectors](https://github.com/bitcoin/bips/blob/master/bip-0086.mediawiki) — the same mnemonic, account xpub, Taproot address, internal key, and output key.

The BIP-0032 test keeps the official upstream `m/0'/1/2'` xpub string as a negative fixture because its checksum is invalid, and verifies the official xprv plus an independently calculated public key instead of silently correcting that source row. No test vector was generated by this repository and then treated as independent evidence.

## Device and browser matrix

| Environment | Result | Notes |
| --- | --- | --- |
| Windows Chromium | PASS | Playwright browser harness on the built `file://` artifact. |
| Windows Firefox | PASS | Playwright browser harness on the built `file://` artifact. |
| macOS Chrome | UNTESTED | No macOS runner in this session. |
| macOS Safari | UNTESTED | No macOS/Safari runner in this session. |
| Linux Firefox | UNTESTED | No Linux runner in this session. |
| iOS local execution | UNTESTED | No physical iOS device was available; no Quick Look or Files result is claimed. |
| Android Chrome Files | UNTESTED | No physical Android device was available. |
| Tor Browser | UNTESTED | No Tor Browser run was available. |

## Assumptions and scope decisions

1. P1.4 is the engine item, not the derivation UI item. P1.3's packet explicitly leaves derivation UI, keys, and addresses to later work, and the P1.4 roadmap line has no UI acceptance criteria. This branch therefore adds the cold engine and tests but no warm controls or protocol message.
2. Watch-only derivation accepts an account-level extended public key. A master xpub or an xpub that still has hardened descendants is rejected because the engine cannot establish the account boundary safely.
3. A public extended key does not contain the master fingerprint. Watch-only results therefore expose `accountFingerprint` from the extended key and leave `fingerprint` null; the API does not invent a master fingerprint.
4. Bare xpub/tpub version bytes are ambiguous across Bitcoin script policies. The API requires an explicit script type for those versions; SLIP-132 ypub/upub and zpub/vpub versions must agree with the requested script type.
5. Batches default to 20 and are capped at 1,000, with index arithmetic checked before derivation.

If any of these assumptions is changed, the public API contract, protocol documentation, and tests must be reviewed together. No vault-format or message-boundary decision was made here.

## What a reviewer should scrutinise hardest

1. The Taproot point handling: even-Y normalization, x-only serialization, tagged TapTweak hashing, and point addition.
2. SLIP-132 version-byte detection and the rejection of script/network mismatches.
3. Zeroization boundaries around seed copies and intermediate HDKey objects, including exception paths.
4. The distinction between a true master fingerprint and the account fingerprint available from an xpub.
5. The fact that no new message handler was added: public derivation results must remain public-only when later UI work consumes this engine.
6. The official BIP-0032 invalid checksum row is intentionally a negative fixture; verify the test does not conceal a source-vector transcription error.

## Self-assessment and gaps

- The focused tests and full suite pass, but this session did not perform a third-party cryptographic audit of the vendored libraries or the new composition code.
- The browser harness verifies the integrated cold realm, but no new UI flow exists in this item, so it cannot prove later UI wiring.
- macOS Safari/Chrome, Linux Firefox, iOS Files/Quick Look, Android Files, and Tor remain untested.
- BigInt and secp256k1 behavior were tested in the available Chromium/Firefox environments, not on Safari.
- No physical hardware-wallet validation was performed; P1.4 has no hardware-wallet acceptance criterion and P1.9 owns final hardware validation.

These are declared test gaps, not claims of support. They do not alter the P1.4 engine acceptance mapping above, but they must remain visible to the independent reviewer.

## Files changed

- `scripts/crypto-bundle.js` — vendored @scure/base, RIPEMD-160, and secp256k1 exposure for the cold bundle.
- `scripts/build.js` — derivation layer manifest entry.
- `src/cold/index.html` — cold script placeholder.
- `src/cold/main.js` — derivation dependency guard.
- `src/cold/derivation.js` — cold-only BIP-32 and Bitcoin script derivation API.
- `test/derivation.test.js` — official vectors and fail-closed negatives.
- `docs/04-reference/derivation-paths.md` — engine contract.
- `docs/05-development/adr/0029-cold-only-bitcoin-derivation-engine.md` and ADR index — structural decision record.
- `docs/05-development/dependencies.md` — bundle dependency description.
- `docs/05-development/packets/p1.4-derivation-engine-bip32-bitcoin.md` — this packet.
- `CHANGELOG.md` and `docs/05-development/ROADMAP.md` — release and roadmap status (`[~]`, pending independent verification).

## Reviewer handoff

Please review this branch against `main` using `docs/05-development/review-protocol.md`. The roadmap marker intentionally remains `[~]`; only an independent reviewer should change it to `[x]` after a PASS. Do not merge as part of this task.
