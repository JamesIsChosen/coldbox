# Batch run 2026-08-10

**Scope requested:** P1.4 – P1.13
**Completed:** P1.4, P1.5, P1.6, P1.7, P1.8
**Stopped at:** P1.9 — second consecutive self-review FAIL

This is a historical batch snapshot. It records the six branches produced by
this run; later PR state and roadmap verification belong to GitHub and the
per-item packets.

## Items

| Item | Branch | PR | Base | Self-review |
|---|---|---:|---|---|
| P1.4 | `p1.4-derivation-engine-bip32-bitcoin` | [#37](https://github.com/JamesIsChosen/coldbox/pull/37) | `main` | PASS (1 round) |
| P1.5 | `p1.5-derivation-evm-arbitrary-path` | [#38](https://github.com/JamesIsChosen/coldbox/pull/38) | `p1.4-derivation-engine-bip32-bitcoin` | PASS (1 round) |
| P1.6 | `p1.6-registry-crud-wallets-accounts-addresses` | [#39](https://github.com/JamesIsChosen/coldbox/pull/39) | `p1.5-derivation-evm-arbitrary-path` | PASS (1 round) |
| P1.7 | `p1.7-notes-tags-concealment` | [#40](https://github.com/JamesIsChosen/coldbox/pull/40) | `p1.6-registry-crud-wallets-accounts-addresses` | PASS (final round after two pre-gate fixes) |
| P1.8 | `p1.8-device-registry` | [#41](https://github.com/JamesIsChosen/coldbox/pull/41) | `p1.7-notes-tags-concealment` | PASS (final round after remediation) |
| P1.9 | `p1.9-verification-workflows` | [#42](https://github.com/JamesIsChosen/coldbox/pull/42) | `p1.8-device-registry` | FAIL (2 rounds; partial branch) |

All six PRs were opened and verified open. None was merged. P1.10, P1.11,
P1.12, and P1.13 were not started and have no branch or PR from this batch.

## Why it stopped

P1.9's first browser round failed because ordinary HTML form submission is
blocked by the cold iframe's intentional omission of `allow-forms`. That
finding was fixed with non-submitting wrappers and explicit button handlers.
The required re-review then failed a second time when the browser harness
tried to fill the receive-address workflow while its `<details>` panel was
collapsed. Per `docs/05-development/batch-run.md`, a second consecutive FAIL
means stop the batch immediately. The P1.9 branch and PR #42 are therefore
preserved as a partial, explicitly failing handoff rather than carried into
P1.10.

The batch did not stop on stack depth: six unmerged branches existed at the
stop. The stop was the second-failure hard condition.

## P1.9 state at stop

- Focused P1.9 tests: 6/6 passed.
- Full Node suite: 290/290 passed before the final stopped-round artifact
  rebuild; it was not rerun after the first-round sandbox fix.
- Lint: passed after the first-round fix.
- Documentation hygiene: 159 files, 0 warnings after the batch packet was
  added.
- Vendor verification: local files and all ten upstream releases passed.
- Committed artifact build: SHA-256
  `4038af7ecc02383341576a50e91d930d7b6ba3c38375eb0713bc571975052834`,
  1,975,647 bytes.
- Browser matrix: Chromium reached the fingerprint workflow on the second
  run, then failed on the collapsed receive-address details; Firefox was not
  reached in that run. The first run's form-submission finding and fix are in
  the per-item self-review.
- Real hardware-wallet validation remains untested and is explicitly required
  by the P1.9 roadmap note.

See [the P1.9 packet](p1.9-verification-workflows.md) and [the P1.9
self-review](p1.9-verification-workflows.self-review.md) for the complete
commands, outputs, assumptions, and unresolved finding.

## Review these in dependency order

Each review must use a fresh session and a separate checkout. Do not treat a
same-session self-review as independent review. The PRs are stacked, so review
the parent before the child. The batch itself did not merge anything.

### #37 — P1.4, first

> Read `docs/05-development/review-protocol.md`, then independently review PR #37 (branch `p1.4-derivation-engine-bip32-bitcoin`) against `main`. Verify every claim yourself, build under a different path/timezone/locale, deliberately tamper and confirm a non-zero fail-closed result, run `npm test`, `npm run verify-vendor`, `npm run lint`, and `npm run test:browser`, check the roadmap criterion verbatim, write `docs/05-development/packets/p1.4-derivation-engine-bip32-bitcoin.review.md`, and end with PASS or FAIL. Do not merge as part of this batch.

### #38 — P1.5, after #37

> Read `docs/05-development/review-protocol.md`, then independently review PR #38 (branch `p1.5-derivation-evm-arbitrary-path`) against `p1.4-derivation-engine-bip32-bitcoin`, after its parent review is complete. Repeat the different-path/timezone/locale build, tamper, full-test, vendor, lint, browser, and verbatim-roadmap checks; write `docs/05-development/packets/p1.5-derivation-evm-arbitrary-path.review.md`, and end with PASS or FAIL. Do not merge as part of this batch.

### #39 — P1.6, after #38

> Read `docs/05-development/review-protocol.md`, then independently review PR #39 (branch `p1.6-registry-crud-wallets-accounts-addresses`) against `p1.5-derivation-evm-arbitrary-path`, after its parent review is complete. Verify registry schemas, persistence, secret rejection, reproducible builds, deliberate tamper, all required checks, browser coverage, and the roadmap criterion; write `docs/05-development/packets/p1.6-registry-crud-wallets-accounts-addresses.review.md`, and end with PASS or FAIL. Do not merge as part of this batch.

### #40 — P1.7, after #39

> Read `docs/05-development/review-protocol.md`, then independently review PR #40 (branch `p1.7-notes-tags-concealment`) against `p1.6-registry-crud-wallets-accounts-addresses`, after its parent review is complete. Verify public/secret note separation, tags, concealment, lock teardown, reproducible builds, tamper, required checks, browser coverage, and the roadmap criterion; write `docs/05-development/packets/p1.7-notes-tags-concealment.review.md`, and end with PASS or FAIL. Do not merge as part of this batch.

### #41 — P1.8, after #40

> Read `docs/05-development/review-protocol.md`, then independently review PR #41 (branch `p1.8-device-registry`) against `p1.7-notes-tags-concealment`, after its parent review is complete. Verify the exact public Device fields, lifecycle/search/hide/lock behavior, secret rejection, reproducible builds, tamper, required checks, browser coverage, and the roadmap criterion; write `docs/05-development/packets/p1.8-device-registry.review.md`, and end with PASS or FAIL. Do not merge as part of this batch.

### #42 — P1.9, after #41

> Read `docs/05-development/review-protocol.md`, then independently review PR #42 (branch `p1.9-verification-workflows`) against `p1.8-device-registry`. Start by reproducing the disclosed second-round browser failure before considering any fix. Verify the cold-local boundary, complete public comparisons, secret teardown, official vectors, the real-device limitation, reproducible builds at the final tip, deliberate tamper, all required checks, and the roadmap criterion verbatim; write `docs/05-development/packets/p1.9-verification-workflows.review.md`, and end with PASS or FAIL. Do not start P1.10 and do not merge as part of this batch.

## What I'd scrutinise hardest

P1.9's cold-frame event wiring and teardown are the highest-risk areas. The
first failure proved that the sandbox's no-`allow-forms` rule is operational,
not just documentation. The unresolved second failure is the collapsed
workflow detail interaction. I would also scrutinise whether the wrapper's
ordinary equality and Bech32-only normalization are being presented as the
stronger P1.12 clipboard authority, and whether every verification secret is
cleared on every lock/panic/health path.

## Known-weak areas

- P1.9 is not a PASS; its browser harness is red and the branch is partial.
- Firefox did not execute in the failed second round.
- No real hardware wallet, mobile browser, Safari, Tor Browser, Android
  Files, or iOS local-execution target was tested.
- The final committed P1.9 tip needs a fresh two-build reproducibility and
  tamper run after the browser issue is fixed.
- P1.9's manual device value is not device authenticity proof and does not
  replace comparing the hardware wallet's own screen on a clean setup.

## Next leg

Do not begin P1.10 from this stack. First fix P1.9 on its existing branch in a
new session, rerun the complete gates, obtain a fresh self-review PASS, and
obtain independent review before any next item. After the reviewer resolves
the stack in dependency order, resume with:

> Read `AGENTS.md` and `docs/05-development/batch-run.md`. Review the P1.9 FAIL handoff and its independent-review result first. If P1.9 is independently fixed and verified, continue the roadmap from the next available item using a fresh dependency-aware batch.
