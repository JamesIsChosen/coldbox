# P1.9 self-review

**Branch:** `p1.9-verification-workflows`
**Reviewed tip:** `691ec60` (`P1.9: add cold-local verification workflows`)
**Rounds:** 2
**Verdict:** FAIL

## Round 1 — FAIL

The initial browser run reached the new panel but timed out waiting for the
fingerprint match. A direct browser diagnostic showed the cold sandbox was
blocking HTML form submission because it intentionally has no `allow-forms`
permission. The implementation was changed to use non-submitting div wrappers,
`type="button"`, and explicit click handlers.

## Round 2 — FAIL

The rerun reached the fingerprint workflow after that fix, then timed out
trying to fill `#cold-verification-receive-xpub` because the receive-address
`<details>` panel is collapsed by default. This is a concrete browser
usability/test failure, not an acceptable warning. The batch rule requires
stopping on the second consecutive FAIL, so the details interaction is left
unfixed for the next session.

## Evidence

- `node --test test/verification.test.js`: 6 passed, 0 failed.
- `npm test`: 290 passed, 0 failed before the final stopped-round artifact
  rebuild; not rerun after the first-round sandbox fix.
- `npm run lint`: passed after the first-round fix.
- `npm run build`: passed at the committed tip with artifact hash
  `4038af7ecc02383341576a50e91d930d7b6ba3c38375eb0713bc571975052834`.
- `npm run test:browser`: failed in Chromium on the second round as described
  above; Firefox was not reached.

The item is not independently reviewable as PASS. Do not start P1.10 or
P1.11 from this branch until a fresh session fixes the second-round finding,
runs the complete gates, and records a new self-review verdict.
