# Fresh independent review append — P0.13 Lock, unlock, save, load

**VERDICT: FAIL**

Findings: 3 (2 blocking, 1 advisory — all must be addressed)
Reviewed commit: `f3eb4474e9658b4032091129d8e10ba3b6fad50a`
Reviewed base: `9a4567a9e371a924b8e43810a759e2e81890b48d` (`main`)
PR: #21 (`p0.13-lock-save-load` → `main`)
Reviewed by: GPT-5.6 Thinking
Date: 2026-08-05

## 1. What I verified

The fresh reviewer evidence matched the exact live PR before and after collection: PR #21 was OPEN, non-draft, based on exact `main` `9a4567a9e371a924b8e43810a759e2e81890b48d`, and headed by exact `f3eb4474e9658b4032091129d8e10ba3b6fad50a`. GitHub reported `MERGEABLE / CLEAN`. The reviewed commit is a two-parent merge with first parent `4f56909dc696a20b96a72d67a3d94daecadbe105` and second parent exact current main.

Independent verification in fresh disposable clones produced:

```text
git diff --check main...f3eb4474...
PASS

npm ci
PASS

npm run verify-vendor
PASS

npm run verify-vendor -- --offline
PASS

npm run lint
PASS

node --test test\vault.test.js test\protocol.test.js
22/22 PASS

npm test
57/57 PASS

npm run test:browser
PASS — Chromium and Firefox
```

The browser harness independently exercised blob download and manual base64/QR save/load, CSP/network blocking, cold-realm failure handling, missing-randomness lockdown, tamper rejection, and both warm- and cold-origin `Esc Esc` panic paths. Both panic paths created an unlocked vault first and then verified locked cold-session state, cleared working bytes, cleared passphrase input, and concealed warm UI.

The P0.13 session implementation was independently inspected. `openSession` is cold-only; the returned session does not retain the DEK; `save()` re-checks vault health and current network mode; online saves fresh-encrypt the public compartment while preserving the secret nonce/ciphertext region opaquely; offline saves rotate both compartment nonces; and `close()` zeroes retained key/plaintext buffers.

The old P0.13 findings are otherwise remediated:

- `.cbx` is the active file extension. The only `.cbxvault` match is a historical packet sentence saying the old spelling was removed.
- `navigator.share`, local multipart QR generation, frame counting, and ordered reassembly are implemented.
- Repeated-save nonce behavior and online opaque-secret preservation pass.
- Warm and cold panic cleanup now has substantive browser coverage.

Two builds from different paths, locales, and timezones were byte-identical:

```text
SHA-256: f82b16efd894b43add0d8ead8148560ae9c172b5d4cf20cd8a2bae09f3826d59
Bytes:   582354

Build A: UTC / C
Build B: Pacific/Auckland / fr_FR.UTF-8
```

Corrupted-vendor and unmanifested-vendor fixtures both failed with non-zero exits. The evidence scan was clean.

The reviewer-owned source-probe's `.cbxvault` failure was a false positive against the historical explanatory sentence, not an implementation defect. The extra no-op `Math.random()` injection was also not counted as a P0.13 finding: the actual candidate contains no executable `Math.random` fallback, the repository's own targeted randomness-fallback mutation test passes, and P0.13 does not modify the forbidden-construct lint contract.

## 2. What I could not verify

A physical iPhone/iOS Safari Files-app run was not available to this independent reviewer. No author, user, or prior-review result was substituted for independent physical-device verification.

Exact current `main` still requires:

> **Accept:** at least one save path works on every device-matrix platform; **the manual base64 path is a complete, usable flow on iOS Safari**, not a stub.

The same current-main roadmap note explicitly says the iOS Safari claim cannot be verified without an iPhone and should remain unverified until P0.19.

Under `review-protocol.md`, an acceptance criterion that cannot be independently verified cannot receive PASS.

## 3. Acceptance criteria

| # | Criterion from exact current `main` | Met? | Evidence |
|---|---|---|---|
| 1 | `Three save paths (File System Access, blob download, manual base64/QR); symmetric load; idle auto-lock; Esc Esc panic hide.` | ✅ | File System Access path is implemented where exposed; Chromium/Firefox independently round-trip blob and manual base64/QR paths; session lock/panic behavior passes in both engines. |
| 2 | `Accept: at least one save path works on every device-matrix platform; the manual base64 path is a complete, usable flow on iOS Safari, not a stub.` | ❌ | Chromium/Firefox pass, but no independent physical iOS Safari execution/save/load result exists. The candidate itself remains `[~]`. |

## 4. Findings

### F1 — Exact-current-main iOS Safari acceptance remains independently unverified

**Severity:** blocking verification gate
**Location:** base `docs/05-development/ROADMAP.md:120-124`; reviewer evidence `environment/ios-verification-limit.txt`.

**Observed:** Exact current main requires a complete usable manual-base64 path on iOS Safari and at least one save path on every device-matrix platform. This independent review has no physical iPhone/iOS Safari Files-app execution result. The candidate and ADR-0010 record the iOS local-execution path as blocked rather than passed.

**Expected:** Every exact-current-main acceptance criterion must be met and independently verified before PASS.

**Required action:** Either independently demonstrate the current-main iOS Safari criterion on real hardware, or complete and merge an explicitly authorized roadmap re-baseline that changes the governing criterion before requesting a new P0.13 review. Quick Look is not evidence of Safari execution.

### F2 — PR #21 rewrites the governing P0.13 acceptance criterion before the required roadmap re-baseline exists

**Severity:** blocking governance finding
**Location:** base `docs/05-development/ROADMAP.md:120-124`; candidate `docs/05-development/ROADMAP.md:120-124`; candidate `docs/05-development/packets/p0.13-lock-save-load.md:9,31,65,93-97`; ADR-0010 `Decision` / `Consequences`.

**Observed:** Exact current main says the manual base64 flow must be complete and usable on iOS Safari. PR #21 changes that acceptance text to a "currently supported execution matrix" formulation and its packet says iOS local execution is not in that matrix. At the same time, the candidate roadmap and packet say P0.13 remains `[~]` pending a separate roadmap-owner re-baseline/dependency decision, and ADR-0010 explicitly says it does not rebaseline the P0.13 → P0.14 dependency chain.

**Expected:** The review protocol automatically fails acceptance criteria that are reinterpreted rather than met. A feature PR cannot use its own not-yet-authorized criterion rewrite to turn a failed/unverifiable base criterion into a pass condition.

**Required action:** Resolve the roadmap-owner decision first. Either restore/review against the exact current-main criterion and satisfy it, or land a separately authorized re-baseline that makes the new criterion authoritative, then fresh-review P0.13 against that new base. P0.14 must remain unstarted meanwhile.

### F3 — The exact-tip P0.13 packet/self-review contains stale evidence and malformed reconciliation Markdown

**Severity:** advisory — must fix under the binary protocol
**Location:** `docs/05-development/packets/p0.13-lock-save-load.md:58,76-78`; `docs/05-development/packets/p0.13-lock-save-load.self-review.md:146,152-183`.

**Observed:** The packet still presents `48` passing tests and a `463,439`-byte artifact with SHA-256 `1108F91F...496528CA4` as the refreshed/final P0.13 evidence. The exact reviewed tip independently produces `57` passing tests and a `582,354`-byte artifact with SHA-256 `f82b16ef...3826d59`. The appended reconciliation section has also been damaged by PowerShell backtick interpolation: the intended `` `test/vault.test.js` `` begins as a tab plus `est/vault.test.js`, and the intended fenced `text` block is rendered as malformed `` `\text `` / single-backtick delimiters.

**Expected:** Review packets and self-review evidence must describe the exact tip accurately and render valid Markdown. `review-protocol.md` explicitly treats stale or contradictory documentation as a defect.

**Required action:** Refresh the packet's test/build evidence to the exact tip, label historical evidence clearly rather than presenting it as final, and repair the reconciliation supplement's Markdown/backtick corruption without changing implementation behavior.

## 5. Verdict rationale

The reconciled P0.13 implementation is substantially improved and the previously identified `.cbx`, nonce, manual QR/share, and panic-cleanup defects reproduce as fixed. The fresh session hardening also passes focused and browser verification. The PR nevertheless cannot pass the repository's binary review protocol because exact current `main` still contains an explicit physical-iOS Safari acceptance criterion that this review cannot verify, PR #21 attempts to rewrite that criterion before its own documents say the roadmap re-baseline has been authorized, and the exact-tip review packet/self-review contains stale and malformed evidence. Address all three findings and request a fresh review on the new exact commit.

FAIL
