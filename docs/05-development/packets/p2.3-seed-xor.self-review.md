# P2.3 self-review — batch gate

**Branch:** `p2.3-seed-xor`

**Reviewed tip:** `e6506588bb037c3cb56747a68a92cdd208f79d65`

**Base:** `main` at `5f0fa5e442524033e91bb31006a0ca6288385dc0`

**Round:** 1

**Verdict:** PASS — ready to push and open the P2.3 PR

## What I verified

I re-read the batch-run and review-protocol gates, checked the P2.3
acceptance text verbatim in `docs/05-development/ROADMAP.md`, and read the
complete product diff. The focused and full test suites, vendor verification,
lint, documentation hygiene, reproducible build, and both-engine browser
harness all passed:

```text
npm.cmd test
tests 327
pass 327
fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 181 markdown file(s) checked, 0 warning(s).

npm.cmd run build (twice)
2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97  2223968 bytes
2acc62bd9d0106e983c27c49f0961bc1c63d0b5e0f38d87a7d83900aebd62f97  2223968 bytes

npm.cmd run test:browser
Chromium: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Firefox: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

I built the exact tip in a separate detached worktree under a different path
with `TZ=Pacific/Kiritimati` and `LANG=fr-FR`. It produced the same hash and
size. I inserted `eval('1')` into the temporary worktree's Seed XOR module;
`npm.cmd run build` exited with code 1 and reported the forbidden construct.
The temporary worktree was restored clean and removed; the primary checkout
remained clean.

The independent vector is Coldcard's published three-part 24-word example,
cross-checked by a separate Node built-in SHA-256 reference for all supported
entropy lengths. The source is kept in `test/seed-xor.test.js`, not generated
from the product implementation.

## What I could not verify

No physical iOS, Android, macOS, Linux, Tor, or hardware-wallet execution was
available. These are documented as untested in the packet and are not being
claimed as passes. The item has no `human-required` marker, so this gap does
not block the software batch gate; it remains an independent-review limitation.

## Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | only 12-, 18-, and 24-word BIP-39 phrases are accepted | ✅ | `test/seed-xor.test.js` and the API entropy-length guards. |
| 2 | every generated part is independently valid BIP-39 | ✅ | Focused deterministic/random tests validate every generated part. |
| 3 | deterministic output matches the published Coldcard construction and independent vectors | ✅ | Coldcard example plus independent Node SHA-256 reference pass. |
| 4 | random output refuses missing `crypto.getRandomValues` | ✅ | Focused missing-randomness test and browser lockdown coverage pass. |
| 5 | combine accepts all parts in any order but rejects missing, malformed, mismatched-length, or invalid-checksum parts | ✅ | Focused negatives, arbitrary-order reconstruction, and browser incomplete-set rejection pass. |
| 6 | the source, parts, and combined phrase never cross the cold boundary or persist, are masked by default, and clear on teardown | ✅ | Source-surface test and both-engine browser isolation/masking/teardown checks pass. |
| 7 | docs state that Seed XOR is N-of-N and that any BIP-39 passphrase is separate | ✅ | Three-depth guide, ADR, UI copy, and docs check pass. |

## Findings

None. No advisory or cosmetic concern is being carried forward; under the
batch rule any such concern would be a FAIL requiring another round.

## Verdict rationale

The implementation meets the roadmap criteria, preserves the cold-only and
fail-closed constraints, reproduces the committed build under a different
environment, and rejects a deliberately forbidden construct with a non-zero
exit. The remaining platform matrix gaps are honestly recorded rather than
represented as passes. **PASS.**
