# P2.3 self-review — batch gate

**Branch:** `p2.3-seed-xor`

**Reviewed product commit:** `e6506588bb037c3cb56747a68a92cdd208f79d65`

**Pull request:** [#50](https://github.com/JamesIsChosen/coldbox/pull/50)

**Base:** `main` at `5f0fa5e442524033e91bb31006a0ca6288385dc0`

**Round:** 3 (independent-review remediation)

**Current remediation commit:** `23c73c4e34506af6be0b364da54c9eff5ac7bc61`

**Current verdict:** **PASS (remediation round 4 evidence correction)** - ready for independent re-review

## Historical round-1 record (superseded)

**Verdict:** PASS — ready to push and open the P2.3 PR

## What I verified

I re-read the batch-run and review-protocol gates, checked the P2.3
acceptance text verbatim in `docs/05-development/ROADMAP.md`, and read the
complete product diff. The focused and full test suites, vendor verification,
lint, documentation hygiene, reproducible build, and both-engine browser
harness all passed:

```text
npm.cmd test
tests 327
pass 327
fail 0

npm.cmd run verify-vendor
Vendor verification passed against local files and upstream releases.

npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

npm.cmd run check-docs
Documentation hygiene check passed: 183 markdown file(s) checked, 0 warning(s).

npm.cmd run build (twice, Git-capable process)
9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778  2223951 bytes
9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778  2223951 bytes

npm.cmd run test:browser
Chromium: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Firefox: Seed XOR official-compatible deterministic flow, random flow, masked reveals, incomplete-set rejection, warm isolation, and teardown passed
Browser harness passed in Chromium and Firefox.
```

I built the exact product commit in a separate detached worktree under a
different path with `TZ=Pacific/Kiritimati` and `LANG=fr-FR`; it produced the
same `9a6a0616...` hash and 2,223,951-byte size. The earlier `2acc62bd...`
artifact is reproducible only when the build child cannot invoke Git: the
build then embeds the documented `unknown (no git commit metadata available)`
date fallback, which accounts for the 17-byte difference. The current packet
records both outputs and their cause. The prior deliberate `eval('1')`
negative build remained non-zero and is retained as evidence that the build
still rejects forbidden constructs.

The independent vector is Coldcard's published three-part 24-word example,
cross-checked by a separate Node built-in SHA-256 reference for all supported
entropy lengths. The source is kept in `test/seed-xor.test.js`, not generated
from the product implementation.

## What I could not verify

No physical iOS, Android, macOS, Linux, Tor, or hardware-wallet execution was
available. These are documented as untested in the packet and are not being
claimed as passes. The item has no `human-required` marker, so this gap does
not block the software batch gate; it remains an independent-review limitation.

## Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | only 12-, 18-, and 24-word BIP-39 phrases are accepted | ✅ | `test/seed-xor.test.js` and the API entropy-length guards. |
| 2 | every generated part is independently valid BIP-39 | ✅ | Focused deterministic/random tests validate every generated part. |
| 3 | deterministic output matches the published Coldcard construction and independent vectors | ✅ | Coldcard example plus independent Node SHA-256 reference pass. |
| 4 | random output refuses missing `crypto.getRandomValues` | ✅ | Focused missing-randomness test and browser lockdown coverage pass. |
| 5 | combine accepts all parts in any order but rejects missing, malformed, mismatched-length, or invalid-checksum parts | ✅ | Focused negatives, arbitrary-order reconstruction, and browser incomplete-set rejection pass. |
| 6 | the source, parts, and combined phrase never cross the cold boundary or persist, are masked by default, and clear on teardown | ✅ | Source-surface test and both-engine browser isolation/masking/teardown checks pass. |
| 7 | docs state that Seed XOR is N-of-N and that any BIP-39 passphrase is separate | ✅ | Three-depth guide, ADR, UI copy, and docs check pass. |

## Findings

None. No advisory or cosmetic concern is being carried forward; under the
batch rule any such concern would be a FAIL requiring another round.

## Verdict rationale

The implementation meets the roadmap criteria, preserves the cold-only and
fail-closed constraints, reproduces the committed build under a different
environment, and rejects a deliberately forbidden construct with a non-zero
exit. The remaining platform matrix gaps are honestly recorded rather than
represented as passes. **PASS.**

## Remediation round 2

The independent review's P2.3 finding is resolved. Two Git-capable builds of
the exact product commit produced `9a6a06160b9dc36557b86b71b565111632536c94149fc773c8e7d53551fb3778`
at 2,223,951 bytes, and the alternate-path build under a different
locale/timezone matched. The superseded `2acc62bd...` result is explained by
the restricted child process's documented Git-date fallback to
`unknown (no git commit metadata available)`; it was not the artifact from a
normal Git-capable checkout. The focused Seed XOR tests, full 327-test suite,
vendor verification, lint, documentation check, and Chromium/Firefox browser
harness all pass. **PASS after remediation.**

## Remediation round 3 — current

The fresh independent review at `761c9bf1e112503750caea83925610ed0f7c8e44`
reviewed product commit `5d580ee5fc6d10026e57ccdf3942ede08446cba0` and found
that the `Math.random()` build gate did not cover cold security modules.

Commit `23c73c4e34506af6be0b364da54c9eff5ac7bc61` extends the gate from the
warm capability file to all `src/cold/` modules, adds
`test/fixtures/lint/cold/math-random.fixture`, and adds a build mutation test
against `src/cold/seed-xor.js`. The focused tests, 14 build tests, 328-test
suite, lint, vendor verification, documentation hygiene, and the complete
Chromium/Firefox `file://` harness all passed.

Both Git-capable builds produced
`d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6` at
2,223,951 bytes. No product source changed, so the product artifact remains
the previously reviewed product behavior. **PASS pending fresh independent re-review.**

## Remediation round 4 — evidence correction

The fresh independent review recorded in
`b5b3bbbd730096bd42ffae4e0628a670c098796c` found only that the round-3 build
hash did not reproduce from the exact committed remediation head
`540a3f2ae0d7b026b5030d21387b01e3c9421da2`. No product or security code was
changed.

Two fresh builds from that exact state produced
`d8b163636686ac080e9f142334aa67dbcba4b25507d673bd226ad5cdb7d97aa6` at
2,223,951 bytes both times. The packet and self-review now carry that exact
committed-head evidence. **PASS pending fresh independent re-review.**
