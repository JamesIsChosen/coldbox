# Review: P0.20 — In-app Appropriate Legal Notices (AGPLv3 §5(d))

**VERDICT: FAIL**

Findings: 3 (1 blocking, 2 advisory; any finding is FAIL under `review-protocol.md`)
Reviewed PR: #31
Reviewed branch: `p0.20-legal-notices`
Reviewed commit: `40c288ddea18a52ef17e8d26e97cc36cd8887e25`
Base: `main` at `e41f9d94a3d6bba69037f7cc7df7e046f606cf56`
Review date: 2026-08-08
Independent evidence bundle: `coldbox-runner-pr31-p020-independent-review-v1.2-20260808-004045.zip`
Evidence SHA-256: `ce03498d5713a61b3cfe63910c70656712d0a1c4e2b6b9a9eeae6eb8a670191c`

## 1. What I verified

I reviewed the exact PR head independently rather than accepting the author packet's verdict or evidence.

Preflight and provenance:

- Source checkout was clean on `p0.20-legal-notices`.
- Local HEAD, remote PR branch, and the reviewed commit were all exactly `40c288ddea18a52ef17e8d26e97cc36cd8887e25`.
- Remote `main`, PR base, and merge-base were exactly `e41f9d94a3d6bba69037f7cc7df7e046f606cf56`.
- Node was the repository-pinned `v24.16.0`.
- `git diff --check` passed.
- P0.20 remained `[~]`; the reviewer did not mark it complete.
- The release checklist gate and `package.json` SPDX identifier were located independently.

Independent normal-path verification from fresh clones:

- `npm ci` — PASS.
- `npx playwright install chromium firefox` — PASS.
- `npm run verify-vendor` — PASS against both local vendored files and upstream releases.
- `npm run lint` — PASS.
- `npm test` — PASS, 158/158.
- `node --test --test-concurrency=1 test/legal-notices.test.js` — PASS, 9/9.
- `node scripts/check-docs.js` — PASS, 123 Markdown files, 0 warnings.
- `npm run test:browser` — **FAIL**, detailed as F1 below.
- Chromium did independently reach the P0.20 legal-notices surface with networking disabled and verified the copyright, no-warranty, may-convey, SPDX, and full licence text before the harness later failed in the help-framework check.

Reproducibility and size:

- Reviewer build A, from a fresh clone under a reviewer-selected path/timezone/locale: SHA-256 `fe55f197035b04408b24d3b3b5ebdbace061ddf6219be0d6d41b16c9f02d7205`, 1,080,408 bytes.
- Reviewer build B, from a second fresh clone at a different path and under a different timezone/locale: the same SHA-256 and the same 1,080,408-byte length.
- Exact PR-base build: 1,040,057 bytes.
- Exact measured delta at the pinned runtime: **+40,351 bytes**.
- SPEC §3 target: 3,145,728 bytes; hard cap: 4,718,592 bytes. The P0.20 output is comfortably within both limits.

Independent licence-byte verification:

- Raw repository `LICENSE`: 34,523 bytes, SHA-256 `f63466fc9bf80edfc020ea91e2fe08bfe0aa85fddcfbdf7d20b641ea0d9f4cee`.
- Parsed embedded `PROVENANCE_LICENSE_TEXT`: 34,523 bytes, same SHA-256.
- Independent raw-byte comparison: equal.
- The former MIT licence at `6fdb4a0^:LICENSE` independently confirms `Copyright (c) 2026 James Kent`.

Deliberate fail-closed probes:

- Corrupted vendored `package.tgz` -> `npm run verify-vendor` exited 1 — PASS-PROBE.
- Same corrupted vendor -> `npm run build` exited 1 — PASS-PROBE.
- Build logic deliberately altered to append content to the embedded licence -> legal-notices tests exited 1 — PASS-PROBE.
- `AGPL-3.0-only` deliberately removed/replaced in the UI -> legal-notices tests exited 1 — PASS-PROBE.
- A literal machine path deliberately appended to `LICENSE` -> build and legal-notices tests exited 0 — **finding F2**.

The normal candidate clone remained clean after review.

## 2. What I could not verify

### Firefox P0.20 browser reachability

I could not complete the Firefox half of the required real-browser verification because the reviewed branch's own `npm run test:browser` aborts during the Chromium iteration before Firefox starts. This is not an environment limitation: Playwright browser installation succeeded, Chromium launched, and Chromium's new P0.20 legal-notices check itself passed. The branch then failed its pre-existing help-framework regression assertion. See F1.

No other required P0.20 evidence remains unavailable; the remaining acceptance properties were independently exercised.

## 3. P0.20 acceptance criteria, verbatim

> **Accept:** the notices are reachable from the app's own UI without a network connection and without leaving the file; the embedded licence text is byte-identical to the repository's `LICENSE` (asserted by a test that compares them, so the two cannot drift); the notice states the licence by SPDX identifier `AGPL-3.0-only`; the bundle remains within the [SPEC §3](../01-spec/SPEC.md) size budget with the delta recorded; **no release may be tagged until this ships**, since tagging is a conveyance and §5(d) applies to it — the gate is recorded in [release-checklist.md](release-checklist.md) rather than left to memory.

| Acceptance criterion | Independent result | Evidence / rationale |
| --- | --- | --- |
| Notices reachable from the app's own UI without network and without leaving the file | **FAIL / incomplete** | Chromium's P0.20 offline check passed, but the full required Chromium+Firefox harness exits 1 before Firefox starts because of F1. |
| Embedded licence byte-identical to repository `LICENSE` | **PASS** | Independent raw-byte comparison: 34,523 bytes on both sides, identical SHA-256 `f63466fc...f4cee`; direct test 9/9. |
| Notice states SPDX `AGPL-3.0-only` | **PASS** | Static/runtime checks pass; deliberate SPDX replacement makes the legal-notices test exit 1. |
| Bundle within SPEC §3 budget with delta recorded | **FAIL** | Size budget passes, but the committed baseline/delta record is inaccurate: actual 1,040,057 -> 1,080,408 = +40,351 bytes, not 1,040,074 -> 1,080,408 = +40,334. See F3. |
| Release gate recorded; no release before P0.20 ships | **PASS** | Gate independently located in `release-checklist.md`; P0.20 remains `[~]`. |

## 4. Findings

### F1 — Blocking — P0.20 makes the real Chromium/Firefox harness fail before Firefox runs

**Location:** `scripts/run-browser-harness.js`, `verifyHelpFramework()` and the P0.20 addition to the contextual-help surface.

**Observed:** P0.20 adds a sixth `button.help-context-button[data-help-topic]` for `glossary:appropriate-legal-notices`. After Chromium successfully verifies the new Appropriate Legal Notices offline, `verifyHelpFramework()` still asserts that exactly five contextual-help buttons must survive initialization:

`assert.equal(settledButtonCount, 5, ...)`

The real review run therefore exits 1 with:

`Chromium: all five contextual help buttons must survive app initialization, found 6`

and

`6 !== 5`

Because the harness runs engines serially, Firefox is never reached.

**Expected:** The branch's full `npm run test:browser` must pass end-to-end in both Chromium and Firefox, and the help-framework regression must account for every shipped contextual-help button, including the new P0.20 mapping.

**Required action:** Update the help-framework browser regression to recognize and exercise the complete post-P0.20 contextual-help set, including `glossary:appropriate-legal-notices`, then run the full harness successfully in both Chromium and Firefox. Do not merely bypass or remove the settled-DOM regression.

### F2 — Advisory — `NO_MACHINE_PATHS` no longer checks machine paths embedded through `LICENSE`

**Location:** `test/build.test.js`, new `stripEmbeddedLicenseText()` and the changed no-machine-path assertion.

**Observed:** P0.20 changes the standing output-leak regression from checking the entire built HTML to checking:

`stripEmbeddedLicenseText(html.toString('utf8'))`

The whole `PROVENANCE_LICENSE_TEXT` assignment is removed before `NO_MACHINE_PATHS` runs. The author rationale says this is safe because `test/legal-notices.test.js` separately proves byte identity to the repository `LICENSE`, but byte identity proves only that the build matches its input; it does not prove that the input is machine-path-free.

The independent probe appended this literal line to `LICENSE` in a disposable exact-head clone:

`REVIEW MACHINE PATH SENTINEL: C:\Users\reviewer\coldbox\secret`

Then it ran:

`node --test --test-concurrency=1 test/build.test.js test/legal-notices.test.js`

Result: **21/21 PASS, exit 0**. The machine path is embedded into `build/coldbox.html`, the no-machine-path test strips it before scanning, and the byte-identity test accepts it because the modified `LICENSE` is its source of truth.

This weakens a standing build-output regression in code changed by P0.20.

**Expected:** A literal machine-specific absolute path introduced through any build input, including `LICENSE`, must still make the machine-path regression fail.

**Required action:** Preserve the JSON-escape false-positive fix without creating an unchecked region. For example, independently scan the raw/parsed licence text with the machine-path rule before excluding its JSON-encoded assignment from the whole-document scan. Add a negative regression that injects a real Windows machine path into `LICENSE` and requires non-zero test failure. Equivalent implementations are acceptable if they restore complete coverage.

### F3 — Advisory — the committed bundle-size delta is inaccurate under the repository-pinned runtime

**Location:** `CHANGELOG.md`, `docs/05-development/ROADMAP.md` P0.20 current-state note, and `docs/05-development/packets/p0.20-legal-notices.md`.

**Observed:** Those records state:

`1,040,074 -> 1,080,408 bytes, +40,334 bytes (≈ +39.4 KB)`

The independent review built both exact commits using the repository-pinned Node `v24.16.0`:

- base `e41f9d94...`: **1,040,057 bytes**
- P0.20 `40c288dd...`: **1,080,408 bytes**
- actual delta: **+40,351 bytes**

The 17-byte discrepancy does not threaten the size ceiling; the result remains far below both the 3 MB target and 4.5 MB hard cap. But P0.20's acceptance criterion explicitly requires the delta to be recorded, so the committed record must be accurate.

**Expected:** The recorded baseline, final size, and delta must match reproducible builds of the exact base/head under the repository-pinned runtime.

**Required action:** After remediating F1/F2, rebuild the then-current exact base and remediation tip with Node `24.16.0` and update every P0.20 size record to the resulting numbers. Because remediation may change the tip size, do not simply substitute `+40,351` without remeasuring the final candidate.

## 5. Verdict rationale

P0.20 has substantial independently verified green evidence: the legal text is present, Chromium can reach it offline, the embedded licence is exactly byte-identical to the repository `LICENSE`, SPDX tampering is caught, vendor integrity fails closed, reproducible reviewer builds match across path/timezone/locale, the copyright notice matches the former MIT record, and the output remains well below the SPEC §3 budget.

It cannot receive PASS because:

1. the branch's required real browser harness exits non-zero and never reaches Firefox;
2. the P0.20 workaround in `test/build.test.js` creates a demonstrated blind spot in the standing no-machine-path regression; and
3. the acceptance-required size delta is recorded inaccurately.

Per `docs/05-development/review-protocol.md`, any finding of any severity makes the verdict FAIL. P0.20 must remain `[~]`. PR #31 must not be merged. A remediation session should address F1-F3, then a fresh independent reviewer must re-review the new exact tip.

FAIL
