# P0.12 author self-review gate

**VERDICT: PASS**

Findings: 0
Reviewed implementation commit: `cdc8ca6`
Base: `p0.11-vault-format` at `304b6d9`
Branch: `p0.12-kdf-profiles`
Date: 2026-08-04

This is the author gate for the final P0.12 implementation tip. The reserved independent-review path, `p0.12-kdf-profiles.review.md`, is intentionally absent and remains available for the independent reviewer.

## Gate results

The final implementation was reviewed after the implementation commit. The pass included the full regression suite, focused crypto/vault tests, a clean rebuild, an alternate temporary filesystem root with two locale/timezone combinations, an isolated corrupted-vendor negative control, the Chromium/Firefox browser matrix, direct inspection of the complete diff, and a static warm/cold benchmark-boundary scan.

```text
$ git diff --check
PASS

$ npm.cmd run verify-vendor -- --offline
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Local vendor verified: argon2-browser@1.18.0
Vendor verification passed in offline mode.

$ npm.cmd run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ node --test test\\crypto.test.js test\\vault.test.js
tests 10
pass 10
fail 0

$ npm.cmd test
tests 47
pass 47
fail 0

$ npm.cmd run build, then clean rebuild
Built build/coldbox.html (7968d840a5fb9128476f92fde2aa44a5f7bd307da70868733e1adae4899de6fe)
7968D840A5FB9128476F92FDE2AA44A5F7BD307DA70868733E1ADAE4899DE6FE
354940

$ alternate temporary-root builds: LC_ALL=de-DE TZ=Pacific/Honolulu, then LC_ALL=ja-JP TZ=Asia/Tokyo
Alternate-path locale/timezone builds match: E981A30683956FE2C4492372FCBC43B3C96009AD8A3423D698ED4567A871C72C

$ deliberate one-byte vendor corruption in an isolated copy
Vendor verification failed: Vendor SHA-256 mismatch for @noble/ciphers@2.2.0
Expected non-zero vendor verification exit: 1

$ final static boundary scan
Warm source/template contain no P0.12 benchmark API or controls.

$ npm.cmd run test:browser
Playwright is dev-only; dependency-free build matches byte-for-byte (7968d840a5fb9128476f92fde2aa44a5f7bd307da70868733e1adae4899de6fe)
Browser harness passed in Chromium and Firefox.
```

The direct benchmark report on this development machine measured Fast at 60.6 ms, Standard at 292.1 ms, and Paranoid at 1,658.7 ms. All three returned `passed`; Paranoid carried the warning `Paranoid uses 256 MiB and may fail to allocate on iOS.` The likely-iOS fixture returned `skipped` for Paranoid before allocation and retained the warning.

This current-tip gate includes the remediated P0.11 parent. Its inherited tests additionally prove that online vault opening never derives `cbx/secret/v1`, explicit offline opening fails while online, offline session saves rotate compartment nonces, and online session saves preserve the encrypted secret compartment opaquely.

## What I could not verify

No P0.12 acceptance criterion remains unverified by the author gate. Physical-device timing and real iOS allocation behavior remain untested and are explicitly deferred to P0.19. The warm-shell vault creation workflow does not exist yet and is intentionally P0.13 scope.

## Acceptance review

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Fast/Standard/Paranoid profiles, stored in the header, with an on-device timing benchmark offered before vault creation. | ✅ | Real vault round-trips verify the exact Fast, Standard, and Paranoid header parameters; the cold API measures all three sequentially; Chromium and Firefox verify that the enabled control and API exist only in the cold frame and begin in the not-run state. |
| 2 | **Accept:** all three profiles round-trip; benchmark reports realistic timings; Paranoid warns about iOS allocation failure. | ✅ | Eight focused tests and the 45-test regression suite pass. The benchmark reports real elapsed milliseconds, always carries the Paranoid warning, and the likely-iOS fixture asserts a pre-allocation `skipped` result. |

## Findings

None. Findings: 0.

## Security checklist

- The benchmark API and UI are embedded only in the cold document. The warm source/template have no benchmark identifier, control, or crypto global.
- Benchmark inputs are fixed and non-secret; no user passphrase, vault bytes, DEK, or seed crosses the realm boundary. Inputs and Argon2 output buffers are cleared after each run.
- Fast, Standard, and Paranoid use the exact profile parameters already authenticated by the vault header. The round-trip test rejects any silent fallback by checking the header fields.
- Profile runs are sequential, and likely-iOS detection prevents the known 256 MiB Paranoid allocation before it starts. Other failures become an unavailable result, never an implicit successful Argon2 timing.
- The browser harness retains the opaque-frame, CSP, network-blocking, protocol, tamper, timeout, lockdown, and missing-randomness checks.
- The complete diff was read, `git diff --check` passed, and the reserved `.review.md` path remains absent for independent review.

## Verdict rationale

Both roadmap criteria are met and reproduced by real profile round-trips, measured benchmark output, a deliberate resource-safety fixture, alternate-environment builds, a non-zero vendor failure, and both browser engines. The remaining limitations are intentional and documented: no physical-device evidence, no repeated-sample calibration, and no warm vault creation workflow. No PR was merged.

PASS
