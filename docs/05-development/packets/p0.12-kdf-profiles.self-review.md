# P0.12 author self-review gate

**VERDICT: PASS**

Findings: 0
Implementation/evidence commit: `c6f17accec3e4560aaac09fc389e65490c37402a`
PR base: `main`
Branch: `p0.12-kdf-profiles`
Date: 2026-08-05

This self-review was regenerated for the PR #20 remediation and treats every prior F1-F13 finding as mandatory.

## Gate evidence

- Offline vendor verification: 9 artifacts
- Focused tests: 19/19 pass
- Full tests: 55/55 pass
- Reproducible build: `74650c0d7b29edc78b9ff85631266fdd3a8615198afed42c885070bab609c61e` / 465521 bytes
- Alternate path/locale/timezone builds: identical
- Deliberate corrupted-vendor exit: 1
- Browser harness: Chromium + Firefox PASS; both exercised F3 runtime-airgap lockdown
- Direct benchmark: `{"activeKdf":{"id":"argon2id-standard","label":"Argon2id WASM (standard)","implementation":"Pure-JS @noble AES-GCM passed its NIST test. Argon2id WASM passed the RFC 9106 test and is the active vault KDF.","memoryKiB":65536,"iterations":3,"parallelism":1},"profiles":[{"profile":"fast","id":"argon2id-fast","memoryKiB":19456,"iterations":2,"parallelism":1,"status":"passed","durationMs":53.9,"warning":null},{"profile":"standard","id":"argon2id-standard","memoryKiB":65536,"iterations":3,"parallelism":1,"status":"passed","durationMs":277.8,"warning":null},{"profile":"paranoid","id":"argon2id-paranoid","memoryKiB":262144,"iterations":4,"parallelism":1,"status":"passed","durationMs":1487.9,"warning":"Paranoid uses 256 MiB and may fail to allocate on iOS."}]}`

The evidence file itself is part of the final tracked tip, so it cannot contain its own commit SHA without self-reference. The remediation runner therefore reruns this complete gate after committing packet/self-review and records the exact final SHA plus matching metrics in the external final-tip evidence bundle, as required by `browser-runner-flow.md`.

## Documentation-governance recheck

The follow-up PR #20 author remediation is documentation-only and does not alter the previously reviewed runtime/crypto implementation. Its candidate scope is exactly `BATCH-2026-08-03.md`, `packets/README.md`, `batch-run.md`, this packet, and this self-review. The author gate requires `git diff --check`, strict documentation encoding checks, `npm run lint`, `npm test`, and `npm run build`, plus guards that `ROADMAP.md`, PR #21 base/head, and `docs/preserve-review-reports` remain unchanged. On a successful remediation-runner exit, those checks have all passed. This paragraph is not an independent review and does not create or modify any `.review.md`.

## Acceptance review

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Fast/Standard/Paranoid profiles, stored in the header, with an on-device timing benchmark offered before vault creation. | Yes | P0.12 proves the profile/header/benchmark behavior. It does not claim that absence of creation UI proves relative placement. Current stacked P0.13 at `4f56909dc696a20b96a72d67a3d94daecadbe105` places the benchmark immediately before the vault creation controls; P0.13 was not modified. |
| 2 | **Accept:** all three profiles round-trip; benchmark reports realistic timings; Paranoid warns about iOS allocation failure. | Yes | Positive ordered real timings, zero-clock negative fixture, real round-trips, iOS warning/skip, and two-engine browser coverage. |

## Findings re-check

F1-F13 are all addressed in the implementation and packet. In particular, the benchmark shares the vault-health predicate, Chromium/Firefox exercise the F3 lockdown path, timing cannot succeed at 0 ms, concurrent requests do not overlap allocations, the CSS accessibility/token issues are enforced, the unreachable rejection branch is removed, Argon2 call shape matches vault derivation, and the P0.13 placement dependency is stated honestly.

No independent-review report is created or edited by this author session. A fresh reviewer must write its own runner and verdict.

PASS
