# Review: P0.8 — CSP canary and airgap guard

**VERDICT: PASS**

Findings: 0
Reviewed commit: 1ad2a7e
Reviewed by: Codex (self-review gate)
Date: 2026-08-03

## 1. What I verified

The review was run on `p0.8-csp-canary-airgap-guard` against its declared base `p0.7-message-handshake`. The working tree was clean and the base diff check was clean:

```text
$ git status --short; git branch --show-current; git diff --check origin/p0.7-message-handshake...HEAD
p0.8-csp-canary-airgap-guard
```

The required dependency and source gates passed:

```text
$ npm ci
added 2 packages, and audited 3 packages in 965ms
found 0 vulnerabilities

$ npm run verify-vendor
Vendor verification passed against local files and upstream releases.

$ npm run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm test
tests 24
pass 24
fail 0
```

Two clean builds, with the generated `build` directory removed and its exact path verified between builds, produced the same artifact:

```text
Built build/coldbox.html (ce6fab9b6e347f43a4a7accb31119b75dce3151e11b3bddeb8d42887bfce4bed)
size=98732
sha256=CE6FAB9B6E347F43A4A7ACCB31119B75DCE3151E11B3BDDEB8D42887BFCE4BED

Built build/coldbox.html (ce6fab9b6e347f43a4a7accb31119b75dce3151e11b3bddeb8d42887bfce4bed)
size=98732
sha256=CE6FAB9B6E347F43A4A7ACCB31119B75DCE3151E11B3BDDEB8D42887BFCE4BED
```

A copied checkout at a different filesystem path was built under `LC_ALL=de-DE`, `TZ=Pacific/Honolulu`, then `LC_ALL=ja-JP`, `TZ=Asia/Tokyo`; both builds produced `CE6FAB9B6E347F43A4A7ACCB31119B75DCE3151E11B3BDDEB8D42887BFCE4BED`.

The deliberate forbidden-construct fixture test passed. Its child build assertion requires a non-zero status and the test would fail if the build only logged an error:

```text
$ node --test --test-name-pattern="build invokes forbidden-construct lint and refuses a bad source file"
✔ the build invokes forbidden-construct lint and refuses a bad source file
tests 6
pass 6
fail 0
```

The final browser harness passed in both engines. It verified the normal warm/cold canaries, exact `connect-src` violations, offline green and online amber transitions, runtime red lockdown, all five cold network primitives, stripped-CSP full lockdown/refused vault operations, opaque-frame isolation, and existing tamper/route/responsive checks:

```text
$ npm run test:browser
Chromium: stripped-CSP build entered full lockdown and refused vault operations
Chromium: reusable frame and viewport assertions passed
Firefox: stripped-CSP build entered full lockdown and refused vault operations
Firefox: reusable frame and viewport assertions passed
Browser harness passed in Chromium and Firefox.
```

The built artifact was also inspected directly. The warm policy retains the documented allowlist, the embedded cold policy contains `connect-src 'none'`, `allow-same-origin` has no match, the canary URL is embedded in both realm sources, and no `__COLDBOX_` placeholder remains. The build test additionally checks the five network-state/lockdown markers and the exact warm allowlist.

The complete committed diff was read. It adds one roadmap item only, introduces no new warm allowlist host, preserves the opaque sandbox and typed message boundary, and updates the security policy, tests, packet, and changelog consistently.

## 2. What I could not verify

None of P0.8's acceptance criteria. The roadmap explicitly assigns physical-device confirmation to P0.19; this review makes no real-device claim and treats that future matrix as outside P0.8 scope rather than as a finding.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | `canary fires correctly in both realms` | ✅ | The Chromium and Firefox harnesses observed `connect-src` violations in the warm document and cold frame, and both readiness states were `passed`. |
| 2 | `a build with CSP stripped goes to full lockdown and refuses vault operations` | ✅ | The stripped-CSP fixture removed both policies; Chromium and Firefox reached red/full lockdown, removed the cold frame, and asserted `data-vault-operations="refused"`. |
| 3 | `banner reflects actual network state within 5 s` | ✅ | Playwright offline emulation reached green with `navigator.onLine=false` and returned to amber with `navigator.onLine=true` within the five-second waits in both engines. |

## 4. Findings

None.

## 5. Verdict rationale

The implementation satisfies all P0.8 acceptance criteria, preserves the existing CSP and message-boundary constraints, fails closed when either canary or the runtime guard cannot establish the airgap, and is independently exercised by deterministic Node/build checks plus Chromium and Firefox browser checks. There are zero findings, so the verdict is PASS.
