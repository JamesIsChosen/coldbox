# Review: P0.12 — KDF profiles and benchmark

**VERDICT: FAIL**

Findings: 1 (0 blocking, 1 advisory — all must be addressed)  
Reviewed commit: `2c966cb2e2e29716349c580d460c206c2548e546`  
Reviewed by: OpenAI GPT-5.6 Thinking  
Date: 2026-08-05

## 1. What I verified

This was a fresh independent exact-tip re-review. I did not execute or reuse the author remediation runner or author evidence as proof.

### Start gate and provenance

- Source checkout was clean before substantive review.
- Source branch was exactly `p0.12-kdf-profiles`.
- Local HEAD, live `origin/p0.12-kdf-profiles`, and PR #20 `headRefOid` were all exactly `2c966cb2e2e29716349c580d460c206c2548e546`.
- PR #20 was OPEN with base `main` and head branch `p0.12-kdf-profiles`.
- Current `main` during review was `c937f30059c8e47508ea46297e9e1401f81244e4`; a valid merge base existed.
- PR #21 remained OPEN with base `p0.12-kdf-profiles`, head `p0.13-lock-save-load`, and exact head `4f56909dc696a20b96a72d67a3d94daecadbe105`.
- `docs/preserve-review-reports` remained exactly `084b53ea548ca9a816883219c1c18da1925047f5`.
- Postflight reconfirmed PR #21, the preserve-review branch, PR #20 remote head, source branch, source HEAD, and source cleanliness were unchanged.

### Exact remediation scope

I independently compared the immediately preceding reviewed runtime/security tip

`5d4804078ec453807d482330b1bf971c571fd604`

to the reviewed tip

`2c966cb2e2e29716349c580d460c206c2548e546`.

`5d4804078...` is an ancestor of the reviewed tip. The diff contains exactly these five files:

- `docs/05-development/batch-run.md`
- `docs/05-development/packets/BATCH-2026-08-03.md`
- `docs/05-development/packets/README.md`
- `docs/05-development/packets/p0.12-kdf-profiles.md`
- `docs/05-development/packets/p0.12-kdf-profiles.self-review.md`

No runtime, crypto, CSS, browser harness, vendor, package, test, spec/security implementation tree, independent `.review.md`, or `ROADMAP.md` change occurred between those two tips.

`git diff --check` passed both for the complete current `main...2c966cb...` PR diff and for `5d4804078...2c966cb...`.

### Documentation-governance remediation

I read the exact-tip `AGENTS.md`, `review-protocol.md`, `doc-hygiene.md`, `batch-run.md`, `ROADMAP.md`, packets README, batch handoff, P0.12 packet/self-review, relevant spec/security/build documents, and the complete PR diff.

Independent checks confirmed:

- `BATCH-2026-08-03.md` is now explicitly a **Frozen historical snapshot**.
- It directs later roadmap state to `ROADMAP.md`.
- It directs independent-review history to the relevant per-item `.review.md`.
- It directs live PR base/head/open/closed state to GitHub.
- The former live-state concepts are removed: `resolved since`, `Publication state updated 2026-08-04`, live `Review state`, the post-batch P0.13 item row, `canonical current-tip`, `Current remediation note`, resolved-later stop wording, mutable `Current state`, and `Current batch status`.
- The handoff records P0.12 publication as not completed in-session because `gh pr create` was unavailable, without later-state reconciliation.
- `docs/05-development/packets/README.md` now states that `.self-review.md` never counts as independent review and that per-item `.review.md` verdict history is the canonical procedure.
- Its 2026-08-04 table is explicitly a **Frozen audit snapshot** and explicitly not synchronized with later reviews, merges, or PR changes.
- The P0.12/P0.13 table rows remain historical to 2026-08-04 instead of being rewritten to the present.
- `batch-run.md` now states that finalized `BATCH-<date>.md` handoffs are historical snapshots, later state comes from canonical sources, and actual historical corrections must be explicitly labeled rather than turning the handoff into a rolling dashboard.
- The P0.12 packet and self-review clearly label the documentation closeout as author-only verification, not independent review.
- Strict UTF-8 decoding of all five remediation files succeeded with no NUL or U+FFFD replacement characters.
- All Markdown links introduced or retained in the five remediation documents resolve to tracked exact-tip paths.

### Dependency, test, build, and failure-path verification

Fresh disposable-clone verification independently ran:

- `npm ci` — PASS.
- `npm run verify-vendor -- --offline` under offline/dead-proxy conditions — PASS for 9 vendored artifacts.
- Networked `npm run verify-vendor` — PASS for all 9 local artifacts and all 9 real upstream releases.
- `npm run lint` — PASS.
- Focused `test/crypto.test.js`, `test/vault.test.js`, and `test/accessibility.test.js` — 19 tests, 19 pass, 0 fail.
- Full `npm test` — 55 tests, 55 pass, 0 fail.
- Corrupted-vendor deliberate fixture — exit 1.
- Unmanifested-vendor deliberate fixture — exit 1.

### Reproducible build

Independent builds produced:

- Build 1: 465521 bytes, SHA-256 `74650c0d7b29edc78b9ff85631266fdd3a8615198afed42c885070bab609c61e`
- Clean Build 2: 465521 bytes, same SHA-256.
- Alternate path containing spaces with `LC_ALL=de-DE` and `TZ=Pacific/Honolulu`: 465521 bytes, same SHA-256.

This independently reproduces the author's claimed deterministic artifact exactly.

The built artifact also retained `connect-src 'none'` for the cold realm and contained no `allow-same-origin` sandbox permission.

### P0.12 KDF and benchmark behavior

Independent focused tests and reviewer-owned browser/source probes established:

- Fast, Standard, and Paranoid all round-trip through real vault creation/opening and retain their profile parameters in the authenticated header.
- Benchmark and vault Argon2 invocation shapes are identical: `pass`, `salt`, `time`, `mem`, `parallelism`, `hashLen`, and `type`; neither benchmark invocation adds `secret` or `ad`.
- A real Chromium benchmark produced:
  - Fast: 52.6 ms
  - Standard: 256.6 ms
  - Paranoid: 1385.4 ms
- Therefore the reproduced ordering was strictly positive `Fast < Standard < Paranoid`.
- Paranoid reported: `Paranoid uses 256 MiB and may fail to allocate on iOS.`
- A likely-iOS fixture skips the Paranoid allocation.
- A constant/non-advancing timing source produced `unavailable` results with `durationMs: null`; it could not yield a successful zero-duration benchmark.
- Concurrent benchmark requests returned the same in-flight promise/report and instrumentation recorded `maxActive=1`, so Argon2 profile allocations did not overlap.
- Chromium and Firefox `file://` runs both triggered the runtime-airgap refusal path.
- In both engines, vault operations became `refused` and the benchmark control became disabled.
- The independent probe forcibly set the button back to enabled and clicked it; Argon2 call count did not increase, vault refusal persisted, and the control returned to disabled.
- The tracked browser harness independently exercised the explicit F3 runtime-airgap path in both Chromium and Firefox.
- At a 360px viewport, Chromium measured the benchmark control at approximately 156.45 x 44 CSS px and Firefox approximately 156.45 x 44 CSS px.
- Both engines exposed the required focus-visible treatment.
- Source retains the exact `0.2rem` solid pink focus outline with `0.18rem` offset and `2.75rem` minimum width/height.
- Disabled styling uses `var(--cold-disabled-fill)`; its exact value matches the canonical design-system `--fill-disabled` value, and the disabled rule contains no inline hex.
- The stale P0.10 build-panel wording is gone.
- The prior unreachable benchmark rejection branch is gone.

### P0.12 / P0.13 integration boundary

P0.12 itself contains no vault-creation controls, so I did not treat their absence as proof of ordering.

Read-only inspection of the required unchanged PR #21 head `4f56909dc696a20b96a72d67a3d94daecadbe105` found:

- `#cold-kdf-benchmark` at source position 1421
- `#cold-vault-controls` at position 1802
- `#cold-vault-create` at position 2393

The benchmark therefore precedes the creation controls in the dependent integration head without modifying PR #21.

## 2. What I could not verify

Nothing required by the P0.12 roadmap acceptance criteria or this re-review prompt remains unverified.

Physical-device timing and real iOS allocation behavior were not claimed as P0.12 acceptance evidence; the packet/ROADMAP defer that physical-device work to P0.19. The P0.12 requirement here is the explicit allocation warning and fail-safe likely-iOS behavior, both of which were independently verified.

## 3. Acceptance criteria verbatim from ROADMAP.md

| # | Criterion | Met? | Independent evidence |
|---|---|---|---|
| 1 | Fast/Standard/Paranoid profiles, stored in the header, with an on-device timing benchmark offered before vault creation. | ✅ | All three real profiles round-trip with stored header parameters; benchmark runs cold-only; read-only PR #21 integration inspection places the benchmark before `#cold-vault-controls` and `#cold-vault-create`. |
| 2 | **Accept:** all three profiles round-trip; benchmark reports realistic timings; Paranoid warns about iOS allocation failure. | ✅ | 19/19 focused tests pass; real timings were 52.6 ms < 256.6 ms < 1385.4 ms; Paranoid carries the 256 MiB/iOS allocation warning; broken timing fails closed. |

Both P0.12 roadmap criteria are independently met.

## 4. Findings

### F1 — P0.12 packet uses a malformed Markdown fence for the direct benchmark evidence

**Severity:** advisory  
**Location:** `docs/05-development/packets/p0.12-kdf-profiles.md:42-44`

**Observed:** The `Fresh direct benchmark:` block opens with ```` ``json ```` and closes with ```` `` ````, i.e. two backticks rather than a valid fenced-code delimiter. The exact raw reviewed commit reproduces the malformed delimiters; this is not an evidence-bundle encoding artifact.

**Expected:** The JSON evidence block should use a valid Markdown fenced code block, opening with three backticks plus `json` and closing with three backticks. Documentation is explicitly in scope, and the review protocol says any cosmetic/advisory finding is a FAIL.

**Required action:** Change only the malformed fence delimiters to a valid ` ```json ` / ` ``` ` pair, preserve the benchmark content, run `git diff --check` and documentation checks, and request a fresh exact-tip independent re-review.

## 5. Verdict rationale

The substantive P0.12 implementation and the documentation-governance remediation independently reproduce successfully at exact commit `2c966cb2e2e29716349c580d460c206c2548e546`. Both ROADMAP acceptance criteria are met; all F1-F13 runtime/security remediation behavior was independently re-exercised; builds are deterministic; vendor provenance passes against upstream; deliberate failures return non-zero; Chromium and Firefox fail closed; PR #21 and `docs/preserve-review-reports` are unchanged; and the five-file historical-document lifecycle remediation is sound.

However, the P0.12 packet at the reviewed tip contains one malformed Markdown evidence fence. The repository review protocol states that any finding of any severity, including cosmetic/advisory documentation defects, requires FAIL. Correcting that fence and obtaining a fresh exact-tip re-review is sufficient for this review to become eligible for PASS if no new finding appears.

FAIL
---

# Fresh exact-tip re-review: P0.12 — fence remediation

**VERDICT: PASS**

Findings: 0  
Reviewed commit: `c8daaf54debfbd3bd84814172fbd92694b8e32eb`  
Immediately preceding reviewed commit: `2c966cb2e2e29716349c580d460c206c2548e546`  
Reviewed by: OpenAI GPT-5.6 Thinking  
Date: 2026-08-05

## 1. What I verified

This is a fresh independent verdict on the new exact commit, not an amendment to the preceding FAIL verdict.

### Start gate and protected prior review

- Source branch was exactly `p0.12-kdf-profiles`.
- Local HEAD, live `origin/p0.12-kdf-profiles`, and PR #20 `headRefOid` were all exactly `c8daaf54debfbd3bd84814172fbd92694b8e32eb`.
- PR #20 remained OPEN with base `main`.
- PR #21 remained OPEN with base `p0.12-kdf-profiles`, head branch `p0.13-lock-save-load`, and exact head `4f56909dc696a20b96a72d67a3d94daecadbe105`.
- The source working tree contained exactly one dirty path: the untracked prior independent review report `docs/05-development/packets/p0.12-kdf-profiles.review.md`.
- The prior report was read before substantive review, retained its previous FAIL verdict and sole malformed-fence finding, and remained byte-identical throughout evidence collection at SHA-256 `489c9f9843673daef81ec74cc7d0e54a5cf1f15742126af4e83629aae9c31cfe`.
- `docs/preserve-review-reports` remained `084b53ea548ca9a816883219c1c18da1925047f5`.
- No PR, ROADMAP, or source mutation occurred during evidence collection.

### Exact remediation scope

I independently compared:

`2c966cb2e2e29716349c580d460c206c2548e546..c8daaf54debfbd3bd84814172fbd92694b8e32eb`

The delta is exactly one file:

`docs/05-development/packets/p0.12-kdf-profiles.md`

`git diff --numstat` is exactly:

`2	2	docs/05-development/packets/p0.12-kdf-profiles.md`

The patch contains exactly these two replacements and nothing else:

- opening fence: two-backtick `json` fence → triple-backtick `json` fence;
- closing fence: two backticks → three backticks.

`git diff --check` passed for both the fence-remediation delta and the complete current `main...c8daaf54...` PR diff.

The `Fresh direct benchmark` body is byte-for-byte identical to the body at `2c966cb2...` and parses successfully as JSON. No runtime, security, CSS, browser, test, vendor, package, governance, self-review, or ROADMAP content changed between the two reviewed tips.

### Required exact-tip protocol gates

Although the remediation delta is documentation-only, the baseline gates required by `review-protocol.md` were rerun independently on the exact new tip:

- Node pin: `v24.16.0` against repository pin `24.16.0`.
- `npm ci` — PASS.
- Offline vendor verification with offline/dead-proxy settings — PASS.
- Real upstream vendor verification — PASS for all 9 vendored artifacts and all 9 upstream releases.
- `npm run lint` — PASS.
- Focused crypto/vault/accessibility suite — 19 tests, 19 pass, 0 fail.
- Full `npm test` — 55 tests, 55 pass, 0 fail.
- Fast/Standard/Paranoid vault round-trip test — PASS.
- Clean build 1 — 465521 bytes, SHA-256 `74650c0d7b29edc78b9ff85631266fdd3a8615198afed42c885070bab609c61e`.
- Clean build 2 — identical bytes/hash.
- Alternate path containing spaces with `LC_ALL=de-DE` and `TZ=Pacific/Honolulu` — identical bytes/hash.
- Corrupted-vendor deliberate negative fixture — non-zero exit 1.
- Unmanifested-vendor deliberate negative fixture — non-zero exit 1.
- Chromium browser harness — PASS, including explicit runtime-airgap benchmark-disable path.
- Firefox browser harness — PASS, including explicit runtime-airgap benchmark-disable path.
- Built cold realm retains `connect-src 'none'`.
- Built artifact contains no `allow-same-origin` sandbox permission.

Because all implementation/browser/test/vendor/package/governance/ROADMAP content is byte-identical to the immediately preceding independently reviewed tip, this exact delta does not invalidate the preceding full F1-F13 runtime/security review. The repository's exact-tip tests, build, failure controls, and Chromium/Firefox harness were nevertheless rerun and passed.

### ROADMAP acceptance criteria

1. `Fast/Standard/Paranoid profiles, stored in the header, with an on-device timing benchmark offered before vault creation.` — **MET**.
2. `**Accept:** all three profiles round-trip; benchmark reports realistic timings; Paranoid warns about iOS allocation failure.` — **MET**.

P0.13 and P0.14 remain unchecked in `ROADMAP.md`. Read-only PR #21 inspection still places `#cold-kdf-benchmark` before `#cold-vault-controls` and `#cold-vault-create`.

## 2. What I could not verify

Nothing required for this exact-tip re-review remains unverified.

## 3. Previous finding re-check

### Prior F1 — malformed Markdown fence around `Fresh direct benchmark`

**Status: CLOSED.**

At `c8daaf54debfbd3bd84814172fbd92694b8e32eb`, the block now opens with a valid triple-backtick `json` fence and closes with a valid triple-backtick fence.

The benchmark JSON body itself is unchanged from `2c966cb2e2e29716349c580d460c206c2548e546` and parses as valid JSON.

No unrelated edit accompanied the correction.

## 4. New findings

None.

## 5. Verdict rationale

The only finding from the immediately preceding independent review is independently confirmed closed. The remediation is exactly the two intended Markdown-fence replacements in one file; no other source, implementation, governance, ROADMAP, test, package, vendor, or browser content changed.

All baseline review-protocol gates rerun on the exact new tip passed, including upstream vendor verification, focused/full tests, deterministic and alternate-environment builds, deliberate non-zero failure fixtures, and Chromium/Firefox browser verification.

Both P0.12 ROADMAP acceptance criteria remain independently met. PR #21 and `docs/preserve-review-reports` remain unchanged. No new finding of any severity was identified.

PASS
