# Self-review: P0.8 — CSP canary and airgap guard remediation

**VERDICT: PASS**

Reviewed commit: `da9832e`
Reviewed base: `p0.7-message-handshake` at `ad9048d`
Date: 2026-08-03

This is the authoring-session gate. The independent reviewer report remains reserved for
`p0.8-csp-canary-airgap-guard.review.md`; this file does not occupy that path.

## Review gates

- `npm ci` completed with 2 packages installed and 0 vulnerabilities.
- `npm run verify-vendor -- --offline` passed against the pinned local vendor artifacts. The upstream registry half was not exercised in this environment.
- `npm run lint` passed.
- `npm test` passed: 36/36.
- `npm run build` passed with artifact SHA-256 `a63e9b41bbac9b1d788deb87f0040b9ac6df65799ad1340293d18d5a9dd6b73b` and size `113550` bytes.
- A second build and a copied different-path build under `de-DE`/Pacific Honolulu and `ja-JP`/Asia Tokyo reproduced the same SHA-256 and size.
- `npm run test:browser` passed in Chromium and Firefox.

## Consolidated review fixes

- The cold canary now uses `http://localhost:9/cold-csp-canary`, permitted by the inherited warm allowlist but denied by the cold `connect-src 'none'` policy; warm and cold `blockedURI` values are asserted exactly.
- Exact canary near-misses and rejected-without-CSP behavior have Node coverage.
- All five runtime blockers, installation failure, descriptor flags, attempt reporting, and direct prototype-owner calls have Node coverage; the browser harness repeats the prototype bypass checks.
- Browser network evidence requires either an individual exact CSP violation in the CSP-only fixture or the labelled `Coldbox airgap blocked <primitive>.` runtime signal; DNS failure alone cannot satisfy a P0.8 runtime assertion.
- Warm-only, cold-only, and both-policy CSP-loss fixtures all enter full lockdown and refuse vault operations; the cold-only fixture refreshes the modified outer script hash so the warm policy remains active for the asymmetric test.
- `SPEC.md`, `architecture.md`, `csp-policy.md`, `ROADMAP.md`, `CHANGELOG.md`, and the packet now use the shipped five-second interval, checking/unknown state vocabulary, corrected canary polarity, prototype-owner guard semantics, and truthful offline verification wording.
- The author PASS was removed from the reserved `.review.md` path and replaced by this `.self-review.md` file.
- The dependency stack retains P0.6's literal throw contract while P0.8 adds its later five-primitive runtime guard and exact native CSP canary evidence.

No merge, rebase, retarget, or PR merge was performed during this remediation gate.
