# Self-review: P0.5 — Warm shell skeleton

**VERDICT: PASS**

Findings: 0
Reviewed commit: fe5dfb9
Reviewed by: Codex self-review gate (not an independent PR review)
Date: 2026-08-03
Rounds: 1

## 1. What I verified

- Re-read `docs/05-development/review-protocol.md` and checked the complete `fe5dfb9` diff.
- Confirmed `git diff main...HEAD -- docs/05-development/ROADMAP.md` changes exactly one roadmap item: P0.5.
- Ran `npm ci`: `added 2 packages, and audited 3 packages`; `found 0 vulnerabilities`.
- Ran `npm run verify-vendor`: local and upstream release verification passed for all six vendored packages.
- Ran `npm run lint`: `Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.`
- Ran `npm test -- --test-concurrency=1`: 18 tests passed, 0 failed.
- Ran `npm run build` twice: both emitted `d2786acaedb51eba80f4be66d24d48de4f8efb50e96ef34eda1364f65743799a`.
- Built a clean copied tree under a different temporary path with `LC_ALL=fr-FR` and `TZ=Pacific/Honolulu`: the SHA-256 was `D2786ACAEDB51EBA80F4BE66D24D48DE4F8EFB50E96EF34EDA1364F65743799A`.
- Ran `npm run test:browser`: Chromium and Firefox both passed route, theme, responsive, overflow-menu focus restoration, CSP, tamper, and reusable-frame checks over `file://`.
- Added `eval` to a temporary LF-preserving copy of `src/main.js`: the build reported `Forbidden construct "eval"` and returned exit code `1`.
- Confirmed the warm policy now contains `worker-src blob:`, the faint-text contrast test passes for both themes, and Escape returns focus to `#mobile-more-tab`.

## 2. What I could not verify

No automated P0.5 criterion remained unverifiable: the project harness ran in Chromium and Firefox. Manual Safari, mobile, macOS, Linux, and Tor device behavior is intentionally deferred to the human-required P0.19 matrix, as stated by the roadmap and packet; it was not inferred from the headless runs.

## 3. Acceptance criteria

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| 1 | Outer document, CSP per [csp-policy.md](../../02-security/csp-policy.md), nav rail and mobile tab bar, routing, dark/light. No features. | ✅ | The committed HTML/CSS/JS provide the feature-free shell; the browser harness checks the rail, tabs, routes, theme, and overflow-menu focus; the build test checks the canonical CSP, including `worker-src blob:`. |
| 2 | **Accept:** loads from `file://` on the full device matrix; no console errors; responsive from 360 px to desktop. | ✅ | Chromium and Firefox pass over `file://` at 360×640 and 1440×900 with no console errors, no CSP violations, and no horizontal overflow. Real-device confirmation is assigned to P0.19 by the roadmap. |

## 4. Findings

None.

## 5. Verdict rationale

Every P0.5 criterion is met within the roadmap’s stated browser-verification boundary. The warm CSP is canonical and hash-pinned, the navigation now meets the tested focus and contrast controls, the build is reproducible across path/locale/timezone variation, deliberate tampering and forbidden constructs fail with non-zero status, and the branch contains exactly one roadmap item. **PASS.**
