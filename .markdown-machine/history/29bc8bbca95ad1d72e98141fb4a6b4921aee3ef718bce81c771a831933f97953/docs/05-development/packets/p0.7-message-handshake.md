# PR packet: P0.7 - MessageChannel handshake and schema validator

## 1. Summary

P0.7 replaces the provisional cold-realm readiness-only path with a private `MessageChannel` handshake and a shared typed protocol validator. The warm shell accepts the payload-free `cold.ready` bootstrap signal only from the created frame and only without transferred ports, creates a channel, and transfers `port2` in one exact `cold.handshake` message. The cold realm attaches `port1`, replies with a validated `ready` message, and both sides discard and log all later traffic received on the global `message` handler.

The protocol exposes only the message types defined by [architecture.md](../../01-spec/architecture.md), requires `{ id, type, payload }` on private-channel messages, strips unknown fields, and projects public records through an explicit allowlist. No cold-to-warm message shape can carry mnemonic, private key, xprv, passphrase, or secret-compartment plaintext fields.

This branch is based on the unmerged `p0.6-cold-realm-bootstrap` branch. Its PR base must therefore be `p0.6-cold-realm-bootstrap`.

## 2. Scope

### In scope

- Shared warm/cold protocol definitions and payload sanitizers in `src/protocol.js`
- Exact warm-to-cold and cold-to-warm message whitelists from the architecture specification
- `MessageChannel` creation, port transfer, typed readiness reply, and terminal handshake state
- Global-message discard and anomaly logging after the private channel is established
- Protocol unit tests for unknown types, unknown-field stripping, public projections, recognizable secret content, and aggregate payload limits
- Visible anomaly warnings in both realms and Node-level handshake guard mutation contracts
- Chromium and Firefox coverage for handshake readiness and post-handshake global-message injection
- Roadmap, changelog, and permanent PR-packet documentation

### Deliberately out of scope

- CSP canary, network-state banner, and runtime network neutering (P0.8)
- Capability detection, cryptography, vault storage, and secret-bearing operations
- Real-device certification, which remains P0.19
- Any merge of this branch or its dependency branches

## 3. How to verify

Run from the repository root:

```text
$ npm ci
added 2 packages, and audited 3 packages in 1s

found 0 vulnerabilities

$ npm run verify-vendor
Local vendor verified: @noble/ciphers@2.2.0
Local vendor verified: @noble/curves@2.2.0
Local vendor verified: @noble/hashes@2.2.0
Local vendor verified: @scure/base@2.2.0
Local vendor verified: @scure/bip32@2.2.0
Local vendor verified: @scure/bip39@2.2.0
Upstream release verified: @noble/ciphers@2.2.0
Upstream release verified: @noble/curves@2.2.0
Upstream release verified: @noble/hashes@2.2.0
Upstream release verified: @scure/base@2.2.0
Upstream release verified: @scure/bip32@2.2.0
Upstream release verified: @scure/bip39@2.2.0
Vendor verification passed against local files and upstream releases.

$ npm run lint
Lint passed: forbidden constructs, JavaScript syntax, and LF source line endings are valid.

$ npm test
tests 30
pass 30
fail 0

$ npm run build
Built build/coldbox.html (696af7e33ffbe32d10cde77a3ceefba152294352f69204f1981a7434e6992b03)

$ npm run build
Built build/coldbox.html (696af7e33ffbe32d10cde77a3ceefba152294352f69204f1981a7434e6992b03)

$ Get-FileHash -Algorithm SHA256 -LiteralPath 'build\\coldbox.html'
696AF7E33FFBE32D10CDE77A3CEEFBA152294352F69204F1981A7434E6992B03

$ npm run test:browser
Chromium: warm shell routes, theme switch, responsive navigation, and cold boundary passed over file://
Chromium: cold realm creation failure produced an explicit lockdown state
Chromium: cold realm readiness timeout removed the frame and locked down
Chromium: typed handshake timeout used distinct copy and locked down
Firefox: warm shell routes, theme switch, responsive navigation, and cold boundary passed over file://
Firefox: cold realm creation failure produced an explicit lockdown state
Firefox: cold realm readiness timeout removed the frame and locked down
Firefox: typed handshake timeout used distinct copy and locked down
Browser harness passed in Chromium and Firefox.
```

The final `build/coldbox.html` is 87,663 bytes. A clean temporary copy at a different path with `LC_ALL=ja-JP` and `TZ=Asia/Tokyo` produced the same SHA-256: `696AF7E33FFBE32D10CDE77A3CEEFBA152294352F69204F1981A7434E6992B03`.

The deliberate temporary `src/cold/deliberate-review-eval.js` fixture made the build exit with status `1`, reporting `Forbidden construct "eval"` and `Build refused: source failed forbidden-construct lint`.

## 4. Acceptance criteria

| Criterion | How satisfied | Test |
|---|---|---|
| Handshake transferring a port; typed whitelist schema per architecture; global `message` handler ignored after handshake. | The warm shell accepts only the exact payload-free `cold.ready` bootstrap signal from its frame, creates a `MessageChannel`, transfers `port2` in one `cold.handshake` message, and accepts the typed `ready` response only on `port1`. The child attaches the transferred port and treats every later global message as anomalous discard/log traffic. | `test:browser` waits for the private-channel ready state, and injects global messages into both the warm shell and cold frame after handshake. |
| Schema rejects unknown types and strips unknown fields. | `validateMessage()` requires a known direction and type, sanitizes each payload shape, returns only `{ id, type, payload }`, and drops unknown envelope/payload/record fields. | `test/protocol.test.js` covers both direction whitelists, unknown types, envelope fields, and payload fields. |
| No message type can carry a mnemonic, private key, xprv, passphrase, or secret-compartment plaintext. | The cold-to-warm public projection has no free-form text fields. It accepts only UUIDs, fingerprints, validated xpubs, validated public addresses, and numeric accounting values; arbitrary strings, string arrays, unknown nested text, recognizable secret encodings, and raw 32-byte private-key hex are rejected. `vault.open` accepts ciphertext bytes only, and `vault.bytes` accepts bytes only. Non-vault messages are capped at 4 MiB; encrypted byte payloads are capped at 64 MiB. | `test/protocol.test.js` exercises every cold-to-warm type, every former free-form carrier, ordinary-looking passphrases, raw private-key hex, arbitrary secret prose, recognizable secret content, and both size limits. |
| Messages injected on the global handler post-handshake are discarded and logged. | Both realms retain only a discard/logging global listener after handshake; no global message is validated or routed as application traffic, and each realm reveals a visible warning as well as a counter/log entry. | Node handshake guard mutation tests and `test:browser` assert source/port checks, one warm-side and one cold-side anomaly counter, fixed warning text, and visible warnings in Chromium and Firefox. |

## 5. Security impact

- Realm boundary: **Preserved.** The cold frame remains `sandbox="allow-scripts allow-downloads"` without `allow-same-origin`; the parent does not read its DOM or variables.
- Message schema: **Yes.** Private-channel traffic is direction-scoped, type-whitelisted, structurally validated, and reduced to safe allowlisted fields. The only global controls are the payload-free `cold.ready` bootstrap signal and the exact `cold.handshake` transfer envelope.
- Secret exposure: **Blocked by construction at this boundary.** The public projection has no free-form text carriers. Only closed structural validators for UUIDs, fingerprints, xpubs, public addresses, and numbers can cross; arbitrary passphrases, private-key text, and secret-compartment prose are rejected. No secret operation exists yet.
- Payload bounds: **Explicit.** Public sanitized payloads are limited to 4 MiB and encrypted vault byte messages to 64 MiB, with fail-closed rejection.
- CSP: **Unchanged and preserved.** The shared protocol source is embedded into both inline realms before the existing hash-pinning and cold-realm CSP assembly.
- Network: **Not expanded.** P0.8 still owns the canary, network-state signals, and runtime neutering; the cold realm retains `connect-src 'none'`.
- Origin handling: The channel uses `'*'` for `postMessage` because the sandboxed `srcdoc` frame has an opaque origin; the transferred port, not a reusable origin, is the private application channel.
- Global handlers: They remain installed solely to discard and log anomalous post-handshake messages. A global message cannot reopen the handshake or reach application protocol handling.

## 6. Test evidence

### New and changed tests

- `src/protocol.js` defines the shared protocol version, direction-specific message tables, payload sanitizers, closed structural public-field allowlists, and exact handshake controls.
- `scripts/build.js` embeds the protocol source into both warm and cold inline realms and rejects unresolved protocol placeholders.
- `src/main.js` creates/transfers the channel, validates the cold `ready` response, records terminal failure, and discards post-handshake global traffic.
- `src/cold/main.js` accepts the transferred port, validates channel messages, emits typed readiness, and discards post-handshake global traffic.
- `test/protocol.test.js` covers the complete documented whitelist and secret-field invariants.
- `test/handshake.test.js` asserts the warm/cold source and port guards and fails on mutation fixtures that remove them.
- `scripts/run-browser-harness.js` and `test/browser/harness.js` verify private-channel readiness, global anomaly counters, warning logs, and existing CSP/opaque-frame checks.

### Negative tests

- Unknown warm-to-cold and cold-to-warm types return `null` from the validator.
- Unknown envelope, payload, and collection fields are stripped when they contain no text; string-bearing unknown public-record fields are rejected so they cannot become covert carriers.
- Secret-bearing fields injected into every cold-to-warm message shape do not survive validation.
- Recognizable xprv-shaped content, a WIF-shaped content string, a 12-word BIP-39 test mnemonic, raw 32-byte private-key hex, ordinary passphrases, and arbitrary secret prose are rejected from every former public text carrier.
- A vault byte payload above 64 MiB and a public projection above 4 MiB are rejected.
- Removing the warm post-handshake discard or cold transferred-port count check fails the Node contract test.
- A global message injected into the warm shell after handshake is discarded and logged.
- A global message injected into the cold frame after handshake is discarded and logged.
- Each injected anomaly reveals a visible warning in its own realm.
- Deliberate iframe creation failure still produces a visible terminal lockdown state.
- A child that signals `cold.ready` but suppresses its typed `ready` response reaches the distinct private-channel timeout copy and is removed.
- Deliberate forbidden `eval` source makes the build exit non-zero.
- Existing cold `fetch`, XHR, and WebSocket blocking, parent opacity, CSP violation, and byte-tampering checks continue to pass.

### Not tested

No real iOS Safari, Android Chrome from Files, macOS Safari, Linux Firefox, Tor Browser, or physical-device behavior was available. Those results are not inferred from headless Chromium and Firefox and remain part of P0.19.

## 7. Device matrix

| Platform | Result | Notes |
|---|---|---|
| Windows Chrome | Automated pass | Bundled headless Chromium, `file://`; MessageChannel readiness, global discard logging, cold CSP, network blocking, opacity, routes, and lockdown check |
| Windows Firefox | Automated pass | Bundled headless Firefox, `file://`; MessageChannel readiness, global discard logging, cold CSP, network blocking, opacity, routes, and lockdown check |
| macOS Safari | Untested | Requires the P0.19 manual matrix |
| macOS Chrome | Untested | Requires the P0.19 manual matrix |
| Linux Firefox | Untested | Requires the P0.19 manual matrix |
| **iOS Safari (Files)** | Untested | Highest-risk manual platform; requires P0.19 |
| Android Chrome (Files) | Untested | Requires the P0.19 manual matrix |
| Tor Browser | Untested | Requires the P0.19 manual matrix |

## 8. Assumptions made

> **Assumed:** The payload-free global `cold.ready` signal remains the liveness bootstrap before the transferred port exists.
> **Basis:** The architecture explicitly separates cold readiness from the subsequent `MessageChannel` transfer; the bootstrap signal carries no identifier, payload, public data, or secret.
> **Not verified on:** Real-device browsers.
> **If wrong:** A later architecture decision must change the bootstrap boundary before secret-bearing operations are added; the private protocol does not depend on accepting global application messages.

> **Assumed:** `'*'` is the correct target origin for both bootstrap and channel-transfer messages.
> **Basis:** The sandboxed `srcdoc` child intentionally has an opaque origin, so no stable origin can be used for an origin-targeted transfer. The parent checks the frame source before accepting readiness, and subsequent application traffic uses the transferred port.
> **Not verified on:** Real-device browsers.
> **If wrong:** The target can be tightened only with a packaging/origin model that preserves the opaque sandbox boundary; no origin check is guessed into the current local-file artifact.

> **Assumed:** A closed structural public projection is the correct initial cross-realm representation.
> **Basis:** Arbitrary prose is indistinguishable from passphrases or secret-compartment plaintext at the warm validator. The boundary therefore permits only UUIDs, fingerprints, validated xpubs, validated public addresses, and numeric accounting values until a provenance-carrying public-text design is independently reviewed.
> **Not verified on:** Future message types or later vault schemas.
> **If wrong:** Adding a message type or public field requires an explicit schema review and a corresponding protocol test.

> **Assumed:** The recognizable secret-content detector is defense in depth, not the primary boundary.
> **Basis:** A false rejection is safer than allowing recognizable secret encodings across the realm, but regex cannot classify arbitrary passphrases. The structural projection rejects all free-form text first, so ordinary prose cannot become a secret carrier.
> **Not verified on:** Every language's mnemonic wordlist or future public-note corpus.
> **If wrong:** Narrow the detector only with an explicit security review and independent tests; never remove content rejection and rely only on field names.

> **Assumed:** 4 MiB for public payloads and 64 MiB for encrypted vault bytes are sufficient protocol bounds.
> **Basis:** The current file target is far below 4.5 MB, while encrypted vault bytes need room for long-lived public history without permitting unbounded cross-realm allocations.
> **Not verified on:** Future vault migration sizes or unusually large public histories.
> **If wrong:** Raise a documented limit with a memory-pressure and denial-of-service review; the validator must continue to reject over-limit messages.

## 9. What to scrutinise

- The exact sequencing of the `cold.ready` signal, `MessageChannel` creation, one-time port transfer, and typed `ready` response.
- The parent no-port requirement on the bootstrap event and the child one-port requirement on the handshake event.
- The direction-specific type tables and whether every payload sanitizer returns only documented safe fields.
- The public collection allowlist against [data-model.md](../../01-spec/data-model.md), especially `disposals`, `basisAllocations`, and `contacts`; unknown collection names must not be silently treated as valid.
- The closed structural public projection and aggregate size bounds; any reintroduction of free-form text requires a provenance design and independent security review.
- The recursive secret-key assertion and the fact that unknown fields are dropped rather than forwarded.
- The post-handshake global listeners: they must only increment an anomaly marker, log a fixed warning, surface a visible warning, and return.
- The opaque sandbox token, inherited child CSP hashes, and the unchanged `connect-src 'none'` policy.
- The fact that P0.8 runtime network neutering and canary behavior remain unimplemented and explicitly out of scope.

## 10. Self-assessment

- P0.7 establishes the private protocol boundary but does not implement vault operations or secret handling; those must not be inferred from the validator alone.
- The global `cold.ready` signal is deliberately a liveness control, not an application message. All application-shaped messages require `{ id, type, payload }` on the transferred port.
- The validator strips harmless unknown fields, rejects unknown types, and rejects string-bearing unknown public records, so new protocol types require a code review and a test rather than silent pass-through.
- The validator rejects free-form public text, recognizable secret content, raw private-key hex, and over-limit payloads rather than merely stripping secret field names.
- The browser harness covers Chromium and Firefox, including injected post-handshake global messages, but makes no physical-device claim.
- The artifact remains local-file compatible and deterministic; no network allowlist was expanded.

## 11. Bundle impact

Measured from the P0.6 artifact on the dependency branch to this branch:

| Artifact | Before | After | Delta |
|---|---:|---:|---:|
| `build/coldbox.html` | 48,231 bytes / 47.10 KB | 87,663 bytes / 85.61 KB | +39,432 bytes / +38.51 KB |

The result remains far below the 3 MB target and 4.5 MB hard cap in the portability contract.

## 12. Docs updated

- `docs/05-development/ROADMAP.md` - marked P0.7 complete and recorded headless browser verification with real-device deferral.
- `CHANGELOG.md` - recorded the MessageChannel handshake, typed validator, secret-field projection, and browser coverage.
- `docs/01-spec/architecture.md` and `docs/01-spec/data-model.md` - aligned the public collection allowlist, closed structural projection, visible anomaly requirement, content-level secret rejection, and payload bounds with the documented entities.
- This packet - permanent implementation, security, test, and handoff evidence.
- `p0.7-message-handshake.self-review.md` - author gate report, kept separate from the reserved independent-review path.
- No new ADR was added; the implementation follows the existing two-realm architecture and message table.
