# Self-review: P1.6 Registry CRUD remediation

**VERDICT: PASS**

Findings: 0
Historical implementation commit before restack: `7090dd5`
Rebased implementation commit: `7c31022`
Exact branch tip used for the refreshed evidence gate: `9db9bb61b106f0df3dd721729394ce6bccecf92d`.
Reviewed by: author self-review gate, remediation round 2
Date: 2026-08-10

## What I verified

- Re-read `docs/05-development/review-protocol.md` and the P1.6 acceptance text before this gate.
- Read the complete remediation diff from the remediated P1.5 base through `7c31022`; `git diff --check` passed.
- `node --test test/protocol.test.js test/registry.test.js`: 18/18 passed.
- `npm.cmd test`: 280/280 passed.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 150 markdown files and 0 warnings.
- `npm.cmd run verify-vendor`: all ten local artifacts and all ten upstream releases passed.
- Production builds from two paths under different timezone/locale settings produced identical SHA-256 `58fc410195000b4d931f16591b98f38822d05cd3dd1702e21acbe4ff77240295` and size 1,872,745 bytes.
- The dependency-free browser harness matched the exact-head build byte-for-byte and passed the P1.6 Registry workflow in both Chromium and Firefox over `file://`.
- The browser workflow created Wallet, Account, and Address records, edited and explicitly cleared an optional label, saved, locked, reopened the canonical `.cbx`, confirmed durable state, and confirmed the locked Registry surface was concealed.
- Whole-compartment orphan checks reject invalid account and address references for both warm snapshots and protocol replacements, while preserving the previous valid state after rejection.
- `clearFields` is allowlisted to optional schema fields; required IDs and relationship fields cannot be cleared. Warm form empty edits use this explicit operation.
- The cold realm still receives only the typed public projection and never receives secret-compartment plaintext.

## What remains untested

Mobile browsers, Safari, and physical hardware are not available in this environment. The required desktop Chromium/Firefox browser coverage for the P1.6 remediation is present; desktop results are not promoted to mobile or hardware evidence.

## Acceptance criteria

The roadmap contains one exact P1.6 line and no separate `Accept:` block:

| Criterion | Result | Evidence |
|---|---|---|
| `P1.6 Registry CRUD: wallets, accounts, addresses` | Met | Warm UI/store CRUD, whole-compartment relationship validation, explicit optional-field clearing, typed public replacement, cold save/reopen, and Chromium/Firefox browser acceptance |

## Security review

- No secret field is accepted by the two public-data message types.
- Unknown text fields fail closed; bounded registry text is validated by collection.
- Whole-compartment relationship validation rejects orphan accounts and addresses before persistence.
- Optional fields clear only through an explicit schema-bound list; required fields and IDs remain mandatory.
- The cold realm still uses `connect-src 'none'`; no host or runtime dependency was added.
- Public replacement cannot change the authenticated Vault ID and does not expose secret-compartment plaintext.
- UUID generation uses `crypto.getRandomValues` only and fails closed when unavailable.
- The registry source is warm-only and has no DOM or secret handling.

## Verdict rationale

The P1.6 findings are addressed with negative unit/protocol coverage and an independent browser flow. The full suite, lint, documentation hygiene, upstream vendor verification, reproducible builds, and both desktop browser engines pass. No self-review finding remains. Mobile and hardware limitations are explicitly disclosed for the independent reviewer.
