# Self-review: P1.7 Notes, tags, and concealment levels

**VERDICT: PASS**

Findings: 0
Historical pre-restack product commit: `0ce59e42e8973cf1eade1c207f9f8f2f16afaa48`
Exact rebased implementation tip reviewed: `b7b1d73ccbdb54366a4517c39472c18b30a72c15`
Reviewed by: author self-review gate after remediation
Date: 2026-08-10

## What I verified

- Re-read `docs/05-development/review-protocol.md` and the P1.7 acceptance line before this gate.
- Read the complete P1.7 product diff, including the cold sandbox button path, offline secret-compartment initialization, hidden-state reversal tests, and both browser workflows.
- `git diff --check` passed before the product commit.
- Focused P1.7/security tests passed 45/45.
- `npm.cmd test`: 284/284 passed.
- `npm.cmd run verify-vendor`: all ten local artifacts and all ten upstream releases passed.
- `npm.cmd run lint`: passed.
- `npm.cmd run check-docs`: passed with 153 markdown files and 0 warnings.
- Two builds at the exact rebased tip under UTC/de_DE.UTF-8 and Pacific/Honolulu/C produced the identical current/final SHA-256 `d952bb27d9aadc1e5395b7f85a76912a827d5902f7e460b34ed4b4dd94b196d2` and identical size 1,922,741 bytes. The earlier `01970aa0fc07c0c4ed949f0bfaf7cbbd5a1b25e78b829ac3969fb51c26c0cf76` value is historical pre-restack evidence only.
- The prescribed browser harness passed over `file://` in Chromium and Firefox, including the new public notes/concealment and cold-local secret-note workflows.
- The cold CSP remains unchanged with `connect-src 'none'`; the new protocol messages carry only empty objects, booleans, or `{ dirty: true }`; online secret-compartment access returns null or rejects; and lock/panic teardown clears secret-note DOM, timers, session state, and byte copies.

## What I could not verify

Physical mobile/iOS/Edge/Safari/Tor/Android Files sessions were unavailable.
The desktop Chromium and Firefox harness did pass, including responsive
viewport checks, but those are not substitutes for the untested device matrix
entries. Independent review must resolve that coverage gap before merge.

## Acceptance criteria

| Criterion | Result | Evidence |
|---|---|---|
| `P1.7 Notes, tags, and concealment levels` | Met | Public note/tag CRUD and search, concealment and privacy-blur tests, explicit hidden reversal, cold-local secret-note search/masking/timed reveal, full suite, and Chromium/Firefox harness |

## Security review

- Public protocol schemas reject secret visibility, secret-shaped values, unknown text carriers, and unbounded note content.
- Secret-note queries and matches stay in the cold editor; no secret-note plaintext, query, phrase, or compartment bytes cross the realm boundary.
- The warm hidden-record reveal request contains no phrase and receives only a boolean result after cold re-authentication.
- The cold realm remains offline-only; no network host, runtime dependency, or CSP exception was added.
- Privacy blur is warm local UI state only and is not represented as cryptographic protection.
- Lock, panic hide, online transition, and failed session paths fail closed and clear secret-note UI and state.
- The cold note control uses an explicit button because the sandbox intentionally does not grant `allow-forms`; no native form-navigation permission was added.

## Review history

- Earlier P1.7 failures involving the forbidden `require` string and incomplete cold lock cleanup remain documented in the branch's prior self-review history and are fixed.
- This remediation gate additionally fixed the offline secret-compartment shape (`{ notes: [] }`) and the sandboxed native-form submission mismatch.
- The final browser gate passed in both available engines after those fixes.

## Verdict rationale

All item-scope behavior and negative tests pass, the full suite and upstream
vendor verification pass, builds are reproducible across locale/timezone
changes, and the browser harness passes in both available desktop engines.
Unverified physical/browser matrix entries are explicitly disclosed. PASS for
the batch self-review gate; independent review remains required before merge.
